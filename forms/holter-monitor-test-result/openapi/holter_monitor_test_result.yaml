# Generated from sql/. Do not edit by hand.
# SOURCE: holter_monitor_test_result
openapi: 3.1.0
info:
  title: Holter Monitor Test Result — HolterMonitorTestResult
  version: 1.0.0
  description: Ambulatory ECG (Holter) monitor result (report). Captures the monitor type and recording quality, clinical history, the rhythm and rate summary (mean / minimum / maximum heart rate, longest pause, ectopy burden), structured findings (AF, significant pauses, VT, SVT, high-grade AV block, symptom-rhythm correlation, normal study), the narrative and impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /holter-monitor-test-results:
    get:
      summary: List HolterMonitorTestResult records
      operationId: listHolterMonitorTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HolterMonitorTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HolterMonitorTestResult"
    post:
      summary: Create a new HolterMonitorTestResult
      operationId: createHolterMonitorTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HolterMonitorTestResult"
      responses:
        "201":
          description: The newly created HolterMonitorTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HolterMonitorTestResult"
        "400":
          description: Validation error
  /holter-monitor-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HolterMonitorTestResult by id
      operationId: getHolterMonitorTestResult
      responses:
        "200":
          description: The requested HolterMonitorTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HolterMonitorTestResult"
        "404":
          description: HolterMonitorTestResult not found
    patch:
      summary: Update an existing HolterMonitorTestResult
      operationId: updateHolterMonitorTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HolterMonitorTestResult"
      responses:
        "200":
          description: The updated HolterMonitorTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HolterMonitorTestResult"
        "404":
          description: HolterMonitorTestResult not found
    delete:
      summary: Soft-delete a HolterMonitorTestResult
      operationId: deleteHolterMonitorTestResult
      responses:
        "204":
          description: HolterMonitorTestResult deleted
        "404":
          description: HolterMonitorTestResult not found
components:
  schemas:
    HolterMonitorTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (cardiologist / cardiac physiologist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating Holter monitor request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        monitor_type:
          type: string
          enum:
            - 24-hour
            - 48-hour
            - 7-day
            - 14-day
            - event-recorder
            - implantable-loop-recorder
            - other
            - ""
          description: "Monitor type actually used: 24-hour, 48-hour, 7-day, 14-day, event-recorder, implantable-loop-recorder, other."
        performed_date:
          type: string
          format: date
          description: Date the Holter recording was performed (recording start / hook-up).
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        recording_duration_hours:
          type: number
          format: double
          description: Total analysable recording duration in hours, for recording-quality assessment.
        analysed_percent:
          type: number
          format: double
          description: Percentage of the recording that was analysable / interpretable (0-100), for recording-quality assessment.
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the monitoring was performed to answer.
        predominant_rhythm:
          type: string
          enum:
            - sinus
            - atrial-fibrillation
            - paced
            - other
            - ""
          description: "Predominant underlying rhythm: sinus, atrial-fibrillation, paced, other."
        mean_heart_rate_bpm:
          type: integer
          format: int32
          description: Mean (24-hour average) heart rate in beats per minute.
        minimum_heart_rate_bpm:
          type: integer
          format: int32
          description: Minimum heart rate in beats per minute (bradycardia screen).
        maximum_heart_rate_bpm:
          type: integer
          format: int32
          description: Maximum heart rate in beats per minute (tachycardia screen).
        longest_pause_seconds:
          type: number
          format: double
          description: Longest R-R pause in seconds; pauses > 3 seconds are clinically significant.
        ventricular_ectopic_percent:
          type: number
          format: double
          description: Ventricular ectopic (VE / PVC) burden as a percentage of total beats.
        supraventricular_ectopic_percent:
          type: number
          format: double
          description: Supraventricular ectopic (SVE / PAC) burden as a percentage of total beats.
        atrial_fibrillation_detected:
          type: boolean
          description: Whether atrial fibrillation (or flutter) was detected during the recording.
        significant_pauses:
          type: boolean
          description: Whether significant pauses (> 3 seconds) were recorded.
        ventricular_tachycardia:
          type: boolean
          description: Whether ventricular tachycardia (sustained or non-sustained) was recorded (critical finding).
        supraventricular_tachycardia:
          type: boolean
          description: Whether supraventricular tachycardia (SVT) was recorded.
        high_grade_av_block:
          type: boolean
          description: Whether high-grade AV block (Mobitz II or third-degree) was recorded (critical finding).
        symptom_rhythm_correlation:
          type: boolean
          description: Whether reported symptoms correlated with a recorded rhythm abnormality (diary correlation).
        normal_study:
          type: boolean
          description: Whether the study is normal (no significant arrhythmia, conduction abnormality, or pause).
        findings_narrative:
          type: string
          maxLength: 2000
          description: Narrative description of the rhythm findings (the body of the report).
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous ambulatory ECG studies.
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 50
          description: Free-text structured-reporting category label (e.g. a rhythm or AF-burden category).
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up monitoring, referral, or management.
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - monitor_type
        - clinical_history
        - predominant_rhythm
        - atrial_fibrillation_detected
        - significant_pauses
        - ventricular_tachycardia
        - supraventricular_tachycardia
        - high_grade_av_block
        - symptom_rhythm_correlation
        - normal_study
        - findings_narrative
        - comparison_with_previous
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Ambulatory ECG (Holter) monitor result (report). Captures the monitor type and recording quality, clinical history, the rhythm and rate summary (mean / minimum / maximum heart rate, longest pause, ectopy burden), structured findings (AF, significant pauses, VT, SVT, high-grade AV block, symptom-rhythm correlation, normal study), the narrative and impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
