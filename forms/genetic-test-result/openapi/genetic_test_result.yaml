# Generated from sql/. Do not edit by hand.
# SOURCE: genetic_test_result
openapi: 3.1.0
info:
  title: Genetic Test Result — GeneticTestResult
  version: 1.0.0
  description: Genetic / genomic test result (report). Captures the performed test, the genes tested and sample type, clinical history and inheritance pattern, the detected variants and their ACMG/AMP-ACGS classification and zygosity, structured finding booleans, the interpretation and impression, the ACMG reporting category, recommended cascade testing and follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /genetic-test-results:
    get:
      summary: List GeneticTestResult records
      operationId: listGeneticTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of GeneticTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeneticTestResult"
    post:
      summary: Create a new GeneticTestResult
      operationId: createGeneticTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneticTestResult"
      responses:
        "201":
          description: The newly created GeneticTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneticTestResult"
        "400":
          description: Validation error
  /genetic-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one GeneticTestResult by id
      operationId: getGeneticTestResult
      responses:
        "200":
          description: The requested GeneticTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneticTestResult"
        "404":
          description: GeneticTestResult not found
    patch:
      summary: Update an existing GeneticTestResult
      operationId: updateGeneticTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneticTestResult"
      responses:
        "200":
          description: The updated GeneticTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneticTestResult"
        "404":
          description: GeneticTestResult not found
    delete:
      summary: Soft-delete a GeneticTestResult
      operationId: deleteGeneticTestResult
      responses:
        "204":
          description: GeneticTestResult deleted
        "404":
          description: GeneticTestResult not found
components:
  schemas:
    GeneticTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (clinical geneticist / genetic counsellor / clinical scientist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating genetic test request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the genetic test / analysis was performed.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        test_type:
          type: string
          enum:
            - diagnostic-single-gene
            - gene-panel
            - whole-exome
            - whole-genome
            - chromosomal-microarray
            - karyotype
            - predictive-presymptomatic
            - carrier-testing
            - pharmacogenomic
            - prenatal
            - other
            - ""
          description: "Type of genomic test performed: diagnostic-single-gene, gene-panel, whole-exome, whole-genome, chromosomal-microarray, karyotype, predictive-presymptomatic, carrier-testing, pharmacogenomic, prenatal, other."
        genes_tested:
          type: string
          description: Genes, panel, or genomic region analysed (free text; gene symbols, panel name, or region).
        sample_type:
          type: string
          enum:
            - blood
            - saliva
            - tissue
            - prenatal
            - ""
          description: "Sample type analysed: blood, saliva, tissue, prenatal."
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history, phenotype, and the question the test was performed to answer.
        inheritance_pattern:
          type: string
          maxLength: 500
          description: Inheritance pattern relevant to the result (e.g. autosomal dominant, autosomal recessive, X-linked, mitochondrial).
        variants_detected:
          type: string
          maxLength: 2000
          description: Narrative description of the variant(s) detected, with HGVS nomenclature, gene, and transcript where applicable (the body of the report).
        variant_classification:
          type: string
          enum:
            - pathogenic
            - likely-pathogenic
            - variant-uncertain-significance
            - likely-benign
            - benign
            - no-variant-detected
            - ""
          description: "Overall ACMG/AMP (ACGS) five-tier variant classification: pathogenic, likely-pathogenic, variant-uncertain-significance, likely-benign, benign, or no-variant-detected."
        zygosity:
          type: string
          enum:
            - heterozygous
            - homozygous
            - hemizygous
            - not-applicable
            - ""
          description: "Zygosity of the reported variant: heterozygous, homozygous, hemizygous, not-applicable."
        pathogenic_variant_found:
          type: boolean
          description: Whether a pathogenic or likely-pathogenic variant was found.
        vus_found:
          type: boolean
          description: Whether a variant of uncertain significance (VUS) was found.
        carrier_status_positive:
          type: boolean
          description: Whether the patient is a carrier of a recessive / X-linked condition.
        secondary_finding:
          type: boolean
          description: Whether a secondary / incidental finding (e.g. an actionable finding unrelated to the indication) was identified.
        no_clinically_significant_variant:
          type: boolean
          description: Whether no clinically significant variant was detected for the indication.
        interpretation:
          type: string
          maxLength: 2000
          description: Interpretation of the variant(s) in the context of the patient phenotype and family history.
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 50
          description: "Structured-reporting category label, free text (e.g. an ACMG/AMP class such as \"Class 5 — pathogenic\")."
        recommended_cascade_testing:
          type: boolean
          description: Whether cascade (predictive) testing of at-risk relatives is recommended.
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: "Recommended follow-up: referral, surveillance, genetic counselling, or further testing."
        critical_result_communicated:
          type: boolean
          description: Whether a critical or actionable result was communicated directly to the referrer / clinical team.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / actionable result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - test_type
        - genes_tested
        - sample_type
        - clinical_history
        - inheritance_pattern
        - variants_detected
        - variant_classification
        - zygosity
        - pathogenic_variant_found
        - vus_found
        - carrier_status_positive
        - secondary_finding
        - no_clinically_significant_variant
        - interpretation
        - impression
        - reporting_category
        - recommended_cascade_testing
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Genetic / genomic test result (report). Captures the performed test, the genes tested and sample type, clinical history and inheritance pattern, the detected variants and their ACMG/AMP-ACGS classification and zygosity, structured finding booleans, the interpretation and impression, the ACMG reporting category, recommended cascade testing and follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
