# Generated from sql/. Do not edit by hand.
# SOURCE: respect_grade
openapi: 3.1.0
info:
  title: Recommended Summary Plan For Emergency Care And Treatment — RespectGrade
  version: 1.0.0
  description: "Computed documentation-completeness result for a ReSPECT plan: status (complete when all eight mandatory rules are satisfied, otherwise incomplete) and completeness percentage."
paths:
  /respect-grades:
    get:
      summary: List RespectGrade records
      operationId: listRespectGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of RespectGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RespectGrade"
    post:
      summary: Create a new RespectGrade
      operationId: createRespectGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RespectGrade"
      responses:
        "201":
          description: The newly created RespectGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RespectGrade"
        "400":
          description: Validation error
  /respect-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one RespectGrade by id
      operationId: getRespectGrade
      responses:
        "200":
          description: The requested RespectGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RespectGrade"
        "404":
          description: RespectGrade not found
    patch:
      summary: Update an existing RespectGrade
      operationId: updateRespectGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RespectGrade"
      responses:
        "200":
          description: The updated RespectGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RespectGrade"
        "404":
          description: RespectGrade not found
    delete:
      summary: Soft-delete a RespectGrade
      operationId: deleteRespectGrade
      responses:
        "204":
          description: RespectGrade deleted
        "404":
          description: RespectGrade not found
components:
  schemas:
    RespectGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        respect_id:
          type: string
          format: uuid
          description: Foreign key to the parent ReSPECT plan (unique, 1:1).
        status:
          type: string
          enum:
            - complete
            - incomplete
            - ""
          description: "Completeness status: complete (all mandatory rules satisfied) or incomplete."
        completeness_percent:
          type: integer
          format: int32
          description: "Completeness percentage (0..100): populated mandatory fields / total mandatory fields x 100, reported for both statuses."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the grade.
      required:
        - created_at
        - updated_at
        - respect_id
        - status
        - graded_at
      description: "Computed documentation-completeness result for a ReSPECT plan: status (complete when all eight mandatory rules are satisfied, otherwise incomplete) and completeness percentage."
