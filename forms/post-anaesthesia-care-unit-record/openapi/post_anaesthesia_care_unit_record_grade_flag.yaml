# Generated from sql/. Do not edit by hand.
# SOURCE: post_anaesthesia_care_unit_record_grade_flag
openapi: 3.1.0
info:
  title: Post Anaesthesia Care Unit Record — PostAnaesthesiaCareUnitRecordGradeFlag
  version: 1.0.0
  description: Red-flag issues that fire independently of the total score, with priority and a suggested action for the recovery team.
paths:
  /post-anaesthesia-care-unit-record-grade-flags:
    get:
      summary: List PostAnaesthesiaCareUnitRecordGradeFlag records
      operationId: listPostAnaesthesiaCareUnitRecordGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PostAnaesthesiaCareUnitRecordGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PostAnaesthesiaCareUnitRecordGradeFlag"
    post:
      summary: Create a new PostAnaesthesiaCareUnitRecordGradeFlag
      operationId: createPostAnaesthesiaCareUnitRecordGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PostAnaesthesiaCareUnitRecordGradeFlag"
      responses:
        "201":
          description: The newly created PostAnaesthesiaCareUnitRecordGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PostAnaesthesiaCareUnitRecordGradeFlag"
        "400":
          description: Validation error
  /post-anaesthesia-care-unit-record-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PostAnaesthesiaCareUnitRecordGradeFlag by id
      operationId: getPostAnaesthesiaCareUnitRecordGradeFlag
      responses:
        "200":
          description: The requested PostAnaesthesiaCareUnitRecordGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PostAnaesthesiaCareUnitRecordGradeFlag"
        "404":
          description: PostAnaesthesiaCareUnitRecordGradeFlag not found
    patch:
      summary: Update an existing PostAnaesthesiaCareUnitRecordGradeFlag
      operationId: updatePostAnaesthesiaCareUnitRecordGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PostAnaesthesiaCareUnitRecordGradeFlag"
      responses:
        "200":
          description: The updated PostAnaesthesiaCareUnitRecordGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PostAnaesthesiaCareUnitRecordGradeFlag"
        "404":
          description: PostAnaesthesiaCareUnitRecordGradeFlag not found
    delete:
      summary: Soft-delete a PostAnaesthesiaCareUnitRecordGradeFlag
      operationId: deletePostAnaesthesiaCareUnitRecordGradeFlag
      responses:
        "204":
          description: PostAnaesthesiaCareUnitRecordGradeFlag deleted
        "404":
          description: PostAnaesthesiaCareUnitRecordGradeFlag not found
components:
  schemas:
    PostAnaesthesiaCareUnitRecordGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        post_anaesthesia_care_unit_record_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-NOT-READY-FOR-DISCHARGE-001).
        category:
          type: string
          enum:
            - not-ready-for-discharge
            - hypoxia
            - unstable-vital-signs
            - uncontrolled-pain
            - uncontrolled-ponv
            - surgical-bleeding
            - incomplete-assessment
            - other
            - ""
          description: Flag category (see CHECK constraint for enumeration).
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical action (e.g. \"continue recovery observation; do not discharge from PACU\")."
      required:
        - created_at
        - updated_at
        - post_anaesthesia_care_unit_record_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Red-flag issues that fire independently of the total score, with priority and a suggested action for the recovery team.
