# Tasks

- [x] Author `index.html` — single self-contained file with the six
      `<fieldset>` step blocks, the 16 yes/no/unanswered radio triplets,
      per-item evidence textareas, embedded CSS, and a vanilla
      JavaScript port of `gradeScorecard()` providing a live, sticky
      total / band / flags preview
- [x] Add a JSON export button that emits a valid
      `AgileConsultingScorecardAssessment`-shaped payload
- [ ] Manual cross-browser smoke test (Firefox, Safari, Chrome)
- [ ] Author a no-JavaScript fallback that hides the live preview but
      still submits the form natively
