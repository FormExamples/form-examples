# Tasks

- [x] Author `index.html` — single self-contained file with embedded
      CSS and JS, the 11-column sortable table, dropdown filters
      (band / sector / size), free-text search, and a click-to-expand
      flag detail row
- [x] Bundle inline sample data: 12 representative scorecards spanning
      all four bands (low / borderline / medium / high)
- [ ] Manual cross-browser smoke test (Firefox, Safari, Chrome)
- [ ] Wire up a live `/api/scorecards` fetch (with the inline sample as
      the fallback) when the Rust backend is available
