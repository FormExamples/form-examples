# Plan

Build the SvelteKit single-page scorecard wizard on top of the existing
TypeScript scoring engine, following the canonical pattern from
`pre-operative-assessment-by-clinician/front-end-form-with-svelte/`.
