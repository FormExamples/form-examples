# Toxicology Test Request — back-end-with-loco tasks

- [ ] Generate crate via `../back-end-with-loco-setup`
- [ ] Implement entity models and migrations parity with `../sql/`
- [ ] Implement the pure four-axis scoring engine (rule IDs shared with front-ends)
- [ ] Triage auto-escalation: deliberate overdose / symptomatic -> stat
- [ ] Paracetamol timing rule: < 4 h post-ingestion -> timing_band invalid + flag
- [ ] JSON API endpoints (CRUD + grade)
- [ ] `cargo test` green (requires local Postgres)
