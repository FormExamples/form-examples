# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: toxicology_test_request_grade_rule
openapi: 3.1.0
info:
  title: Toxicology Test Request — ToxicologyTestRequestGradeRule
  version: 1.0.0
  description: Audit trail of every appropriateness / timing / completeness / triage rule that fired for this request.
paths:
  /toxicology-test-request-grade-rules:
    get:
      summary: List ToxicologyTestRequestGradeRule records
      operationId: listToxicologyTestRequestGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ToxicologyTestRequestGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ToxicologyTestRequestGradeRule"
    post:
      summary: Create a new ToxicologyTestRequestGradeRule
      operationId: createToxicologyTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToxicologyTestRequestGradeRule"
      responses:
        "201":
          description: The newly created ToxicologyTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToxicologyTestRequestGradeRule"
        "400":
          description: Validation error
  /toxicology-test-request-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ToxicologyTestRequestGradeRule by id
      operationId: getToxicologyTestRequestGradeRule
      responses:
        "200":
          description: The requested ToxicologyTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToxicologyTestRequestGradeRule"
        "404":
          description: ToxicologyTestRequestGradeRule not found
    patch:
      summary: Update an existing ToxicologyTestRequestGradeRule
      operationId: updateToxicologyTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToxicologyTestRequestGradeRule"
      responses:
        "200":
          description: The updated ToxicologyTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToxicologyTestRequestGradeRule"
        "404":
          description: ToxicologyTestRequestGradeRule not found
    delete:
      summary: Soft-delete a ToxicologyTestRequestGradeRule
      operationId: deleteToxicologyTestRequestGradeRule
      responses:
        "204":
          description: ToxicologyTestRequestGradeRule deleted
        "404":
          description: ToxicologyTestRequestGradeRule not found
components:
  schemas:
    ToxicologyTestRequestGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        toxicology_test_request_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-TIMING-PARACETAMOL-01).
        axis:
          type: string
          enum:
            - appropriateness
            - timing
            - completeness
            - triage
            - ""
          description: "Scoring axis the rule belongs to: appropriateness, timing, completeness, triage."
        category:
          type: string
          maxLength: 50
          description: Category or indication the rule relates to.
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - toxicology_test_request_grade_id
        - rule_id
        - axis
        - category
        - description
      description: Audit trail of every appropriateness / timing / completeness / triage rule that fired for this request.
