# Generated from sql/. Do not edit by hand.
# SOURCE: toxicology_test_request_grade_flag
openapi: 3.1.0
info:
  title: Toxicology Test Request — ToxicologyTestRequestGradeFlag
  version: 1.0.0
  description: Safety-critical flags that fire independently of the four axes, with priority and a suggested action for the toxicology / vetting team.
paths:
  /toxicology-test-request-grade-flags:
    get:
      summary: List ToxicologyTestRequestGradeFlag records
      operationId: listToxicologyTestRequestGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ToxicologyTestRequestGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ToxicologyTestRequestGradeFlag"
    post:
      summary: Create a new ToxicologyTestRequestGradeFlag
      operationId: createToxicologyTestRequestGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToxicologyTestRequestGradeFlag"
      responses:
        "201":
          description: The newly created ToxicologyTestRequestGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToxicologyTestRequestGradeFlag"
        "400":
          description: Validation error
  /toxicology-test-request-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ToxicologyTestRequestGradeFlag by id
      operationId: getToxicologyTestRequestGradeFlag
      responses:
        "200":
          description: The requested ToxicologyTestRequestGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToxicologyTestRequestGradeFlag"
        "404":
          description: ToxicologyTestRequestGradeFlag not found
    patch:
      summary: Update an existing ToxicologyTestRequestGradeFlag
      operationId: updateToxicologyTestRequestGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToxicologyTestRequestGradeFlag"
      responses:
        "200":
          description: The updated ToxicologyTestRequestGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToxicologyTestRequestGradeFlag"
        "404":
          description: ToxicologyTestRequestGradeFlag not found
    delete:
      summary: Soft-delete a ToxicologyTestRequestGradeFlag
      operationId: deleteToxicologyTestRequestGradeFlag
      responses:
        "204":
          description: ToxicologyTestRequestGradeFlag deleted
        "404":
          description: ToxicologyTestRequestGradeFlag not found
components:
  schemas:
    ToxicologyTestRequestGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        toxicology_test_request_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-PARACETAMOL-TIMING-001).
        category:
          type: string
          enum:
            - suspected-overdose-stat
            - paracetamol-timing-critical
            - deliberate-self-harm-safeguarding
            - specimen-not-collected
            - missing-clinical-details
            - no-test-selected
            - other
            - ""
          description: "Flag category: suspected-overdose-stat, paracetamol-timing-critical, deliberate-self-harm-safeguarding, specimen-not-collected, missing-clinical-details, no-test-selected, other."
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical action (e.g. \"contact NPIS / TOXBASE and arrange stat level\")."
      required:
        - created_at
        - updated_at
        - toxicology_test_request_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Safety-critical flags that fire independently of the four axes, with priority and a suggested action for the toxicology / vetting team.
