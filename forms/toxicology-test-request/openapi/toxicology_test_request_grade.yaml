# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: toxicology_test_request_grade
openapi: 3.1.0
info:
  title: Toxicology Test Request — ToxicologyTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (1-9 indication match), timing band (ingestion-time validity), request completeness, and triage priority."
paths:
  /toxicology-test-request-grades:
    get:
      summary: List ToxicologyTestRequestGrade records
      operationId: listToxicologyTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ToxicologyTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ToxicologyTestRequestGrade"
    post:
      summary: Create a new ToxicologyTestRequestGrade
      operationId: createToxicologyTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToxicologyTestRequestGrade"
      responses:
        "201":
          description: The newly created ToxicologyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToxicologyTestRequestGrade"
        "400":
          description: Validation error
  /toxicology-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ToxicologyTestRequestGrade by id
      operationId: getToxicologyTestRequestGrade
      responses:
        "200":
          description: The requested ToxicologyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToxicologyTestRequestGrade"
        "404":
          description: ToxicologyTestRequestGrade not found
    patch:
      summary: Update an existing ToxicologyTestRequestGrade
      operationId: updateToxicologyTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToxicologyTestRequestGrade"
      responses:
        "200":
          description: The updated ToxicologyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToxicologyTestRequestGrade"
        "404":
          description: ToxicologyTestRequestGrade not found
    delete:
      summary: Soft-delete a ToxicologyTestRequestGrade
      operationId: deleteToxicologyTestRequestGrade
      responses:
        "204":
          description: ToxicologyTestRequestGrade deleted
        "404":
          description: ToxicologyTestRequestGrade not found
components:
  schemas:
    ToxicologyTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        toxicology_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9). No single published toxicology-ordering score exists; anchored on TOXBASE / NPIS guidance and indication-to-assay match. Decision support only.
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        timing_band:
          type: string
          enum:
            - ok
            - caution
            - invalid
            - ""
          description: "Timing validity band: ok, caution, invalid. E.g. a paracetamol level taken <4 h post-ingestion is invalid for nomogram interpretation."
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - stat
            - ""
          description: "Assigned triage tier: routine, urgent, stat."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the assay implied by the triage tier (deliberate overdose / symptomatic -> stat).
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - toxicology_test_request_id
        - appropriateness_band
        - timing_band
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (1-9 indication match), timing band (ingestion-time validity), request completeness, and triage priority."
