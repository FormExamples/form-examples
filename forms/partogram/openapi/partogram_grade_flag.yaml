# Generated from sql/. Do not edit by hand.
# SOURCE: partogram_grade_flag
openapi: 3.1.0
info:
  title: Partogram — PartogramGradeFlag
  version: 1.0.0
  description: Flagged issues raised independently of the progress classification, scanned across the whole observation series, with priority and a suggested action for the labour-ward team.
paths:
  /partogram-grade-flags:
    get:
      summary: List PartogramGradeFlag records
      operationId: listPartogramGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PartogramGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PartogramGradeFlag"
    post:
      summary: Create a new PartogramGradeFlag
      operationId: createPartogramGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartogramGradeFlag"
      responses:
        "201":
          description: The newly created PartogramGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartogramGradeFlag"
        "400":
          description: Validation error
  /partogram-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PartogramGradeFlag by id
      operationId: getPartogramGradeFlag
      responses:
        "200":
          description: The requested PartogramGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartogramGradeFlag"
        "404":
          description: PartogramGradeFlag not found
    patch:
      summary: Update an existing PartogramGradeFlag
      operationId: updatePartogramGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartogramGradeFlag"
      responses:
        "200":
          description: The updated PartogramGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartogramGradeFlag"
        "404":
          description: PartogramGradeFlag not found
    delete:
      summary: Soft-delete a PartogramGradeFlag
      operationId: deletePartogramGradeFlag
      responses:
        "204":
          description: PartogramGradeFlag deleted
        "404":
          description: PartogramGradeFlag not found
components:
  schemas:
    PartogramGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        partogram_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-FETAL-HEART-ABNORMAL-001).
        category:
          type: string
          enum:
            - action-line-crossed
            - alert-line-crossed
            - fetal-heart-abnormal
            - meconium
            - maternal-obs-abnormal
            - poor-progress
            - incomplete
            - other
            - ""
          description: "Flag category: action-line-crossed, alert-line-crossed, fetal-heart-abnormal, meconium, maternal-obs-abnormal, poor-progress, incomplete, or other."
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical action (e.g. \"escalate to the obstetric registrar for review\")."
      required:
        - created_at
        - updated_at
        - partogram_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Flagged issues raised independently of the progress classification, scanned across the whole observation series, with priority and a suggested action for the labour-ward team.
