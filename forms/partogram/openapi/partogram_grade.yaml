# Generated from sql/. Do not edit by hand.
# SOURCE: partogram_grade
openapi: 3.1.0
info:
  title: Partogram — PartogramGrade
  version: 1.0.0
  description: "Computed labour-progress grading result for a partogram: overall progress classification and the latest cervical dilatation plotted against the alert and action lines."
paths:
  /partogram-grades:
    get:
      summary: List PartogramGrade records
      operationId: listPartogramGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PartogramGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PartogramGrade"
    post:
      summary: Create a new PartogramGrade
      operationId: createPartogramGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartogramGrade"
      responses:
        "201":
          description: The newly created PartogramGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartogramGrade"
        "400":
          description: Validation error
  /partogram-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PartogramGrade by id
      operationId: getPartogramGrade
      responses:
        "200":
          description: The requested PartogramGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartogramGrade"
        "404":
          description: PartogramGrade not found
    patch:
      summary: Update an existing PartogramGrade
      operationId: updatePartogramGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartogramGrade"
      responses:
        "200":
          description: The updated PartogramGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartogramGrade"
        "404":
          description: PartogramGrade not found
    delete:
      summary: Soft-delete a PartogramGrade
      operationId: deletePartogramGrade
      responses:
        "204":
          description: PartogramGrade deleted
        "404":
          description: PartogramGrade not found
components:
  schemas:
    PartogramGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        partogram_id:
          type: string
          format: uuid
          description: Foreign key to the parent partogram labour record (unique, 1:1).
        progress_status:
          type: string
          enum:
            - normal
            - alert-line-crossed
            - action-line-crossed
            - ""
          description: "Overall labour-progress classification: normal, alert-line-crossed, or action-line-crossed."
        latest_dilatation_cm:
          type: number
          format: double
          description: Latest observed cervical dilatation in centimetres (0-10) that the engine plotted against the reference lines.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the grade.
      required:
        - created_at
        - updated_at
        - partogram_id
        - progress_status
        - graded_at
      description: "Computed labour-progress grading result for a partogram: overall progress classification and the latest cervical dilatation plotted against the alert and action lines."
