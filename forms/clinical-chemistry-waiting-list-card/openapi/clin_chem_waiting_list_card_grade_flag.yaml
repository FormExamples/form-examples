# Generated from sql/. Do not edit by hand.
# SOURCE: clin_chem_waiting_list_card_grade_flag
openapi: 3.1.0
info:
  title: Clinical Chemistry Waiting List Card — ClinChemWaitingListCardGradeFlag
  version: 1.0.0
  description: Safety / operational flags that fire independently of the Waiting Time Status, with priority and a suggested action for the booking team.
paths:
  /clin-chem-waiting-list-card-grade-flags:
    get:
      summary: List ClinChemWaitingListCardGradeFlag records
      operationId: listClinChemWaitingListCardGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ClinChemWaitingListCardGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ClinChemWaitingListCardGradeFlag"
    post:
      summary: Create a new ClinChemWaitingListCardGradeFlag
      operationId: createClinChemWaitingListCardGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClinChemWaitingListCardGradeFlag"
      responses:
        "201":
          description: The newly created ClinChemWaitingListCardGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClinChemWaitingListCardGradeFlag"
        "400":
          description: Validation error
  /clin-chem-waiting-list-card-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ClinChemWaitingListCardGradeFlag by id
      operationId: getClinChemWaitingListCardGradeFlag
      responses:
        "200":
          description: The requested ClinChemWaitingListCardGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClinChemWaitingListCardGradeFlag"
        "404":
          description: ClinChemWaitingListCardGradeFlag not found
    patch:
      summary: Update an existing ClinChemWaitingListCardGradeFlag
      operationId: updateClinChemWaitingListCardGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClinChemWaitingListCardGradeFlag"
      responses:
        "200":
          description: The updated ClinChemWaitingListCardGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClinChemWaitingListCardGradeFlag"
        "404":
          description: ClinChemWaitingListCardGradeFlag not found
    delete:
      summary: Soft-delete a ClinChemWaitingListCardGradeFlag
      operationId: deleteClinChemWaitingListCardGradeFlag
      responses:
        "204":
          description: ClinChemWaitingListCardGradeFlag deleted
        "404":
          description: ClinChemWaitingListCardGradeFlag not found
components:
  schemas:
    ClinChemWaitingListCardGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        clin_chem_waiting_list_card_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade row.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-BREACH-RISK-001).
        category:
          type: string
          enum:
            - breach-risk
            - long-waiter-52-week
            - priority-1-escalation
            - two-week-wait-cancer
            - missing-appointment
            - accessibility-unmet
            - interpreter-required
            - contact-details-missing
            - other
            - ""
          description: Flag category.
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested action (e.g. \"contact patient to confirm next appointment\")."
      required:
        - created_at
        - updated_at
        - clin_chem_waiting_list_card_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Safety / operational flags that fire independently of the Waiting Time Status, with priority and a suggested action for the booking team.
