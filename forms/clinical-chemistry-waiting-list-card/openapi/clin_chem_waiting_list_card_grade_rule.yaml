# Generated from sql/. Do not edit by hand.
# SOURCE: clin_chem_waiting_list_card_grade_rule
openapi: 3.1.0
info:
  title: Clinical Chemistry Waiting List Card — ClinChemWaitingListCardGradeRule
  version: 1.0.0
  description: Audit trail of every waiting-time-status / clinical-priority / long-wait rule that fired during grading of a waiting list card.
paths:
  /clin-chem-waiting-list-card-grade-rules:
    get:
      summary: List ClinChemWaitingListCardGradeRule records
      operationId: listClinChemWaitingListCardGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ClinChemWaitingListCardGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ClinChemWaitingListCardGradeRule"
    post:
      summary: Create a new ClinChemWaitingListCardGradeRule
      operationId: createClinChemWaitingListCardGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClinChemWaitingListCardGradeRule"
      responses:
        "201":
          description: The newly created ClinChemWaitingListCardGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClinChemWaitingListCardGradeRule"
        "400":
          description: Validation error
  /clin-chem-waiting-list-card-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ClinChemWaitingListCardGradeRule by id
      operationId: getClinChemWaitingListCardGradeRule
      responses:
        "200":
          description: The requested ClinChemWaitingListCardGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClinChemWaitingListCardGradeRule"
        "404":
          description: ClinChemWaitingListCardGradeRule not found
    patch:
      summary: Update an existing ClinChemWaitingListCardGradeRule
      operationId: updateClinChemWaitingListCardGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClinChemWaitingListCardGradeRule"
      responses:
        "200":
          description: The updated ClinChemWaitingListCardGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClinChemWaitingListCardGradeRule"
        "404":
          description: ClinChemWaitingListCardGradeRule not found
    delete:
      summary: Soft-delete a ClinChemWaitingListCardGradeRule
      operationId: deleteClinChemWaitingListCardGradeRule
      responses:
        "204":
          description: ClinChemWaitingListCardGradeRule deleted
        "404":
          description: ClinChemWaitingListCardGradeRule not found
components:
  schemas:
    ClinChemWaitingListCardGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        clin_chem_waiting_list_card_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade row.
        rule_id:
          type: string
          maxLength: 50
          description: Stable rule identifier (e.g. R-WTS-BREACHED-001).
        instrument:
          type: string
          enum:
            - waiting-time-status
            - clinical-priority
            - long-wait
          description: "Scoring instrument the rule belongs to: waiting-time-status, clinical-priority, long-wait."
        band:
          type: string
          enum:
            - within-target
            - approaching-breach
            - breached
            - long-wait
            - ""
          description: Band contributed by this rule (e.g. breached).
        category:
          type: string
          maxLength: 50
          description: Category descriptor (e.g. rtt-18-week, priority-target, long-waiter).
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - clin_chem_waiting_list_card_grade_id
        - rule_id
        - instrument
        - band
        - category
        - description
      description: Audit trail of every waiting-time-status / clinical-priority / long-wait rule that fired during grading of a waiting list card.
