# Generated from sql/. Do not edit by hand.
# SOURCE: hearing_test_result
openapi: 3.1.0
info:
  title: Hearing Test Result — HearingTestResult
  version: 1.0.0
  description: Audiology / hearing test result (report). Captures the performed test, test reliability, clinical history, the pure-tone averages and per-ear interpretation (loss type and severity), tympanometry types, the narrative and structured findings, the impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /hearing-test-results:
    get:
      summary: List HearingTestResult records
      operationId: listHearingTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HearingTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HearingTestResult"
    post:
      summary: Create a new HearingTestResult
      operationId: createHearingTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HearingTestResult"
      responses:
        "201":
          description: The newly created HearingTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HearingTestResult"
        "400":
          description: Validation error
  /hearing-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HearingTestResult by id
      operationId: getHearingTestResult
      responses:
        "200":
          description: The requested HearingTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HearingTestResult"
        "404":
          description: HearingTestResult not found
    patch:
      summary: Update an existing HearingTestResult
      operationId: updateHearingTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HearingTestResult"
      responses:
        "200":
          description: The updated HearingTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HearingTestResult"
        "404":
          description: HearingTestResult not found
    delete:
      summary: Soft-delete a HearingTestResult
      operationId: deleteHearingTestResult
      responses:
        "204":
          description: HearingTestResult deleted
        "404":
          description: HearingTestResult not found
components:
  schemas:
    HearingTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (audiologist / ENT surgeon / hearing therapist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating hearing test request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the hearing test was performed.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        test_type:
          type: string
          enum:
            - pure-tone-audiometry
            - tympanometry
            - speech-audiometry
            - otoacoustic-emissions
            - auditory-brainstem-response
            - newborn-hearing-screen
            - other
            - ""
          description: "Performed audiology test type: pure-tone-audiometry, tympanometry, speech-audiometry, otoacoustic-emissions, auditory-brainstem-response, newborn-hearing-screen, other."
        test_reliability:
          type: string
          enum:
            - good
            - fair
            - poor
            - ""
          description: "Reliability / validity of the test result: good, fair, poor (e.g. patient cooperation, masking, test conditions)."
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the test was performed to answer.
        pure_tone_average_right_db:
          type: number
          format: double
          description: "Right-ear pure-tone average (PTA) in dB HL (BSA: mean of thresholds across the standard frequencies; no-response frequencies counted at 130 dB HL)."
        pure_tone_average_left_db:
          type: number
          format: double
          description: "Left-ear pure-tone average (PTA) in dB HL (BSA: mean of thresholds across the standard frequencies; no-response frequencies counted at 130 dB HL)."
        hearing_loss_type_right:
          type: string
          enum:
            - none
            - conductive
            - sensorineural
            - mixed
            - ""
          description: "Right-ear hearing-loss type: none, conductive, sensorineural, mixed."
        hearing_loss_type_left:
          type: string
          enum:
            - none
            - conductive
            - sensorineural
            - mixed
            - ""
          description: "Left-ear hearing-loss type: none, conductive, sensorineural, mixed."
        hearing_loss_severity_right:
          type: string
          enum:
            - normal
            - mild
            - moderate
            - moderately-severe
            - severe
            - profound
            - ""
          description: "Right-ear hearing-loss severity (BSA descriptors): normal, mild, moderate, moderately-severe, severe, profound."
        hearing_loss_severity_left:
          type: string
          enum:
            - normal
            - mild
            - moderate
            - moderately-severe
            - severe
            - profound
            - ""
          description: "Left-ear hearing-loss severity (BSA descriptors): normal, mild, moderate, moderately-severe, severe, profound."
        tympanometry_type_right:
          type: string
          enum:
            - A
            - As
            - Ad
            - B
            - C
            - ""
          description: "Right-ear tympanogram type (Jerger): A, As, Ad, B, C."
        tympanometry_type_left:
          type: string
          enum:
            - A
            - As
            - Ad
            - B
            - C
            - ""
          description: "Left-ear tympanogram type (Jerger): A, As, Ad, B, C."
        hearing_loss_present:
          type: boolean
          description: Whether any hearing loss is present on this test.
        asymmetric_loss:
          type: boolean
          description: Whether there is a marked asymmetry between ears (red flag for retrocochlear pathology).
        sudden_sensorineural_loss:
          type: boolean
          description: Whether the result is consistent with sudden sensorineural hearing loss (otological emergency).
        conductive_component:
          type: boolean
          description: Whether a conductive component is present (e.g. air-bone gap, abnormal tympanometry).
        normal_hearing:
          type: boolean
          description: Whether the test demonstrates normal hearing bilaterally.
        findings_narrative:
          type: string
          maxLength: 2000
          description: Narrative description of the audiometric findings (the body of the report).
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 50
          description: Free-text structured-reporting category label (e.g. a configuration or audiometric descriptor).
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up testing, referral, or management (e.g. hearing-aid fitting, ENT referral, MRI IAM).
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - test_type
        - test_reliability
        - clinical_history
        - hearing_loss_type_right
        - hearing_loss_type_left
        - hearing_loss_severity_right
        - hearing_loss_severity_left
        - tympanometry_type_right
        - tympanometry_type_left
        - hearing_loss_present
        - asymmetric_loss
        - sudden_sensorineural_loss
        - conductive_component
        - normal_hearing
        - findings_narrative
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Audiology / hearing test result (report). Captures the performed test, test reliability, clinical history, the pure-tone averages and per-ear interpretation (loss type and severity), tympanometry types, the narrative and structured findings, the impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
