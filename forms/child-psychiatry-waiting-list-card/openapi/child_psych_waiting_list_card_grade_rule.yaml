# Generated from sql/. Do not edit by hand.
# SOURCE: child_psych_waiting_list_card_grade_rule
openapi: 3.1.0
info:
  title: Child Psychiatry Waiting List Card — ChildPsychWaitingListCardGradeRule
  version: 1.0.0
  description: Audit trail of every waiting-time-status / clinical-priority / long-wait rule that fired during grading of a waiting list card.
paths:
  /child-psych-waiting-list-card-grade-rules:
    get:
      summary: List ChildPsychWaitingListCardGradeRule records
      operationId: listChildPsychWaitingListCardGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ChildPsychWaitingListCardGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChildPsychWaitingListCardGradeRule"
    post:
      summary: Create a new ChildPsychWaitingListCardGradeRule
      operationId: createChildPsychWaitingListCardGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChildPsychWaitingListCardGradeRule"
      responses:
        "201":
          description: The newly created ChildPsychWaitingListCardGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildPsychWaitingListCardGradeRule"
        "400":
          description: Validation error
  /child-psych-waiting-list-card-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ChildPsychWaitingListCardGradeRule by id
      operationId: getChildPsychWaitingListCardGradeRule
      responses:
        "200":
          description: The requested ChildPsychWaitingListCardGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildPsychWaitingListCardGradeRule"
        "404":
          description: ChildPsychWaitingListCardGradeRule not found
    patch:
      summary: Update an existing ChildPsychWaitingListCardGradeRule
      operationId: updateChildPsychWaitingListCardGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChildPsychWaitingListCardGradeRule"
      responses:
        "200":
          description: The updated ChildPsychWaitingListCardGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildPsychWaitingListCardGradeRule"
        "404":
          description: ChildPsychWaitingListCardGradeRule not found
    delete:
      summary: Soft-delete a ChildPsychWaitingListCardGradeRule
      operationId: deleteChildPsychWaitingListCardGradeRule
      responses:
        "204":
          description: ChildPsychWaitingListCardGradeRule deleted
        "404":
          description: ChildPsychWaitingListCardGradeRule not found
components:
  schemas:
    ChildPsychWaitingListCardGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        child_psych_waiting_list_card_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade row.
        rule_id:
          type: string
          maxLength: 50
          description: Stable rule identifier (e.g. R-WTS-BREACHED-001).
        instrument:
          type: string
          enum:
            - waiting-time-status
            - clinical-priority
            - long-wait
          description: "Scoring instrument the rule belongs to: waiting-time-status, clinical-priority, long-wait."
        band:
          type: string
          enum:
            - within-target
            - approaching-breach
            - breached
            - long-wait
            - ""
          description: Band contributed by this rule (e.g. breached).
        category:
          type: string
          maxLength: 50
          description: Category descriptor (e.g. rtt-18-week, priority-target, long-waiter).
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - child_psych_waiting_list_card_grade_id
        - rule_id
        - instrument
        - band
        - category
        - description
      description: Audit trail of every waiting-time-status / clinical-priority / long-wait rule that fired during grading of a waiting list card.
