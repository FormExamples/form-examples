# Generated from sql/. Do not edit by hand.
# SOURCE: biopsy_test_result
openapi: 3.1.0
info:
  title: Biopsy Test Result — BiopsyTestResult
  version: 1.0.0
  description: Biopsy histopathology result (report). Captures the specimen and procedure, clinical history, the macroscopic and microscopic descriptions, the diagnosis with malignancy status, tumour type, histological grade, resection margins, lymphovascular invasion, immunohistochemistry and molecular results, the impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /biopsy-test-results:
    get:
      summary: List BiopsyTestResult records
      operationId: listBiopsyTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of BiopsyTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BiopsyTestResult"
    post:
      summary: Create a new BiopsyTestResult
      operationId: createBiopsyTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BiopsyTestResult"
      responses:
        "201":
          description: The newly created BiopsyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BiopsyTestResult"
        "400":
          description: Validation error
  /biopsy-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one BiopsyTestResult by id
      operationId: getBiopsyTestResult
      responses:
        "200":
          description: The requested BiopsyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BiopsyTestResult"
        "404":
          description: BiopsyTestResult not found
    patch:
      summary: Update an existing BiopsyTestResult
      operationId: updateBiopsyTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BiopsyTestResult"
      responses:
        "200":
          description: The updated BiopsyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BiopsyTestResult"
        "404":
          description: BiopsyTestResult not found
    delete:
      summary: Soft-delete a BiopsyTestResult
      operationId: deleteBiopsyTestResult
      responses:
        "204":
          description: BiopsyTestResult deleted
        "404":
          description: BiopsyTestResult not found
components:
  schemas:
    BiopsyTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (consultant histopathologist / cytopathologist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating biopsy test request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - supplementary
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, supplementary, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the biopsy specimen was taken / received.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        biopsy_site:
          type: string
          enum:
            - skin
            - breast
            - lymph-node
            - liver
            - kidney
            - prostate
            - lung
            - bone-marrow
            - gi-tract
            - thyroid
            - soft-tissue
            - other
            - ""
          description: "Anatomical biopsy site: skin, breast, lymph-node, liver, kidney, prostate, lung, bone-marrow, gi-tract, thyroid, soft-tissue, other."
        biopsy_method:
          type: string
          enum:
            - punch
            - excision
            - incision
            - core-needle
            - fine-needle-aspiration
            - image-guided
            - endoscopic
            - other
            - ""
          description: "Biopsy method: punch, excision, incision, core-needle, fine-needle-aspiration, image-guided, endoscopic, other."
        specimen_adequacy:
          type: string
          enum:
            - adequate
            - suboptimal
            - inadequate
            - ""
          description: "Diagnostic adequacy of the specimen: adequate, suboptimal, inadequate."
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the biopsy was performed to answer.
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous histopathology or cytology.
        macroscopic_description:
          type: string
          maxLength: 2000
          description: Macroscopic (gross) description of the specimen as received.
        microscopic_description:
          type: string
          maxLength: 2000
          description: Microscopic (histological) description of the specimen.
        diagnosis:
          type: string
          maxLength: 2000
          description: Definitive histopathological diagnosis (the body of the report).
        malignancy_present:
          type: boolean
          description: Whether malignancy is present in the specimen.
        tumour_type:
          type: string
          maxLength: 255
          description: Tumour type / morphology (e.g. invasive ductal carcinoma, squamous cell carcinoma).
        histological_grade:
          type: string
          enum:
            - well-differentiated
            - moderately-differentiated
            - poorly-differentiated
            - undifferentiated
            - not-applicable
            - ""
          description: "Histological differentiation grade: well-differentiated, moderately-differentiated, poorly-differentiated, undifferentiated, not-applicable."
        resection_margins:
          type: string
          enum:
            - clear
            - involved
            - close
            - not-applicable
            - ""
          description: "Resection margin status: clear, involved, close, not-applicable."
        lymphovascular_invasion:
          type: boolean
          description: Whether lymphovascular invasion is present.
        immunohistochemistry:
          type: string
          maxLength: 1000
          description: Immunohistochemistry panel results and interpretation.
        molecular_results:
          type: string
          maxLength: 1000
          description: Molecular / genetic test results relevant to diagnosis or therapy.
        snomed_code:
          type: string
          maxLength: 50
          description: SNOMED CT morphology / topography code for the diagnosis.
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 100
          description: Free-text grade / diagnosis summary label (e.g. an RCPath dataset or TNM8 category).
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up, multidisciplinary-team (MDT) discussion, referral, or management.
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - biopsy_site
        - biopsy_method
        - specimen_adequacy
        - clinical_history
        - comparison_with_previous
        - macroscopic_description
        - microscopic_description
        - diagnosis
        - malignancy_present
        - tumour_type
        - histological_grade
        - resection_margins
        - lymphovascular_invasion
        - immunohistochemistry
        - molecular_results
        - snomed_code
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Biopsy histopathology result (report). Captures the specimen and procedure, clinical history, the macroscopic and microscopic descriptions, the diagnosis with malignancy status, tumour type, histological grade, resection margins, lymphovascular invasion, immunohistochemistry and molecular results, the impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
