# arc42 — Front-end Form (HTML)

Static single-page HTML + Alpine.js wizard for the arc42 architecture
documentation assessment (14 steps). Serves from the file system without a
build step.

**Status:** deferred — scheduled after the SvelteKit form (Phase 2) is complete.

Open `index.html` directly or serve with `python3 -m http.server 8080`.
