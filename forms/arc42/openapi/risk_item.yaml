# Generated from sql/. Do not edit by hand.
# SOURCE: risk_item
openapi: 3.1.0
info:
  title: Arc42 — RiskItem
  version: 1.0.0
  description: Risk or technical debt item captured in §11 Risks & Technical Debt of the arc42 documentation.
paths:
  /risk-items:
    get:
      summary: List RiskItem records
      operationId: listRiskItem
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of RiskItem records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RiskItem"
    post:
      summary: Create a new RiskItem
      operationId: createRiskItem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RiskItem"
      responses:
        "201":
          description: The newly created RiskItem
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiskItem"
        "400":
          description: Validation error
  /risk-items/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one RiskItem by id
      operationId: getRiskItem
      responses:
        "200":
          description: The requested RiskItem
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiskItem"
        "404":
          description: RiskItem not found
    patch:
      summary: Update an existing RiskItem
      operationId: updateRiskItem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RiskItem"
      responses:
        "200":
          description: The updated RiskItem
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiskItem"
        "404":
          description: RiskItem not found
    delete:
      summary: Soft-delete a RiskItem
      operationId: deleteRiskItem
      responses:
        "204":
          description: RiskItem deleted
        "404":
          description: RiskItem not found
components:
  schemas:
    RiskItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        arc42_documentation_id:
          type: string
          format: uuid
          description: Parent documentation (cascade-deleted).
        ordinal:
          type: integer
          format: int32
          description: Display/order index within the parent documentation.
        kind:
          type: string
          enum:
            - risk
            - technical-debt
            - ""
          description: "Kind of item: risk (future threat) or technical-debt (known liability)."
        name:
          type: string
          description: Short name or title of the risk or technical debt item.
        probability:
          type: string
          enum:
            - high
            - medium
            - low
            - ""
          description: "Likelihood of the risk materialising: high, medium, or low."
        impact:
          type: string
          enum:
            - high
            - medium
            - low
            - ""
          description: "Impact if the risk materialises: high, medium, or low."
        mitigation:
          type: string
          description: Action taken or planned to mitigate the risk or address the technical debt.
      required:
        - created_at
        - updated_at
        - arc42_documentation_id
        - ordinal
        - kind
        - name
        - probability
        - impact
        - mitigation
      description: Risk or technical debt item captured in §11 Risks & Technical Debt of the arc42 documentation.
