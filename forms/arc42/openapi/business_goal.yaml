# Generated from sql/. Do not edit by hand.
# SOURCE: business_goal
openapi: 3.1.0
info:
  title: Arc42 — BusinessGoal
  version: 1.0.0
  description: Business goal captured in §1 Introduction & Goals of the arc42 documentation.
paths:
  /business-goals:
    get:
      summary: List BusinessGoal records
      operationId: listBusinessGoal
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of BusinessGoal records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BusinessGoal"
    post:
      summary: Create a new BusinessGoal
      operationId: createBusinessGoal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BusinessGoal"
      responses:
        "201":
          description: The newly created BusinessGoal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BusinessGoal"
        "400":
          description: Validation error
  /business-goals/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one BusinessGoal by id
      operationId: getBusinessGoal
      responses:
        "200":
          description: The requested BusinessGoal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BusinessGoal"
        "404":
          description: BusinessGoal not found
    patch:
      summary: Update an existing BusinessGoal
      operationId: updateBusinessGoal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BusinessGoal"
      responses:
        "200":
          description: The updated BusinessGoal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BusinessGoal"
        "404":
          description: BusinessGoal not found
    delete:
      summary: Soft-delete a BusinessGoal
      operationId: deleteBusinessGoal
      responses:
        "204":
          description: BusinessGoal deleted
        "404":
          description: BusinessGoal not found
components:
  schemas:
    BusinessGoal:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        arc42_documentation_id:
          type: string
          format: uuid
          description: Parent documentation (cascade-deleted).
        ordinal:
          type: integer
          format: int32
          description: Display/order index within the parent documentation.
        name:
          type: string
          description: Short name or title of the business goal.
        description:
          type: string
          description: Detailed description of the business goal.
      required:
        - created_at
        - updated_at
        - arc42_documentation_id
        - ordinal
        - name
        - description
      description: Business goal captured in §1 Introduction & Goals of the arc42 documentation.
