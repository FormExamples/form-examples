# arc42 — Dashboard (HTML)

Static HTML + Alpine.js dashboard listing recent arc42 assessments with the
computed maturity, total score, and top-three gaps. Read-only review view.

**Status:** deferred — scheduled after the SvelteKit dashboard (Phase 2) is complete.

Open `index.html` directly or serve with `python3 -m http.server 8080`.
