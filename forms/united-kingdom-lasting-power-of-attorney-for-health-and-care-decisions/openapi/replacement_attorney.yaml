# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: replacement_attorney
openapi: 3.1.0
info:
  title: United Kingdom Lasting Power Of Attorney For Health And Care Decisions — ReplacementAttorney
  version: 1.0.0
  description: "Replacement attorney: steps in if a primary attorney cannot act. Same MCA 2005 s.10 eligibility rules apply."
paths:
  /replacement-attorneys:
    get:
      summary: List ReplacementAttorney records
      operationId: listReplacementAttorney
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ReplacementAttorney records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReplacementAttorney"
    post:
      summary: Create a new ReplacementAttorney
      operationId: createReplacementAttorney
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplacementAttorney"
      responses:
        "201":
          description: The newly created ReplacementAttorney
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplacementAttorney"
        "400":
          description: Validation error
  /replacement-attorneys/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ReplacementAttorney by id
      operationId: getReplacementAttorney
      responses:
        "200":
          description: The requested ReplacementAttorney
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplacementAttorney"
        "404":
          description: ReplacementAttorney not found
    patch:
      summary: Update an existing ReplacementAttorney
      operationId: updateReplacementAttorney
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplacementAttorney"
      responses:
        "200":
          description: The updated ReplacementAttorney
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplacementAttorney"
        "404":
          description: ReplacementAttorney not found
    delete:
      summary: Soft-delete a ReplacementAttorney
      operationId: deleteReplacementAttorney
      responses:
        "204":
          description: ReplacementAttorney deleted
        "404":
          description: ReplacementAttorney not found
components:
  schemas:
    ReplacementAttorney:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        title:
          type: string
          maxLength: 20
          description: Honorific title.
        given_names:
          type: string
          maxLength: 255
          description: Given names.
        family_name:
          type: string
          maxLength: 255
          description: Family name.
        birth_date:
          type: string
          format: date
          description: Date of birth. Used to verify age ≥ 18.
        email:
          type: string
          description: Email address.
        phone:
          type: string
          description: Phone number.
        postal_address_as_full_text:
          type: string
          description: Postal address as full text.
        country_as_iso_3166_1_alpha_2:
          type: string
          maxLength: 2
          description: Country as ISO 3166-1 alpha-2.
        postcode:
          type: string
          description: Postal code.
        relationship_to_donor:
          type: string
          enum:
            - spouse
            - civil-partner
            - child
            - parent
            - sibling
            - friend
            - professional
            - other
            - ""
          description: Stated relationship to the donor.
        is_bankrupt:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the replacement attorney is currently bankrupt.
        capacity_declared:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the replacement attorney has declared capacity to act.
      required:
        - created_at
        - updated_at
        - title
        - given_names
        - family_name
        - email
        - phone
        - postal_address_as_full_text
        - country_as_iso_3166_1_alpha_2
        - postcode
        - relationship_to_donor
        - is_bankrupt
        - capacity_declared
      description: "Replacement attorney: steps in if a primary attorney cannot act. Same MCA 2005 s.10 eligibility rules apply."
