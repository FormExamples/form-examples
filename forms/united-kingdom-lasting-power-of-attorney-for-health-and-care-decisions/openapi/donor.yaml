# Generated from sql/. Do not edit by hand.
# SOURCE: donor
openapi: 3.1.0
info:
  title: United Kingdom Lasting Power Of Attorney For Health And Care Decisions — Donor
  version: 1.0.0
  description: The person creating the LPA. Must be ≥ 18 with mental capacity at signing per Mental Capacity Act 2005 s.9.
paths:
  /donors:
    get:
      summary: List Donor records
      operationId: listDonor
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Donor records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Donor"
    post:
      summary: Create a new Donor
      operationId: createDonor
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Donor"
      responses:
        "201":
          description: The newly created Donor
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Donor"
        "400":
          description: Validation error
  /donors/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Donor by id
      operationId: getDonor
      responses:
        "200":
          description: The requested Donor
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Donor"
        "404":
          description: Donor not found
    patch:
      summary: Update an existing Donor
      operationId: updateDonor
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Donor"
      responses:
        "200":
          description: The updated Donor
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Donor"
        "404":
          description: Donor not found
    delete:
      summary: Soft-delete a Donor
      operationId: deleteDonor
      responses:
        "204":
          description: Donor deleted
        "404":
          description: Donor not found
components:
  schemas:
    Donor:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        title:
          type: string
          maxLength: 20
          description: Honorific title (Mr, Mrs, Dr, …).
        given_names:
          type: string
          maxLength: 255
          description: Given names of the donor.
        family_name:
          type: string
          maxLength: 255
          description: Family name of the donor.
        other_names_used:
          type: string
          maxLength: 500
          description: Any other names the donor uses (LP1H s.1 requires this be listed).
        birth_date:
          type: string
          format: date
          description: Date of birth. Used to verify donor age ≥ 18 at signing (R-MCA-S9-AGE).
        email:
          type: string
          description: Email address.
        phone:
          type: string
          description: Phone number.
        postal_address_as_full_text:
          type: string
          description: Postal address as full text.
        country_as_iso_3166_1_alpha_2:
          type: string
          maxLength: 2
          description: Country as ISO 3166-1 alpha-2 (expected GB for England and Wales).
        postcode:
          type: string
          description: Postal code.
        united_kingdom_nhs_number:
          type: string
          maxLength: 20
          description: United Kingdom NHS number, optional but useful for cross-form ADRT check.
        jurisdiction:
          type: string
          enum:
            - england
            - wales
            - ""
          description: "Jurisdiction within the UK: england or wales. Scotland and Northern Ireland use different schemes."
        preferred_language:
          type: string
          enum:
            - en
            - cy
            - ""
          description: "Preferred language: en (English) or cy (Cymraeg / Welsh)."
        capacity_declared:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the donor has declared capacity at the point of signing the LPA.
        capacity_declared_at:
          type: string
          format: date-time
          description: Timestamp at which capacity was declared.
      required:
        - created_at
        - updated_at
        - title
        - given_names
        - family_name
        - other_names_used
        - email
        - phone
        - postal_address_as_full_text
        - country_as_iso_3166_1_alpha_2
        - postcode
        - jurisdiction
        - preferred_language
        - capacity_declared
      description: The person creating the LPA. Must be ≥ 18 with mental capacity at signing per Mental Capacity Act 2005 s.9.
