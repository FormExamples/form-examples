# Tasks: HTML LPA dashboard

- [x] `index.html` shell with custom CSS and ordered classic `<script>` tags.
- [x] `js/types.js` — JSDoc type aliases.
- [x] `js/data.js` — fallback sample dataset (5 LPAs across all statuses).
- [x] `js/api.js` — `fetch` wrapper for the Loco backend.
- [x] `js/app.js` — filters, sort, render, status banner on fallback.
- [x] Sortable column headers with `aria-sort` indicators.
- [x] Validity-status, registration-stage, jurisdiction, and decision-rule filters.
- [x] Search box matching reference, donor name, or fired-rule ID.
- [x] Inline fired-rules summary chip per row.
- [x] `css/style.css` — vanilla CSS, no framework dependency.
- [ ] Inline drilldown row with fired rules and flags (deferred — drilldown
      lives on the SvelteKit dashboard).
- [ ] Print stylesheet for case-file printout (deferred).
- [ ] Cross-browser smoke test (deferred).
