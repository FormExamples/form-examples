# Generated from sql/. Do not edit by hand.
# SOURCE: hypertension_review_grade_flag
openapi: 3.1.0
info:
  title: Hypertension Review — HypertensionReviewGradeFlag
  version: 1.0.0
  description: Flags that fire independently of the control class and completeness status, with priority and a suggested action for the clinician or governance team.
paths:
  /hypertension-review-grade-flags:
    get:
      summary: List HypertensionReviewGradeFlag records
      operationId: listHypertensionReviewGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HypertensionReviewGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HypertensionReviewGradeFlag"
    post:
      summary: Create a new HypertensionReviewGradeFlag
      operationId: createHypertensionReviewGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HypertensionReviewGradeFlag"
      responses:
        "201":
          description: The newly created HypertensionReviewGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HypertensionReviewGradeFlag"
        "400":
          description: Validation error
  /hypertension-review-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HypertensionReviewGradeFlag by id
      operationId: getHypertensionReviewGradeFlag
      responses:
        "200":
          description: The requested HypertensionReviewGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HypertensionReviewGradeFlag"
        "404":
          description: HypertensionReviewGradeFlag not found
    patch:
      summary: Update an existing HypertensionReviewGradeFlag
      operationId: updateHypertensionReviewGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HypertensionReviewGradeFlag"
      responses:
        "200":
          description: The updated HypertensionReviewGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HypertensionReviewGradeFlag"
        "404":
          description: HypertensionReviewGradeFlag not found
    delete:
      summary: Soft-delete a HypertensionReviewGradeFlag
      operationId: deleteHypertensionReviewGradeFlag
      responses:
        "204":
          description: HypertensionReviewGradeFlag deleted
        "404":
          description: HypertensionReviewGradeFlag not found
components:
  schemas:
    HypertensionReviewGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        hypertension_review_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-SEVERE-HYPERTENSION-001).
        category:
          type: string
          enum:
            - severe-hypertension
            - uncontrolled-bp
            - missing-bloods
            - missing-acr
            - high-cv-risk-untreated
            - adherence-concern
            - postural-drop
            - incomplete
            - other
            - ""
          description: "Flag category: severe-hypertension, uncontrolled-bp, missing-bloods, missing-acr, high-cv-risk-untreated, adherence-concern, postural-drop, incomplete, or other."
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical or governance action (e.g. \"arrange same-day assessment\", \"step up antihypertensive medication\")."
      required:
        - created_at
        - updated_at
        - hypertension_review_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Flags that fire independently of the control class and completeness status, with priority and a suggested action for the clinician or governance team.
