# Generated from sql/. Do not edit by hand.
# SOURCE: hypertension_review_grade_rule
openapi: 3.1.0
info:
  title: Hypertension Review — HypertensionReviewGradeRule
  version: 1.0.0
  description: "Audit trail of every classification or completeness rule that fired during grading: section, category, and description."
paths:
  /hypertension-review-grade-rules:
    get:
      summary: List HypertensionReviewGradeRule records
      operationId: listHypertensionReviewGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HypertensionReviewGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HypertensionReviewGradeRule"
    post:
      summary: Create a new HypertensionReviewGradeRule
      operationId: createHypertensionReviewGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HypertensionReviewGradeRule"
      responses:
        "201":
          description: The newly created HypertensionReviewGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HypertensionReviewGradeRule"
        "400":
          description: Validation error
  /hypertension-review-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HypertensionReviewGradeRule by id
      operationId: getHypertensionReviewGradeRule
      responses:
        "200":
          description: The requested HypertensionReviewGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HypertensionReviewGradeRule"
        "404":
          description: HypertensionReviewGradeRule not found
    patch:
      summary: Update an existing HypertensionReviewGradeRule
      operationId: updateHypertensionReviewGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HypertensionReviewGradeRule"
      responses:
        "200":
          description: The updated HypertensionReviewGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HypertensionReviewGradeRule"
        "404":
          description: HypertensionReviewGradeRule not found
    delete:
      summary: Soft-delete a HypertensionReviewGradeRule
      operationId: deleteHypertensionReviewGradeRule
      responses:
        "204":
          description: HypertensionReviewGradeRule deleted
        "404":
          description: HypertensionReviewGradeRule not found
components:
  schemas:
    HypertensionReviewGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        hypertension_review_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-TARGET-CKD-TIGHT-01).
        section:
          type: string
          enum:
            - target
            - control
            - stage
            - completeness
            - ""
          description: "Section the rule concerns: target, control, stage, or completeness."
        category:
          type: string
          maxLength: 50
          description: Subject category (e.g. target-selection, control-classification, staging, required-component).
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - hypertension_review_grade_id
        - rule_id
        - section
        - category
        - description
      description: "Audit trail of every classification or completeness rule that fired during grading: section, category, and description."
