# Generated from sql/. Do not edit by hand.
# SOURCE: hypertension_review
openapi: 3.1.0
info:
  title: Hypertension Review — HypertensionReview
  version: 1.0.0
  description: "Main hypertension annual-review record (NICE NG136): patient and clinician identification, review context, diagnosis and comorbidity target drivers, clinic and home/ambulatory blood pressure, medication and adherence, cardiovascular risk, annual bloods, urine ACR, lifestyle, and complications. The computed control classification, completeness grade, fired rules, and flags live in child tables."
paths:
  /hypertension-reviews:
    get:
      summary: List HypertensionReview records
      operationId: listHypertensionReview
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HypertensionReview records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HypertensionReview"
    post:
      summary: Create a new HypertensionReview
      operationId: createHypertensionReview
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HypertensionReview"
      responses:
        "201":
          description: The newly created HypertensionReview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HypertensionReview"
        "400":
          description: Validation error
  /hypertension-reviews/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HypertensionReview by id
      operationId: getHypertensionReview
      responses:
        "200":
          description: The requested HypertensionReview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HypertensionReview"
        "404":
          description: HypertensionReview not found
    patch:
      summary: Update an existing HypertensionReview
      operationId: updateHypertensionReview
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HypertensionReview"
      responses:
        "200":
          description: The updated HypertensionReview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HypertensionReview"
        "404":
          description: HypertensionReview not found
    delete:
      summary: Soft-delete a HypertensionReview
      operationId: deleteHypertensionReview
      responses:
        "204":
          description: HypertensionReview deleted
        "404":
          description: HypertensionReview not found
components:
  schemas:
    HypertensionReview:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient this review documents (restrict delete).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reviewing clinician (restrict delete); optional.
        clinician_role:
          type: string
          enum:
            - gp
            - practice-nurse
            - pharmacist
            - other
            - ""
          description: "Reviewing clinician role: gp, practice-nurse, pharmacist, or other."
        reviewed_at:
          type: string
          format: date
          description: Date the annual review was conducted.
        practice_site:
          type: string
          maxLength: 200
          description: Practice or site where the review was conducted.
        patient_identifier:
          type: string
          maxLength: 100
          description: NHS number or local patient identifier as recorded on the review.
        age_band:
          type: string
          enum:
            - 18-39
            - 40-59
            - 60-79
            - >=80
            - ""
          description: "Adult age band that drives the blood-pressure target: 18-39, 40-59, 60-79, or >=80 (>=80 relaxes the clinic target to 150/90)."
        sex:
          type: string
          enum:
            - female
            - male
            - intersex
            - unknown
            - ""
          description: "Patient sex: female, male, intersex, or unknown."
        ethnicity:
          type: string
          enum:
            - white
            - black-african-caribbean
            - south-asian
            - mixed
            - other
            - ""
          description: "Patient ethnicity: white, black-african-caribbean, south-asian, mixed, or other."
        diagnosis_date:
          type: string
          format: date
          description: Date of hypertension diagnosis.
        type2_diabetes:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient has type 2 diabetes (yes/no); a blood-pressure target driver.
        chronic_kidney_disease:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient has chronic kidney disease (yes/no); with diabetes or ACR >= 70 it tightens the target to 130/80.
        established_cvd:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient has established cardiovascular disease (yes/no).
        atrial_fibrillation:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient has atrial fibrillation (yes/no).
        clinic_systolic:
          type: integer
          format: int32
          description: Clinic systolic blood pressure in mmHg (best of repeated seated readings).
        clinic_diastolic:
          type: integer
          format: int32
          description: Clinic diastolic blood pressure in mmHg (best of repeated seated readings).
        home_systolic:
          type: integer
          format: int32
          description: Home/ambulatory (HBPM/ABPM) daytime-average systolic blood pressure in mmHg.
        home_diastolic:
          type: integer
          format: int32
          description: Home/ambulatory (HBPM/ABPM) daytime-average diastolic blood pressure in mmHg.
        monitoring_method:
          type: string
          enum:
            - clinic-only
            - hbpm
            - abpm
            - ""
          description: "Blood-pressure monitoring method: clinic-only, hbpm (home), or abpm (ambulatory)."
        postural_drop:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether a symptomatic postural systolic fall >= 20 mmHg is present (yes/no).
        antihypertensive_agents:
          type: integer
          format: int32
          description: Count of current antihypertensive agents.
        adherence:
          type: string
          enum:
            - good
            - partial
            - poor
            - ""
          description: "Self-reported medication adherence: good, partial, or poor."
        side_effects:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether troublesome medication side effects are present (yes/no).
        qrisk_percent:
          type: number
          format: double
          description: QRISK 10-year cardiovascular risk as a percentage.
        smoking_status:
          type: string
          enum:
            - never
            - ex
            - current
            - ""
          description: "Smoking status: never, ex, or current."
        statin_therapy:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient is on a statin (yes/no).
        bmi:
          type: number
          format: double
          description: Body mass index in kg/m^2.
        lifestyle_advice:
          type: string
          description: Free-text record of lifestyle advice given (diet, exercise, alcohol, salt, smoking cessation).
        serum_creatinine:
          type: number
          format: double
          description: Serum creatinine in micromol/L (U&E).
        egfr:
          type: number
          format: double
          description: Estimated glomerular filtration rate in mL/min/1.73m^2 (U&E).
        serum_potassium:
          type: number
          format: double
          description: Serum potassium in mmol/L (U&E).
        hba1c:
          type: number
          format: double
          description: HbA1c in mmol/mol (glycaemic control).
        total_cholesterol:
          type: number
          format: double
          description: Total cholesterol in mmol/L (lipids).
        hdl_cholesterol:
          type: number
          format: double
          description: HDL cholesterol in mmol/L (lipids).
        urine_acr:
          type: number
          format: double
          description: Urine albumin:creatinine ratio in mg/mmol.
        complications:
          type: string
          description: Free-text record of hypertension-related complications or target-organ damage.
        review_context:
          type: string
          description: Optional free-text review context or narrative shown in the summary.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_role
        - practice_site
        - patient_identifier
        - age_band
        - sex
        - ethnicity
        - type2_diabetes
        - chronic_kidney_disease
        - established_cvd
        - atrial_fibrillation
        - monitoring_method
        - postural_drop
        - adherence
        - side_effects
        - smoking_status
        - statin_therapy
        - lifestyle_advice
        - complications
        - review_context
      description: "Main hypertension annual-review record (NICE NG136): patient and clinician identification, review context, diagnosis and comorbidity target drivers, clinic and home/ambulatory blood pressure, medication and adherence, cardiovascular risk, annual bloods, urine ACR, lifestyle, and complications. The computed control classification, completeness grade, fired rules, and flags live in child tables."
