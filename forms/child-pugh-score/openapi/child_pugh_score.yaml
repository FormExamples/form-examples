# Generated from sql/. Do not edit by hand.
# SOURCE: child_pugh_score
openapi: 3.1.0
info:
  title: Child Pugh Score — ChildPughScore
  version: 1.0.0
  description: "Main Child-Pugh assessment record: assessment context, patient identification, and the five scored parameter inputs (total bilirubin, serum albumin, coagulation, ascites, hepatic encephalopathy)."
paths:
  /child-pugh-scores:
    get:
      summary: List ChildPughScore records
      operationId: listChildPughScore
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ChildPughScore records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChildPughScore"
    post:
      summary: Create a new ChildPughScore
      operationId: createChildPughScore
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChildPughScore"
      responses:
        "201":
          description: The newly created ChildPughScore
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildPughScore"
        "400":
          description: Validation error
  /child-pugh-scores/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ChildPughScore by id
      operationId: getChildPughScore
      responses:
        "200":
          description: The requested ChildPughScore
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildPughScore"
        "404":
          description: ChildPughScore not found
    patch:
      summary: Update an existing ChildPughScore
      operationId: updateChildPughScore
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChildPughScore"
      responses:
        "200":
          description: The updated ChildPughScore
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildPughScore"
        "404":
          description: ChildPughScore not found
    delete:
      summary: Soft-delete a ChildPughScore
      operationId: deleteChildPughScore
      responses:
        "204":
          description: ChildPughScore deleted
        "404":
          description: ChildPughScore not found
components:
  schemas:
    ChildPughScore:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient being assessed (delete restricted).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the assessing clinician (optional; delete restricted).
        clinician_name:
          type: string
          maxLength: 255
          description: Name of the assessing clinician as recorded on the assessment.
        clinician_role:
          type: string
          enum:
            - hepatologist
            - surgeon
            - anaesthetist
            - physician
            - nurse
            - other
            - ""
          description: "Role of the assessing clinician: hepatologist, surgeon, anaesthetist, physician, nurse, or other."
        assessed_at:
          type: string
          format: date-time
          description: Date and time the assessment was performed.
        care_setting:
          type: string
          enum:
            - hepatology-clinic
            - ward
            - pre-operative
            - intensive-care
            - other
            - ""
          description: "Care setting: hepatology-clinic, ward, pre-operative, intensive-care, or other."
        aetiology:
          type: string
          enum:
            - alcohol
            - viral-hepatitis
            - nafld
            - autoimmune
            - cholestatic
            - other
            - ""
          description: "Aetiology of liver disease: alcohol, viral-hepatitis, nafld, autoimmune, cholestatic, or other."
        patient_identifier:
          type: string
          maxLength: 100
          description: Local patient identifier as recorded on the assessment.
        age_band:
          type: string
          enum:
            - 16-39
            - 40-59
            - 60-74
            - 75-plus
            - ""
          description: "Adult age band: 16-39, 40-59, 60-74, or 75-plus."
        sex:
          type: string
          enum:
            - female
            - male
            - intersex
            - unknown
            - ""
          description: "Patient sex recorded for the assessment: female, male, intersex, or unknown."
        total_bilirubin_umol_l:
          type: number
          format: double
          description: Parameter 1 — measured total bilirubin in micromoles per litre (umol/L).
        serum_albumin_g_l:
          type: number
          format: double
          description: Parameter 2 — measured serum albumin in grams per litre (g/L).
        inr:
          type: number
          format: double
          description: Parameter 3 — International Normalised Ratio (INR); preferred coagulation measure.
        prothrombin_time_seconds:
          type: number
          format: double
          description: Parameter 3 — prothrombin-time prolongation in seconds; fallback used when INR is unavailable.
        ascites:
          type: string
          enum:
            - none
            - mild
            - moderate-severe
            - ""
          description: "Parameter 4 — clinical ascites grade: none, mild (diuretic-responsive), or moderate-severe (refractory)."
        hepatic_encephalopathy:
          type: string
          enum:
            - none
            - grade-1-2
            - grade-3-4
            - ""
          description: "Parameter 5 — hepatic encephalopathy grade: none, grade-1-2, or grade-3-4."
        clinical_note:
          type: string
          description: Free-text clinical note recorded with the assessment.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_name
        - clinician_role
        - care_setting
        - aetiology
        - patient_identifier
        - age_band
        - sex
        - ascites
        - hepatic_encephalopathy
        - clinical_note
      description: "Main Child-Pugh assessment record: assessment context, patient identification, and the five scored parameter inputs (total bilirubin, serum albumin, coagulation, ascites, hepatic encephalopathy)."
