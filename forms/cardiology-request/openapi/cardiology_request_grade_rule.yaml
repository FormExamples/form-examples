# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: cardiology_request_grade_rule
openapi: 3.1.0
info:
  title: Cardiology Request — CardiologyRequestGradeRule
  version: 1.0.0
  description: Audit trail of every appropriateness / safety / completeness / triage rule that fired for this referral.
paths:
  /cardiology-request-grade-rules:
    get:
      summary: List CardiologyRequestGradeRule records
      operationId: listCardiologyRequestGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CardiologyRequestGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CardiologyRequestGradeRule"
    post:
      summary: Create a new CardiologyRequestGradeRule
      operationId: createCardiologyRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardiologyRequestGradeRule"
      responses:
        "201":
          description: The newly created CardiologyRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiologyRequestGradeRule"
        "400":
          description: Validation error
  /cardiology-request-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CardiologyRequestGradeRule by id
      operationId: getCardiologyRequestGradeRule
      responses:
        "200":
          description: The requested CardiologyRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiologyRequestGradeRule"
        "404":
          description: CardiologyRequestGradeRule not found
    patch:
      summary: Update an existing CardiologyRequestGradeRule
      operationId: updateCardiologyRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardiologyRequestGradeRule"
      responses:
        "200":
          description: The updated CardiologyRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiologyRequestGradeRule"
        "404":
          description: CardiologyRequestGradeRule not found
    delete:
      summary: Soft-delete a CardiologyRequestGradeRule
      operationId: deleteCardiologyRequestGradeRule
      responses:
        "204":
          description: CardiologyRequestGradeRule deleted
        "404":
          description: CardiologyRequestGradeRule not found
components:
  schemas:
    CardiologyRequestGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        cardiology_request_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-APPROP-CHEST-PAIN-01).
        axis:
          type: string
          enum:
            - appropriateness
            - safety
            - completeness
            - triage
            - ""
          description: "Scoring axis the rule belongs to: appropriateness, safety, completeness, triage."
        category:
          type: string
          maxLength: 50
          description: Category or reason the rule relates to.
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - cardiology_request_grade_id
        - rule_id
        - axis
        - category
        - description
      description: Audit trail of every appropriateness / safety / completeness / triage rule that fired for this referral.
