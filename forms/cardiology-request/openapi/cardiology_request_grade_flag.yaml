# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: cardiology_request_grade_flag
openapi: 3.1.0
info:
  title: Cardiology Request — CardiologyRequestGradeFlag
  version: 1.0.0
  description: Safety-critical flags that fire independently of the four axes, with priority and a suggested action for the cardiology triage team.
paths:
  /cardiology-request-grade-flags:
    get:
      summary: List CardiologyRequestGradeFlag records
      operationId: listCardiologyRequestGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CardiologyRequestGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CardiologyRequestGradeFlag"
    post:
      summary: Create a new CardiologyRequestGradeFlag
      operationId: createCardiologyRequestGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardiologyRequestGradeFlag"
      responses:
        "201":
          description: The newly created CardiologyRequestGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiologyRequestGradeFlag"
        "400":
          description: Validation error
  /cardiology-request-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CardiologyRequestGradeFlag by id
      operationId: getCardiologyRequestGradeFlag
      responses:
        "200":
          description: The requested CardiologyRequestGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiologyRequestGradeFlag"
        "404":
          description: CardiologyRequestGradeFlag not found
    patch:
      summary: Update an existing CardiologyRequestGradeFlag
      operationId: updateCardiologyRequestGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardiologyRequestGradeFlag"
      responses:
        "200":
          description: The updated CardiologyRequestGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiologyRequestGradeFlag"
        "404":
          description: CardiologyRequestGradeFlag not found
    delete:
      summary: Soft-delete a CardiologyRequestGradeFlag
      operationId: deleteCardiologyRequestGradeFlag
      responses:
        "204":
          description: CardiologyRequestGradeFlag deleted
        "404":
          description: CardiologyRequestGradeFlag not found
components:
  schemas:
    CardiologyRequestGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        cardiology_request_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-SUSPECTED-ACS-001).
        category:
          type: string
          enum:
            - suspected-acs
            - exertional-syncope
            - new-onset-heart-failure
            - red-flag-chest-pain
            - missing-reason
            - missing-clinical-question
            - other
            - ""
          description: Flag category.
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical action (e.g. \"divert to emergency ACS pathway; do not wait for routine clinic\")."
      required:
        - created_at
        - updated_at
        - cardiology_request_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Safety-critical flags that fire independently of the four axes, with priority and a suggested action for the cardiology triage team.
