# Cardiology Request — HTML front-end (form + dashboard)

Consolidated single-directory HTML front-end: `index.html` is the single-page
referral wizard; `dashboard.html` is the vetting dashboard. Shared `css/` and
`js/` (the four-axis vetting engine in `js/{types,rules,flags,grader}.js`).
