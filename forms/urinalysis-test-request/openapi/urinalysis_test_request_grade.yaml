# Generated from sql/. Do not edit by hand.
# SOURCE: urinalysis_test_request_grade
openapi: 3.1.0
info:
  title: Urinalysis Test Request — UrinalysisTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (indication match / NICE UTI, 1-9), preanalytical specimen suitability, request completeness, and triage priority."
paths:
  /urinalysis-test-request-grades:
    get:
      summary: List UrinalysisTestRequestGrade records
      operationId: listUrinalysisTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of UrinalysisTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UrinalysisTestRequestGrade"
    post:
      summary: Create a new UrinalysisTestRequestGrade
      operationId: createUrinalysisTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UrinalysisTestRequestGrade"
      responses:
        "201":
          description: The newly created UrinalysisTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UrinalysisTestRequestGrade"
        "400":
          description: Validation error
  /urinalysis-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one UrinalysisTestRequestGrade by id
      operationId: getUrinalysisTestRequestGrade
      responses:
        "200":
          description: The requested UrinalysisTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UrinalysisTestRequestGrade"
        "404":
          description: UrinalysisTestRequestGrade not found
    patch:
      summary: Update an existing UrinalysisTestRequestGrade
      operationId: updateUrinalysisTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UrinalysisTestRequestGrade"
      responses:
        "200":
          description: The updated UrinalysisTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UrinalysisTestRequestGrade"
        "404":
          description: UrinalysisTestRequestGrade not found
    delete:
      summary: Soft-delete a UrinalysisTestRequestGrade
      operationId: deleteUrinalysisTestRequestGrade
      responses:
        "204":
          description: UrinalysisTestRequestGrade deleted
        "404":
          description: UrinalysisTestRequestGrade not found
components:
  schemas:
    UrinalysisTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        urinalysis_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9) anchored on indication-to-test match and guideline appropriateness (no single published urinalysis score exists).
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        preanalytical_band:
          type: string
          enum:
            - ok
            - caution
            - reject-risk
            - ""
          description: "Preanalytical specimen-suitability band: ok, caution, reject-risk (driven by specimen type / collected / timing / contamination risk per UK SMI B41)."
        fasting_or_specimen:
          type: string
          maxLength: 500
          description: "Specimen / preanalytical advisory note (e.g. \"MSU not yet collected; refrigerate or use boric acid if >4 h to lab\")."
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields, clinical details and indication weighted highest (0-100).
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - stat
            - ""
          description: "Assigned triage tier: routine, urgent, stat."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the test implied by the triage tier.
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - urinalysis_test_request_id
        - appropriateness_band
        - preanalytical_band
        - fasting_or_specimen
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (indication match / NICE UTI, 1-9), preanalytical specimen suitability, request completeness, and triage priority."
