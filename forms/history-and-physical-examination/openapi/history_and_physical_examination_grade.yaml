# Generated from sql/. Do not edit by hand.
# SOURCE: history_and_physical_examination_grade
openapi: 3.1.0
info:
  title: History And Physical Examination — HistoryAndPhysicalExaminationGrade
  version: 1.0.0
  description: "Computed documentation-completeness grading result for an H&P clerking record: status (complete/partial/incomplete) and completeness percentage over the ten required components."
paths:
  /history-and-physical-examination-grades:
    get:
      summary: List HistoryAndPhysicalExaminationGrade records
      operationId: listHistoryAndPhysicalExaminationGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HistoryAndPhysicalExaminationGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HistoryAndPhysicalExaminationGrade"
    post:
      summary: Create a new HistoryAndPhysicalExaminationGrade
      operationId: createHistoryAndPhysicalExaminationGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HistoryAndPhysicalExaminationGrade"
      responses:
        "201":
          description: The newly created HistoryAndPhysicalExaminationGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HistoryAndPhysicalExaminationGrade"
        "400":
          description: Validation error
  /history-and-physical-examination-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HistoryAndPhysicalExaminationGrade by id
      operationId: getHistoryAndPhysicalExaminationGrade
      responses:
        "200":
          description: The requested HistoryAndPhysicalExaminationGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HistoryAndPhysicalExaminationGrade"
        "404":
          description: HistoryAndPhysicalExaminationGrade not found
    patch:
      summary: Update an existing HistoryAndPhysicalExaminationGrade
      operationId: updateHistoryAndPhysicalExaminationGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HistoryAndPhysicalExaminationGrade"
      responses:
        "200":
          description: The updated HistoryAndPhysicalExaminationGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HistoryAndPhysicalExaminationGrade"
        "404":
          description: HistoryAndPhysicalExaminationGrade not found
    delete:
      summary: Soft-delete a HistoryAndPhysicalExaminationGrade
      operationId: deleteHistoryAndPhysicalExaminationGrade
      responses:
        "204":
          description: HistoryAndPhysicalExaminationGrade deleted
        "404":
          description: HistoryAndPhysicalExaminationGrade not found
components:
  schemas:
    HistoryAndPhysicalExaminationGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        history_and_physical_examination_id:
          type: string
          format: uuid
          description: Foreign key to the parent H&P clerking record (unique, 1:1).
        status:
          type: string
          enum:
            - complete
            - partial
            - incomplete
            - ""
          description: "Completeness status: complete, partial, or incomplete."
        completeness_percent:
          type: integer
          format: int32
          description: "Completeness percentage (0..100): satisfied required components / ten total x 100."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the grade.
      required:
        - created_at
        - updated_at
        - history_and_physical_examination_id
        - status
        - graded_at
      description: "Computed documentation-completeness grading result for an H&P clerking record: status (complete/partial/incomplete) and completeness percentage over the ten required components."
