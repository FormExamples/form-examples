# Generated from sql/. Do not edit by hand.
# SOURCE: structured_medication_review_grade_flag
openapi: 3.1.0
info:
  title: Structured Medication Review — StructuredMedicationReviewGradeFlag
  version: 1.0.0
  description: Flagged issues raised independently of the burden bands, with priority and a suggested action for the reviewing clinician.
paths:
  /structured-medication-review-grade-flags:
    get:
      summary: List StructuredMedicationReviewGradeFlag records
      operationId: listStructuredMedicationReviewGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of StructuredMedicationReviewGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StructuredMedicationReviewGradeFlag"
    post:
      summary: Create a new StructuredMedicationReviewGradeFlag
      operationId: createStructuredMedicationReviewGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StructuredMedicationReviewGradeFlag"
      responses:
        "201":
          description: The newly created StructuredMedicationReviewGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StructuredMedicationReviewGradeFlag"
        "400":
          description: Validation error
  /structured-medication-review-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one StructuredMedicationReviewGradeFlag by id
      operationId: getStructuredMedicationReviewGradeFlag
      responses:
        "200":
          description: The requested StructuredMedicationReviewGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StructuredMedicationReviewGradeFlag"
        "404":
          description: StructuredMedicationReviewGradeFlag not found
    patch:
      summary: Update an existing StructuredMedicationReviewGradeFlag
      operationId: updateStructuredMedicationReviewGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StructuredMedicationReviewGradeFlag"
      responses:
        "200":
          description: The updated StructuredMedicationReviewGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StructuredMedicationReviewGradeFlag"
        "404":
          description: StructuredMedicationReviewGradeFlag not found
    delete:
      summary: Soft-delete a StructuredMedicationReviewGradeFlag
      operationId: deleteStructuredMedicationReviewGradeFlag
      responses:
        "204":
          description: StructuredMedicationReviewGradeFlag deleted
        "404":
          description: StructuredMedicationReviewGradeFlag not found
components:
  schemas:
    StructuredMedicationReviewGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        structured_medication_review_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-HIGH-ACB-001).
        category:
          type: string
          enum:
            - high-acb
            - stopp-trigger
            - start-omission
            - missing-monitoring
            - adherence-concern
            - high-risk-no-indication
            - incomplete
            - other
            - ""
          description: "Flag category: high-acb, stopp-trigger, start-omission, missing-monitoring, adherence-concern, high-risk-no-indication, incomplete, or other."
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical action (e.g. \"review sedating and anticholinergic medicines\")."
      required:
        - created_at
        - updated_at
        - structured_medication_review_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Flagged issues raised independently of the burden bands, with priority and a suggested action for the reviewing clinician.
