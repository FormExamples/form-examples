# Generated from sql/. Do not edit by hand.
# SOURCE: nursing_care_plan_grade_flag
openapi: 3.1.0
info:
  title: Nursing Care Plan — NursingCarePlanGradeFlag
  version: 1.0.0
  description: Flagged issues raised independently of the completeness status, with priority and a suggested action for the nursing team.
paths:
  /nursing-care-plan-grade-flags:
    get:
      summary: List NursingCarePlanGradeFlag records
      operationId: listNursingCarePlanGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of NursingCarePlanGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NursingCarePlanGradeFlag"
    post:
      summary: Create a new NursingCarePlanGradeFlag
      operationId: createNursingCarePlanGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NursingCarePlanGradeFlag"
      responses:
        "201":
          description: The newly created NursingCarePlanGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NursingCarePlanGradeFlag"
        "400":
          description: Validation error
  /nursing-care-plan-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one NursingCarePlanGradeFlag by id
      operationId: getNursingCarePlanGradeFlag
      responses:
        "200":
          description: The requested NursingCarePlanGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NursingCarePlanGradeFlag"
        "404":
          description: NursingCarePlanGradeFlag not found
    patch:
      summary: Update an existing NursingCarePlanGradeFlag
      operationId: updateNursingCarePlanGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NursingCarePlanGradeFlag"
      responses:
        "200":
          description: The updated NursingCarePlanGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NursingCarePlanGradeFlag"
        "404":
          description: NursingCarePlanGradeFlag not found
    delete:
      summary: Soft-delete a NursingCarePlanGradeFlag
      operationId: deleteNursingCarePlanGradeFlag
      responses:
        "204":
          description: NursingCarePlanGradeFlag deleted
        "404":
          description: NursingCarePlanGradeFlag not found
components:
  schemas:
    NursingCarePlanGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        nursing_care_plan_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-RISK-WITHOUT-INTERVENTION-001).
        category:
          type: string
          enum:
            - risk-without-intervention
            - high-risk-not-actioned
            - missing-evaluation
            - unmet-goal-overdue
            - no-review-date
            - incomplete-problem
            - other
            - ""
          description: "Flag category: risk-without-intervention, high-risk-not-actioned, missing-evaluation, unmet-goal-overdue, no-review-date, incomplete-problem, or other."
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested nursing action (e.g. \"add an intervention for the high falls risk\")."
      required:
        - created_at
        - updated_at
        - nursing_care_plan_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Flagged issues raised independently of the completeness status, with priority and a suggested action for the nursing team.
