# Generated from sql/. Do not edit by hand.
# SOURCE: nursing_care_plan_goal
openapi: 3.1.0
info:
  title: Nursing Care Plan — NursingCarePlanGoal
  version: 1.0.0
  description: SMART goals for a nursing problem; cascades from the parent problem.
paths:
  /nursing-care-plan-goals:
    get:
      summary: List NursingCarePlanGoal records
      operationId: listNursingCarePlanGoal
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of NursingCarePlanGoal records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NursingCarePlanGoal"
    post:
      summary: Create a new NursingCarePlanGoal
      operationId: createNursingCarePlanGoal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NursingCarePlanGoal"
      responses:
        "201":
          description: The newly created NursingCarePlanGoal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NursingCarePlanGoal"
        "400":
          description: Validation error
  /nursing-care-plan-goals/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one NursingCarePlanGoal by id
      operationId: getNursingCarePlanGoal
      responses:
        "200":
          description: The requested NursingCarePlanGoal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NursingCarePlanGoal"
        "404":
          description: NursingCarePlanGoal not found
    patch:
      summary: Update an existing NursingCarePlanGoal
      operationId: updateNursingCarePlanGoal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NursingCarePlanGoal"
      responses:
        "200":
          description: The updated NursingCarePlanGoal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NursingCarePlanGoal"
        "404":
          description: NursingCarePlanGoal not found
    delete:
      summary: Soft-delete a NursingCarePlanGoal
      operationId: deleteNursingCarePlanGoal
      responses:
        "204":
          description: NursingCarePlanGoal deleted
        "404":
          description: NursingCarePlanGoal not found
components:
  schemas:
    NursingCarePlanGoal:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        nursing_care_plan_problem_id:
          type: string
          format: uuid
          description: Foreign key to the parent nursing problem.
        goal_text:
          type: string
          description: The SMART (specific, measurable, achievable, relevant, time-bound) goal.
        target_date:
          type: string
          format: date
          description: Target or review date for the goal.
        met:
          type: string
          enum:
            - met
            - partially-met
            - not-met
            - not-evaluated
            - ""
          description: "Evaluation of whether the goal was met: met, partially-met, not-met, or not-evaluated."
      required:
        - created_at
        - updated_at
        - nursing_care_plan_problem_id
        - goal_text
        - met
      description: SMART goals for a nursing problem; cascades from the parent problem.
