# Generated from sql/. Do not edit by hand.
# SOURCE: genetic_test_request_grade
openapi: 3.1.0
info:
  title: Genetic Test Request — GeneticTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (NHS National Genomic Test Directory eligibility match, 1-9), consent and pre-test counselling, request completeness, and triage priority."
paths:
  /genetic-test-request-grades:
    get:
      summary: List GeneticTestRequestGrade records
      operationId: listGeneticTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of GeneticTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeneticTestRequestGrade"
    post:
      summary: Create a new GeneticTestRequestGrade
      operationId: createGeneticTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneticTestRequestGrade"
      responses:
        "201":
          description: The newly created GeneticTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneticTestRequestGrade"
        "400":
          description: Validation error
  /genetic-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one GeneticTestRequestGrade by id
      operationId: getGeneticTestRequestGrade
      responses:
        "200":
          description: The requested GeneticTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneticTestRequestGrade"
        "404":
          description: GeneticTestRequestGrade not found
    patch:
      summary: Update an existing GeneticTestRequestGrade
      operationId: updateGeneticTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneticTestRequestGrade"
      responses:
        "200":
          description: The updated GeneticTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneticTestRequestGrade"
        "404":
          description: GeneticTestRequestGrade not found
    delete:
      summary: Soft-delete a GeneticTestRequestGrade
      operationId: deleteGeneticTestRequestGrade
      responses:
        "204":
          description: GeneticTestRequestGrade deleted
        "404":
          description: GeneticTestRequestGrade not found
components:
  schemas:
    GeneticTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        genetic_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9) anchored on NHS National Genomic Test Directory eligibility match; there is no single published 1-9 genetic-ordering score, so the scale maps Test Directory eligibility to 7-9 (eligible), 4-6 (partial / borderline eligibility), 1-3 (not eligible).
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        consent_counselling_band:
          type: string
          enum:
            - ok
            - caution
            - not-met
            - ""
          description: "Consent and pre-test counselling band: ok, caution, not-met. Not-met is mandatory-blocking for predictive / presymptomatic testing."
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - ""
          description: "Assigned triage tier: routine, urgent."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the test implied by the triage tier (e.g. prenatal -> time-critical).
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - genetic_test_request_id
        - appropriateness_band
        - consent_counselling_band
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (NHS National Genomic Test Directory eligibility match, 1-9), consent and pre-test counselling, request completeness, and triage priority."
