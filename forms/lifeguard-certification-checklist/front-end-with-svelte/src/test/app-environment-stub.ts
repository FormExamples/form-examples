// Test stub for SvelteKit's `$app/environment` virtual module so the reactive
// store (which imports `browser`) can be unit-tested under plain Vitest.
export const browser = false;
export const building = false;
export const dev = false;
export const version = 'test';
