# Generated from sql/. Do not edit by hand.
# SOURCE: cystoscopy_test_request_grade
openapi: 3.1.0
info:
  title: Cystoscopy Test Request — CystoscopyTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (NICE NG12 / BAUS, 1-9), cancer-pathway urgency, request completeness, and pre-procedure risk."
paths:
  /cystoscopy-test-request-grades:
    get:
      summary: List CystoscopyTestRequestGrade records
      operationId: listCystoscopyTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CystoscopyTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CystoscopyTestRequestGrade"
    post:
      summary: Create a new CystoscopyTestRequestGrade
      operationId: createCystoscopyTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CystoscopyTestRequestGrade"
      responses:
        "201":
          description: The newly created CystoscopyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CystoscopyTestRequestGrade"
        "400":
          description: Validation error
  /cystoscopy-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CystoscopyTestRequestGrade by id
      operationId: getCystoscopyTestRequestGrade
      responses:
        "200":
          description: The requested CystoscopyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CystoscopyTestRequestGrade"
        "404":
          description: CystoscopyTestRequestGrade not found
    patch:
      summary: Update an existing CystoscopyTestRequestGrade
      operationId: updateCystoscopyTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CystoscopyTestRequestGrade"
      responses:
        "200":
          description: The updated CystoscopyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CystoscopyTestRequestGrade"
        "404":
          description: CystoscopyTestRequestGrade not found
    delete:
      summary: Soft-delete a CystoscopyTestRequestGrade
      operationId: deleteCystoscopyTestRequestGrade
      responses:
        "204":
          description: CystoscopyTestRequestGrade deleted
        "404":
          description: CystoscopyTestRequestGrade not found
components:
  schemas:
    CystoscopyTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        cystoscopy_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9), grounded in NICE NG12 and BAUS haematuria guidance.
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - two-week-wait
            - emergency
            - ""
          description: "Assigned cancer-pathway triage tier: routine, urgent, two-week-wait, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the cystoscopy implied by the triage tier.
        two_week_wait_eligible:
          type: boolean
          description: Whether the request meets NICE NG12 suspected-cancer two-week-wait criteria.
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        risk_band:
          type: string
          enum:
            - low
            - moderate
            - high
            - ""
          description: "Pre-procedure bleeding / safety risk band: low, moderate, high."
        anticoagulant_action:
          type: string
          maxLength: 500
          description: Recommended pre-procedure action for anticoagulation or active UTI (e.g. defer for active UTI, hold anticoagulant).
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - cystoscopy_test_request_id
        - appropriateness_band
        - triage_tier
        - target_timeframe
        - two_week_wait_eligible
        - risk_band
        - anticoagulant_action
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (NICE NG12 / BAUS, 1-9), cancer-pathway urgency, request completeness, and pre-procedure risk."
