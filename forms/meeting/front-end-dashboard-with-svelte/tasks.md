# Tasks: Meeting — Front-end Dashboard (SvelteKit)

- [ ] Scaffold the SvelteKit 2.x + TypeScript + Tailwind project.
- [ ] Install `@svar-ui/svelte-grid` and the Willow theme.
- [ ] Configure Tailwind CSS 4 with the monorepo `@theme` tokens.
- [ ] Author `src/lib/types.ts` mirroring the meeting type model.
- [ ] Author `src/lib/api.ts` with `/api/meetings` fetch and sample fallback.
- [ ] Author `src/lib/columns.ts` with the SVAR column definitions.
- [ ] Author `src/routes/+page.svelte` mounting the DataGrid.
- [ ] Implement status, category, and result dropdown filters.
- [ ] Implement free-text search debounced over `title`.
- [ ] Wire row click to deep-link the front-end form.
- [ ] Run `bin/test-form meeting` and resolve failures.
