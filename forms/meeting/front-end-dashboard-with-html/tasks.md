# Tasks: Meeting — Front-end Dashboard (HTML)

- [ ] Author `styles.css` with the table and filter bar layout.
- [ ] Author `app.js` with fetch, sort, and filter handlers.
- [ ] Author `index.html` with the table head, filter bar, and empty state.
- [ ] Author `sample.json` covering every status and result.
- [ ] Implement free-text search debounced over `title`.
- [ ] Implement status, category, and result dropdown filters.
- [ ] Wire row click to deep-link the front-end form.
- [ ] Run `bin/test-form meeting` and resolve failures.
