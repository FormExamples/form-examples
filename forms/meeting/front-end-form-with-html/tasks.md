# Tasks: Meeting — Front-end Form (HTML)

- [ ] Author `styles.css` with the wizard, flag panel, and report layout.
- [ ] Author `app.js` with the wizard state machine.
- [ ] Port `validateMeeting()` from the design spec into `app.js`.
- [ ] Author `index.html` with the 10 wizard sections.
- [ ] Author `sample.json` with a realistic example record.
- [ ] Wire LocalStorage autosave keyed by meeting `id`.
- [ ] Wire `pdfmake` PDF export.
- [ ] Add print-only stylesheet for the report preview.
- [ ] Run `bin/test-form meeting` and resolve failures.
