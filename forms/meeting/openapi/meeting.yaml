# Generated from sql/. Do not edit by hand.
# SOURCE: meeting
openapi: 3.1.0
info:
  title: Meeting — Meeting
  version: 1.0.0
  description: A meeting record captured via a 10-step single-page wizard. Holds the invitation metadata, the 250-character summary, and the sign-off. Variable-length collections live in child tables.
paths:
  /meetings:
    get:
      summary: List Meeting records
      operationId: listMeeting
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Meeting records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Meeting"
    post:
      summary: Create a new Meeting
      operationId: createMeeting
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Meeting"
      responses:
        "201":
          description: The newly created Meeting
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Meeting"
        "400":
          description: Validation error
  /meetings/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Meeting by id
      operationId: getMeeting
      responses:
        "200":
          description: The requested Meeting
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Meeting"
        "404":
          description: Meeting not found
    patch:
      summary: Update an existing Meeting
      operationId: updateMeeting
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Meeting"
      responses:
        "200":
          description: The updated Meeting
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Meeting"
        "404":
          description: Meeting not found
    delete:
      summary: Soft-delete a Meeting
      operationId: deleteMeeting
      responses:
        "204":
          description: Meeting deleted
        "404":
          description: Meeting not found
components:
  schemas:
    Meeting:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        organizer_id:
          type: string
          format: uuid
          description: Foreign key to the organizer who scheduled the meeting.
        status:
          type: string
          enum:
            - draft
            - scheduled
            - in-progress
            - completed
            - cancelled
            - no-show
          description: "Lifecycle status: draft, scheduled, in-progress, completed, cancelled, no-show."
        title:
          type: string
          maxLength: 255
          description: Meeting title shown in calendar invites and dashboards.
        purpose:
          type: string
          maxLength: 500
          description: One-sentence purpose statement.
        long_description:
          type: string
          description: Free-text long description of the meeting context.
        category:
          type: string
          enum:
            - stand-up
            - review
            - planning
            - retrospective
            - training
            - one-to-one
            - interview
            - governance
            - multidisciplinary-team
            - social
            - kickoff
            - all-hands
            - workshop
            - demo
            - other
            - ""
          description: "Meeting category: stand-up, review, planning, retrospective, training, one-to-one, interview, governance, multidisciplinary-team, social, kickoff, all-hands, workshop, demo, other."
        visibility:
          type: string
          enum:
            - public
            - internal
            - confidential
            - restricted
            - ""
          description: "Information classification: public, internal, confidential, restricted."
        scheduled_start_at:
          type: string
          format: date-time
          description: Scheduled start timestamp (UTC).
        scheduled_end_at:
          type: string
          format: date-time
          description: Scheduled end timestamp (UTC).
        timezone:
          type: string
          maxLength: 64
          description: IANA timezone identifier the meeting is presented in (e.g. Europe/London).
        location:
          type: string
          maxLength: 255
          description: Physical location, room name, or address.
        video_url:
          type: string
          maxLength: 500
          description: Video conference URL (Zoom, Teams, Google Meet, Whereby, etc.).
        phone_number:
          type: string
          maxLength: 64
          description: Dial-in phone number for audio attendees.
        dial_in_code:
          type: string
          maxLength: 64
          description: Dial-in PIN or meeting code for audio attendees.
        joining_instructions:
          type: string
          description: Free-text joining instructions — e.g. parking, lobby access, breakout-room links.
        calendar_uid:
          type: string
          maxLength: 255
          description: External calendar UID used when round-tripping with iCalendar / Google Calendar / Outlook.
        summary:
          type: string
          maxLength: 250
          description: "Results section: single paragraph summary, hard-capped at 250 characters per the form specification."
        actual_start_at:
          type: string
          format: date-time
          description: Actual start timestamp recorded after the meeting.
        actual_end_at:
          type: string
          format: date-time
          description: Actual end timestamp recorded after the meeting.
        overall_result:
          type: string
          enum:
            - productive
            - partial
            - unproductive
            - cancelled
            - ""
          description: "Sign-off result: productive, partial, unproductive, cancelled."
        additional_notes:
          type: string
          description: Free-text additional notes from the organizer at sign-off.
        signed_by_name:
          type: string
          maxLength: 255
          description: Name of the person who signed the record.
        signed_at:
          type: string
          format: date-time
          description: Timestamp the record was signed.
      required:
        - created_at
        - updated_at
        - organizer_id
        - status
        - title
        - purpose
        - long_description
        - category
        - visibility
        - timezone
        - location
        - video_url
        - phone_number
        - dial_in_code
        - joining_instructions
        - calendar_uid
        - summary
        - overall_result
        - additional_notes
        - signed_by_name
      description: A meeting record captured via a 10-step single-page wizard. Holds the invitation metadata, the 250-character summary, and the sign-off. Variable-length collections live in child tables.
