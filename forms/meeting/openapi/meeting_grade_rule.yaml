# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: meeting_grade_rule
openapi: 3.1.0
info:
  title: Meeting — MeetingGradeRule
  version: 1.0.0
  description: Audit trail of every validation rule that fired for this meeting, across the validation instruments (invitation, agenda, participants, resources, recurrence, summary, action-items, outputs, outcomes, composite).
paths:
  /meeting-grade-rules:
    get:
      summary: List MeetingGradeRule records
      operationId: listMeetingGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of MeetingGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MeetingGradeRule"
    post:
      summary: Create a new MeetingGradeRule
      operationId: createMeetingGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MeetingGradeRule"
      responses:
        "201":
          description: The newly created MeetingGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeetingGradeRule"
        "400":
          description: Validation error
  /meeting-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one MeetingGradeRule by id
      operationId: getMeetingGradeRule
      responses:
        "200":
          description: The requested MeetingGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeetingGradeRule"
        "404":
          description: MeetingGradeRule not found
    patch:
      summary: Update an existing MeetingGradeRule
      operationId: updateMeetingGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MeetingGradeRule"
      responses:
        "200":
          description: The updated MeetingGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeetingGradeRule"
        "404":
          description: MeetingGradeRule not found
    delete:
      summary: Soft-delete a MeetingGradeRule
      operationId: deleteMeetingGradeRule
      responses:
        "204":
          description: MeetingGradeRule deleted
        "404":
          description: MeetingGradeRule not found
components:
  schemas:
    MeetingGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        meeting_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 50
          description: Stable rule identifier (e.g. R-SUMMARY-OVER-LIMIT, R-NO-AGENDA).
        instrument:
          type: string
          enum:
            - invitation
            - agenda
            - participants
            - resources
            - recurrence
            - summary
            - action-items
            - outputs
            - outcomes
            - composite
            - ""
          description: Validation instrument the rule belongs to.
        grade:
          type: string
          maxLength: 10
          description: Grade contributed by this rule (e.g. red for blocking, amber for warning).
        category:
          type: string
          maxLength: 50
          description: Category for grouping rules in the report (e.g. data-quality, completeness, scheduling).
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - meeting_grade_id
        - rule_id
        - instrument
        - grade
        - category
        - description
      description: Audit trail of every validation rule that fired for this meeting, across the validation instruments (invitation, agenda, participants, resources, recurrence, summary, action-items, outputs, outcomes, composite).
