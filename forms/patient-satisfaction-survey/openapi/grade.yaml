# Generated from sql/. Do not edit by hand.
# SOURCE: grade
openapi: 3.1.0
info:
  title: Patient Satisfaction Survey — Grade
  version: 1.0.0
  description: Computed satisfaction grading result. Normalized score 0-100 with domain sub-scores. One-to-one child of assessment.
paths:
  /grades:
    get:
      summary: List Grade records
      operationId: listGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Grade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      summary: Create a new Grade
      operationId: createGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "201":
          description: The newly created Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "400":
          description: Validation error
  /grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Grade by id
      operationId: getGrade
      responses:
        "200":
          description: The requested Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    patch:
      summary: Update an existing Grade
      operationId: updateGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "200":
          description: The updated Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    delete:
      summary: Soft-delete a Grade
      operationId: deleteGrade
      responses:
        "204":
          description: Grade deleted
        "404":
          description: Grade not found
components:
  schemas:
    Grade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the parent assessment (unique, enforcing 1:1).
        normalized_score:
          type: number
          format: double
          description: Overall normalized satisfaction score (0-100).
        satisfaction_category:
          type: string
          enum:
            - excellent
            - good
            - satisfactory
            - poor
            - very-poor
            - ""
          description: "Satisfaction category: excellent, good, satisfactory, poor, very-poor, or empty."
        domain_access_score:
          type: number
          format: double
          description: Normalized score for access and waiting times domain (0-100).
        domain_communication_score:
          type: number
          format: double
          description: Normalized score for communication and information domain (0-100).
        domain_clinical_care_score:
          type: number
          format: double
          description: Normalized score for clinical care quality domain (0-100).
        domain_staff_score:
          type: number
          format: double
          description: Normalized score for staff attitude domain (0-100).
        domain_environment_score:
          type: number
          format: double
          description: Normalized score for environment and facilities domain (0-100).
        domain_discharge_score:
          type: number
          format: double
          description: Normalized score for discharge and follow-up domain (0-100).
        domain_overall_score:
          type: number
          format: double
          description: Normalized score for overall experience domain (0-100).
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the grading was computed.
      required:
        - created_at
        - updated_at
        - assessment_id
        - normalized_score
        - satisfaction_category
        - graded_at
      description: Computed satisfaction grading result. Normalized score 0-100 with domain sub-scores. One-to-one child of assessment.
