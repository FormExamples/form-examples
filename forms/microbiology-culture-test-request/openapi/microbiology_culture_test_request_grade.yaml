# Generated from sql/. Do not edit by hand.
# SOURCE: microbiology_culture_test_request_grade
openapi: 3.1.0
info:
  title: Microbiology Culture Test Request — MicrobiologyCultureTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (1-9 anchored on UKHSA SMI specimen / indication match), pre-analytical / specimen safety, request completeness, and triage priority."
paths:
  /microbiology-culture-test-request-grades:
    get:
      summary: List MicrobiologyCultureTestRequestGrade records
      operationId: listMicrobiologyCultureTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of MicrobiologyCultureTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MicrobiologyCultureTestRequestGrade"
    post:
      summary: Create a new MicrobiologyCultureTestRequestGrade
      operationId: createMicrobiologyCultureTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MicrobiologyCultureTestRequestGrade"
      responses:
        "201":
          description: The newly created MicrobiologyCultureTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MicrobiologyCultureTestRequestGrade"
        "400":
          description: Validation error
  /microbiology-culture-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one MicrobiologyCultureTestRequestGrade by id
      operationId: getMicrobiologyCultureTestRequestGrade
      responses:
        "200":
          description: The requested MicrobiologyCultureTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MicrobiologyCultureTestRequestGrade"
        "404":
          description: MicrobiologyCultureTestRequestGrade not found
    patch:
      summary: Update an existing MicrobiologyCultureTestRequestGrade
      operationId: updateMicrobiologyCultureTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MicrobiologyCultureTestRequestGrade"
      responses:
        "200":
          description: The updated MicrobiologyCultureTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MicrobiologyCultureTestRequestGrade"
        "404":
          description: MicrobiologyCultureTestRequestGrade not found
    delete:
      summary: Soft-delete a MicrobiologyCultureTestRequestGrade
      operationId: deleteMicrobiologyCultureTestRequestGrade
      responses:
        "204":
          description: MicrobiologyCultureTestRequestGrade deleted
        "404":
          description: MicrobiologyCultureTestRequestGrade not found
components:
  schemas:
    MicrobiologyCultureTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        microbiology_culture_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9) anchored on UKHSA SMI specimen / indication match. No single published microbiology 1-9 score exists; this anchors on specimen-and-indication appropriateness analogous to the ACR scale.
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        preanalytical_band:
          type: string
          enum:
            - ok
            - caution
            - reject-risk
            - ""
          description: "Pre-analytical / specimen safety band: ok, caution, reject-risk (specimen type/timing/transport correct? sampled before antibiotics? collected?)."
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - stat
            - ""
          description: "Assigned triage tier: routine, urgent, stat."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for processing implied by the triage tier (e.g. suspected sepsis -> stat / within the hour).
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - microbiology_culture_test_request_id
        - appropriateness_band
        - preanalytical_band
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (1-9 anchored on UKHSA SMI specimen / indication match), pre-analytical / specimen safety, request completeness, and triage priority."
