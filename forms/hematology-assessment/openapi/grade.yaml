# Generated from sql/. Do not edit by hand.
# SOURCE: grade
openapi: 3.1.0
info:
  title: Hematology Assessment — Grade
  version: 1.0.0
  description: 1:1 with assessment. Stores computed hematology abnormality level, score, and optional clinician override.
paths:
  /grades:
    get:
      summary: List Grade records
      operationId: listGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Grade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      summary: Create a new Grade
      operationId: createGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "201":
          description: The newly created Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "400":
          description: Validation error
  /grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Grade by id
      operationId: getGrade
      responses:
        "200":
          description: The requested Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    patch:
      summary: Update an existing Grade
      operationId: updateGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "200":
          description: The updated Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    delete:
      summary: Soft-delete a Grade
      operationId: deleteGrade
      responses:
        "204":
          description: Grade deleted
        "404":
          description: Grade not found
components:
  schemas:
    Grade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: FK to assessment (UNIQUE = 1:1 relationship).
        abnormality_level:
          type: string
          enum:
            - draft
            - normal
            - mildAbnormality
            - moderateAbnormality
            - severeAbnormality
            - critical
          description: "Computed abnormality level: draft (insufficient data), normal (0%), mildAbnormality (1-20%), moderateAbnormality (21-50%), severeAbnormality (51-75%), critical (76-100%)."
        abnormality_score:
          type: number
          format: double
          description: Composite abnormality score as percentage (0.0-100.0).
        abnormality_level_override:
          type: string
          enum:
            - normal
            - mildAbnormality
            - moderateAbnormality
            - severeAbnormality
            - critical
          description: Clinician-assigned abnormality level override. NULL means no override.
        override_reason:
          type: string
          description: Free-text justification for overriding the computed level. Required when override is set.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the grading engine produced this result.
      required:
        - created_at
        - updated_at
        - assessment_id
        - abnormality_level
        - abnormality_score
        - override_reason
        - graded_at
      description: 1:1 with assessment. Stores computed hematology abnormality level, score, and optional clinician override.
