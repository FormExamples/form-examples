# Generated from sql/. Do not edit by hand.
# SOURCE: hem_rule
openapi: 3.1.0
info:
  title: Hematology Assessment — HemRule
  version: 1.0.0
  description: Catalogue of 20 hematology rules evaluated during grading.
paths:
  /hem-rules:
    get:
      summary: List HemRule records
      operationId: listHemRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HemRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HemRule"
    post:
      summary: Create a new HemRule
      operationId: createHemRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HemRule"
      responses:
        "201":
          description: The newly created HemRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HemRule"
        "400":
          description: Validation error
  /hem-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HemRule by id
      operationId: getHemRule
      responses:
        "200":
          description: The requested HemRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HemRule"
        "404":
          description: HemRule not found
    patch:
      summary: Update an existing HemRule
      operationId: updateHemRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HemRule"
      responses:
        "200":
          description: The updated HemRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HemRule"
        "404":
          description: HemRule not found
    delete:
      summary: Soft-delete a HemRule
      operationId: deleteHemRule
      responses:
        "204":
          description: HemRule deleted
        "404":
          description: HemRule not found
components:
  schemas:
    HemRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        code:
          type: string
          description: Human-readable rule code (e.g. DM-001). UNIQUE stable identifier used by application code and audit records.
        category:
          type: string
          description: Clinical category (e.g. Blood Count, Coagulation, Iron Studies).
        description:
          type: string
          description: Human-readable description of the rule condition.
        concern_level:
          type: string
          enum:
            - high
            - medium
            - low
          description: "Concern level: high, medium, or low."
      required:
        - created_at
        - updated_at
        - code
        - category
        - description
        - concern_level
      description: Catalogue of 20 hematology rules evaluated during grading.
