# Generated from sql/. Do not edit by hand.
# SOURCE: cage_alcohol_questionnaire_grade
openapi: 3.1.0
info:
  title: Cage Alcohol Questionnaire — CageAlcoholQuestionnaireGrade
  version: 1.0.0
  description: "Computed CAGE grading result: per-criterion points, summed total (0-4), and derived binary interpretation (negative/positive) against the >= 2 cut-off."
paths:
  /cage-alcohol-questionnaire-grades:
    get:
      summary: List CageAlcoholQuestionnaireGrade records
      operationId: listCageAlcoholQuestionnaireGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CageAlcoholQuestionnaireGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CageAlcoholQuestionnaireGrade"
    post:
      summary: Create a new CageAlcoholQuestionnaireGrade
      operationId: createCageAlcoholQuestionnaireGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CageAlcoholQuestionnaireGrade"
      responses:
        "201":
          description: The newly created CageAlcoholQuestionnaireGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CageAlcoholQuestionnaireGrade"
        "400":
          description: Validation error
  /cage-alcohol-questionnaire-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CageAlcoholQuestionnaireGrade by id
      operationId: getCageAlcoholQuestionnaireGrade
      responses:
        "200":
          description: The requested CageAlcoholQuestionnaireGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CageAlcoholQuestionnaireGrade"
        "404":
          description: CageAlcoholQuestionnaireGrade not found
    patch:
      summary: Update an existing CageAlcoholQuestionnaireGrade
      operationId: updateCageAlcoholQuestionnaireGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CageAlcoholQuestionnaireGrade"
      responses:
        "200":
          description: The updated CageAlcoholQuestionnaireGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CageAlcoholQuestionnaireGrade"
        "404":
          description: CageAlcoholQuestionnaireGrade not found
    delete:
      summary: Soft-delete a CageAlcoholQuestionnaireGrade
      operationId: deleteCageAlcoholQuestionnaireGrade
      responses:
        "204":
          description: CageAlcoholQuestionnaireGrade deleted
        "404":
          description: CageAlcoholQuestionnaireGrade not found
components:
  schemas:
    CageAlcoholQuestionnaireGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        cage_alcohol_questionnaire_id:
          type: string
          format: uuid
          description: Foreign key to the parent assessment (unique, 1:1).
        cut_down_point:
          type: integer
          format: int32
          description: Point (0-1) awarded for criterion C, cut down.
        annoyed_point:
          type: integer
          format: int32
          description: Point (0-1) awarded for criterion A, annoyed.
        guilty_point:
          type: integer
          format: int32
          description: Point (0-1) awarded for criterion G, guilty.
        eye_opener_point:
          type: integer
          format: int32
          description: Point (0-1) awarded for criterion E, eye-opener.
        total_score:
          type: integer
          format: int32
          description: Summed CAGE score across the four criteria (0-4).
        interpretation:
          type: string
          enum:
            - negative
            - positive
            - ""
          description: "Derived binary interpretation: positive (total >= 2) or negative (total < 2)."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the grade.
      required:
        - created_at
        - updated_at
        - cage_alcohol_questionnaire_id
        - interpretation
        - graded_at
      description: "Computed CAGE grading result: per-criterion points, summed total (0-4), and derived binary interpretation (negative/positive) against the >= 2 cut-off."
