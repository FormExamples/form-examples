# Generated from sql/. Do not edit by hand.
# SOURCE: cage_alcohol_questionnaire
openapi: 3.1.0
info:
  title: Cage Alcohol Questionnaire — CageAlcoholQuestionnaire
  version: 1.0.0
  description: "Main CAGE alcohol questionnaire record: assessment context, patient identification, and the four scored criterion inputs (cut down, annoyed, guilty, eye-opener)."
paths:
  /cage-alcohol-questionnaires:
    get:
      summary: List CageAlcoholQuestionnaire records
      operationId: listCageAlcoholQuestionnaire
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CageAlcoholQuestionnaire records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CageAlcoholQuestionnaire"
    post:
      summary: Create a new CageAlcoholQuestionnaire
      operationId: createCageAlcoholQuestionnaire
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CageAlcoholQuestionnaire"
      responses:
        "201":
          description: The newly created CageAlcoholQuestionnaire
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CageAlcoholQuestionnaire"
        "400":
          description: Validation error
  /cage-alcohol-questionnaires/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CageAlcoholQuestionnaire by id
      operationId: getCageAlcoholQuestionnaire
      responses:
        "200":
          description: The requested CageAlcoholQuestionnaire
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CageAlcoholQuestionnaire"
        "404":
          description: CageAlcoholQuestionnaire not found
    patch:
      summary: Update an existing CageAlcoholQuestionnaire
      operationId: updateCageAlcoholQuestionnaire
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CageAlcoholQuestionnaire"
      responses:
        "200":
          description: The updated CageAlcoholQuestionnaire
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CageAlcoholQuestionnaire"
        "404":
          description: CageAlcoholQuestionnaire not found
    delete:
      summary: Soft-delete a CageAlcoholQuestionnaire
      operationId: deleteCageAlcoholQuestionnaire
      responses:
        "204":
          description: CageAlcoholQuestionnaire deleted
        "404":
          description: CageAlcoholQuestionnaire not found
components:
  schemas:
    CageAlcoholQuestionnaire:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient being screened (delete restricted).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the screening clinician (optional; delete restricted).
        clinician_name:
          type: string
          maxLength: 255
          description: Name of the screening clinician as recorded on the assessment.
        clinician_role:
          type: string
          enum:
            - doctor
            - nurse
            - midwife
            - other
            - ""
          description: "Role of the screening clinician: doctor, nurse, midwife, or other."
        assessed_at:
          type: string
          format: date-time
          description: Date and time the assessment was performed.
        care_setting:
          type: string
          enum:
            - primary-care
            - ward
            - emergency-department
            - antenatal
            - other
            - ""
          description: "Care setting: primary-care, ward, emergency-department, antenatal, or other."
        patient_identifier:
          type: string
          maxLength: 100
          description: Local patient identifier as recorded on the assessment.
        age_band:
          type: string
          enum:
            - 16-39
            - 40-59
            - 60-74
            - 75-plus
            - ""
          description: "Adult age band: 16-39, 40-59, 60-74, or 75-plus."
        sex:
          type: string
          enum:
            - female
            - male
            - intersex
            - unknown
            - ""
          description: "Patient sex recorded for the assessment: female, male, intersex, or unknown."
        cut_down:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion C — has the patient felt they should cut down on their drinking: yes, no, or unanswered."
        annoyed:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion A — have people annoyed the patient by criticising their drinking: yes, no, or unanswered."
        guilty:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion G — has the patient felt bad or guilty about their drinking: yes, no, or unanswered."
        eye_opener:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion E — has the patient had a morning drink to steady nerves or cure a hangover (eye-opener): yes, no, or unanswered."
        clinical_note:
          type: string
          description: Free-text clinical note recorded with the assessment.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_name
        - clinician_role
        - care_setting
        - patient_identifier
        - age_band
        - sex
        - cut_down
        - annoyed
        - guilty
        - eye_opener
        - clinical_note
      description: "Main CAGE alcohol questionnaire record: assessment context, patient identification, and the four scored criterion inputs (cut down, annoyed, guilty, eye-opener)."
