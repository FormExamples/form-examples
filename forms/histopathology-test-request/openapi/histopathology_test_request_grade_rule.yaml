# Generated from sql/. Do not edit by hand.
# SOURCE: histopathology_test_request_grade_rule
openapi: 3.1.0
info:
  title: Histopathology Test Request — HistopathologyTestRequestGradeRule
  version: 1.0.0
  description: Audit trail of every appropriateness / specimen / completeness / urgency rule that fired for this request.
paths:
  /histopathology-test-request-grade-rules:
    get:
      summary: List HistopathologyTestRequestGradeRule records
      operationId: listHistopathologyTestRequestGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HistopathologyTestRequestGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HistopathologyTestRequestGradeRule"
    post:
      summary: Create a new HistopathologyTestRequestGradeRule
      operationId: createHistopathologyTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HistopathologyTestRequestGradeRule"
      responses:
        "201":
          description: The newly created HistopathologyTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HistopathologyTestRequestGradeRule"
        "400":
          description: Validation error
  /histopathology-test-request-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HistopathologyTestRequestGradeRule by id
      operationId: getHistopathologyTestRequestGradeRule
      responses:
        "200":
          description: The requested HistopathologyTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HistopathologyTestRequestGradeRule"
        "404":
          description: HistopathologyTestRequestGradeRule not found
    patch:
      summary: Update an existing HistopathologyTestRequestGradeRule
      operationId: updateHistopathologyTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HistopathologyTestRequestGradeRule"
      responses:
        "200":
          description: The updated HistopathologyTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HistopathologyTestRequestGradeRule"
        "404":
          description: HistopathologyTestRequestGradeRule not found
    delete:
      summary: Soft-delete a HistopathologyTestRequestGradeRule
      operationId: deleteHistopathologyTestRequestGradeRule
      responses:
        "204":
          description: HistopathologyTestRequestGradeRule deleted
        "404":
          description: HistopathologyTestRequestGradeRule not found
components:
  schemas:
    HistopathologyTestRequestGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        histopathology_test_request_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-APPROP-SUSPECTED-MALIGNANCY-01).
        axis:
          type: string
          enum:
            - appropriateness
            - specimen
            - completeness
            - urgency
            - ""
          description: "Scoring axis the rule belongs to: appropriateness, specimen, completeness, urgency."
        category:
          type: string
          maxLength: 50
          description: Category or indication the rule relates to.
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - histopathology_test_request_grade_id
        - rule_id
        - axis
        - category
        - description
      description: Audit trail of every appropriateness / specimen / completeness / urgency rule that fired for this request.
