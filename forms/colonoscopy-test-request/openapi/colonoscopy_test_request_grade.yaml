# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: colonoscopy_test_request_grade
openapi: 3.1.0
info:
  title: Colonoscopy Test Request — ColonoscopyTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (ASGE / EPAGE / NICE 1-9), cancer-pathway urgency / triage tier, request completeness, and pre-procedure risk."
paths:
  /colonoscopy-test-request-grades:
    get:
      summary: List ColonoscopyTestRequestGrade records
      operationId: listColonoscopyTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ColonoscopyTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ColonoscopyTestRequestGrade"
    post:
      summary: Create a new ColonoscopyTestRequestGrade
      operationId: createColonoscopyTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ColonoscopyTestRequestGrade"
      responses:
        "201":
          description: The newly created ColonoscopyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ColonoscopyTestRequestGrade"
        "400":
          description: Validation error
  /colonoscopy-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ColonoscopyTestRequestGrade by id
      operationId: getColonoscopyTestRequestGrade
      responses:
        "200":
          description: The requested ColonoscopyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ColonoscopyTestRequestGrade"
        "404":
          description: ColonoscopyTestRequestGrade not found
    patch:
      summary: Update an existing ColonoscopyTestRequestGrade
      operationId: updateColonoscopyTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ColonoscopyTestRequestGrade"
      responses:
        "200":
          description: The updated ColonoscopyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ColonoscopyTestRequestGrade"
        "404":
          description: ColonoscopyTestRequestGrade not found
    delete:
      summary: Soft-delete a ColonoscopyTestRequestGrade
      operationId: deleteColonoscopyTestRequestGrade
      responses:
        "204":
          description: ColonoscopyTestRequestGrade deleted
        "404":
          description: ColonoscopyTestRequestGrade not found
components:
  schemas:
    ColonoscopyTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        colonoscopy_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9), ASGE / EPAGE / NICE style.
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - two-week-wait
            - emergency
            - ""
          description: "Assigned triage tier: routine, urgent, two-week-wait, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: "Target timeframe for the procedure implied by the triage tier (e.g. \"<= 14 days (2WW)\")."
        two_week_wait_eligible:
          type: boolean
          description: Whether the request meets NICE NG12 / DG56 suspected-cancer two-week-wait criteria.
        two_week_wait_rationale:
          type: string
          maxLength: 500
          description: Rationale explaining the two-week-wait eligibility decision.
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        risk_band:
          type: string
          enum:
            - low
            - moderate
            - high
            - ""
          description: "Pre-procedure risk band: low, moderate, high."
        anticoagulant_action:
          type: string
          maxLength: 500
          description: Recommended anticoagulant / antiplatelet management action per BSG / ESGE periprocedural bleeding-risk stratification.
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - colonoscopy_test_request_id
        - appropriateness_band
        - triage_tier
        - target_timeframe
        - two_week_wait_eligible
        - two_week_wait_rationale
        - risk_band
        - anticoagulant_action
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (ASGE / EPAGE / NICE 1-9), cancer-pathway urgency / triage tier, request completeness, and pre-procedure risk."
