# Generated from sql/. Do not edit by hand.
# SOURCE: mri_scan_test_result
openapi: 3.1.0
info:
  title: Mri Scan Test Result — MriScanTestResult
  version: 1.0.0
  description: MRI (magnetic resonance imaging) scan result (report). Captures the performed examination, contrast and pulse sequences, clinical history, the narrative and structured findings, the largest-lesion measurement, the impression and structured-reporting category (e.g. PI-RADS for prostate or BI-RADS for breast MRI), recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /mri-scan-test-results:
    get:
      summary: List MriScanTestResult records
      operationId: listMriScanTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of MriScanTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MriScanTestResult"
    post:
      summary: Create a new MriScanTestResult
      operationId: createMriScanTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MriScanTestResult"
      responses:
        "201":
          description: The newly created MriScanTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MriScanTestResult"
        "400":
          description: Validation error
  /mri-scan-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one MriScanTestResult by id
      operationId: getMriScanTestResult
      responses:
        "200":
          description: The requested MriScanTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MriScanTestResult"
        "404":
          description: MriScanTestResult not found
    patch:
      summary: Update an existing MriScanTestResult
      operationId: updateMriScanTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MriScanTestResult"
      responses:
        "200":
          description: The updated MriScanTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MriScanTestResult"
        "404":
          description: MriScanTestResult not found
    delete:
      summary: Soft-delete a MriScanTestResult
      operationId: deleteMriScanTestResult
      responses:
        "204":
          description: MriScanTestResult deleted
        "404":
          description: MriScanTestResult not found
components:
  schemas:
    MriScanTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (radiologist / reporting radiographer).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating MRI scan request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the MRI examination was performed.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        body_region:
          type: string
          enum:
            - brain
            - spine-cervical
            - spine-thoracic
            - spine-lumbar
            - head-neck
            - chest
            - abdomen
            - pelvis
            - cardiac
            - mr-angiogram
            - breast
            - musculoskeletal-joint
            - whole-body
            - other
            - ""
          description: "Examined MRI body region: brain, spine-cervical, spine-thoracic, spine-lumbar, head-neck, chest, abdomen, pelvis, cardiac, mr-angiogram, breast, musculoskeletal-joint, whole-body, other."
        contrast_used:
          type: string
          enum:
            - none
            - iv-gadolinium
            - ""
          description: "Contrast actually administered: none, iv-gadolinium."
        sequences_performed:
          type: string
          maxLength: 1000
          description: Pulse sequences and protocol performed (e.g. T1, T2, FLAIR, DWI, DCE), with any deviations.
        examination_adequacy:
          type: string
          enum:
            - adequate
            - limited
            - non-diagnostic
            - ""
          description: "Diagnostic adequacy of the examination: adequate, limited, non-diagnostic."
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the examination was performed to answer.
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous imaging studies.
        findings_narrative:
          type: string
          maxLength: 2000
          description: Narrative description of the imaging findings (the body of the report).
        mass_or_lesion:
          type: boolean
          description: Whether a mass or focal lesion is present.
        haemorrhage:
          type: boolean
          description: Whether haemorrhage is present.
        infarct:
          type: boolean
          description: Whether infarction (e.g. acute ischaemic stroke on diffusion-weighted imaging) is present.
        demyelination:
          type: boolean
          description: Whether demyelinating lesions (e.g. multiple sclerosis plaques) are present.
        disc_herniation:
          type: boolean
          description: Whether intervertebral disc herniation or protrusion is present.
        cord_compression:
          type: boolean
          description: Whether spinal-cord or cauda-equina compression is present (a critical finding).
        infection_inflammation:
          type: boolean
          description: Whether infection or inflammation (e.g. abscess, discitis, collection) is present.
        incidental_finding:
          type: boolean
          description: Whether one or more incidental findings warranting documentation are present.
        largest_lesion_size_mm:
          type: number
          format: double
          description: Largest lesion long-axis size in millimetres, for surveillance and categorisation.
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 50
          description: Structured-reporting score / category label as applicable to the body region, stored as free text (e.g. PI-RADS 1-5 for prostate MRI, BI-RADS 0-6 for breast MRI, or a Likert score).
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up imaging, referral, or management.
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - body_region
        - contrast_used
        - sequences_performed
        - examination_adequacy
        - clinical_history
        - comparison_with_previous
        - findings_narrative
        - mass_or_lesion
        - haemorrhage
        - infarct
        - demyelination
        - disc_herniation
        - cord_compression
        - infection_inflammation
        - incidental_finding
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: MRI (magnetic resonance imaging) scan result (report). Captures the performed examination, contrast and pulse sequences, clinical history, the narrative and structured findings, the largest-lesion measurement, the impression and structured-reporting category (e.g. PI-RADS for prostate or BI-RADS for breast MRI), recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
