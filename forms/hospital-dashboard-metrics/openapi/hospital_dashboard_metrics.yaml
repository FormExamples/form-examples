# Generated from sql/. Do not edit by hand.
# SOURCE: hospital_dashboard_metrics
openapi: 3.1.0
info:
  title: Hospital Dashboard Metrics — HospitalDashboardMetrics
  version: 1.0.0
  description: "One hospital KPI reporting period: hospital/site, period month/year, prepared-by, and sign-off notes."
paths:
  /hospital-dashboard-metricses:
    get:
      summary: List HospitalDashboardMetrics records
      operationId: listHospitalDashboardMetrics
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HospitalDashboardMetrics records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HospitalDashboardMetrics"
    post:
      summary: Create a new HospitalDashboardMetrics
      operationId: createHospitalDashboardMetrics
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HospitalDashboardMetrics"
      responses:
        "201":
          description: The newly created HospitalDashboardMetrics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HospitalDashboardMetrics"
        "400":
          description: Validation error
  /hospital-dashboard-metricses/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HospitalDashboardMetrics by id
      operationId: getHospitalDashboardMetrics
      responses:
        "200":
          description: The requested HospitalDashboardMetrics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HospitalDashboardMetrics"
        "404":
          description: HospitalDashboardMetrics not found
    patch:
      summary: Update an existing HospitalDashboardMetrics
      operationId: updateHospitalDashboardMetrics
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HospitalDashboardMetrics"
      responses:
        "200":
          description: The updated HospitalDashboardMetrics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HospitalDashboardMetrics"
        "404":
          description: HospitalDashboardMetrics not found
    delete:
      summary: Soft-delete a HospitalDashboardMetrics
      operationId: deleteHospitalDashboardMetrics
      responses:
        "204":
          description: HospitalDashboardMetrics deleted
        "404":
          description: HospitalDashboardMetrics not found
components:
  schemas:
    HospitalDashboardMetrics:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        hospital_name:
          type: string
          maxLength: 255
          description: Name of the hospital or facility being reported on.
        period_month:
          type: integer
          format: int32
          description: Reporting period month, 1-12.
        period_year:
          type: integer
          format: int32
          description: Reporting period year.
        prepared_by_name:
          type: string
          maxLength: 255
          description: Name of the person who compiled this report.
        overall_notes:
          type: string
          description: Free-text overall notes recorded at sign-off.
        status:
          type: string
          enum:
            - draft
            - submitted
          description: "Submission status: draft or submitted."
        signed_at:
          type: string
          format: date-time
          description: Timestamp the preparer signed off this report.
      required:
        - created_at
        - updated_at
        - hospital_name
        - prepared_by_name
        - overall_notes
        - status
      description: "One hospital KPI reporting period: hospital/site, period month/year, prepared-by, and sign-off notes."
