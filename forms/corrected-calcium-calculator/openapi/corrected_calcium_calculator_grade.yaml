# Generated from sql/. Do not edit by hand.
# SOURCE: corrected_calcium_calculator_grade
openapi: 3.1.0
info:
  title: Corrected Calcium Calculator — CorrectedCalciumCalculatorGrade
  version: 1.0.0
  description: "Computed corrected-calcium result: the albumin-adjusted corrected calcium value and its classification against the adult reference range (1:1 with the parent calculation record)."
paths:
  /corrected-calcium-calculator-grades:
    get:
      summary: List CorrectedCalciumCalculatorGrade records
      operationId: listCorrectedCalciumCalculatorGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CorrectedCalciumCalculatorGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CorrectedCalciumCalculatorGrade"
    post:
      summary: Create a new CorrectedCalciumCalculatorGrade
      operationId: createCorrectedCalciumCalculatorGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CorrectedCalciumCalculatorGrade"
      responses:
        "201":
          description: The newly created CorrectedCalciumCalculatorGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CorrectedCalciumCalculatorGrade"
        "400":
          description: Validation error
  /corrected-calcium-calculator-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CorrectedCalciumCalculatorGrade by id
      operationId: getCorrectedCalciumCalculatorGrade
      responses:
        "200":
          description: The requested CorrectedCalciumCalculatorGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CorrectedCalciumCalculatorGrade"
        "404":
          description: CorrectedCalciumCalculatorGrade not found
    patch:
      summary: Update an existing CorrectedCalciumCalculatorGrade
      operationId: updateCorrectedCalciumCalculatorGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CorrectedCalciumCalculatorGrade"
      responses:
        "200":
          description: The updated CorrectedCalciumCalculatorGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CorrectedCalciumCalculatorGrade"
        "404":
          description: CorrectedCalciumCalculatorGrade not found
    delete:
      summary: Soft-delete a CorrectedCalciumCalculatorGrade
      operationId: deleteCorrectedCalciumCalculatorGrade
      responses:
        "204":
          description: CorrectedCalciumCalculatorGrade deleted
        "404":
          description: CorrectedCalciumCalculatorGrade not found
components:
  schemas:
    CorrectedCalciumCalculatorGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        corrected_calcium_calculator_id:
          type: string
          format: uuid
          description: Foreign key to the parent calculation record (unique, 1:1).
        corrected_calcium_mmol_l:
          type: number
          format: double
          description: Albumin-adjusted corrected calcium in mmol/L (null when either input is missing).
        classification:
          type: string
          enum:
            - hypocalcaemia
            - normal
            - hypercalcaemia
            - unknown
            - ""
          description: "Classification against the adult reference range 2.20-2.60 mmol/L: hypocalcaemia, normal, hypercalcaemia, or unknown."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - corrected_calcium_calculator_id
        - classification
        - graded_at
      description: "Computed corrected-calcium result: the albumin-adjusted corrected calcium value and its classification against the adult reference range (1:1 with the parent calculation record)."
