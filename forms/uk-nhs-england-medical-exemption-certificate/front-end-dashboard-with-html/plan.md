# Plan — front-end dashboard with HTML (FP92A)

- [x] `index.html` — filter bar + applications table
- [x] `css/style.css` — NHS theme
- [x] `js/types.js` — label dictionaries
- [x] `js/data.js` — sample applications
- [x] `js/api.js` — fetch with sample-data fallback
- [x] `js/app.js` — filter + render
