# Generated from sql/. Do not edit by hand.
# SOURCE: curb_65_pneumonia_severity_score
openapi: 3.1.0
info:
  title: Curb 65 Pneumonia Severity Score — Curb65PneumoniaSeverityScore
  version: 1.0.0
  description: "Main CURB-65 pneumonia severity assessment record: assessment context, patient identification, the five scored criterion inputs (confusion, urea, respiratory rate, blood pressure, age), advisory adjuncts, and the clinician disposition override."
paths:
  /curb-65-pneumonia-severity-scores:
    get:
      summary: List Curb65PneumoniaSeverityScore records
      operationId: listCurb65PneumoniaSeverityScore
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Curb65PneumoniaSeverityScore records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Curb65PneumoniaSeverityScore"
    post:
      summary: Create a new Curb65PneumoniaSeverityScore
      operationId: createCurb65PneumoniaSeverityScore
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Curb65PneumoniaSeverityScore"
      responses:
        "201":
          description: The newly created Curb65PneumoniaSeverityScore
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Curb65PneumoniaSeverityScore"
        "400":
          description: Validation error
  /curb-65-pneumonia-severity-scores/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Curb65PneumoniaSeverityScore by id
      operationId: getCurb65PneumoniaSeverityScore
      responses:
        "200":
          description: The requested Curb65PneumoniaSeverityScore
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Curb65PneumoniaSeverityScore"
        "404":
          description: Curb65PneumoniaSeverityScore not found
    patch:
      summary: Update an existing Curb65PneumoniaSeverityScore
      operationId: updateCurb65PneumoniaSeverityScore
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Curb65PneumoniaSeverityScore"
      responses:
        "200":
          description: The updated Curb65PneumoniaSeverityScore
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Curb65PneumoniaSeverityScore"
        "404":
          description: Curb65PneumoniaSeverityScore not found
    delete:
      summary: Soft-delete a Curb65PneumoniaSeverityScore
      operationId: deleteCurb65PneumoniaSeverityScore
      responses:
        "204":
          description: Curb65PneumoniaSeverityScore deleted
        "404":
          description: Curb65PneumoniaSeverityScore not found
components:
  schemas:
    Curb65PneumoniaSeverityScore:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient being assessed (delete restricted).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the assessing clinician (optional; delete restricted).
        clinician_name:
          type: string
          maxLength: 255
          description: Name of the assessing clinician as recorded on the assessment.
        clinician_role:
          type: string
          enum:
            - physician
            - general-practitioner
            - advanced-nurse-practitioner
            - nurse
            - paramedic
            - pharmacist
            - other
            - ""
          description: "Role of the assessing clinician: physician, general-practitioner, advanced-nurse-practitioner, nurse, paramedic, pharmacist, or other."
        assessed_at:
          type: string
          format: date-time
          description: Date and time the assessment was performed.
        care_setting:
          type: string
          enum:
            - primary-care
            - emergency-department
            - acute-medical-unit
            - ward
            - community
            - other
            - ""
          description: "Care setting: primary-care, emergency-department, acute-medical-unit, ward, community, or other."
        patient_identifier:
          type: string
          maxLength: 100
          description: Local patient identifier as recorded on the assessment.
        sex:
          type: string
          enum:
            - female
            - male
            - intersex
            - unknown
            - ""
          description: "Patient sex recorded for the assessment: female, male, intersex, or unknown."
        age_years:
          type: integer
          format: int32
          description: Criterion A65 — patient age in years, derived from date of birth and confirmed at assessment; scores 1 when >= 65.
        confusion_present:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion C — whether new-onset confusion is present: yes, no, or unanswered."
        amt_score:
          type: integer
          format: int32
          description: Supporting evidence for confusion — Abbreviated Mental Test score (0-10); not scored directly.
        urea_measured:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether serum urea was measured; when no, the four-criterion CRB-65 fallback is computed instead of CURB-65.
        urea_mmol_l:
          type: number
          format: double
          description: Criterion U — serum urea in millimoles per litre (mmol/L); scores 1 when > 7.
        respiratory_rate:
          type: integer
          format: int32
          description: Criterion R — respiratory rate in breaths per minute; scores 1 when >= 30.
        systolic_bp:
          type: integer
          format: int32
          description: Criterion B — systolic blood pressure in mmHg; scores 1 when < 90 (or diastolic <= 60).
        diastolic_bp:
          type: integer
          format: int32
          description: Criterion B — diastolic blood pressure in mmHg; scores 1 when <= 60 (or systolic < 90).
        oxygen_saturation:
          type: integer
          format: int32
          description: Advisory adjunct — peripheral oxygen saturation (SpO2) as a percentage; not scored, raises the hypoxia flag when < 92.
        temperature_c:
          type: number
          format: double
          description: Advisory adjunct — body temperature in degrees Celsius; not scored.
        significant_comorbidity:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Advisory adjunct — whether a significant comorbidity is present: yes, no, or unanswered; not scored."
        multilobar_changes:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Advisory adjunct — whether multilobar changes are present on chest imaging: yes, no, or unanswered; not scored."
        clinician_override_band:
          type: string
          enum:
            - low
            - intermediate
            - high
            - ""
          description: "Optional clinician-set final risk band overriding the computed band: low, intermediate, or high."
        override_reason:
          type: string
          description: Documented reason for the clinician disposition override (mandatory when an override band is set).
        clinical_note:
          type: string
          description: Free-text clinical note recorded with the assessment.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_name
        - clinician_role
        - care_setting
        - patient_identifier
        - sex
        - confusion_present
        - urea_measured
        - significant_comorbidity
        - multilobar_changes
        - clinician_override_band
        - override_reason
        - clinical_note
      description: "Main CURB-65 pneumonia severity assessment record: assessment context, patient identification, the five scored criterion inputs (confusion, urea, respiratory rate, blood pressure, age), advisory adjuncts, and the clinician disposition override."
