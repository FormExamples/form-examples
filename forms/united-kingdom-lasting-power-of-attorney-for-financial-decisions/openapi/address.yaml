# Generated from sql/. Do not edit by hand.
# SOURCE: address
openapi: 3.1.0
info:
  title: United Kingdom Lasting Power Of Attorney For Financial Decisions — Address
  version: 1.0.0
  description: Address — postal address used by an LPA, normalised across persons and roles.
paths:
  /addresses:
    get:
      summary: List Address records
      operationId: listAddress
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Address records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Address"
    post:
      summary: Create a new Address
      operationId: createAddress
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Address"
      responses:
        "201":
          description: The newly created Address
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Address"
        "400":
          description: Validation error
  /addresses/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Address by id
      operationId: getAddress
      responses:
        "200":
          description: The requested Address
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Address"
        "404":
          description: Address not found
    patch:
      summary: Update an existing Address
      operationId: updateAddress
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Address"
      responses:
        "200":
          description: The updated Address
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Address"
        "404":
          description: Address not found
    delete:
      summary: Soft-delete a Address
      operationId: deleteAddress
      responses:
        "204":
          description: Address deleted
        "404":
          description: Address not found
components:
  schemas:
    Address:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        address_line_1:
          type: string
          description: First line of the postal address, typically house number or name and street.
        address_line_2:
          type: string
          description: Second line of the postal address, typically locality or village.
        address_line_3:
          type: string
          description: Third line of the postal address, typically town or city.
        postcode:
          type: string
          description: Postal code (UK postcode for GB addresses).
        country_as_iso_3166_1_alpha_2:
          type: string
          description: Country as ISO 3166-1 alpha-2 code; defaults to GB because LP1F is a UK instrument.
      required:
        - created_at
        - updated_at
        - address_line_1
        - address_line_2
        - address_line_3
        - postcode
        - country_as_iso_3166_1_alpha_2
      description: Address — postal address used by an LPA, normalised across persons and roles.
