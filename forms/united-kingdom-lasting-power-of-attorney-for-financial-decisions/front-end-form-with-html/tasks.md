# Tasks: UK LPA HTML wizard

- [x] `index.html` (15-step single-page wizard)
- [x] `css/styles.css` (component overrides on top of Tailwind)
- [x] `js/validator.js` (every blocker + flag rule, `validateLpa(lpa)`)
- [x] `js/sample-data.js` (clean-deed fixture)
- [x] `js/app.js` (Alpine `lpaWizard()` factory)
- [x] localStorage autosave
- [x] Sticky validation summary panel with citations and remediation
- [x] Sample-fixture and reset controls
- [ ] PDF export (deferred — out of scope for HTML build)
- [ ] Welsh locale (deferred)
- [ ] Service worker (deferred)
