# Tasks: First Aid Training Checklist — consolidated front-end (SvelteKit)

## Done

- [x] Port the FAW competency engine to TypeScript (`src/lib/engine/`)
- [x] Build the ten-step examiner wizard and id-keyed store
- [x] Build the SVAR DataGrid coordinator dashboard (`ssr=false`)
- [x] Build the report view and PDF endpoint
- [x] Vitest engine tests; `pnpm check` / `build` / `vitest` all green
