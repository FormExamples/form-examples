# Generated from sql/. Do not edit by hand.
# SOURCE: nuclear_medicine_test_request
openapi: 3.1.0
info:
  title: Nuclear Medicine Test Request — NuclearMedicineTestRequest
  version: 1.0.0
  description: Nuclear medicine (radionuclide imaging) request (referral). Captures the requested scan type, the clinical indication and question, relevant history, radiation-safety governance (pregnancy, breastfeeding, renal function, recent radionuclide exposure), IR(ME)R justification, and triage urgency.
paths:
  /nuclear-medicine-test-requests:
    get:
      summary: List NuclearMedicineTestRequest records
      operationId: listNuclearMedicineTestRequest
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of NuclearMedicineTestRequest records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NuclearMedicineTestRequest"
    post:
      summary: Create a new NuclearMedicineTestRequest
      operationId: createNuclearMedicineTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NuclearMedicineTestRequest"
      responses:
        "201":
          description: The newly created NuclearMedicineTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NuclearMedicineTestRequest"
        "400":
          description: Validation error
  /nuclear-medicine-test-requests/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one NuclearMedicineTestRequest by id
      operationId: getNuclearMedicineTestRequest
      responses:
        "200":
          description: The requested NuclearMedicineTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NuclearMedicineTestRequest"
        "404":
          description: NuclearMedicineTestRequest not found
    patch:
      summary: Update an existing NuclearMedicineTestRequest
      operationId: updateNuclearMedicineTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NuclearMedicineTestRequest"
      responses:
        "200":
          description: The updated NuclearMedicineTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NuclearMedicineTestRequest"
        "404":
          description: NuclearMedicineTestRequest not found
    delete:
      summary: Soft-delete a NuclearMedicineTestRequest
      operationId: deleteNuclearMedicineTestRequest
      responses:
        "204":
          description: NuclearMedicineTestRequest deleted
        "404":
          description: NuclearMedicineTestRequest not found
components:
  schemas:
    NuclearMedicineTestRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the requesting clinician (referrer).
        status:
          type: string
          enum:
            - draft
            - submitted
            - vetted
            - scheduled
            - rejected
            - ""
          description: "Request lifecycle status: draft, submitted, vetted, scheduled, rejected."
        site_name:
          type: string
          maxLength: 255
          description: Name of the requesting site or clinic.
        setting:
          type: string
          enum:
            - outpatient
            - inpatient
            - community
            - emergency
            - ""
          description: "Care setting: outpatient, inpatient, community, emergency."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        requested_by_date:
          type: string
          format: date
          description: Date by which the scan is requested to be performed.
        scan_type:
          type: string
          enum:
            - bone-scan
            - myocardial-perfusion
            - vq-lung-scan
            - thyroid-uptake
            - renal-dmsa
            - renal-mag3
            - gallium-octreotide
            - white-cell-scan
            - sentinel-node
            - other
            - ""
          description: "Requested radionuclide scan type: bone-scan, myocardial-perfusion, vq-lung-scan, thyroid-uptake, renal-dmsa, renal-mag3, gallium-octreotide, white-cell-scan, sentinel-node, other."
        primary_indication:
          type: string
          enum:
            - suspected-bone-metastases
            - cardiac-ischaemia
            - pulmonary-embolism
            - thyroid-function
            - renal-function
            - infection-localisation
            - tumour-localisation
            - sentinel-node-mapping
            - other
            - ""
          description: Primary clinical indication, used for appropriateness scoring.
        clinical_question:
          type: string
          maxLength: 1000
          description: Specific clinical question the scan should answer (highest-value field).
        relevant_history:
          type: string
          maxLength: 1000
          description: Relevant medical, surgical, and oncological history.
        pregnancy_status:
          type: string
          enum:
            - not-pregnant
            - pregnant
            - possible
            - unknown
            - not-applicable
            - ""
          description: "Pregnancy status for radiation justification: not-pregnant, pregnant, possible, unknown, not-applicable."
        breastfeeding:
          type: boolean
          description: Whether the patient is currently breastfeeding (affects radiopharmaceutical choice and interruption advice).
        egfr:
          type: number
          format: double
          description: Estimated glomerular filtration rate (mL/min/1.73m2), relevant to renal radiopharmaceutical handling.
        recent_other_nuclear_scan:
          type: boolean
          description: Whether the patient had another recent radionuclide study (residual activity may interfere with imaging).
        weight_kg:
          type: number
          format: double
          description: Patient weight in kg, used for administered-activity calculation.
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Requested triage urgency: routine, urgent, emergency."
        ir_me_r_justification:
          type: string
          maxLength: 1000
          description: IR(ME)R justification statement for the exposure (referrer-supplied clinical rationale).
        supervising_consultant:
          type: string
          maxLength: 255
          description: Name of the supervising / responsible consultant.
        requester_contact:
          type: string
          maxLength: 255
          description: Contact details (bleep / phone) for the requester.
        notes:
          type: string
          maxLength: 1000
          description: Free-text additional notes.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - status
        - site_name
        - setting
        - scan_type
        - primary_indication
        - clinical_question
        - relevant_history
        - pregnancy_status
        - breastfeeding
        - recent_other_nuclear_scan
        - urgency
        - ir_me_r_justification
        - supervising_consultant
        - requester_contact
        - notes
      description: Nuclear medicine (radionuclide imaging) request (referral). Captures the requested scan type, the clinical indication and question, relevant history, radiation-safety governance (pregnancy, breastfeeding, renal function, recent radionuclide exposure), IR(ME)R justification, and triage urgency.
