# Generated from sql/. Do not edit by hand.
# SOURCE: nuclear_medicine_test_request_grade_rule
openapi: 3.1.0
info:
  title: Nuclear Medicine Test Request — NuclearMedicineTestRequestGradeRule
  version: 1.0.0
  description: Audit trail of every appropriateness / safety / completeness / triage rule that fired for this request.
paths:
  /nuclear-medicine-test-request-grade-rules:
    get:
      summary: List NuclearMedicineTestRequestGradeRule records
      operationId: listNuclearMedicineTestRequestGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of NuclearMedicineTestRequestGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NuclearMedicineTestRequestGradeRule"
    post:
      summary: Create a new NuclearMedicineTestRequestGradeRule
      operationId: createNuclearMedicineTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NuclearMedicineTestRequestGradeRule"
      responses:
        "201":
          description: The newly created NuclearMedicineTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NuclearMedicineTestRequestGradeRule"
        "400":
          description: Validation error
  /nuclear-medicine-test-request-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one NuclearMedicineTestRequestGradeRule by id
      operationId: getNuclearMedicineTestRequestGradeRule
      responses:
        "200":
          description: The requested NuclearMedicineTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NuclearMedicineTestRequestGradeRule"
        "404":
          description: NuclearMedicineTestRequestGradeRule not found
    patch:
      summary: Update an existing NuclearMedicineTestRequestGradeRule
      operationId: updateNuclearMedicineTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NuclearMedicineTestRequestGradeRule"
      responses:
        "200":
          description: The updated NuclearMedicineTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NuclearMedicineTestRequestGradeRule"
        "404":
          description: NuclearMedicineTestRequestGradeRule not found
    delete:
      summary: Soft-delete a NuclearMedicineTestRequestGradeRule
      operationId: deleteNuclearMedicineTestRequestGradeRule
      responses:
        "204":
          description: NuclearMedicineTestRequestGradeRule deleted
        "404":
          description: NuclearMedicineTestRequestGradeRule not found
components:
  schemas:
    NuclearMedicineTestRequestGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        nuclear_medicine_test_request_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-APPROP-BONE-01).
        axis:
          type: string
          enum:
            - appropriateness
            - safety
            - completeness
            - triage
            - ""
          description: "Scoring axis the rule belongs to: appropriateness, safety, completeness, triage."
        category:
          type: string
          maxLength: 50
          description: Category or indication the rule relates to.
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - nuclear_medicine_test_request_grade_id
        - rule_id
        - axis
        - category
        - description
      description: Audit trail of every appropriateness / safety / completeness / triage rule that fired for this request.
