# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: nuclear_medicine_test_request_grade
openapi: 3.1.0
info:
  title: Nuclear Medicine Test Request — NuclearMedicineTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (ACR / RCR iRefer 1-9), preparation & radiation safety, request completeness, and triage priority."
paths:
  /nuclear-medicine-test-request-grades:
    get:
      summary: List NuclearMedicineTestRequestGrade records
      operationId: listNuclearMedicineTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of NuclearMedicineTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NuclearMedicineTestRequestGrade"
    post:
      summary: Create a new NuclearMedicineTestRequestGrade
      operationId: createNuclearMedicineTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NuclearMedicineTestRequestGrade"
      responses:
        "201":
          description: The newly created NuclearMedicineTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NuclearMedicineTestRequestGrade"
        "400":
          description: Validation error
  /nuclear-medicine-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one NuclearMedicineTestRequestGrade by id
      operationId: getNuclearMedicineTestRequestGrade
      responses:
        "200":
          description: The requested NuclearMedicineTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NuclearMedicineTestRequestGrade"
        "404":
          description: NuclearMedicineTestRequestGrade not found
    patch:
      summary: Update an existing NuclearMedicineTestRequestGrade
      operationId: updateNuclearMedicineTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NuclearMedicineTestRequestGrade"
      responses:
        "200":
          description: The updated NuclearMedicineTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NuclearMedicineTestRequestGrade"
        "404":
          description: NuclearMedicineTestRequestGrade not found
    delete:
      summary: Soft-delete a NuclearMedicineTestRequestGrade
      operationId: deleteNuclearMedicineTestRequestGrade
      responses:
        "204":
          description: NuclearMedicineTestRequestGrade deleted
        "404":
          description: NuclearMedicineTestRequestGrade not found
components:
  schemas:
    NuclearMedicineTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        nuclear_medicine_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: ACR Appropriateness Criteria / RCR iRefer score (1-9).
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        prep_safety_band:
          type: string
          enum:
            - ok
            - caution
            - contraindicated
            - ""
          description: "Preparation and radiation-safety band: ok, caution, contraindicated (pregnancy, breastfeeding, recent radionuclide interference)."
        radiation_dose_band:
          type: string
          enum:
            - low
            - moderate
            - high
            - ""
          description: "Effective-dose band for the requested radiopharmaceutical: low, moderate, high."
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Assigned triage tier: routine, urgent, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the scan implied by the triage tier.
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - nuclear_medicine_test_request_id
        - appropriateness_band
        - prep_safety_band
        - radiation_dose_band
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (ACR / RCR iRefer 1-9), preparation & radiation safety, request completeness, and triage priority."
