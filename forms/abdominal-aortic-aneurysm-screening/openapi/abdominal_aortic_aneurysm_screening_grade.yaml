# Generated from sql/. Do not edit by hand.
# SOURCE: abdominal_aortic_aneurysm_screening_grade
openapi: 3.1.0
info:
  title: Abdominal Aortic Aneurysm Screening — AbdominalAorticAneurysmScreeningGrade
  version: 1.0.0
  description: "Computed diameter classification for an abdominal-aortic-aneurysm-screening record: category, surveillance/referral band, recommended action, and interval growth."
paths:
  /abdominal-aortic-aneurysm-screening-grades:
    get:
      summary: List AbdominalAorticAneurysmScreeningGrade records
      operationId: listAbdominalAorticAneurysmScreeningGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of AbdominalAorticAneurysmScreeningGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AbdominalAorticAneurysmScreeningGrade"
    post:
      summary: Create a new AbdominalAorticAneurysmScreeningGrade
      operationId: createAbdominalAorticAneurysmScreeningGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbdominalAorticAneurysmScreeningGrade"
      responses:
        "201":
          description: The newly created AbdominalAorticAneurysmScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbdominalAorticAneurysmScreeningGrade"
        "400":
          description: Validation error
  /abdominal-aortic-aneurysm-screening-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one AbdominalAorticAneurysmScreeningGrade by id
      operationId: getAbdominalAorticAneurysmScreeningGrade
      responses:
        "200":
          description: The requested AbdominalAorticAneurysmScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbdominalAorticAneurysmScreeningGrade"
        "404":
          description: AbdominalAorticAneurysmScreeningGrade not found
    patch:
      summary: Update an existing AbdominalAorticAneurysmScreeningGrade
      operationId: updateAbdominalAorticAneurysmScreeningGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbdominalAorticAneurysmScreeningGrade"
      responses:
        "200":
          description: The updated AbdominalAorticAneurysmScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbdominalAorticAneurysmScreeningGrade"
        "404":
          description: AbdominalAorticAneurysmScreeningGrade not found
    delete:
      summary: Soft-delete a AbdominalAorticAneurysmScreeningGrade
      operationId: deleteAbdominalAorticAneurysmScreeningGrade
      responses:
        "204":
          description: AbdominalAorticAneurysmScreeningGrade deleted
        "404":
          description: AbdominalAorticAneurysmScreeningGrade not found
components:
  schemas:
    AbdominalAorticAneurysmScreeningGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        abdominal_aortic_aneurysm_screening_id:
          type: string
          format: uuid
          description: Foreign key to the parent abdominal-aortic-aneurysm-screening record (unique, 1:1).
        category:
          type: string
          enum:
            - normal
            - small
            - medium
            - large
            - non-visualised
            - ""
          description: "Diameter category: normal (< 3.0 cm), small (3.0-4.4 cm), medium (4.5-5.4 cm), large (>= 5.5 cm), or non-visualised."
        surveillance_band:
          type: string
          enum:
            - discharge
            - annual
            - three-monthly
            - refer-vascular
            - rescan
            - ""
          description: "Surveillance/referral band: discharge, annual, three-monthly, refer-vascular, or rescan."
        recommended_action:
          type: string
          description: Human-readable recommended action derived from the category.
        growth_cm:
          type: number
          format: double
          description: Interval growth in centimetres (current minus prior diameter) when both are present; null otherwise.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the classification.
      required:
        - created_at
        - updated_at
        - abdominal_aortic_aneurysm_screening_id
        - category
        - surveillance_band
        - recommended_action
        - graded_at
      description: "Computed diameter classification for an abdominal-aortic-aneurysm-screening record: category, surveillance/referral band, recommended action, and interval growth."
