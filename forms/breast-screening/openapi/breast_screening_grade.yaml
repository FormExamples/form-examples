# Generated from sql/. Do not edit by hand.
# SOURCE: breast_screening_grade
openapi: 3.1.0
info:
  title: Breast Screening — BreastScreeningGrade
  version: 1.0.0
  description: "Computed result classification for a breast-screening record: eligibility status, outcome band (result class), the recommended screening outcome / next action, and completeness status."
paths:
  /breast-screening-grades:
    get:
      summary: List BreastScreeningGrade records
      operationId: listBreastScreeningGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of BreastScreeningGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BreastScreeningGrade"
    post:
      summary: Create a new BreastScreeningGrade
      operationId: createBreastScreeningGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BreastScreeningGrade"
      responses:
        "201":
          description: The newly created BreastScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BreastScreeningGrade"
        "400":
          description: Validation error
  /breast-screening-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one BreastScreeningGrade by id
      operationId: getBreastScreeningGrade
      responses:
        "200":
          description: The requested BreastScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BreastScreeningGrade"
        "404":
          description: BreastScreeningGrade not found
    patch:
      summary: Update an existing BreastScreeningGrade
      operationId: updateBreastScreeningGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BreastScreeningGrade"
      responses:
        "200":
          description: The updated BreastScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BreastScreeningGrade"
        "404":
          description: BreastScreeningGrade not found
    delete:
      summary: Soft-delete a BreastScreeningGrade
      operationId: deleteBreastScreeningGrade
      responses:
        "204":
          description: BreastScreeningGrade deleted
        "404":
          description: BreastScreeningGrade not found
components:
  schemas:
    BreastScreeningGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        breast_screening_id:
          type: string
          format: uuid
          description: Foreign key to the parent breast-screening record (unique, 1:1).
        eligibility_status:
          type: string
          enum:
            - eligible
            - outside-age-range
            - higher-risk-surveillance
            - symptomatic-referral
            - ""
          description: "Derived eligibility status: eligible, outside-age-range, higher-risk-surveillance, or symptomatic-referral."
        result_class:
          type: string
          enum:
            - routine
            - repeat
            - assessment
            - urgent
            - referral
            - incomplete
            - ""
          description: "Outcome band classifying the screening outcome: routine, repeat, assessment, urgent, referral, or incomplete."
        management_action:
          type: string
          enum:
            - routine-recall
            - technical-repeat
            - recall-to-assessment-clinic
            - short-interval-follow-up
            - urgent-breast-clinic
            - symptomatic-pathway-referral
            - ""
          description: "Recommended screening outcome / next action: routine-recall, technical-repeat, recall-to-assessment-clinic, short-interval-follow-up, urgent-breast-clinic, or symptomatic-pathway-referral."
        status:
          type: string
          enum:
            - complete
            - incomplete
            - ""
          description: "Completeness status: complete when eligibility, consent, views, image adequacy, the reading outcome, and (after a recall) the imaging classification are all present; otherwise incomplete."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the classification.
      required:
        - created_at
        - updated_at
        - breast_screening_id
        - eligibility_status
        - result_class
        - management_action
        - status
        - graded_at
      description: "Computed result classification for a breast-screening record: eligibility status, outcome band (result class), the recommended screening outcome / next action, and completeness status."
