# Generated from sql/. Do not edit by hand.
# SOURCE: breast_screening_grade_rule
openapi: 3.1.0
info:
  title: Breast Screening — BreastScreeningGradeRule
  version: 1.0.0
  description: "Audit trail of every classification rule that fired during grading: pathway stage, category, and description."
paths:
  /breast-screening-grade-rules:
    get:
      summary: List BreastScreeningGradeRule records
      operationId: listBreastScreeningGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of BreastScreeningGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BreastScreeningGradeRule"
    post:
      summary: Create a new BreastScreeningGradeRule
      operationId: createBreastScreeningGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BreastScreeningGradeRule"
      responses:
        "201":
          description: The newly created BreastScreeningGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BreastScreeningGradeRule"
        "400":
          description: Validation error
  /breast-screening-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one BreastScreeningGradeRule by id
      operationId: getBreastScreeningGradeRule
      responses:
        "200":
          description: The requested BreastScreeningGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BreastScreeningGradeRule"
        "404":
          description: BreastScreeningGradeRule not found
    patch:
      summary: Update an existing BreastScreeningGradeRule
      operationId: updateBreastScreeningGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BreastScreeningGradeRule"
      responses:
        "200":
          description: The updated BreastScreeningGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BreastScreeningGradeRule"
        "404":
          description: BreastScreeningGradeRule not found
    delete:
      summary: Soft-delete a BreastScreeningGradeRule
      operationId: deleteBreastScreeningGradeRule
      responses:
        "204":
          description: BreastScreeningGradeRule deleted
        "404":
          description: BreastScreeningGradeRule not found
components:
  schemas:
    BreastScreeningGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        breast_screening_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-ELIGIBILITY-SYMPTOMATIC-01).
        stage:
          type: string
          enum:
            - eligibility
            - reading
            - assessment
            - outcome
            - completeness
            - ""
          description: "Pathway stage the rule concerns: eligibility, reading, assessment, outcome, or completeness."
        category:
          type: string
          maxLength: 50
          description: Subject category (e.g. gate, classification, conditional-requirement).
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - breast_screening_grade_id
        - rule_id
        - stage
        - category
        - description
      description: "Audit trail of every classification rule that fired during grading: pathway stage, category, and description."
