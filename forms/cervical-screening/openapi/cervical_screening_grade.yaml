# Generated from sql/. Do not edit by hand.
# SOURCE: cervical_screening_grade
openapi: 3.1.0
info:
  title: Cervical Screening — CervicalScreeningGrade
  version: 1.0.0
  description: "Computed result classification for a cervical-screening record: result class, recommended management action, and completeness status."
paths:
  /cervical-screening-grades:
    get:
      summary: List CervicalScreeningGrade records
      operationId: listCervicalScreeningGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CervicalScreeningGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CervicalScreeningGrade"
    post:
      summary: Create a new CervicalScreeningGrade
      operationId: createCervicalScreeningGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CervicalScreeningGrade"
      responses:
        "201":
          description: The newly created CervicalScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CervicalScreeningGrade"
        "400":
          description: Validation error
  /cervical-screening-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CervicalScreeningGrade by id
      operationId: getCervicalScreeningGrade
      responses:
        "200":
          description: The requested CervicalScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CervicalScreeningGrade"
        "404":
          description: CervicalScreeningGrade not found
    patch:
      summary: Update an existing CervicalScreeningGrade
      operationId: updateCervicalScreeningGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CervicalScreeningGrade"
      responses:
        "200":
          description: The updated CervicalScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CervicalScreeningGrade"
        "404":
          description: CervicalScreeningGrade not found
    delete:
      summary: Soft-delete a CervicalScreeningGrade
      operationId: deleteCervicalScreeningGrade
      responses:
        "204":
          description: CervicalScreeningGrade deleted
        "404":
          description: CervicalScreeningGrade not found
components:
  schemas:
    CervicalScreeningGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        cervical_screening_id:
          type: string
          format: uuid
          description: Foreign key to the parent cervical-screening record (unique, 1:1).
        result_class:
          type: string
          enum:
            - inadequate
            - hpv-negative
            - hpv-positive-cytology-normal
            - hpv-positive-cytology-abnormal-low
            - hpv-positive-cytology-abnormal-high
            - hpv-positive-cytology-pending
            - cease-not-eligible
            - pending
            - ""
          description: "Result classification: inadequate, hpv-negative, hpv-positive-cytology-normal, hpv-positive-cytology-abnormal-low, hpv-positive-cytology-abnormal-high, hpv-positive-cytology-pending, cease-not-eligible, or pending."
        management_action:
          type: string
          enum:
            - routine-recall
            - early-repeat-12-months
            - colposcopy-referral
            - urgent-colposcopy-referral
            - repeat-sample-3-months
            - cease-screening
            - awaiting-cytology
            - awaiting-result
            - ""
          description: "Recommended management action: routine-recall, early-repeat-12-months, colposcopy-referral, urgent-colposcopy-referral, repeat-sample-3-months, cease-screening, awaiting-cytology, or awaiting-result."
        status:
          type: string
          enum:
            - complete
            - incomplete
            - ""
          description: "Completeness status: complete when eligibility, consent, adequacy, and the required result fields for the reached branch are all present; otherwise incomplete."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the classification.
      required:
        - created_at
        - updated_at
        - cervical_screening_id
        - result_class
        - management_action
        - status
        - graded_at
      description: "Computed result classification for a cervical-screening record: result class, recommended management action, and completeness status."
