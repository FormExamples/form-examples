# Generated from sql/. Do not edit by hand.
# SOURCE: cervical_screening
openapi: 3.1.0
info:
  title: Cervical Screening — CervicalScreening
  version: 1.0.0
  description: "Main cervical-screening record for one HPV-primary screening encounter: encounter context, patient identification, eligibility, consent, symptoms, sample adequacy, the primary hrHPV result, and reflex cytology. Result classification, fired rules, and flags live in child tables."
paths:
  /cervical-screenings:
    get:
      summary: List CervicalScreening records
      operationId: listCervicalScreening
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CervicalScreening records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CervicalScreening"
    post:
      summary: Create a new CervicalScreening
      operationId: createCervicalScreening
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CervicalScreening"
      responses:
        "201":
          description: The newly created CervicalScreening
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CervicalScreening"
        "400":
          description: Validation error
  /cervical-screenings/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CervicalScreening by id
      operationId: getCervicalScreening
      responses:
        "200":
          description: The requested CervicalScreening
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CervicalScreening"
        "404":
          description: CervicalScreening not found
    patch:
      summary: Update an existing CervicalScreening
      operationId: updateCervicalScreening
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CervicalScreening"
      responses:
        "200":
          description: The updated CervicalScreening
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CervicalScreening"
        "404":
          description: CervicalScreening not found
    delete:
      summary: Soft-delete a CervicalScreening
      operationId: deleteCervicalScreening
      responses:
        "204":
          description: CervicalScreening deleted
        "404":
          description: CervicalScreening not found
components:
  schemas:
    CervicalScreening:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient this screening documents (restrict delete).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the sample-taker clinician (restrict delete); optional.
        care_setting:
          type: string
          enum:
            - general-practice
            - sexual-health
            - other
            - ""
          description: "Care setting where the sample was taken: general-practice, sexual-health, or other."
        sample_taker_role:
          type: string
          enum:
            - practice-nurse
            - gp
            - smear-taker
            - other
            - ""
          description: "Role of the person taking the sample: practice-nurse, gp, smear-taker, or other."
        sample_taken_at:
          type: string
          format: date-time
          description: Date and time the screening sample was taken.
        patient_identifier:
          type: string
          maxLength: 100
          description: Local patient or encounter identifier as recorded on the record.
        age:
          type: integer
          format: int32
          description: Patient age in years; drives eligibility (25-64) and the recall interval.
        recall_interval:
          type: string
          enum:
            - three-yearly
            - five-yearly
            - not-applicable
            - ""
          description: "Age-appropriate routine recall interval: three-yearly (25-49), five-yearly (50-64), or not-applicable."
        screen_due_date:
          type: string
          format: date
          description: Date the screen was due.
        last_screen_date:
          type: string
          format: date
          description: Date of the most recent previous screening, when known.
        overdue:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the screening is overdue (yes/no); drives the patient-overdue flag.
        previously_ceased:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the person has been formally ceased from screening (yes/no); yes classifies as cease-not-eligible.
        consent_given:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether informed consent to sample and process was given (yes/no); anything other than yes raises the missing-consent flag.
        symptomatic:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient reports symptoms (abnormal bleeding, discharge, pain) (yes/no); yes raises the symptomatic-refer-regardless flag.
        symptom_detail:
          type: string
          description: Free-text description of reported symptoms.
        sample_adequacy:
          type: string
          enum:
            - adequate
            - inadequate
            - ""
          description: "Cytological sample adequacy: adequate or inadequate; an inadequate sample is never HPV-classified and is repeated."
        inadequate_reason:
          type: string
          enum:
            - insufficient-cells
            - obscuring-blood
            - inflammation
            - labelling
            - other
            - ""
          description: "Reason a sample was inadequate: insufficient-cells, obscuring-blood, inflammation, labelling, or other."
        hpv_result:
          type: string
          enum:
            - negative
            - positive
            - not-tested
            - ""
          description: "Primary high-risk HPV (hrHPV) result: negative, positive, or not-tested."
        cytology_grade:
          type: string
          enum:
            - negative
            - borderline
            - low-grade
            - high-grade
            - not-performed
            - ""
          description: "Reflex cytology dyskaryosis grade (recorded only when hpv_result is positive): negative, borderline, low-grade, high-grade, or not-performed."
        clinical_context:
          type: string
          description: Optional free-text clinical context shown in the summary.
      required:
        - created_at
        - updated_at
        - patient_id
        - care_setting
        - sample_taker_role
        - patient_identifier
        - recall_interval
        - overdue
        - previously_ceased
        - consent_given
        - symptomatic
        - symptom_detail
        - sample_adequacy
        - inadequate_reason
        - hpv_result
        - cytology_grade
        - clinical_context
      description: "Main cervical-screening record for one HPV-primary screening encounter: encounter context, patient identification, eligibility, consent, symptoms, sample adequacy, the primary hrHPV result, and reflex cytology. Result classification, fired rules, and flags live in child tables."
