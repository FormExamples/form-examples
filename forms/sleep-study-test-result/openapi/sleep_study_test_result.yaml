# Generated from sql/. Do not edit by hand.
# SOURCE: sleep_study_test_result
openapi: 3.1.0
info:
  title: Sleep Study Test Result — SleepStudyTestResult
  version: 1.0.0
  description: Sleep study / polysomnography result (report). Captures the performed study type and adequacy, clinical history, the key quantitative metrics (recording and sleep time, apnoea-hypopnoea index, oxygen desaturation index, minimum and time-below-90% SpO2, mean heart rate), the OSA severity band, the narrative and structured findings, the impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /sleep-study-test-results:
    get:
      summary: List SleepStudyTestResult records
      operationId: listSleepStudyTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of SleepStudyTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SleepStudyTestResult"
    post:
      summary: Create a new SleepStudyTestResult
      operationId: createSleepStudyTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SleepStudyTestResult"
      responses:
        "201":
          description: The newly created SleepStudyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SleepStudyTestResult"
        "400":
          description: Validation error
  /sleep-study-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one SleepStudyTestResult by id
      operationId: getSleepStudyTestResult
      responses:
        "200":
          description: The requested SleepStudyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SleepStudyTestResult"
        "404":
          description: SleepStudyTestResult not found
    patch:
      summary: Update an existing SleepStudyTestResult
      operationId: updateSleepStudyTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SleepStudyTestResult"
      responses:
        "200":
          description: The updated SleepStudyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SleepStudyTestResult"
        "404":
          description: SleepStudyTestResult not found
    delete:
      summary: Soft-delete a SleepStudyTestResult
      operationId: deleteSleepStudyTestResult
      responses:
        "204":
          description: SleepStudyTestResult deleted
        "404":
          description: SleepStudyTestResult not found
components:
  schemas:
    SleepStudyTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (respiratory / sleep physician or clinical physiologist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating sleep study request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the sleep study was performed (night of recording).
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        study_type:
          type: string
          enum:
            - home-sleep-apnoea-test
            - polysomnography
            - overnight-oximetry
            - multiple-sleep-latency-test
            - actigraphy
            - other
            - ""
          description: "Performed study type: home-sleep-apnoea-test, polysomnography, overnight-oximetry, multiple-sleep-latency-test, actigraphy, other."
        study_adequacy:
          type: string
          enum:
            - adequate
            - limited
            - failed
            - ""
          description: "Diagnostic adequacy of the study: adequate, limited, failed (e.g. insufficient recording time or signal loss)."
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the study was performed to answer.
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous sleep studies.
        total_recording_time_hours:
          type: number
          format: double
          description: Total recording time in hours.
        total_sleep_time_hours:
          type: number
          format: double
          description: Total sleep time in hours (for attended polysomnography with sleep staging).
        apnoea_hypopnoea_index:
          type: number
          format: double
          description: "Apnoea-Hypopnoea Index (AHI): events per hour. AASM bands: normal <5, mild 5 to <15, moderate 15 to <30, severe >=30."
        oxygen_desaturation_index:
          type: number
          format: double
          description: "Oxygen Desaturation Index (ODI): >=3% (or >=4%) desaturations per hour."
        minimum_spo2_percent:
          type: number
          format: double
          description: Lowest (nadir) oxygen saturation recorded, as a percentage.
        time_below_90_percent_spo2:
          type: number
          format: double
          description: Percentage of recording / sleep time spent with SpO2 below 90% (T90).
        mean_heart_rate_bpm:
          type: integer
          format: int32
          description: Mean heart rate during the study, in beats per minute.
        osa_severity:
          type: string
          enum:
            - none
            - mild
            - moderate
            - severe
            - ""
          description: "OSA severity band derived from AHI: none, mild, moderate, severe."
        obstructive_sleep_apnoea:
          type: boolean
          description: Whether obstructive sleep apnoea is present.
        central_sleep_apnoea:
          type: boolean
          description: Whether central sleep apnoea (e.g. Cheyne-Stokes respiration) is present.
        periodic_limb_movements:
          type: boolean
          description: Whether periodic limb movements of sleep are present.
        nocturnal_hypoventilation:
          type: boolean
          description: Whether nocturnal hypoventilation (e.g. sustained hypercapnia / obesity hypoventilation) is present.
        significant_desaturation:
          type: boolean
          description: Whether significant oxygen desaturation is present.
        normal_study:
          type: boolean
          description: Whether the study is normal (no significant sleep-disordered breathing).
        findings_narrative:
          type: string
          maxLength: 2000
          description: Narrative description of the study findings (the body of the report).
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 50
          description: "Free-text structured-reporting label for the AHI severity band (e.g. \"Severe OSA (AHI >=30)\")."
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up management (e.g. CPAP titration, ventilation review, sleep-clinic referral).
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - study_type
        - study_adequacy
        - clinical_history
        - comparison_with_previous
        - osa_severity
        - obstructive_sleep_apnoea
        - central_sleep_apnoea
        - periodic_limb_movements
        - nocturnal_hypoventilation
        - significant_desaturation
        - normal_study
        - findings_narrative
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Sleep study / polysomnography result (report). Captures the performed study type and adequacy, clinical history, the key quantitative metrics (recording and sleep time, apnoea-hypopnoea index, oxygen desaturation index, minimum and time-below-90% SpO2, mean heart rate), the OSA severity band, the narrative and structured findings, the impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
