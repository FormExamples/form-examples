# Front-end form with HTML — HTML front-end (form + dashboard)

Consolidated single-directory HTML front-end: `index.html` is the single-page
wizard; `dashboard.html` is the dashboard. Shared `css/` and `js/`.
