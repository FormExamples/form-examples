# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: okr_objective
openapi: 3.1.0
info:
  title: Objectives And Key Results Tracker — OkrObjective
  version: 1.0.0
  description: Main OKR objective row. Holds reporter and metadata, all step fields, and the seven raw input scores. Computed scores live in okr_grade.
paths:
  /okr-objectives:
    get:
      summary: List OkrObjective records
      operationId: listOkrObjective
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of OkrObjective records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OkrObjective"
    post:
      summary: Create a new OkrObjective
      operationId: createOkrObjective
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OkrObjective"
      responses:
        "201":
          description: The newly created OkrObjective
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkrObjective"
        "400":
          description: Validation error
  /okr-objectives/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one OkrObjective by id
      operationId: getOkrObjective
      responses:
        "200":
          description: The requested OkrObjective
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkrObjective"
        "404":
          description: OkrObjective not found
    patch:
      summary: Update an existing OkrObjective
      operationId: updateOkrObjective
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OkrObjective"
      responses:
        "200":
          description: The updated OkrObjective
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkrObjective"
        "404":
          description: OkrObjective not found
    delete:
      summary: Soft-delete a OkrObjective
      operationId: deleteOkrObjective
      responses:
        "204":
          description: OkrObjective deleted
        "404":
          description: OkrObjective not found
components:
  schemas:
    OkrObjective:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was soft-deleted.
        reporter_id:
          type: string
          format: uuid
          description: Foreign key to the reporter who submitted the objective.
        parent_objective_id:
          type: string
          format: uuid
          description: "Self-referential foreign key: the parent objective this one ladders up to. NULL for top-level objectives."
        status:
          type: string
          enum:
            - draft
            - active
            - at-risk
            - achieved
            - missed
            - retired
            - cancelled
          description: "Lifecycle status: draft, active, at-risk, achieved, missed, retired, cancelled."
        level:
          type: string
          enum:
            - individual
            - team
            - department
            - company
            - ""
          description: "Organisational level: individual, team, department, company."
        cycle:
          type: string
          enum:
            - monthly
            - quarterly
            - half-yearly
            - annual
            - custom
            - ""
          description: "Time horizon: monthly, quarterly, half-yearly, annual, custom."
        cycle_start_date:
          type: string
          format: date
          description: First day of the OKR cycle.
        cycle_end_date:
          type: string
          format: date
          description: Last day of the OKR cycle.
        team_or_org_name:
          type: string
          description: Owning team or organisational unit name.
        strategic_theme:
          type: string
          description: Strategic theme or pillar this objective ladders to.
        external_reference:
          type: string
          description: External tracker id, ticket, or strategy doc reference.
        obj_title:
          type: string
          description: "Step 2: one-sentence qualitative objective statement."
        obj_long_description:
          type: string
          description: "Step 2: free-text long description of the objective."
        sa_parent_summary:
          type: string
          description: "Step 4: how this objective ladders to the parent or to the mission."
        sa_business_value_statement:
          type: string
          description: "Step 4: business value statement — why this objective matters."
        in_initiatives:
          type: string
          description: "Step 6: planned actions, projects, programmes that drive each KR."
        in_supporting_links:
          type: string
          description: "Step 6: free-text supporting links and references."
        rk_known_risks:
          type: string
          description: "Step 7: known risks to achieving the objective."
        rk_dependencies:
          type: string
          description: "Step 7: external dependencies."
        rk_blockers:
          type: string
          description: "Step 7: current blockers."
        rk_mitigation_plans:
          type: string
          description: "Step 7: planned mitigations for the risks above."
        fc_expected_end_state:
          type: string
          description: "Step 9: forecast — expected end-of-cycle state per KR (free text)."
        fc_residual_risk:
          type: string
          description: "Step 9: residual risk to the objective at end-of-cycle."
        score_by_progress_percent:
          type: number
          format: double
          description: "Score 1 of 7: progress percent 0-100. Mean of per-KR progress_fraction (clamped) expressed as percent."
        score_by_confidence_decile:
          type: integer
          format: int32
          description: "Score 2 of 7: owner confidence decile 1-10."
        score_by_stretch_tier:
          type: integer
          format: int32
          description: "Score 3 of 7: stretch tier 1-3 — 1=committed, 2=aspirational, 3=moonshot."
        score_by_alignment_grade:
          type: integer
          format: int32
          description: "Score 4 of 7: alignment grade 1-5 to parent or strategic theme."
        score_by_impact_tier:
          type: integer
          format: int32
          description: "Score 5 of 7: impact tier 1-5 — business value if achieved."
        score_by_smart_quality:
          type: integer
          format: int32
          description: "Score 6 of 7: SMART criteria count 0-5 (Specific, Measurable, Achievable, Relevant, Time-bound)."
        score_by_pace_deviation_percent:
          type: number
          format: double
          description: "Score 7 of 7: pace deviation percent -100..+100 from expected linear pace at this point in the cycle."
      required:
        - created_at
        - updated_at
        - reporter_id
        - status
        - level
        - cycle
        - team_or_org_name
        - strategic_theme
        - external_reference
        - obj_title
        - obj_long_description
        - sa_parent_summary
        - sa_business_value_statement
        - in_initiatives
        - in_supporting_links
        - rk_known_risks
        - rk_dependencies
        - rk_blockers
        - rk_mitigation_plans
        - fc_expected_end_state
        - fc_residual_risk
      description: Main OKR objective row. Holds reporter and metadata, all step fields, and the seven raw input scores. Computed scores live in okr_grade.
