# Generated from sql/. Do not edit by hand.
# SOURCE: grade
openapi: 3.1.0
info:
  title: Vaccinations Checklist — Grade
  version: 1.0.0
  description: Computed vaccination compliance grading result. Compliance status and risk level. One-to-one child of assessment.
paths:
  /grades:
    get:
      summary: List Grade records
      operationId: listGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Grade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      summary: Create a new Grade
      operationId: createGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "201":
          description: The newly created Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "400":
          description: Validation error
  /grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Grade by id
      operationId: getGrade
      responses:
        "200":
          description: The requested Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    patch:
      summary: Update an existing Grade
      operationId: updateGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "200":
          description: The updated Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    delete:
      summary: Soft-delete a Grade
      operationId: deleteGrade
      responses:
        "204":
          description: Grade deleted
        "404":
          description: Grade not found
components:
  schemas:
    Grade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the parent assessment (unique, enforcing 1:1).
        compliance_status:
          type: string
          enum:
            - fully-immunised
            - partially-immunised
            - non-compliant
            - contraindicated
            - ""
          description: "Overall compliance: fully-immunised, partially-immunised, non-compliant, contraindicated, or empty."
        overall_risk_level:
          type: string
          enum:
            - low
            - moderate
            - high
            - critical
            - ""
          description: "Overall vaccination risk level: low, moderate, high, critical, or empty."
        childhood_complete:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Whether childhood immunisation schedule is complete: yes, no, or empty."
        occupational_complete:
          type: string
          enum:
            - "yes"
            - "no"
            - not-required
            - ""
          description: "Whether occupational vaccine requirements are met: yes, no, not-required, or empty."
        covid_complete:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Whether COVID-19 vaccination is up to date: yes, no, or empty."
        flu_current:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Whether current season flu vaccine has been received: yes, no, or empty."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the grading was computed.
      required:
        - created_at
        - updated_at
        - assessment_id
        - compliance_status
        - overall_risk_level
        - childhood_complete
        - occupational_complete
        - covid_complete
        - flu_current
        - graded_at
      description: Computed vaccination compliance grading result. Compliance status and risk level. One-to-one child of assessment.
