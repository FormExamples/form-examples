# Generated from sql/. Do not edit by hand.
# SOURCE: mammography_test_request
openapi: 3.1.0
info:
  title: Mammography Test Request — MammographyTestRequest
  version: 1.0.0
  description: Breast mammography imaging request (referral). Captures the requested examination, the clinical indication and question, symptoms, breast history / risk factors, and triage urgency.
paths:
  /mammography-test-requests:
    get:
      summary: List MammographyTestRequest records
      operationId: listMammographyTestRequest
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of MammographyTestRequest records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MammographyTestRequest"
    post:
      summary: Create a new MammographyTestRequest
      operationId: createMammographyTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MammographyTestRequest"
      responses:
        "201":
          description: The newly created MammographyTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MammographyTestRequest"
        "400":
          description: Validation error
  /mammography-test-requests/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one MammographyTestRequest by id
      operationId: getMammographyTestRequest
      responses:
        "200":
          description: The requested MammographyTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MammographyTestRequest"
        "404":
          description: MammographyTestRequest not found
    patch:
      summary: Update an existing MammographyTestRequest
      operationId: updateMammographyTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MammographyTestRequest"
      responses:
        "200":
          description: The updated MammographyTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MammographyTestRequest"
        "404":
          description: MammographyTestRequest not found
    delete:
      summary: Soft-delete a MammographyTestRequest
      operationId: deleteMammographyTestRequest
      responses:
        "204":
          description: MammographyTestRequest deleted
        "404":
          description: MammographyTestRequest not found
components:
  schemas:
    MammographyTestRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the requesting clinician (referrer).
        status:
          type: string
          enum:
            - draft
            - submitted
            - vetted
            - scheduled
            - rejected
            - ""
          description: "Request lifecycle status: draft, submitted, vetted, scheduled, rejected."
        site_name:
          type: string
          maxLength: 255
          description: Name of the requesting site or clinic.
        setting:
          type: string
          enum:
            - outpatient
            - inpatient
            - community
            - screening-unit
            - emergency
            - ""
          description: "Care setting: outpatient, inpatient, community, screening-unit, emergency."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        requested_by_date:
          type: string
          format: date
          description: Date by which the examination is requested to be performed.
        exam_type:
          type: string
          enum:
            - screening
            - diagnostic
            - symptomatic
            - surveillance
            - other
            - ""
          description: "Type of mammography requested: screening, diagnostic, symptomatic, surveillance, other."
        primary_indication:
          type: string
          enum:
            - routine-screening
            - breast-lump
            - breast-pain
            - nipple-discharge
            - skin-change
            - family-history
            - follow-up-known-cancer
            - post-treatment-surveillance
            - recall-from-screening
            - other
            - ""
          description: Primary clinical indication, used for appropriateness scoring.
        clinical_question:
          type: string
          maxLength: 1000
          description: Specific clinical question the examination should answer (highest-value field).
        relevant_history:
          type: string
          maxLength: 1000
          description: Relevant medical, surgical, and breast history.
        laterality:
          type: string
          enum:
            - left
            - right
            - bilateral
            - not-applicable
            - ""
          description: "Affected side: left, right, bilateral, not-applicable."
        symptom_lump:
          type: boolean
          description: Whether the patient reports a breast lump.
        symptom_pain:
          type: boolean
          description: Whether the patient reports breast pain.
        symptom_nipple_discharge:
          type: boolean
          description: Whether the patient reports nipple discharge.
        symptom_skin_change:
          type: boolean
          description: Whether the patient reports a skin change (dimpling, peau d'orange, erythema).
        symptom_nipple_inversion:
          type: boolean
          description: Whether the patient reports new nipple inversion or retraction.
        previous_mammogram:
          type: string
          enum:
            - none
            - normal
            - abnormal
            - unknown
            - ""
          description: "Previous mammogram outcome: none, normal, abnormal, unknown."
        previous_mammogram_date:
          type: string
          format: date
          description: Date of the most recent previous mammogram.
        family_history_breast_cancer:
          type: boolean
          description: Whether there is a family history of breast cancer.
        breast_implants:
          type: boolean
          description: Whether the patient has breast implants (affects technique and views).
        pregnancy_or_lactating:
          type: string
          enum:
            - "no"
            - pregnant
            - lactating
            - unknown
            - ""
          description: "Pregnancy / lactation status: no, pregnant, lactating, unknown (affects radiation justification)."
        hormone_replacement_therapy:
          type: boolean
          description: Whether the patient is on hormone replacement therapy (affects breast density).
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - two-week-wait
            - emergency
            - ""
          description: "Requested triage urgency: routine, urgent, two-week-wait, emergency."
        supervising_consultant:
          type: string
          maxLength: 255
          description: Name of the supervising / responsible consultant.
        requester_contact:
          type: string
          maxLength: 255
          description: Contact details (bleep / phone) for the requester.
        notes:
          type: string
          maxLength: 1000
          description: Free-text additional notes.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - status
        - site_name
        - setting
        - exam_type
        - primary_indication
        - clinical_question
        - relevant_history
        - laterality
        - symptom_lump
        - symptom_pain
        - symptom_nipple_discharge
        - symptom_skin_change
        - symptom_nipple_inversion
        - previous_mammogram
        - family_history_breast_cancer
        - breast_implants
        - pregnancy_or_lactating
        - hormone_replacement_therapy
        - urgency
        - supervising_consultant
        - requester_contact
        - notes
      description: Breast mammography imaging request (referral). Captures the requested examination, the clinical indication and question, symptoms, breast history / risk factors, and triage urgency.
