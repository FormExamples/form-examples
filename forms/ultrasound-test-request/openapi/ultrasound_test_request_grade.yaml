# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: ultrasound_test_request_grade
openapi: 3.1.0
info:
  title: Ultrasound Test Request — UltrasoundTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (ACR 1-9), preparation / technical suitability, request completeness, and triage priority."
paths:
  /ultrasound-test-request-grades:
    get:
      summary: List UltrasoundTestRequestGrade records
      operationId: listUltrasoundTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of UltrasoundTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UltrasoundTestRequestGrade"
    post:
      summary: Create a new UltrasoundTestRequestGrade
      operationId: createUltrasoundTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UltrasoundTestRequestGrade"
      responses:
        "201":
          description: The newly created UltrasoundTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UltrasoundTestRequestGrade"
        "400":
          description: Validation error
  /ultrasound-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one UltrasoundTestRequestGrade by id
      operationId: getUltrasoundTestRequestGrade
      responses:
        "200":
          description: The requested UltrasoundTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UltrasoundTestRequestGrade"
        "404":
          description: UltrasoundTestRequestGrade not found
    patch:
      summary: Update an existing UltrasoundTestRequestGrade
      operationId: updateUltrasoundTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UltrasoundTestRequestGrade"
      responses:
        "200":
          description: The updated UltrasoundTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UltrasoundTestRequestGrade"
        "404":
          description: UltrasoundTestRequestGrade not found
    delete:
      summary: Soft-delete a UltrasoundTestRequestGrade
      operationId: deleteUltrasoundTestRequestGrade
      responses:
        "204":
          description: UltrasoundTestRequestGrade deleted
        "404":
          description: UltrasoundTestRequestGrade not found
components:
  schemas:
    UltrasoundTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        ultrasound_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: ACR Appropriateness Criteria score (1-9).
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        suitability_band:
          type: string
          enum:
            - ok
            - caution
            - limited
            - ""
          description: "Preparation / technical suitability band: ok, caution, limited (driven by fasting / full-bladder prep and body habitus)."
        prep_requirements:
          type: string
          description: Human-readable preparation requirements and technical caveats (fasting, full bladder, body habitus).
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Assigned triage tier: routine, urgent, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the scan implied by the triage tier.
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - ultrasound_test_request_id
        - appropriateness_band
        - suitability_band
        - prep_requirements
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (ACR 1-9), preparation / technical suitability, request completeness, and triage priority."
