# Generated from sql/. Do not edit by hand.
# SOURCE: diabetic_eye_screening_grade_rule
openapi: 3.1.0
info:
  title: Diabetic Eye Screening — DiabeticEyeScreeningGradeRule
  version: 1.0.0
  description: "Audit trail of every classification rule that fired during grading: pathway stage, category, and description."
paths:
  /diabetic-eye-screening-grade-rules:
    get:
      summary: List DiabeticEyeScreeningGradeRule records
      operationId: listDiabeticEyeScreeningGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of DiabeticEyeScreeningGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DiabeticEyeScreeningGradeRule"
    post:
      summary: Create a new DiabeticEyeScreeningGradeRule
      operationId: createDiabeticEyeScreeningGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiabeticEyeScreeningGradeRule"
      responses:
        "201":
          description: The newly created DiabeticEyeScreeningGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiabeticEyeScreeningGradeRule"
        "400":
          description: Validation error
  /diabetic-eye-screening-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one DiabeticEyeScreeningGradeRule by id
      operationId: getDiabeticEyeScreeningGradeRule
      responses:
        "200":
          description: The requested DiabeticEyeScreeningGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiabeticEyeScreeningGradeRule"
        "404":
          description: DiabeticEyeScreeningGradeRule not found
    patch:
      summary: Update an existing DiabeticEyeScreeningGradeRule
      operationId: updateDiabeticEyeScreeningGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiabeticEyeScreeningGradeRule"
      responses:
        "200":
          description: The updated DiabeticEyeScreeningGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiabeticEyeScreeningGradeRule"
        "404":
          description: DiabeticEyeScreeningGradeRule not found
    delete:
      summary: Soft-delete a DiabeticEyeScreeningGradeRule
      operationId: deleteDiabeticEyeScreeningGradeRule
      responses:
        "204":
          description: DiabeticEyeScreeningGradeRule deleted
        "404":
          description: DiabeticEyeScreeningGradeRule not found
components:
  schemas:
    DiabeticEyeScreeningGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        diabetic_eye_screening_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-RECALL-R3A-01).
        stage:
          type: string
          enum:
            - eligibility
            - retinopathy
            - maculopathy
            - ungradable
            - recall
            - completeness
            - ""
          description: "Pathway stage the rule concerns: eligibility, retinopathy, maculopathy, ungradable, recall, or completeness."
        category:
          type: string
          maxLength: 50
          description: Subject category (e.g. gate, classification, conditional-requirement).
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - diabetic_eye_screening_grade_id
        - rule_id
        - stage
        - category
        - description
      description: "Audit trail of every classification rule that fired during grading: pathway stage, category, and description."
