# Generated from sql/. Do not edit by hand.
# SOURCE: anaesthetic_record_grade
openapi: 3.1.0
info:
  title: Anaesthetic Record — AnaestheticRecordGrade
  version: 1.0.0
  description: "Computed completeness grading result for an anaesthetic record: overall status and completeness percent."
paths:
  /anaesthetic-record-grades:
    get:
      summary: List AnaestheticRecordGrade records
      operationId: listAnaestheticRecordGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of AnaestheticRecordGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AnaestheticRecordGrade"
    post:
      summary: Create a new AnaestheticRecordGrade
      operationId: createAnaestheticRecordGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnaestheticRecordGrade"
      responses:
        "201":
          description: The newly created AnaestheticRecordGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnaestheticRecordGrade"
        "400":
          description: Validation error
  /anaesthetic-record-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one AnaestheticRecordGrade by id
      operationId: getAnaestheticRecordGrade
      responses:
        "200":
          description: The requested AnaestheticRecordGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnaestheticRecordGrade"
        "404":
          description: AnaestheticRecordGrade not found
    patch:
      summary: Update an existing AnaestheticRecordGrade
      operationId: updateAnaestheticRecordGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnaestheticRecordGrade"
      responses:
        "200":
          description: The updated AnaestheticRecordGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnaestheticRecordGrade"
        "404":
          description: AnaestheticRecordGrade not found
    delete:
      summary: Soft-delete a AnaestheticRecordGrade
      operationId: deleteAnaestheticRecordGrade
      responses:
        "204":
          description: AnaestheticRecordGrade deleted
        "404":
          description: AnaestheticRecordGrade not found
components:
  schemas:
    AnaestheticRecordGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        anaesthetic_record_id:
          type: string
          format: uuid
          description: Foreign key to the parent anaesthetic record (unique, 1:1).
        status:
          type: string
          enum:
            - complete
            - partial
            - incomplete
            - ""
          description: "Overall completeness status: complete, partial, or incomplete."
        completeness_percent:
          type: integer
          format: int32
          description: Proportion (0-100) of mandatory-item rules satisfied.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the grade.
      required:
        - created_at
        - updated_at
        - anaesthetic_record_id
        - status
        - completeness_percent
        - graded_at
      description: "Computed completeness grading result for an anaesthetic record: overall status and completeness percent."
