# Generated from sql/. Do not edit by hand.
# SOURCE: anaesthetic_record_grade_flag
openapi: 3.1.0
info:
  title: Anaesthetic Record — AnaestheticRecordGradeFlag
  version: 1.0.0
  description: Safety flags raised independently of the completeness status, with priority and a suggested action for the anaesthetic team.
paths:
  /anaesthetic-record-grade-flags:
    get:
      summary: List AnaestheticRecordGradeFlag records
      operationId: listAnaestheticRecordGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of AnaestheticRecordGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AnaestheticRecordGradeFlag"
    post:
      summary: Create a new AnaestheticRecordGradeFlag
      operationId: createAnaestheticRecordGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnaestheticRecordGradeFlag"
      responses:
        "201":
          description: The newly created AnaestheticRecordGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnaestheticRecordGradeFlag"
        "400":
          description: Validation error
  /anaesthetic-record-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one AnaestheticRecordGradeFlag by id
      operationId: getAnaestheticRecordGradeFlag
      responses:
        "200":
          description: The requested AnaestheticRecordGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnaestheticRecordGradeFlag"
        "404":
          description: AnaestheticRecordGradeFlag not found
    patch:
      summary: Update an existing AnaestheticRecordGradeFlag
      operationId: updateAnaestheticRecordGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnaestheticRecordGradeFlag"
      responses:
        "200":
          description: The updated AnaestheticRecordGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnaestheticRecordGradeFlag"
        "404":
          description: AnaestheticRecordGradeFlag not found
    delete:
      summary: Soft-delete a AnaestheticRecordGradeFlag
      operationId: deleteAnaestheticRecordGradeFlag
      responses:
        "204":
          description: AnaestheticRecordGradeFlag deleted
        "404":
          description: AnaestheticRecordGradeFlag not found
components:
  schemas:
    AnaestheticRecordGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        anaesthetic_record_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-WHO-CHECKLIST-NOT-DONE-001).
        category:
          type: string
          enum:
            - who-checklist-not-done
            - allergy-conflict
            - difficult-airway
            - drug-anaphylaxis
            - unlogged-consent
            - physiological-derangement
            - incomplete
            - other
            - ""
          description: "Flag category: who-checklist-not-done, allergy-conflict, difficult-airway, drug-anaphylaxis, unlogged-consent, physiological-derangement, incomplete, or other."
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested anaesthetic action (e.g. \"complete the WHO Time Out before proceeding\")."
      required:
        - created_at
        - updated_at
        - anaesthetic_record_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Safety flags raised independently of the completeness status, with priority and a suggested action for the anaesthetic team.
