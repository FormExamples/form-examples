# Plan: Systematic Coronary Risk Evaluation 2 Diabetes — patient form (SvelteKit)

## Current status

Scaffold only. Project structure and documentation are in place; the interactive patient form (SvelteKit) is pending.

## Future work

- Build the scoring engine in `src/lib/engine/` with Vitest tests
- Build the step components in `src/lib/components/steps/`
- Build the assessment store in `src/lib/stores/`
- Wire routes for `/`, `/assessment`, and `/report`
