# Generated from sql/. Do not edit by hand.
# SOURCE: pulmonary_function_test_result
openapi: 3.1.0
info:
  title: Pulmonary Function Test Result — PulmonaryFunctionTestResult
  version: 1.0.0
  description: Pulmonary function test (lung-function / spirometry) result (report). Captures the performed test type and quality, clinical history, the measured spirometry / gas-transfer values, the structured ventilatory interpretation (pattern, severity, bronchodilator reversibility), the narrative and structured findings, the impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /pulmonary-function-test-results:
    get:
      summary: List PulmonaryFunctionTestResult records
      operationId: listPulmonaryFunctionTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PulmonaryFunctionTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PulmonaryFunctionTestResult"
    post:
      summary: Create a new PulmonaryFunctionTestResult
      operationId: createPulmonaryFunctionTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PulmonaryFunctionTestResult"
      responses:
        "201":
          description: The newly created PulmonaryFunctionTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PulmonaryFunctionTestResult"
        "400":
          description: Validation error
  /pulmonary-function-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PulmonaryFunctionTestResult by id
      operationId: getPulmonaryFunctionTestResult
      responses:
        "200":
          description: The requested PulmonaryFunctionTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PulmonaryFunctionTestResult"
        "404":
          description: PulmonaryFunctionTestResult not found
    patch:
      summary: Update an existing PulmonaryFunctionTestResult
      operationId: updatePulmonaryFunctionTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PulmonaryFunctionTestResult"
      responses:
        "200":
          description: The updated PulmonaryFunctionTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PulmonaryFunctionTestResult"
        "404":
          description: PulmonaryFunctionTestResult not found
    delete:
      summary: Soft-delete a PulmonaryFunctionTestResult
      operationId: deletePulmonaryFunctionTestResult
      responses:
        "204":
          description: PulmonaryFunctionTestResult deleted
        "404":
          description: PulmonaryFunctionTestResult not found
components:
  schemas:
    PulmonaryFunctionTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (respiratory physician / clinical physiologist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating pulmonary function test request (referral).
        test_type:
          type: string
          enum:
            - spirometry
            - spirometry-with-reversibility
            - full-lung-function
            - gas-transfer-dlco
            - peak-flow
            - feno
            - other
            - ""
          description: "Performed test type: spirometry, spirometry-with-reversibility, full-lung-function, gas-transfer-dlco, peak-flow, feno, other."
        performed_date:
          type: string
          format: date
          description: Date the lung-function test was performed.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        test_quality:
          type: string
          enum:
            - acceptable
            - sub-optimal
            - unacceptable
            - ""
          description: "ATS/ERS acceptability and repeatability grade for the manoeuvres: acceptable, sub-optimal, unacceptable."
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the test was performed to answer.
        fev1_litres:
          type: number
          format: double
          description: Forced expiratory volume in one second (FEV1) in litres (post-bronchodilator where reversibility tested).
        fev1_percent_predicted:
          type: number
          format: double
          description: FEV1 as a percentage of the predicted (reference) value.
        fvc_litres:
          type: number
          format: double
          description: Forced vital capacity (FVC) in litres.
        fvc_percent_predicted:
          type: number
          format: double
          description: FVC as a percentage of the predicted (reference) value.
        fev1_fvc_ratio:
          type: number
          format: double
          description: FEV1/FVC ratio (0-1). Post-bronchodilator ratio < 0.70 confirms airflow obstruction (NICE NG115 / GOLD).
        peak_expiratory_flow:
          type: number
          format: double
          description: Peak expiratory flow (PEF) in litres per minute.
        dlco_percent_predicted:
          type: number
          format: double
          description: Diffusing capacity of the lung for carbon monoxide (DLCO / transfer factor) as a percentage of predicted.
        ventilatory_pattern:
          type: string
          enum:
            - normal
            - obstructive
            - restrictive
            - mixed
            - ""
          description: "Overall ventilatory pattern: normal, obstructive, restrictive, mixed."
        severity:
          type: string
          enum:
            - none
            - mild
            - moderate
            - severe
            - very-severe
            - ""
          description: "Severity of the lung-function impairment: none, mild, moderate, severe, very-severe (ATS/ERS z-score or GOLD percent-predicted banding)."
        bronchodilator_reversibility:
          type: string
          enum:
            - positive
            - negative
            - not-tested
            - ""
          description: "Bronchodilator reversibility result: positive, negative, not-tested."
        airflow_obstruction:
          type: boolean
          description: Whether airflow obstruction (reduced FEV1/FVC) is present.
        restriction:
          type: boolean
          description: Whether a restrictive pattern (reduced FVC / TLC with preserved ratio) is present.
        reduced_gas_transfer:
          type: boolean
          description: Whether reduced gas transfer (low DLCO / transfer factor) is present.
        significant_reversibility:
          type: boolean
          description: Whether significant bronchodilator reversibility was demonstrated.
        normal_spirometry:
          type: boolean
          description: Whether the spirometry is within normal limits.
        findings_narrative:
          type: string
          maxLength: 2000
          description: Narrative description of the lung-function findings (the body of the report).
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous lung-function studies (e.g. decline in FEV1).
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 50
          description: Free-text structured-reporting category label (e.g. a GOLD 1-4 or ATS/ERS severity band).
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up testing, referral, or management.
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - test_type
        - report_status
        - test_quality
        - clinical_history
        - ventilatory_pattern
        - severity
        - bronchodilator_reversibility
        - airflow_obstruction
        - restriction
        - reduced_gas_transfer
        - significant_reversibility
        - normal_spirometry
        - findings_narrative
        - comparison_with_previous
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Pulmonary function test (lung-function / spirometry) result (report). Captures the performed test type and quality, clinical history, the measured spirometry / gas-transfer values, the structured ventilatory interpretation (pattern, severity, bronchodilator reversibility), the narrative and structured findings, the impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
