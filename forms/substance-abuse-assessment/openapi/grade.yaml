# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: grade
openapi: 3.1.0
info:
  title: Substance Abuse Assessment — Grade
  version: 1.0.0
  description: Computed substance abuse grading result. AUDIT score 0-40, DAST-10 score 0-10, and combined risk level. One-to-one child of assessment.
paths:
  /grades:
    get:
      summary: List Grade records
      operationId: listGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Grade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      summary: Create a new Grade
      operationId: createGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "201":
          description: The newly created Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "400":
          description: Validation error
  /grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Grade by id
      operationId: getGrade
      responses:
        "200":
          description: The requested Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    patch:
      summary: Update an existing Grade
      operationId: updateGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "200":
          description: The updated Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    delete:
      summary: Soft-delete a Grade
      operationId: deleteGrade
      responses:
        "204":
          description: Grade deleted
        "404":
          description: Grade not found
components:
  schemas:
    Grade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the parent assessment (unique, enforcing 1:1).
        audit_score:
          type: integer
          format: int32
          description: AUDIT total score (0-40).
        audit_risk_category:
          type: string
          enum:
            - low-risk
            - hazardous
            - harmful
            - dependence-likely
            - ""
          description: "AUDIT risk category: low-risk, hazardous, harmful, dependence-likely, or empty."
        dast_score:
          type: integer
          format: int32
          description: DAST-10 total score (0-10).
        dast_risk_category:
          type: string
          enum:
            - no-problems
            - low
            - moderate
            - substantial
            - severe
            - ""
          description: "DAST risk category: no-problems, low, moderate, substantial, severe, or empty."
        overall_risk_level:
          type: string
          enum:
            - low
            - moderate
            - high
            - critical
            - ""
          description: "Overall substance abuse risk level: low, moderate, high, critical, or empty."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the grading was computed.
      required:
        - created_at
        - updated_at
        - assessment_id
        - audit_risk_category
        - dast_risk_category
        - overall_risk_level
        - graded_at
      description: Computed substance abuse grading result. AUDIT score 0-40, DAST-10 score 0-10, and combined risk level. One-to-one child of assessment.
