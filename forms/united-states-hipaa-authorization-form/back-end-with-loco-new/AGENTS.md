# back-end-with-loco-new — Agent Instructions

Scratch directory for re-bootstrapping the Loco full-stack crate via
`loco new`. The `00-new.sh` script (when present) is generated by
`bin/create-form` and runs the bootstrap.

Canonical full-stack code lives in
`../back-end-with-loco/`. Do not implement business
logic here.
