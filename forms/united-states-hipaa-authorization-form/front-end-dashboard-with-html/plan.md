# Plan: front-end-dashboard-with-html

Static HTML dashboard listing signed HIPAA authorizations.

## Build order

1. [x] `index.html` with a `<table>` skeleton.
2. [x] `css/style.css` with status badges.
3. [x] `js/app.js` to load and render rows.
4. [x] `js/sample-data.js` for standalone preview.
5. [ ] Column-sort handlers.
6. [ ] Dropdown filters (validity, purpose, expiration band).
7. [ ] Click-through to authorization detail (HTML form).
