# US HIPAA Authorization — full-stack (in-progress new template)

Working scratch space for the next iteration of the
`full-stack-with-loco-tera-htmx-alpine/` crate. The companion
`00-new.sh` shell script is generated by `bin/create-form` and runs
`loco new` to produce a fresh project skeleton.

This subdirectory is intentionally minimal — the canonical full-stack
implementation lives in `../full-stack-with-loco-tera-htmx-alpine/`.
