# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: expiration
openapi: 3.1.0
info:
  title: United States Hipaa Authorization Form — Expiration
  version: 1.0.0
  description: "Expiration date or expiration event for the authorization. One-to-one child of hipaa_authorization. Required by 45 CFR § 164.508(c)(1)(v); the string \"none\" is not permitted (except for research)."
paths:
  /expirations:
    get:
      summary: List Expiration records
      operationId: listExpiration
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Expiration records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Expiration"
    post:
      summary: Create a new Expiration
      operationId: createExpiration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Expiration"
      responses:
        "201":
          description: The newly created Expiration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Expiration"
        "400":
          description: Validation error
  /expirations/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Expiration by id
      operationId: getExpiration
      responses:
        "200":
          description: The requested Expiration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Expiration"
        "404":
          description: Expiration not found
    patch:
      summary: Update an existing Expiration
      operationId: updateExpiration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Expiration"
      responses:
        "200":
          description: The updated Expiration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Expiration"
        "404":
          description: Expiration not found
    delete:
      summary: Soft-delete a Expiration
      operationId: deleteExpiration
      responses:
        "204":
          description: Expiration deleted
        "404":
          description: Expiration not found
components:
  schemas:
    Expiration:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        hipaa_authorization_id:
          type: string
          format: uuid
          description: Foreign key to the parent HIPAA authorization (unique, enforcing 1:1).
        kind:
          type: string
          enum:
            - ""
            - date
            - event
            - duration
          description: "Expiration mechanism: date, event, duration, or empty if unanswered."
        expiration_date:
          type: string
          format: date
          description: Calendar expiration date (used when kind is date).
        expiration_event:
          type: string
          description: "Free-text expiration event (e.g. \"upon conclusion of my claim\"). Used when kind is event. Cannot be the string \"none\"."
        duration_months:
          type: integer
          format: int32
          description: Validity duration in months from signature (e.g. 12). Used when kind is duration.
        duration_label:
          type: string
          maxLength: 80
          description: "Human-readable duration label (e.g. \"12 months from signature\")."
      required:
        - created_at
        - updated_at
        - hipaa_authorization_id
        - kind
        - expiration_event
        - duration_label
      description: "Expiration date or expiration event for the authorization. One-to-one child of hipaa_authorization. Required by 45 CFR § 164.508(c)(1)(v); the string \"none\" is not permitted (except for research)."
