# Generated from sql/. Do not edit by hand.
# SOURCE: has_bled_score_for_major_bleeding_risk
openapi: 3.1.0
info:
  title: Has Bled Score For Major Bleeding Risk — HasBledScoreForMajorBleedingRisk
  version: 1.0.0
  description: "Main HAS-BLED assessment record: assessment context, patient identification, and the nine scored criterion inputs (hypertension, abnormal renal function, abnormal liver function, stroke, bleeding, labile INR, elderly, drugs, alcohol)."
paths:
  /has-bled-score-for-major-bleeding-risks:
    get:
      summary: List HasBledScoreForMajorBleedingRisk records
      operationId: listHasBledScoreForMajorBleedingRisk
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HasBledScoreForMajorBleedingRisk records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HasBledScoreForMajorBleedingRisk"
    post:
      summary: Create a new HasBledScoreForMajorBleedingRisk
      operationId: createHasBledScoreForMajorBleedingRisk
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HasBledScoreForMajorBleedingRisk"
      responses:
        "201":
          description: The newly created HasBledScoreForMajorBleedingRisk
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HasBledScoreForMajorBleedingRisk"
        "400":
          description: Validation error
  /has-bled-score-for-major-bleeding-risks/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HasBledScoreForMajorBleedingRisk by id
      operationId: getHasBledScoreForMajorBleedingRisk
      responses:
        "200":
          description: The requested HasBledScoreForMajorBleedingRisk
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HasBledScoreForMajorBleedingRisk"
        "404":
          description: HasBledScoreForMajorBleedingRisk not found
    patch:
      summary: Update an existing HasBledScoreForMajorBleedingRisk
      operationId: updateHasBledScoreForMajorBleedingRisk
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HasBledScoreForMajorBleedingRisk"
      responses:
        "200":
          description: The updated HasBledScoreForMajorBleedingRisk
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HasBledScoreForMajorBleedingRisk"
        "404":
          description: HasBledScoreForMajorBleedingRisk not found
    delete:
      summary: Soft-delete a HasBledScoreForMajorBleedingRisk
      operationId: deleteHasBledScoreForMajorBleedingRisk
      responses:
        "204":
          description: HasBledScoreForMajorBleedingRisk deleted
        "404":
          description: HasBledScoreForMajorBleedingRisk not found
components:
  schemas:
    HasBledScoreForMajorBleedingRisk:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient being assessed (delete restricted).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the assessing clinician (optional; delete restricted).
        clinician_name:
          type: string
          maxLength: 255
          description: Name of the assessing clinician as recorded on the assessment.
        clinician_role:
          type: string
          enum:
            - doctor
            - nurse
            - pharmacist
            - other
            - ""
          description: "Role of the assessing clinician: doctor, nurse, pharmacist, or other."
        assessed_at:
          type: string
          format: date-time
          description: Date and time the assessment was performed.
        care_setting:
          type: string
          enum:
            - cardiology
            - general-practice
            - anticoagulation-clinic
            - acute-medical
            - other
            - ""
          description: "Care setting: cardiology, general-practice, anticoagulation-clinic, acute-medical, or other."
        anticoagulation_status:
          type: string
          enum:
            - "on"
            - considering
            - ""
          description: "Whether the patient is already on, or is being considered for, oral anticoagulation: on or considering."
        cha_ds_vasc_score:
          type: integer
          format: int32
          description: Optional paired CHA2DS2-VASc stroke-risk score (0-9), recorded as context only; not computed by this form.
        patient_identifier:
          type: string
          maxLength: 100
          description: Local patient identifier as recorded on the assessment.
        age_years:
          type: integer
          format: int32
          description: Patient age in whole years; drives criterion E (elderly, > 65). NULL when unanswered, contributing 0 points and raising a completeness flag.
        sex:
          type: string
          enum:
            - female
            - male
            - intersex
            - unknown
            - ""
          description: "Patient sex recorded for the assessment: female, male, intersex, or unknown."
        hypertension_uncontrolled:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion H — uncontrolled hypertension (systolic blood pressure > 160 mmHg): yes or no."
        abnormal_renal_function:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion A (renal) — abnormal renal function (dialysis, transplant, or creatinine >= 200 umol/L): yes or no."
        abnormal_liver_function:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion A (liver) — abnormal liver function (cirrhosis, or bilirubin > 2x ULN with transaminases > 3x ULN): yes or no."
        stroke_history:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion S — prior stroke: yes or no."
        bleeding_history:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion B — prior major bleed, bleeding diathesis, or anaemia: yes or no."
        labile_inr:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion L — labile INR (unstable or high INR, or time in therapeutic range < 60%): yes or no."
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_name
        - clinician_role
        - care_setting
        - anticoagulation_status
        - patient_identifier
        - sex
        - hypertension_uncontrolled
        - abnormal_renal_function
        - abnormal_liver_function
        - stroke_history
        - bleeding_history
        - labile_inr
      description: "Main HAS-BLED assessment record: assessment context, patient identification, and the nine scored criterion inputs (hypertension, abnormal renal function, abnormal liver function, stroke, bleeding, labile INR, elderly, drugs, alcohol)."
