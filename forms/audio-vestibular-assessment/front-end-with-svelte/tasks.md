# Tasks: Audio-Vestibular Assessment — front-end (SvelteKit)

## Done

- [x] Port the scoring engine (types, rules, grader, flagged-issues, utils) to TypeScript
- [x] Vitest coverage of the WHO PTA grade and DHI scoring
- [x] Nine step components (`StepNName.svelte`)
- [x] Id-keyed reactive store with `deepAssign` merge and localStorage persistence
- [x] RESTful routes: dashboard (`ssr = false`) + wizard + report + report PDF
- [x] Welcome page, themed layout, 45 Lily themes, `ThemeSelect`
- [x] Lily token utilities only (palette grep clean)
- [x] `pnpm run check` 0/0, `pnpm run build` succeeds, `pnpm exec vitest run` passes
