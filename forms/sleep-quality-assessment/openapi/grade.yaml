# Generated from sql/. Do not edit by hand.
# SOURCE: grade
openapi: 3.1.0
info:
  title: Sleep Quality Assessment — Grade
  version: 1.0.0
  description: Computed PSQI sleep quality grading result with 7 component scores. One-to-one child of assessment.
paths:
  /grades:
    get:
      summary: List Grade records
      operationId: listGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Grade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      summary: Create a new Grade
      operationId: createGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "201":
          description: The newly created Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "400":
          description: Validation error
  /grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Grade by id
      operationId: getGrade
      responses:
        "200":
          description: The requested Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    patch:
      summary: Update an existing Grade
      operationId: updateGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "200":
          description: The updated Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    delete:
      summary: Soft-delete a Grade
      operationId: deleteGrade
      responses:
        "204":
          description: Grade deleted
        "404":
          description: Grade not found
components:
  schemas:
    Grade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the parent assessment (unique, enforcing 1:1).
        psqi_global_score:
          type: integer
          format: int32
          description: PSQI global score (0-21); 0-5 good, 6-10 poor, 11-21 very poor sleep quality.
        psqi_component1_subjective_quality:
          type: integer
          format: int32
          description: "PSQI Component 1: subjective sleep quality (0-3)."
        psqi_component2_sleep_latency:
          type: integer
          format: int32
          description: "PSQI Component 2: sleep latency (0-3)."
        psqi_component3_sleep_duration:
          type: integer
          format: int32
          description: "PSQI Component 3: sleep duration (0-3)."
        psqi_component4_sleep_efficiency:
          type: integer
          format: int32
          description: "PSQI Component 4: habitual sleep efficiency (0-3)."
        psqi_component5_sleep_disturbances:
          type: integer
          format: int32
          description: "PSQI Component 5: sleep disturbances (0-3)."
        psqi_component6_sleep_medication:
          type: integer
          format: int32
          description: "PSQI Component 6: use of sleeping medication (0-3)."
        psqi_component7_daytime_dysfunction:
          type: integer
          format: int32
          description: "PSQI Component 7: daytime dysfunction (0-3)."
        sleep_quality_category:
          type: string
          enum:
            - good
            - poor
            - very_poor
            - ""
          description: "Sleep quality category: good (0-5), poor (6-10), very_poor (11-21), or empty string if not yet graded."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the grading was computed.
      required:
        - created_at
        - updated_at
        - assessment_id
        - sleep_quality_category
        - graded_at
      description: Computed PSQI sleep quality grading result with 7 component scores. One-to-one child of assessment.
