# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: lumbar_puncture_test_request_grade
openapi: 3.1.0
info:
  title: Lumbar Puncture Test Request — LumbarPunctureTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (1-9 indication match), safety / contraindication band, request completeness, and triage priority."
paths:
  /lumbar-puncture-test-request-grades:
    get:
      summary: List LumbarPunctureTestRequestGrade records
      operationId: listLumbarPunctureTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of LumbarPunctureTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LumbarPunctureTestRequestGrade"
    post:
      summary: Create a new LumbarPunctureTestRequestGrade
      operationId: createLumbarPunctureTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LumbarPunctureTestRequestGrade"
      responses:
        "201":
          description: The newly created LumbarPunctureTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LumbarPunctureTestRequestGrade"
        "400":
          description: Validation error
  /lumbar-puncture-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one LumbarPunctureTestRequestGrade by id
      operationId: getLumbarPunctureTestRequestGrade
      responses:
        "200":
          description: The requested LumbarPunctureTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LumbarPunctureTestRequestGrade"
        "404":
          description: LumbarPunctureTestRequestGrade not found
    patch:
      summary: Update an existing LumbarPunctureTestRequestGrade
      operationId: updateLumbarPunctureTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LumbarPunctureTestRequestGrade"
      responses:
        "200":
          description: The updated LumbarPunctureTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LumbarPunctureTestRequestGrade"
        "404":
          description: LumbarPunctureTestRequestGrade not found
    delete:
      summary: Soft-delete a LumbarPunctureTestRequestGrade
      operationId: deleteLumbarPunctureTestRequestGrade
      responses:
        "204":
          description: LumbarPunctureTestRequestGrade deleted
        "404":
          description: LumbarPunctureTestRequestGrade not found
components:
  schemas:
    LumbarPunctureTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        lumbar_puncture_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9 ordinal) for the indication match.
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        contraindication_band:
          type: string
          enum:
            - ok
            - caution
            - contraindicated
            - ""
          description: "Safety / contraindication band: ok, caution, contraindicated — driven by raised ICP needing imaging first, coagulopathy / anticoagulation, thrombocytopenia, and local infection."
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Assigned triage tier: routine, urgent, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the procedure implied by the triage tier.
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - lumbar_puncture_test_request_id
        - appropriateness_band
        - contraindication_band
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (1-9 indication match), safety / contraindication band, request completeness, and triage priority."
