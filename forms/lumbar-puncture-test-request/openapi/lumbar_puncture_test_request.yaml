# Generated from sql/. Do not edit by hand.
# SOURCE: lumbar_puncture_test_request
openapi: 3.1.0
info:
  title: Lumbar Puncture Test Request — LumbarPunctureTestRequest
  version: 1.0.0
  description: Lumbar puncture request (referral) for CSF sampling and/or manometry. Captures the procedure intent, the clinical indication and question, raised-intracranial-pressure and bleeding safety screening, opening-pressure requirement, and triage urgency.
paths:
  /lumbar-puncture-test-requests:
    get:
      summary: List LumbarPunctureTestRequest records
      operationId: listLumbarPunctureTestRequest
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of LumbarPunctureTestRequest records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LumbarPunctureTestRequest"
    post:
      summary: Create a new LumbarPunctureTestRequest
      operationId: createLumbarPunctureTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LumbarPunctureTestRequest"
      responses:
        "201":
          description: The newly created LumbarPunctureTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LumbarPunctureTestRequest"
        "400":
          description: Validation error
  /lumbar-puncture-test-requests/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one LumbarPunctureTestRequest by id
      operationId: getLumbarPunctureTestRequest
      responses:
        "200":
          description: The requested LumbarPunctureTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LumbarPunctureTestRequest"
        "404":
          description: LumbarPunctureTestRequest not found
    patch:
      summary: Update an existing LumbarPunctureTestRequest
      operationId: updateLumbarPunctureTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LumbarPunctureTestRequest"
      responses:
        "200":
          description: The updated LumbarPunctureTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LumbarPunctureTestRequest"
        "404":
          description: LumbarPunctureTestRequest not found
    delete:
      summary: Soft-delete a LumbarPunctureTestRequest
      operationId: deleteLumbarPunctureTestRequest
      responses:
        "204":
          description: LumbarPunctureTestRequest deleted
        "404":
          description: LumbarPunctureTestRequest not found
components:
  schemas:
    LumbarPunctureTestRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the requesting clinician (referrer).
        status:
          type: string
          enum:
            - draft
            - submitted
            - vetted
            - scheduled
            - rejected
            - ""
          description: "Request lifecycle status: draft, submitted, vetted, scheduled, rejected."
        site_name:
          type: string
          maxLength: 255
          description: Name of the requesting site or clinic.
        setting:
          type: string
          enum:
            - outpatient
            - inpatient
            - community
            - emergency
            - ""
          description: "Care setting: outpatient, inpatient, community, emergency."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        requested_by_date:
          type: string
          format: date
          description: Date by which the procedure is requested to be performed.
        procedure_intent:
          type: string
          enum:
            - diagnostic
            - therapeutic
            - other
            - ""
          description: "Procedure intent: diagnostic (CSF sampling / manometry), therapeutic (e.g. CSF drainage), other."
        primary_indication:
          type: string
          enum:
            - suspected-meningitis
            - suspected-subarachnoid-haemorrhage
            - suspected-multiple-sclerosis
            - suspected-guillain-barre
            - idiopathic-intracranial-hypertension
            - suspected-cns-malignancy
            - cns-infection
            - other
            - ""
          description: "Primary clinical indication, used for appropriateness scoring: suspected-meningitis, suspected-subarachnoid-haemorrhage, suspected-multiple-sclerosis, suspected-guillain-barre, idiopathic-intracranial-hypertension, suspected-cns-malignancy, cns-infection, other."
        clinical_question:
          type: string
          maxLength: 1000
          description: Specific clinical question the CSF analysis should answer (highest-value field).
        relevant_history:
          type: string
          maxLength: 1000
          description: Relevant medical, neurological, and medication history.
        suspected_raised_intracranial_pressure:
          type: boolean
          description: Whether raised intracranial pressure is clinically suspected (LP may risk cerebral herniation; imaging first).
        focal_neurological_signs:
          type: boolean
          description: Whether new focal neurological signs are present (risk factor for space-occupying lesion; image before LP).
        reduced_consciousness:
          type: boolean
          description: Whether the patient has reduced consciousness (e.g. GCS 9 or less; image and stabilise before LP).
        ct_head_status:
          type: string
          enum:
            - done-normal
            - done-abnormal
            - not-required
            - awaited
            - ""
          description: "CT head imaging status prior to LP: done-normal, done-abnormal, not-required, awaited."
        taking_anticoagulant:
          type: boolean
          description: Whether the patient is taking an anticoagulant (bleeding / spinal haematoma risk).
        anticoagulant_agent:
          type: string
          maxLength: 255
          description: Name of the anticoagulant agent (e.g. warfarin, apixaban, rivaroxaban, dabigatran, heparin).
        taking_antiplatelet:
          type: boolean
          description: Whether the patient is taking an antiplatelet agent (bleeding risk).
        antiplatelet_agent:
          type: string
          maxLength: 255
          description: Name of the antiplatelet agent (e.g. aspirin, clopidogrel, ticagrelor, prasugrel).
        inr:
          type: number
          format: double
          description: International normalised ratio (INR); LP generally avoided or delayed if INR > 1.5.
        platelet_count:
          type: number
          format: double
          description: Platelet count (x10^9/L); LP generally avoided if platelets < 40-50.
        bleeding_disorder:
          type: boolean
          description: Whether the patient has a known bleeding disorder or coagulopathy.
        local_skin_infection:
          type: boolean
          description: Whether there is local skin or soft-tissue infection at the puncture site (contraindication).
        opening_pressure_required:
          type: boolean
          description: Whether manometry (CSF opening pressure measurement) is required, e.g. for idiopathic intracranial hypertension.
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Requested triage urgency: routine, urgent, emergency."
        supervising_consultant:
          type: string
          maxLength: 255
          description: Name of the supervising / responsible consultant.
        requester_contact:
          type: string
          maxLength: 255
          description: Contact details (bleep / phone) for the requester.
        notes:
          type: string
          maxLength: 1000
          description: Free-text additional notes.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - status
        - site_name
        - setting
        - procedure_intent
        - primary_indication
        - clinical_question
        - relevant_history
        - suspected_raised_intracranial_pressure
        - focal_neurological_signs
        - reduced_consciousness
        - ct_head_status
        - taking_anticoagulant
        - anticoagulant_agent
        - taking_antiplatelet
        - antiplatelet_agent
        - bleeding_disorder
        - local_skin_infection
        - opening_pressure_required
        - urgency
        - supervising_consultant
        - requester_contact
        - notes
      description: Lumbar puncture request (referral) for CSF sampling and/or manometry. Captures the procedure intent, the clinical indication and question, raised-intracranial-pressure and bleeding safety screening, opening-pressure requirement, and triage urgency.
