# Generated from sql/. Do not edit by hand.
# SOURCE: medication_reconciliation_grade_rule
openapi: 3.1.0
info:
  title: Medication Reconciliation — MedicationReconciliationGradeRule
  version: 1.0.0
  description: "Audit trail of every reconciliation rule that fired during grading: category and description."
paths:
  /medication-reconciliation-grade-rules:
    get:
      summary: List MedicationReconciliationGradeRule records
      operationId: listMedicationReconciliationGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of MedicationReconciliationGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MedicationReconciliationGradeRule"
    post:
      summary: Create a new MedicationReconciliationGradeRule
      operationId: createMedicationReconciliationGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MedicationReconciliationGradeRule"
      responses:
        "201":
          description: The newly created MedicationReconciliationGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicationReconciliationGradeRule"
        "400":
          description: Validation error
  /medication-reconciliation-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one MedicationReconciliationGradeRule by id
      operationId: getMedicationReconciliationGradeRule
      responses:
        "200":
          description: The requested MedicationReconciliationGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicationReconciliationGradeRule"
        "404":
          description: MedicationReconciliationGradeRule not found
    patch:
      summary: Update an existing MedicationReconciliationGradeRule
      operationId: updateMedicationReconciliationGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MedicationReconciliationGradeRule"
      responses:
        "200":
          description: The updated MedicationReconciliationGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicationReconciliationGradeRule"
        "404":
          description: MedicationReconciliationGradeRule not found
    delete:
      summary: Soft-delete a MedicationReconciliationGradeRule
      operationId: deleteMedicationReconciliationGradeRule
      responses:
        "204":
          description: MedicationReconciliationGradeRule deleted
        "404":
          description: MedicationReconciliationGradeRule not found
components:
  schemas:
    MedicationReconciliationGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        medication_reconciliation_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-STATUS-INCOMPLETE-01).
        category:
          type: string
          maxLength: 50
          description: Subject category (e.g. status, discrepancy-classification).
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - medication_reconciliation_grade_id
        - rule_id
        - category
        - description
      description: "Audit trail of every reconciliation rule that fired during grading: category and description."
