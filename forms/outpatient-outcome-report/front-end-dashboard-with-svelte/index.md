# Outpatient Outcome Report — SvelteKit dashboard

Minimal SvelteKit scaffold for the outpatient outcome report clinician dashboard. The interactive SVAR DataGrid implementation is pending; see `../plan.md`.

## Run

```sh
pnpm install
pnpm run dev
```
