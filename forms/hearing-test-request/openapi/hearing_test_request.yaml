# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: hearing_test_request
openapi: 3.1.0
info:
  title: Hearing Test Request — HearingTestRequest
  version: 1.0.0
  description: Audiology / hearing-assessment request (referral). Captures the requested test, laterality, the clinical indication and question, relevant history, symptoms / red flags, and triage urgency.
paths:
  /hearing-test-requests:
    get:
      summary: List HearingTestRequest records
      operationId: listHearingTestRequest
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HearingTestRequest records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HearingTestRequest"
    post:
      summary: Create a new HearingTestRequest
      operationId: createHearingTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HearingTestRequest"
      responses:
        "201":
          description: The newly created HearingTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HearingTestRequest"
        "400":
          description: Validation error
  /hearing-test-requests/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HearingTestRequest by id
      operationId: getHearingTestRequest
      responses:
        "200":
          description: The requested HearingTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HearingTestRequest"
        "404":
          description: HearingTestRequest not found
    patch:
      summary: Update an existing HearingTestRequest
      operationId: updateHearingTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HearingTestRequest"
      responses:
        "200":
          description: The updated HearingTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HearingTestRequest"
        "404":
          description: HearingTestRequest not found
    delete:
      summary: Soft-delete a HearingTestRequest
      operationId: deleteHearingTestRequest
      responses:
        "204":
          description: HearingTestRequest deleted
        "404":
          description: HearingTestRequest not found
components:
  schemas:
    HearingTestRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the requesting clinician (referrer).
        status:
          type: string
          enum:
            - draft
            - submitted
            - vetted
            - scheduled
            - rejected
            - ""
          description: "Request lifecycle status: draft, submitted, vetted, scheduled, rejected."
        site_name:
          type: string
          maxLength: 255
          description: Name of the requesting site or clinic.
        setting:
          type: string
          enum:
            - outpatient
            - inpatient
            - community
            - emergency
            - ""
          description: "Care setting: outpatient, inpatient, community, emergency."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        requested_by_date:
          type: string
          format: date
          description: Date by which the test is requested to be performed.
        test_type:
          type: string
          enum:
            - pure-tone-audiometry
            - tympanometry
            - speech-audiometry
            - otoacoustic-emissions
            - auditory-brainstem-response
            - newborn-hearing-screen
            - hearing-aid-assessment
            - other
            - ""
          description: "Requested audiology test type: pure-tone-audiometry, tympanometry, speech-audiometry, otoacoustic-emissions, auditory-brainstem-response, newborn-hearing-screen, hearing-aid-assessment, other."
        laterality:
          type: string
          enum:
            - left
            - right
            - bilateral
            - not-applicable
            - ""
          description: "Affected side: left, right, bilateral, not-applicable."
        primary_indication:
          type: string
          enum:
            - hearing-loss
            - tinnitus
            - vertigo
            - ear-discharge
            - suspected-otosclerosis
            - occupational-noise
            - ototoxic-monitoring
            - developmental-delay-child
            - hearing-aid-review
            - sudden-hearing-loss
            - other
            - ""
          description: Primary clinical indication, used for appropriateness scoring.
        clinical_question:
          type: string
          maxLength: 1000
          description: Specific clinical question the test should answer (highest-value field).
        relevant_history:
          type: string
          maxLength: 1000
          description: Relevant medical, otological, and noise-exposure history.
        symptom_hearing_loss:
          type: boolean
          description: Whether the patient reports hearing loss.
        symptom_tinnitus:
          type: boolean
          description: Whether the patient reports tinnitus.
        symptom_vertigo:
          type: boolean
          description: Whether the patient reports vertigo or imbalance.
        symptom_otalgia:
          type: boolean
          description: Whether the patient reports otalgia (ear pain).
        sudden_onset:
          type: boolean
          description: Whether onset was sudden (red flag for sudden sensorineural hearing loss).
        ear_discharge:
          type: boolean
          description: Whether the patient has ear discharge / otorrhoea (red flag).
        ototoxic_medication:
          type: boolean
          description: Whether the patient is on ototoxic medication requiring monitoring.
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Requested triage urgency: routine, urgent, emergency."
        supervising_consultant:
          type: string
          maxLength: 255
          description: Name of the supervising / responsible consultant.
        requester_contact:
          type: string
          maxLength: 255
          description: Contact details (bleep / phone) for the requester.
        notes:
          type: string
          maxLength: 1000
          description: Free-text additional notes.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - status
        - site_name
        - setting
        - test_type
        - laterality
        - primary_indication
        - clinical_question
        - relevant_history
        - symptom_hearing_loss
        - symptom_tinnitus
        - symptom_vertigo
        - symptom_otalgia
        - sudden_onset
        - ear_discharge
        - ototoxic_medication
        - urgency
        - supervising_consultant
        - requester_contact
        - notes
      description: Audiology / hearing-assessment request (referral). Captures the requested test, laterality, the clinical indication and question, relevant history, symptoms / red flags, and triage urgency.
