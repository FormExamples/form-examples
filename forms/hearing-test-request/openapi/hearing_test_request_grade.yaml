# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: hearing_test_request_grade
openapi: 3.1.0
info:
  title: Hearing Test Request — HearingTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (1-9 indication match), urgency triage tier, request completeness, and clinical priority."
paths:
  /hearing-test-request-grades:
    get:
      summary: List HearingTestRequestGrade records
      operationId: listHearingTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HearingTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HearingTestRequestGrade"
    post:
      summary: Create a new HearingTestRequestGrade
      operationId: createHearingTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HearingTestRequestGrade"
      responses:
        "201":
          description: The newly created HearingTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HearingTestRequestGrade"
        "400":
          description: Validation error
  /hearing-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HearingTestRequestGrade by id
      operationId: getHearingTestRequestGrade
      responses:
        "200":
          description: The requested HearingTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HearingTestRequestGrade"
        "404":
          description: HearingTestRequestGrade not found
    patch:
      summary: Update an existing HearingTestRequestGrade
      operationId: updateHearingTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HearingTestRequestGrade"
      responses:
        "200":
          description: The updated HearingTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HearingTestRequestGrade"
        "404":
          description: HearingTestRequestGrade not found
    delete:
      summary: Soft-delete a HearingTestRequestGrade
      operationId: deleteHearingTestRequestGrade
      responses:
        "204":
          description: HearingTestRequestGrade deleted
        "404":
          description: HearingTestRequestGrade not found
components:
  schemas:
    HearingTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        hearing_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9) anchored on British Society of Audiology / NICE NG98 indication match. There is no single published 1-9 audiology score; this axis adapts the ordinal-rating convention to indication appropriateness.
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Assigned urgency triage tier: routine, urgent, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe implied by the triage tier (e.g. sudden sensorineural hearing loss -> urgent, within days).
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        priority_band:
          type: string
          enum:
            - low
            - moderate
            - high
            - ""
          description: "Clinical priority band: low, moderate, high."
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - hearing_test_request_id
        - appropriateness_band
        - triage_tier
        - target_timeframe
        - priority_band
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (1-9 indication match), urgency triage tier, request completeness, and clinical priority."
