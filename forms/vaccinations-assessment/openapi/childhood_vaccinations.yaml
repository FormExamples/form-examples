# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: childhood_vaccinations
openapi: 3.1.0
info:
  title: Vaccinations Assessment — ChildhoodVaccinations
  version: 1.0.0
  description: Childhood vaccination status (0=Not Given, 1=Partial, 2=Complete). One-to-one child of assessment.
paths:
  /childhood-vaccinationses:
    get:
      summary: List ChildhoodVaccinations records
      operationId: listChildhoodVaccinations
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ChildhoodVaccinations records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChildhoodVaccinations"
    post:
      summary: Create a new ChildhoodVaccinations
      operationId: createChildhoodVaccinations
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChildhoodVaccinations"
      responses:
        "201":
          description: The newly created ChildhoodVaccinations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildhoodVaccinations"
        "400":
          description: Validation error
  /childhood-vaccinationses/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ChildhoodVaccinations by id
      operationId: getChildhoodVaccinations
      responses:
        "200":
          description: The requested ChildhoodVaccinations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildhoodVaccinations"
        "404":
          description: ChildhoodVaccinations not found
    patch:
      summary: Update an existing ChildhoodVaccinations
      operationId: updateChildhoodVaccinations
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChildhoodVaccinations"
      responses:
        "200":
          description: The updated ChildhoodVaccinations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildhoodVaccinations"
        "404":
          description: ChildhoodVaccinations not found
    delete:
      summary: Soft-delete a ChildhoodVaccinations
      operationId: deleteChildhoodVaccinations
      responses:
        "204":
          description: ChildhoodVaccinations deleted
        "404":
          description: ChildhoodVaccinations not found
components:
  schemas:
    ChildhoodVaccinations:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the assessment table.
        dtap_ipv_hib_hepb:
          type: integer
          format: int32
          description: Dtap ipv hib hepb.
        pneumococcal:
          type: integer
          format: int32
          description: Pneumococcal.
        rotavirus:
          type: integer
          format: int32
          description: Rotavirus.
        meningitis_b:
          type: integer
          format: int32
          description: Meningitis b.
        mmr:
          type: integer
          format: int32
          description: Mmr.
        hib_menc:
          type: integer
          format: int32
          description: Hib menc.
        preschool_booster:
          type: integer
          format: int32
          description: Preschool booster.
      required:
        - created_at
        - updated_at
        - assessment_id
      description: Childhood vaccination status (0=Not Given, 1=Partial, 2=Complete). One-to-one child of assessment.
