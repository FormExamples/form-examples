# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: consent_information
openapi: 3.1.0
info:
  title: Vaccinations Assessment — ConsentInformation
  version: 1.0.0
  description: Consent quality scoring (1-5 Likert) and consent status. One-to-one child of assessment.
paths:
  /consent-informations:
    get:
      summary: List ConsentInformation records
      operationId: listConsentInformation
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ConsentInformation records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ConsentInformation"
    post:
      summary: Create a new ConsentInformation
      operationId: createConsentInformation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConsentInformation"
      responses:
        "201":
          description: The newly created ConsentInformation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConsentInformation"
        "400":
          description: Validation error
  /consent-informations/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ConsentInformation by id
      operationId: getConsentInformation
      responses:
        "200":
          description: The requested ConsentInformation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConsentInformation"
        "404":
          description: ConsentInformation not found
    patch:
      summary: Update an existing ConsentInformation
      operationId: updateConsentInformation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConsentInformation"
      responses:
        "200":
          description: The updated ConsentInformation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConsentInformation"
        "404":
          description: ConsentInformation not found
    delete:
      summary: Soft-delete a ConsentInformation
      operationId: deleteConsentInformation
      responses:
        "204":
          description: ConsentInformation deleted
        "404":
          description: ConsentInformation not found
components:
  schemas:
    ConsentInformation:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the assessment table.
        information_provided:
          type: integer
          format: int32
          description: Information provided.
        risks_explained:
          type: integer
          format: int32
          description: Risks explained.
        benefits_explained:
          type: integer
          format: int32
          description: Benefits explained.
        questions_answered:
          type: integer
          format: int32
          description: Questions answered.
        consent_given:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Consent given. One of: yes, no."
        consent_date:
          type: string
          format: date
          description: Consent date.
        guardian_consent:
          type: string
          enum:
            - "yes"
            - "no"
            - notApplicable
            - ""
          description: "Guardian consent. One of: yes, no, notApplicable."
      required:
        - created_at
        - updated_at
        - assessment_id
        - consent_given
        - guardian_consent
      description: Consent quality scoring (1-5 Likert) and consent status. One-to-one child of assessment.
