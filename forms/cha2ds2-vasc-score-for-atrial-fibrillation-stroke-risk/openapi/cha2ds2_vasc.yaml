# Generated from sql/. Do not edit by hand.
# SOURCE: cha2ds2_vasc
openapi: 3.1.0
info:
  title: Cha2ds2 Vasc Score For Atrial Fibrillation Stroke Risk — Cha2ds2Vasc
  version: 1.0.0
  description: "Main CHA2DS2-VASc assessment record: assessment context, patient identification, and the weighted criterion inputs (congestive heart failure, hypertension, age, diabetes, prior stroke/TIA/thromboembolism, vascular disease, and sex category)."
paths:
  /cha2ds2-vascs:
    get:
      summary: List Cha2ds2Vasc records
      operationId: listCha2ds2Vasc
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Cha2ds2Vasc records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Cha2ds2Vasc"
    post:
      summary: Create a new Cha2ds2Vasc
      operationId: createCha2ds2Vasc
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Cha2ds2Vasc"
      responses:
        "201":
          description: The newly created Cha2ds2Vasc
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cha2ds2Vasc"
        "400":
          description: Validation error
  /cha2ds2-vascs/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Cha2ds2Vasc by id
      operationId: getCha2ds2Vasc
      responses:
        "200":
          description: The requested Cha2ds2Vasc
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cha2ds2Vasc"
        "404":
          description: Cha2ds2Vasc not found
    patch:
      summary: Update an existing Cha2ds2Vasc
      operationId: updateCha2ds2Vasc
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Cha2ds2Vasc"
      responses:
        "200":
          description: The updated Cha2ds2Vasc
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cha2ds2Vasc"
        "404":
          description: Cha2ds2Vasc not found
    delete:
      summary: Soft-delete a Cha2ds2Vasc
      operationId: deleteCha2ds2Vasc
      responses:
        "204":
          description: Cha2ds2Vasc deleted
        "404":
          description: Cha2ds2Vasc not found
components:
  schemas:
    Cha2ds2Vasc:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient being assessed (delete restricted).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the assessing clinician (optional; delete restricted).
        clinician_name:
          type: string
          maxLength: 255
          description: Name of the assessing clinician as recorded on the assessment.
        clinician_role:
          type: string
          enum:
            - doctor
            - nurse
            - pharmacist
            - other
            - ""
          description: "Role of the assessing clinician: doctor, nurse, pharmacist, or other."
        assessed_at:
          type: string
          format: date-time
          description: Date and time the assessment was performed.
        care_setting:
          type: string
          enum:
            - primary-care
            - cardiology
            - anticoagulation-clinic
            - emergency-department
            - other
            - ""
          description: "Care setting: primary-care, cardiology, anticoagulation-clinic, emergency-department, or other."
        atrial_fibrillation_type:
          type: string
          enum:
            - paroxysmal
            - persistent
            - permanent
            - flutter
            - ""
          description: "Type of atrial fibrillation: paroxysmal, persistent, permanent, or flutter."
        patient_identifier:
          type: string
          maxLength: 100
          description: Local patient identifier as recorded on the assessment.
        age_years:
          type: integer
          format: int32
          description: Patient age in years; drives the age criterion (>= 75 scores 2, 65-74 scores 1). NULL when unanswered.
        sex:
          type: string
          enum:
            - female
            - male
            - other
            - ""
          description: "Patient sex driving the sex-category point (female scores 1): female, male, or other."
        congestive_heart_failure:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion C — congestive heart failure or left-ventricular dysfunction (scores 1 when yes): yes or no."
        hypertension:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion H — hypertension, treated or with blood pressure > 140/90 (scores 1 when yes): yes or no."
        diabetes:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion D — diabetes mellitus (scores 1 when yes): yes or no."
        stroke_tia_thromboembolism:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion S2 — prior stroke, transient ischaemic attack, or thromboembolism (scores 2 when yes): yes or no."
        vascular_disease:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Criterion V — vascular disease (prior MI, peripheral arterial disease, or aortic plaque; scores 1 when yes): yes or no."
        clinical_note:
          type: string
          description: Free-text clinical note recorded with the assessment.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_name
        - clinician_role
        - care_setting
        - atrial_fibrillation_type
        - patient_identifier
        - sex
        - congestive_heart_failure
        - hypertension
        - diabetes
        - stroke_tia_thromboembolism
        - vascular_disease
        - clinical_note
      description: "Main CHA2DS2-VASc assessment record: assessment context, patient identification, and the weighted criterion inputs (congestive heart failure, hypertension, age, diabetes, prior stroke/TIA/thromboembolism, vascular disease, and sex category)."
