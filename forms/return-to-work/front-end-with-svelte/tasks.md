# Tasks: Return to Work — consolidated front-end (SvelteKit)

## Done

- [x] Engine ported to TypeScript (`rtw-grader`, `restriction-rules`,
      `flagged-issues`, `types`, `utils`) with Vitest tests.
- [x] 12-step single-page wizard (`Step1Clinician` … `Step12SignOff`).
- [x] Id-keyed reactive store with localStorage persistence.
- [x] RESTful routes: dashboard, wizard, report, PDF endpoint.
- [x] SVAR DataGrid dashboard with engine-derived sample rows and filters.
- [x] Lily tokens only (no hardcoded palette); `pnpm check`/`build`/tests green.
