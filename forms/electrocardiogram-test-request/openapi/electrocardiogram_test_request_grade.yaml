# Generated from sql/. Do not edit by hand.
# SOURCE: electrocardiogram_test_request_grade
openapi: 3.1.0
info:
  title: Electrocardiogram Test Request — ElectrocardiogramTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (AHA/ACC 1-9), urgency triage tier, request completeness, and clinical priority."
paths:
  /electrocardiogram-test-request-grades:
    get:
      summary: List ElectrocardiogramTestRequestGrade records
      operationId: listElectrocardiogramTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ElectrocardiogramTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ElectrocardiogramTestRequestGrade"
    post:
      summary: Create a new ElectrocardiogramTestRequestGrade
      operationId: createElectrocardiogramTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ElectrocardiogramTestRequestGrade"
      responses:
        "201":
          description: The newly created ElectrocardiogramTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ElectrocardiogramTestRequestGrade"
        "400":
          description: Validation error
  /electrocardiogram-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ElectrocardiogramTestRequestGrade by id
      operationId: getElectrocardiogramTestRequestGrade
      responses:
        "200":
          description: The requested ElectrocardiogramTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ElectrocardiogramTestRequestGrade"
        "404":
          description: ElectrocardiogramTestRequestGrade not found
    patch:
      summary: Update an existing ElectrocardiogramTestRequestGrade
      operationId: updateElectrocardiogramTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ElectrocardiogramTestRequestGrade"
      responses:
        "200":
          description: The updated ElectrocardiogramTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ElectrocardiogramTestRequestGrade"
        "404":
          description: ElectrocardiogramTestRequestGrade not found
    delete:
      summary: Soft-delete a ElectrocardiogramTestRequestGrade
      operationId: deleteElectrocardiogramTestRequestGrade
      responses:
        "204":
          description: ElectrocardiogramTestRequestGrade deleted
        "404":
          description: ElectrocardiogramTestRequestGrade not found
components:
  schemas:
    ElectrocardiogramTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        electrocardiogram_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: AHA/ACC ECG-use appropriateness score (1-9).
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Assigned urgency triage tier: routine, urgent, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the ECG implied by the triage tier (e.g. same hour for suspected ACS).
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        priority_band:
          type: string
          enum:
            - low
            - moderate
            - high
            - ""
          description: "Overall clinical priority band: low, moderate, high."
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - electrocardiogram_test_request_id
        - appropriateness_band
        - triage_tier
        - target_timeframe
        - priority_band
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (AHA/ACC 1-9), urgency triage tier, request completeness, and clinical priority."
