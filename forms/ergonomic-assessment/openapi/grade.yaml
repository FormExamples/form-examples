# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: grade
openapi: 3.1.0
info:
  title: Ergonomic Assessment — Grade
  version: 1.0.0
  description: Computed REBA grading result for an assessment. Includes optional clinician override.
paths:
  /grades:
    get:
      summary: List Grade records
      operationId: listGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Grade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      summary: Create a new Grade
      operationId: createGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "201":
          description: The newly created Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "400":
          description: Validation error
  /grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Grade by id
      operationId: getGrade
      responses:
        "200":
          description: The requested Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    patch:
      summary: Update an existing Grade
      operationId: updateGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "200":
          description: The updated Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    delete:
      summary: Soft-delete a Grade
      operationId: deleteGrade
      responses:
        "204":
          description: Grade deleted
        "404":
          description: Grade not found
components:
  schemas:
    Grade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the assessment table.
        reba_score:
          type: integer
          format: int32
          description: Computed REBA score (1-15), sum of fired rule scores clamped to range.
        risk_level:
          type: string
          enum:
            - Negligible risk
            - Low risk
            - Medium risk
            - High risk
            - Very high risk
          description: "Risk level derived from REBA score: Negligible, Low, Medium, High, or Very high risk."
        reba_score_override:
          type: integer
          format: int32
          description: Clinician override of the computed REBA score. NULL if no override.
        override_reason:
          type: string
          description: Required reason text when reba_score_override is set.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the REBA grading was computed.
        a:
          type: string
          description: A.
      required:
        - created_at
        - updated_at
        - assessment_id
        - reba_score
        - risk_level
        - override_reason
        - graded_at
      description: Computed REBA grading result for an assessment. Includes optional clinician override.
