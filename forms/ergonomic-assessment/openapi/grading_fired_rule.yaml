# Generated from sql/. Do not edit by hand.
# SOURCE: grading_fired_rule
openapi: 3.1.0
info:
  title: Ergonomic Assessment — GradingFiredRule
  version: 1.0.0
  description: Denormalized record of each REBA rule that fired during grading. Preserves exact context for clinical audit.
paths:
  /grading-fired-rules:
    get:
      summary: List GradingFiredRule records
      operationId: listGradingFiredRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of GradingFiredRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GradingFiredRule"
    post:
      summary: Create a new GradingFiredRule
      operationId: createGradingFiredRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GradingFiredRule"
      responses:
        "201":
          description: The newly created GradingFiredRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GradingFiredRule"
        "400":
          description: Validation error
  /grading-fired-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one GradingFiredRule by id
      operationId: getGradingFiredRule
      responses:
        "200":
          description: The requested GradingFiredRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GradingFiredRule"
        "404":
          description: GradingFiredRule not found
    patch:
      summary: Update an existing GradingFiredRule
      operationId: updateGradingFiredRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GradingFiredRule"
      responses:
        "200":
          description: The updated GradingFiredRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GradingFiredRule"
        "404":
          description: GradingFiredRule not found
    delete:
      summary: Soft-delete a GradingFiredRule
      operationId: deleteGradingFiredRule
      responses:
        "204":
          description: GradingFiredRule deleted
        "404":
          description: GradingFiredRule not found
components:
  schemas:
    GradingFiredRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        grade_id:
          type: string
          format: uuid
          description: FK to the grading result this fired rule belongs to.
        rule_id:
          type: string
          description: FK to the REBA rule that fired.
        system:
          type: string
          description: Body system or category (denormalized from reba_rule at grading time).
        description:
          type: string
          description: Rule description (denormalized from reba_rule at grading time).
        score:
          type: integer
          format: int32
          description: Score contribution (denormalized from reba_rule at grading time).
      required:
        - created_at
        - updated_at
        - grade_id
        - rule_id
        - system
        - description
        - score
      description: Denormalized record of each REBA rule that fired during grading. Preserves exact context for clinical audit.
