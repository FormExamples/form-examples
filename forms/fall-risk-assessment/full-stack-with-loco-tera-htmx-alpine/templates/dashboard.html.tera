{% extends "base.html.tera" %}

{% block title %}Fall Risk Assessment - Dashboard{% endblock %}

{% block content %}
<div class="dashboard-header">
    <h1>Fall Risk Dashboard</h1>
    <a href="/" class="btn btn-primary">New Assessment</a>
</div>

<form method="GET" action="/dashboard" class="filter-bar">
    <div class="form-group">
        <label for="search">Search</label>
        <input type="text" id="search" name="search" value="{{ search }}"
               placeholder="Patient name or diagnosis...">
    </div>
    <div class="form-group">
        <label for="severity">Severity</label>
        <select id="severity" name="severity">
            <option value="">All</option>
            <option value="low" {% if severity == "low" %}selected{% endif %}>Low</option>
            <option value="moderate" {% if severity == "moderate" %}selected{% endif %}>Moderate</option>
            <option value="high" {% if severity == "high" %}selected{% endif %}>High</option>
            <option value="critical" {% if severity == "critical" %}selected{% endif %}>Critical</option>
        </select>
    </div>
    <div class="form-group">
        <label for="care_setting">Care Setting</label>
        <input type="text" id="care_setting" name="care_setting" value="{{ care_setting }}"
               placeholder="e.g. inpatient">
    </div>
    <div class="form-group">
        <label for="has_flags">High-Priority Flags</label>
        <select id="has_flags" name="has_flags">
            <option value="">All</option>
            <option value="yes" {% if has_flags == "yes" %}selected{% endif %}>With flags</option>
            <option value="no" {% if has_flags == "no" %}selected{% endif %}>Without flags</option>
        </select>
    </div>
    <div class="form-group" style="display: flex; gap: 0.5rem; align-items: flex-end;">
        <button type="submit" class="btn btn-primary">Filter</button>
        <a href="/dashboard" class="btn btn-secondary">Clear</a>
    </div>
</form>

<p class="total-count">Showing {{ total }} completed assessment{% if total != 1 %}s{% endif %}.</p>

{% if total > 0 %}
<table class="patient-table">
    <thead>
        <tr>
            <th>Created</th>
            <th>Patient</th>
            <th>Date of Birth</th>
            <th>Care Setting</th>
            <th>Primary Diagnosis</th>
            <th>MFS</th>
            <th>Severity</th>
            <th>Flags</th>
        </tr>
    </thead>
    <tbody>
        {% for c in cases %}
        <tr>
            <td><a href="/assessment/{{ c.id }}/report">{{ c.createdAt }}</a></td>
            <td>{{ c.patientName }}</td>
            <td>{{ c.patientDateOfBirth }}</td>
            <td>{{ c.careSetting }}</td>
            <td>{{ c.primaryDiagnosis }}</td>
            <td>{{ c.mfsScore }}</td>
            <td>
                <span class="severity-badge severity-{{ c.severity }}" style="font-size: 0.75rem; padding: 0.15rem 0.5rem;">
                    {{ c.severity }}{% if c.criticalOverride %} *{% endif %}
                </span>
            </td>
            <td>
                {% if c.highPriorityFlagCount > 0 %}
                    <span class="badge badge-no">{{ c.highPriorityFlagCount }}</span>
                {% else %}
                    <span class="badge badge-yes">0</span>
                {% endif %}
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{% else %}
<div class="card" style="text-align: center; color: var(--gray-500);">
    <p>No completed assessments found matching your filters.</p>
</div>
{% endif %}
{% endblock %}
