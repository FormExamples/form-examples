# Generated from sql/. Do not edit by hand.
# SOURCE: sleep_study_test_request
openapi: 3.1.0
info:
  title: Sleep Study Test Request — SleepStudyTestRequest
  version: 1.0.0
  description: Sleep study / polysomnography request (referral), mainly for obstructive sleep apnoea. Captures the requested study, the clinical indication and question, Epworth and STOP-BANG scores, anthropometry, symptoms / risk factors, and triage urgency.
paths:
  /sleep-study-test-requests:
    get:
      summary: List SleepStudyTestRequest records
      operationId: listSleepStudyTestRequest
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of SleepStudyTestRequest records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SleepStudyTestRequest"
    post:
      summary: Create a new SleepStudyTestRequest
      operationId: createSleepStudyTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SleepStudyTestRequest"
      responses:
        "201":
          description: The newly created SleepStudyTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SleepStudyTestRequest"
        "400":
          description: Validation error
  /sleep-study-test-requests/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one SleepStudyTestRequest by id
      operationId: getSleepStudyTestRequest
      responses:
        "200":
          description: The requested SleepStudyTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SleepStudyTestRequest"
        "404":
          description: SleepStudyTestRequest not found
    patch:
      summary: Update an existing SleepStudyTestRequest
      operationId: updateSleepStudyTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SleepStudyTestRequest"
      responses:
        "200":
          description: The updated SleepStudyTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SleepStudyTestRequest"
        "404":
          description: SleepStudyTestRequest not found
    delete:
      summary: Soft-delete a SleepStudyTestRequest
      operationId: deleteSleepStudyTestRequest
      responses:
        "204":
          description: SleepStudyTestRequest deleted
        "404":
          description: SleepStudyTestRequest not found
components:
  schemas:
    SleepStudyTestRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the requesting clinician (referrer).
        status:
          type: string
          enum:
            - draft
            - submitted
            - vetted
            - scheduled
            - rejected
            - ""
          description: "Request lifecycle status: draft, submitted, vetted, scheduled, rejected."
        site_name:
          type: string
          maxLength: 255
          description: Name of the requesting site or clinic.
        setting:
          type: string
          enum:
            - outpatient
            - inpatient
            - community
            - home
            - ""
          description: "Care setting: outpatient, inpatient, community, home."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        requested_by_date:
          type: string
          format: date
          description: Date by which the study is requested to be performed.
        study_type:
          type: string
          enum:
            - home-sleep-apnoea-test
            - polysomnography
            - overnight-oximetry
            - multiple-sleep-latency-test
            - actigraphy
            - other
            - ""
          description: "Requested study type: home-sleep-apnoea-test, polysomnography, overnight-oximetry, multiple-sleep-latency-test, actigraphy, other."
        primary_indication:
          type: string
          enum:
            - suspected-osa
            - snoring
            - daytime-sleepiness
            - suspected-narcolepsy
            - insomnia
            - restless-legs
            - copd-overlap
            - pre-bariatric
            - driver-assessment
            - other
            - ""
          description: Primary clinical indication, used for appropriateness scoring.
        clinical_question:
          type: string
          maxLength: 1000
          description: Specific clinical question the study should answer (highest-value field).
        relevant_history:
          type: string
          maxLength: 1000
          description: Relevant medical, surgical, and sleep history.
        epworth_score:
          type: integer
          format: int32
          description: Epworth Sleepiness Scale score (0-24); higher indicates greater daytime sleepiness.
        stop_bang_score:
          type: integer
          format: int32
          description: STOP-BANG questionnaire score (0-8); higher indicates greater OSA risk.
        body_mass_index:
          type: number
          format: double
          description: Body mass index (BMI), a STOP-BANG and OSA-risk factor.
        neck_circumference_cm:
          type: number
          format: double
          description: Neck circumference in cm, a STOP-BANG and OSA-risk factor.
        witnessed_apnoeas:
          type: boolean
          description: Whether witnessed apnoeas have been reported.
        occupational_driver:
          type: boolean
          description: Whether the patient is an occupational / vocational driver (DVLA relevance).
        cardiovascular_disease:
          type: boolean
          description: Whether the patient has established cardiovascular disease.
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - ""
          description: "Requested triage urgency: routine, urgent."
        supervising_consultant:
          type: string
          maxLength: 255
          description: Name of the supervising / responsible consultant.
        requester_contact:
          type: string
          maxLength: 255
          description: Contact details (bleep / phone) for the requester.
        notes:
          type: string
          maxLength: 1000
          description: Free-text additional notes.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - status
        - site_name
        - setting
        - study_type
        - primary_indication
        - clinical_question
        - relevant_history
        - witnessed_apnoeas
        - occupational_driver
        - cardiovascular_disease
        - urgency
        - supervising_consultant
        - requester_contact
        - notes
      description: Sleep study / polysomnography request (referral), mainly for obstructive sleep apnoea. Captures the requested study, the clinical indication and question, Epworth and STOP-BANG scores, anthropometry, symptoms / risk factors, and triage urgency.
