# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: sleep_study_test_request_grade
openapi: 3.1.0
info:
  title: Sleep Study Test Request — SleepStudyTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (1-9, NICE NG202 / SIGN), clinical priority, request completeness, and triage."
paths:
  /sleep-study-test-request-grades:
    get:
      summary: List SleepStudyTestRequestGrade records
      operationId: listSleepStudyTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of SleepStudyTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SleepStudyTestRequestGrade"
    post:
      summary: Create a new SleepStudyTestRequestGrade
      operationId: createSleepStudyTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SleepStudyTestRequestGrade"
      responses:
        "201":
          description: The newly created SleepStudyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SleepStudyTestRequestGrade"
        "400":
          description: Validation error
  /sleep-study-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one SleepStudyTestRequestGrade by id
      operationId: getSleepStudyTestRequestGrade
      responses:
        "200":
          description: The requested SleepStudyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SleepStudyTestRequestGrade"
        "404":
          description: SleepStudyTestRequestGrade not found
    patch:
      summary: Update an existing SleepStudyTestRequestGrade
      operationId: updateSleepStudyTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SleepStudyTestRequestGrade"
      responses:
        "200":
          description: The updated SleepStudyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SleepStudyTestRequestGrade"
        "404":
          description: SleepStudyTestRequestGrade not found
    delete:
      summary: Soft-delete a SleepStudyTestRequestGrade
      operationId: deleteSleepStudyTestRequestGrade
      responses:
        "204":
          description: SleepStudyTestRequestGrade deleted
        "404":
          description: SleepStudyTestRequestGrade not found
components:
  schemas:
    SleepStudyTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        sleep_study_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9), from Epworth / STOP-BANG and indication match (NICE NG202 / SIGN).
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        priority_band:
          type: string
          enum:
            - low
            - moderate
            - high
            - ""
          description: "Clinical priority band: low, moderate, high (raised by occupational driving and severe sleepiness)."
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - ""
          description: "Assigned triage tier: routine, urgent."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the study implied by the triage tier.
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - sleep_study_test_request_id
        - appropriateness_band
        - priority_band
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (1-9, NICE NG202 / SIGN), clinical priority, request completeness, and triage."
