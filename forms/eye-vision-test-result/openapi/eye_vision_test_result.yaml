# Generated from sql/. Do not edit by hand.
# SOURCE: eye_vision_test_result
openapi: 3.1.0
info:
  title: Eye Vision Test Result — EyeVisionTestResult
  version: 1.0.0
  description: Eye vision test result (report) for an ophthalmic / optometric examination. Captures the performed test type, clinical history, the visual-acuity / intraocular-pressure / visual-field measurements, the narrative and structured findings (including diabetic retinopathy grading), the impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /eye-vision-test-results:
    get:
      summary: List EyeVisionTestResult records
      operationId: listEyeVisionTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of EyeVisionTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EyeVisionTestResult"
    post:
      summary: Create a new EyeVisionTestResult
      operationId: createEyeVisionTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EyeVisionTestResult"
      responses:
        "201":
          description: The newly created EyeVisionTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EyeVisionTestResult"
        "400":
          description: Validation error
  /eye-vision-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one EyeVisionTestResult by id
      operationId: getEyeVisionTestResult
      responses:
        "200":
          description: The requested EyeVisionTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EyeVisionTestResult"
        "404":
          description: EyeVisionTestResult not found
    patch:
      summary: Update an existing EyeVisionTestResult
      operationId: updateEyeVisionTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EyeVisionTestResult"
      responses:
        "200":
          description: The updated EyeVisionTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EyeVisionTestResult"
        "404":
          description: EyeVisionTestResult not found
    delete:
      summary: Soft-delete a EyeVisionTestResult
      operationId: deleteEyeVisionTestResult
      responses:
        "204":
          description: EyeVisionTestResult deleted
        "404":
          description: EyeVisionTestResult not found
components:
  schemas:
    EyeVisionTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (ophthalmologist / optometrist / orthoptist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating eye vision test request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        test_type:
          type: string
          enum:
            - visual-acuity
            - visual-fields
            - refraction
            - fundus-examination
            - optical-coherence-tomography
            - fluorescein-angiography
            - tonometry
            - slit-lamp
            - orthoptic-assessment
            - other
            - ""
          description: "Performed eye examination type: visual-acuity, visual-fields, refraction, fundus-examination, optical-coherence-tomography, fluorescein-angiography, tonometry, slit-lamp, orthoptic-assessment, other."
        performed_date:
          type: string
          format: date
          description: Date the eye examination was performed.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the examination was performed to answer.
        visual_acuity_right:
          type: string
          maxLength: 20
          description: Best-corrected visual acuity for the right eye, as text (e.g. 6/6, 6/9, CF, HM).
        visual_acuity_left:
          type: string
          maxLength: 20
          description: Best-corrected visual acuity for the left eye, as text (e.g. 6/6, 6/9, CF, HM).
        intraocular_pressure_right_mmhg:
          type: number
          format: double
          description: Intraocular pressure of the right eye in mmHg.
        intraocular_pressure_left_mmhg:
          type: number
          format: double
          description: Intraocular pressure of the left eye in mmHg.
        visual_field_result:
          type: string
          enum:
            - full
            - defect-right
            - defect-left
            - bilateral-defect
            - ""
          description: "Visual-field result: full, defect-right, defect-left, bilateral-defect."
        reduced_visual_acuity:
          type: boolean
          description: Whether reduced visual acuity is present in one or both eyes.
        visual_field_defect:
          type: boolean
          description: Whether a visual-field defect is present.
        raised_intraocular_pressure:
          type: boolean
          description: Whether intraocular pressure is raised (e.g. ocular hypertension / suspected glaucoma).
        diabetic_retinopathy:
          type: boolean
          description: Whether diabetic retinopathy is present.
        optic_disc_abnormality:
          type: boolean
          description: Whether an optic-disc abnormality (e.g. cupping, swelling, pallor) is present.
        macular_abnormality:
          type: boolean
          description: Whether a macular abnormality (e.g. maculopathy, oedema, degeneration) is present.
        normal_examination:
          type: boolean
          description: Whether the examination is entirely normal.
        retinopathy_grade:
          type: string
          enum:
            - none
            - background
            - pre-proliferative
            - proliferative
            - maculopathy
            - not-applicable
            - ""
          description: "Diabetic-retinopathy grade: none, background, pre-proliferative, proliferative, maculopathy, not-applicable."
        findings_narrative:
          type: string
          maxLength: 2000
          description: Narrative description of the examination findings (the body of the report).
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 100
          description: Free-text structured-reporting category label (e.g. a diabetic eye screening grade such as R0/R1/R2/R3/M1).
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up examination, referral, or management.
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - test_type
        - clinical_history
        - visual_acuity_right
        - visual_acuity_left
        - visual_field_result
        - reduced_visual_acuity
        - visual_field_defect
        - raised_intraocular_pressure
        - diabetic_retinopathy
        - optic_disc_abnormality
        - macular_abnormality
        - normal_examination
        - retinopathy_grade
        - findings_narrative
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Eye vision test result (report) for an ophthalmic / optometric examination. Captures the performed test type, clinical history, the visual-acuity / intraocular-pressure / visual-field measurements, the narrative and structured findings (including diabetic retinopathy grading), the impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
