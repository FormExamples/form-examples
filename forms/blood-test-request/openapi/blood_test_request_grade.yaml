# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: blood_test_request_grade
openapi: 3.1.0
info:
  title: Blood Test Request — BloodTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (1-9 anchored on RCPath retesting intervals + indication match), pre-analytical / specimen safety, request completeness, and triage priority."
paths:
  /blood-test-request-grades:
    get:
      summary: List BloodTestRequestGrade records
      operationId: listBloodTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of BloodTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BloodTestRequestGrade"
    post:
      summary: Create a new BloodTestRequestGrade
      operationId: createBloodTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BloodTestRequestGrade"
      responses:
        "201":
          description: The newly created BloodTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BloodTestRequestGrade"
        "400":
          description: Validation error
  /blood-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one BloodTestRequestGrade by id
      operationId: getBloodTestRequestGrade
      responses:
        "200":
          description: The requested BloodTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BloodTestRequestGrade"
        "404":
          description: BloodTestRequestGrade not found
    patch:
      summary: Update an existing BloodTestRequestGrade
      operationId: updateBloodTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BloodTestRequestGrade"
      responses:
        "200":
          description: The updated BloodTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BloodTestRequestGrade"
        "404":
          description: BloodTestRequestGrade not found
    delete:
      summary: Soft-delete a BloodTestRequestGrade
      operationId: deleteBloodTestRequestGrade
      responses:
        "204":
          description: BloodTestRequestGrade deleted
        "404":
          description: BloodTestRequestGrade not found
components:
  schemas:
    BloodTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        blood_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9). Anchored on RCPath National Minimum Retesting Intervals appropriateness and indication match; no single published 1-9 score exists for bloods.
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        preanalytical_band:
          type: string
          enum:
            - ok
            - caution
            - reject-risk
            - ""
          description: "Pre-analytical / specimen safety band: ok, caution, reject-risk (fasting met? specimen labelled / collected? right tubes?)."
        fasting_violation:
          type: boolean
          description: Whether a fasting-required test was collected non-fasting (or fasting status unknown).
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100); clinical details + indication weighted highest.
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - stat
            - ""
          description: "Assigned triage tier: routine, urgent, stat."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for collection / analysis implied by the triage tier.
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - blood_test_request_id
        - appropriateness_band
        - preanalytical_band
        - fasting_violation
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (1-9 anchored on RCPath retesting intervals + indication match), pre-analytical / specimen safety, request completeness, and triage priority."
