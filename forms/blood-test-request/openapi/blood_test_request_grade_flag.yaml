# Generated from sql/. Do not edit by hand.
# SOURCE: blood_test_request_grade_flag
openapi: 3.1.0
info:
  title: Blood Test Request — BloodTestRequestGradeFlag
  version: 1.0.0
  description: Safety-critical flags that fire independently of the four axes, with priority and a suggested action for the laboratory / phlebotomy team.
paths:
  /blood-test-request-grade-flags:
    get:
      summary: List BloodTestRequestGradeFlag records
      operationId: listBloodTestRequestGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of BloodTestRequestGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BloodTestRequestGradeFlag"
    post:
      summary: Create a new BloodTestRequestGradeFlag
      operationId: createBloodTestRequestGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BloodTestRequestGradeFlag"
      responses:
        "201":
          description: The newly created BloodTestRequestGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BloodTestRequestGradeFlag"
        "400":
          description: Validation error
  /blood-test-request-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one BloodTestRequestGradeFlag by id
      operationId: getBloodTestRequestGradeFlag
      responses:
        "200":
          description: The requested BloodTestRequestGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BloodTestRequestGradeFlag"
        "404":
          description: BloodTestRequestGradeFlag not found
    patch:
      summary: Update an existing BloodTestRequestGradeFlag
      operationId: updateBloodTestRequestGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BloodTestRequestGradeFlag"
      responses:
        "200":
          description: The updated BloodTestRequestGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BloodTestRequestGradeFlag"
        "404":
          description: BloodTestRequestGradeFlag not found
    delete:
      summary: Soft-delete a BloodTestRequestGradeFlag
      operationId: deleteBloodTestRequestGradeFlag
      responses:
        "204":
          description: BloodTestRequestGradeFlag deleted
        "404":
          description: BloodTestRequestGradeFlag not found
components:
  schemas:
    BloodTestRequestGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        blood_test_request_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-RETEST-INTERVAL-BREACH-001).
        category:
          type: string
          enum:
            - duplicate-recent-test
            - retest-interval-breach
            - fasting-required-not-met
            - missing-clinical-details
            - missing-indication
            - blood-borne-virus-precaution
            - stat-critical
            - no-test-selected
            - other
            - ""
          description: Flag category.
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested action (e.g. \"query referrer before bleeding\")."
      required:
        - created_at
        - updated_at
        - blood_test_request_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Safety-critical flags that fire independently of the four axes, with priority and a suggested action for the laboratory / phlebotomy team.
