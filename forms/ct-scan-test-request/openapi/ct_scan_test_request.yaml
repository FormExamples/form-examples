# Generated from sql/. Do not edit by hand.
# SOURCE: ct_scan_test_request
openapi: 3.1.0
info:
  title: Ct Scan Test Request — CtScanTestRequest
  version: 1.0.0
  description: CT (computed tomography) scan request (referral). Captures the requested body region, the clinical indication and question, relevant history, contrast and radiation-safety factors (eGFR, allergy, metformin, pregnancy), the IR(ME)R radiation justification, and triage urgency.
paths:
  /ct-scan-test-requests:
    get:
      summary: List CtScanTestRequest records
      operationId: listCtScanTestRequest
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CtScanTestRequest records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CtScanTestRequest"
    post:
      summary: Create a new CtScanTestRequest
      operationId: createCtScanTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CtScanTestRequest"
      responses:
        "201":
          description: The newly created CtScanTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CtScanTestRequest"
        "400":
          description: Validation error
  /ct-scan-test-requests/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CtScanTestRequest by id
      operationId: getCtScanTestRequest
      responses:
        "200":
          description: The requested CtScanTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CtScanTestRequest"
        "404":
          description: CtScanTestRequest not found
    patch:
      summary: Update an existing CtScanTestRequest
      operationId: updateCtScanTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CtScanTestRequest"
      responses:
        "200":
          description: The updated CtScanTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CtScanTestRequest"
        "404":
          description: CtScanTestRequest not found
    delete:
      summary: Soft-delete a CtScanTestRequest
      operationId: deleteCtScanTestRequest
      responses:
        "204":
          description: CtScanTestRequest deleted
        "404":
          description: CtScanTestRequest not found
components:
  schemas:
    CtScanTestRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the requesting clinician (referrer).
        status:
          type: string
          enum:
            - draft
            - submitted
            - vetted
            - scheduled
            - rejected
            - ""
          description: "Request lifecycle status: draft, submitted, vetted, scheduled, rejected."
        site_name:
          type: string
          maxLength: 255
          description: Name of the requesting site or clinic.
        setting:
          type: string
          enum:
            - outpatient
            - inpatient
            - community
            - emergency
            - ""
          description: "Care setting: outpatient, inpatient, community, emergency."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        requested_by_date:
          type: string
          format: date
          description: Date by which the scan is requested to be performed.
        body_region:
          type: string
          enum:
            - head
            - neck
            - chest
            - abdomen
            - pelvis
            - abdomen-pelvis
            - spine
            - ct-angiogram
            - ct-colonography
            - whole-body
            - extremity
            - other
            - ""
          description: "Requested CT body region: head, neck, chest, abdomen, pelvis, abdomen-pelvis, spine, ct-angiogram, ct-colonography, whole-body, extremity, other."
        primary_indication:
          type: string
          enum:
            - trauma
            - suspected-stroke
            - suspected-malignancy
            - cancer-staging
            - pulmonary-embolism
            - abdominal-pain
            - renal-colic
            - infection-abscess
            - pre-surgical-planning
            - follow-up-surveillance
            - headache
            - other
            - ""
          description: Primary clinical indication, used for ACR appropriateness scoring.
        clinical_question:
          type: string
          maxLength: 1000
          description: Specific clinical question the scan should answer (highest-value field).
        relevant_history:
          type: string
          maxLength: 1000
          description: Relevant medical, surgical, and oncological history.
        contrast_required:
          type: string
          enum:
            - none
            - iv-iodinated
            - oral
            - both
            - unknown
            - ""
          description: "Contrast requirement: none, iv-iodinated, oral, both, unknown."
        pregnancy_status:
          type: string
          enum:
            - not-pregnant
            - pregnant
            - possible
            - unknown
            - not-applicable
            - ""
          description: "Pregnancy status for radiation justification: not-pregnant, pregnant, possible, unknown, not-applicable."
        egfr:
          type: number
          format: double
          description: Estimated glomerular filtration rate (mL/min/1.73m2), for contrast nephropathy risk.
        previous_contrast_reaction:
          type: string
          enum:
            - none
            - mild
            - moderate
            - severe
            - unknown
            - ""
          description: "Severity of any previous iodinated contrast reaction: none, mild, moderate, severe, unknown."
        iodine_contrast_allergy:
          type: boolean
          description: Whether the patient has a known iodinated contrast allergy.
        metformin:
          type: boolean
          description: Whether the patient is taking metformin (contrast / renal interaction).
        diabetes:
          type: boolean
          description: Whether the patient has diabetes.
        renal_impairment:
          type: boolean
          description: Whether the patient has known renal impairment.
        weight_kg:
          type: number
          format: double
          description: Patient weight in kilograms, affecting dose and scanner table limits.
        relevant_previous_imaging:
          type: string
          maxLength: 1000
          description: Details of relevant previous imaging being followed up or compared.
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Requested triage urgency: routine, urgent, emergency."
        supervising_consultant:
          type: string
          maxLength: 255
          description: Name of the supervising / responsible consultant.
        ir_me_r_justification:
          type: string
          maxLength: 1000
          description: "IR(ME)R radiation justification: why the benefit of the exposure outweighs the radiation detriment."
        notes:
          type: string
          maxLength: 1000
          description: Free-text additional notes.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - status
        - site_name
        - setting
        - body_region
        - primary_indication
        - clinical_question
        - relevant_history
        - contrast_required
        - pregnancy_status
        - previous_contrast_reaction
        - iodine_contrast_allergy
        - metformin
        - diabetes
        - renal_impairment
        - relevant_previous_imaging
        - urgency
        - supervising_consultant
        - ir_me_r_justification
        - notes
      description: CT (computed tomography) scan request (referral). Captures the requested body region, the clinical indication and question, relevant history, contrast and radiation-safety factors (eGFR, allergy, metformin, pregnancy), the IR(ME)R radiation justification, and triage urgency.
