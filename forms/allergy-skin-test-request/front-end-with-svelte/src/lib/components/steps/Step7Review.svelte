<script lang="ts">
	import Fieldset from '#lib/components/ui/Fieldset.svelte';
</script>

<Fieldset legend="Review and submit">
	<p class="hint">
		Submit to compute the four-axis grade — appropriateness, validity and safety, request
		completeness, and triage priority — plus the safety flags and the vetting recommendation.
		Required fields (clinician name, patient name, requested test type, at least one allergen panel,
		primary indication, clinical question, and requested urgency) must be completed first.
	</p>
</Fieldset>
