# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: allergy_skin_test_request
openapi: 3.1.0
info:
  title: Allergy Skin Test Request — AllergySkinTestRequest
  version: 1.0.0
  description: Allergy skin test request (referral) for skin-prick, intradermal, patch, specific-IgE blood, or drug-provocation testing. Captures the requested test type, allergen panels, the clinical indication and question, antihistamine / beta-blocker / skin-disease validity-and-safety history, and triage urgency.
paths:
  /allergy-skin-test-requests:
    get:
      summary: List AllergySkinTestRequest records
      operationId: listAllergySkinTestRequest
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of AllergySkinTestRequest records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AllergySkinTestRequest"
    post:
      summary: Create a new AllergySkinTestRequest
      operationId: createAllergySkinTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AllergySkinTestRequest"
      responses:
        "201":
          description: The newly created AllergySkinTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AllergySkinTestRequest"
        "400":
          description: Validation error
  /allergy-skin-test-requests/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one AllergySkinTestRequest by id
      operationId: getAllergySkinTestRequest
      responses:
        "200":
          description: The requested AllergySkinTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AllergySkinTestRequest"
        "404":
          description: AllergySkinTestRequest not found
    patch:
      summary: Update an existing AllergySkinTestRequest
      operationId: updateAllergySkinTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AllergySkinTestRequest"
      responses:
        "200":
          description: The updated AllergySkinTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AllergySkinTestRequest"
        "404":
          description: AllergySkinTestRequest not found
    delete:
      summary: Soft-delete a AllergySkinTestRequest
      operationId: deleteAllergySkinTestRequest
      responses:
        "204":
          description: AllergySkinTestRequest deleted
        "404":
          description: AllergySkinTestRequest not found
components:
  schemas:
    AllergySkinTestRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the requesting clinician (referrer).
        status:
          type: string
          enum:
            - draft
            - submitted
            - vetted
            - scheduled
            - rejected
            - ""
          description: "Request lifecycle status: draft, submitted, vetted, scheduled, rejected."
        site_name:
          type: string
          maxLength: 255
          description: Name of the requesting site or clinic.
        setting:
          type: string
          enum:
            - outpatient
            - inpatient
            - community
            - emergency
            - ""
          description: "Care setting: outpatient, inpatient, community, emergency."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        requested_by_date:
          type: string
          format: date
          description: Date by which the test is requested to be performed.
        test_type:
          type: string
          enum:
            - skin-prick-test
            - intradermal-test
            - patch-test
            - specific-ige-blood
            - drug-provocation-challenge
            - other
            - ""
          description: "Requested test type: skin-prick-test, intradermal-test, patch-test, specific-ige-blood, drug-provocation-challenge, other."
        allergen_aeroallergens:
          type: boolean
          description: Whether aeroallergen testing is requested (pollens, house dust mite, animal dander, moulds).
        allergen_food:
          type: boolean
          description: Whether food allergen testing is requested.
        allergen_drug:
          type: boolean
          description: Whether drug allergen testing is requested.
        allergen_venom:
          type: boolean
          description: Whether insect venom allergen testing is requested.
        allergen_latex:
          type: boolean
          description: Whether latex allergen testing is requested.
        allergen_contact:
          type: boolean
          description: Whether contact-allergen patch testing is requested.
        primary_indication:
          type: string
          enum:
            - suspected-food-allergy
            - suspected-drug-allergy
            - rhinitis-asthma
            - anaphylaxis-investigation
            - venom-allergy
            - contact-dermatitis
            - urticaria
            - other
            - ""
          description: Primary clinical indication, used for appropriateness scoring.
        clinical_question:
          type: string
          maxLength: 1000
          description: Specific clinical question the test should answer (highest-value field).
        clinical_details:
          type: string
          maxLength: 1000
          description: "Relevant clinical details: history, reaction description, timing, suspected triggers."
        previous_anaphylaxis:
          type: boolean
          description: Whether the patient has a history of anaphylaxis (resuscitation-readiness flag).
        on_antihistamines:
          type: boolean
          description: Whether the patient is currently taking antihistamines (invalidates skin-prick / intradermal tests).
        on_beta_blocker:
          type: boolean
          description: Whether the patient is taking a beta-blocker (impairs adrenaline response in anaphylaxis).
        current_skin_disease:
          type: boolean
          description: Whether the patient has active skin disease at the test site (e.g. eczema, dermographism).
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - ""
          description: "Requested triage urgency: routine, urgent."
        supervising_consultant:
          type: string
          maxLength: 255
          description: Name of the supervising / responsible consultant.
        requester_contact:
          type: string
          maxLength: 255
          description: Contact details (bleep / phone) for the requester.
        notes:
          type: string
          maxLength: 1000
          description: Free-text additional notes.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - status
        - site_name
        - setting
        - test_type
        - allergen_aeroallergens
        - allergen_food
        - allergen_drug
        - allergen_venom
        - allergen_latex
        - allergen_contact
        - primary_indication
        - clinical_question
        - clinical_details
        - previous_anaphylaxis
        - on_antihistamines
        - on_beta_blocker
        - current_skin_disease
        - urgency
        - supervising_consultant
        - requester_contact
        - notes
      description: Allergy skin test request (referral) for skin-prick, intradermal, patch, specific-IgE blood, or drug-provocation testing. Captures the requested test type, allergen panels, the clinical indication and question, antihistamine / beta-blocker / skin-disease validity-and-safety history, and triage urgency.
