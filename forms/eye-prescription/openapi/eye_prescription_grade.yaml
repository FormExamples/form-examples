# Generated from sql/. Do not edit by hand.
# SOURCE: eye_prescription_grade
openapi: 3.1.0
info:
  title: Eye Prescription — EyePrescriptionGrade
  version: 1.0.0
  description: Computed and signed-off grading result for the prescription. Captures per-eye refractive class, presbyopia, anisometropia, and overall complexity. Stores both computed and final complexity with an override reason.
paths:
  /eye-prescription-grades:
    get:
      summary: List EyePrescriptionGrade records
      operationId: listEyePrescriptionGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of EyePrescriptionGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EyePrescriptionGrade"
    post:
      summary: Create a new EyePrescriptionGrade
      operationId: createEyePrescriptionGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EyePrescriptionGrade"
      responses:
        "201":
          description: The newly created EyePrescriptionGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EyePrescriptionGrade"
        "400":
          description: Validation error
  /eye-prescription-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one EyePrescriptionGrade by id
      operationId: getEyePrescriptionGrade
      responses:
        "200":
          description: The requested EyePrescriptionGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EyePrescriptionGrade"
        "404":
          description: EyePrescriptionGrade not found
    patch:
      summary: Update an existing EyePrescriptionGrade
      operationId: updateEyePrescriptionGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EyePrescriptionGrade"
      responses:
        "200":
          description: The updated EyePrescriptionGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EyePrescriptionGrade"
        "404":
          description: EyePrescriptionGrade not found
    delete:
      summary: Soft-delete a EyePrescriptionGrade
      operationId: deleteEyePrescriptionGrade
      responses:
        "204":
          description: EyePrescriptionGrade deleted
        "404":
          description: EyePrescriptionGrade not found
components:
  schemas:
    EyePrescriptionGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        eye_prescription_id:
          type: string
          format: uuid
          description: Foreign key to the parent eye_prescription (unique, 1:1).
        right_eye_sphere_class:
          type: string
          enum:
            - emmetropia
            - low-myopia
            - moderate-myopia
            - high-myopia
            - low-hyperopia
            - moderate-hyperopia
            - high-hyperopia
            - ""
          description: Refractive class for the right eye sphere component.
        right_eye_cylinder_class:
          type: string
          enum:
            - none
            - mild-astigmatism
            - moderate-astigmatism
            - high-astigmatism
            - ""
          description: Refractive class for the right eye cylinder component.
        left_eye_sphere_class:
          type: string
          enum:
            - emmetropia
            - low-myopia
            - moderate-myopia
            - high-myopia
            - low-hyperopia
            - moderate-hyperopia
            - high-hyperopia
            - ""
          description: Refractive class for the left eye sphere component.
        left_eye_cylinder_class:
          type: string
          enum:
            - none
            - mild-astigmatism
            - moderate-astigmatism
            - high-astigmatism
            - ""
          description: Refractive class for the left eye cylinder component.
        presbyopia_class:
          type: string
          enum:
            - none
            - early-presbyopia
            - established-presbyopia
            - advanced-presbyopia
            - ""
          description: Presbyopia class based on addition power.
        anisometropia_diopters:
          type: number
          format: double
          description: Absolute difference in sphere between right and left eye in dioptres.
        anisometropia_significant:
          type: boolean
          description: Whether anisometropia exceeds the 2.00 D safety-flag threshold.
        prism_present:
          type: boolean
          description: Whether any prism is prescribed (in either eye, either direction).
        computed_complexity:
          type: string
          enum:
            - simple
            - moderate
            - complex
            - ""
          description: "Complexity grade computed by the engine: simple, moderate, complex."
        final_complexity:
          type: string
          enum:
            - simple
            - moderate
            - complex
            - ""
          description: Complexity grade signed off by the prescriber (may equal or differ from computed).
        override_reason:
          type: string
          maxLength: 500
          description: Reason the prescriber set final differently from computed (mandatory when they differ).
        follow_up_interval_months:
          type: integer
          format: int32
          description: Recommended follow-up interval in months (typically 12 or 24).
        prescriber_notes:
          type: string
          description: Free-text prescriber summary notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of prescriber electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - eye_prescription_id
        - right_eye_sphere_class
        - right_eye_cylinder_class
        - left_eye_sphere_class
        - left_eye_cylinder_class
        - presbyopia_class
        - anisometropia_significant
        - prism_present
        - computed_complexity
        - final_complexity
        - override_reason
        - prescriber_notes
        - graded_at
      description: Computed and signed-off grading result for the prescription. Captures per-eye refractive class, presbyopia, anisometropia, and overall complexity. Stores both computed and final complexity with an override reason.
