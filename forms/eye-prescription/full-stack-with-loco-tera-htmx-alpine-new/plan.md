# Plan: Eye Prescription — Loco "new" bootstrap

Use the canonical setup script instead:

- [`../full-stack-with-loco-tera-htmx-alpine-setup`](../full-stack-with-loco-tera-htmx-alpine-setup)

This directory exists only because `bin/create-form` writes a `00-new.sh`
into it. Once the canonical setup script has been used, this directory
can be deleted.
