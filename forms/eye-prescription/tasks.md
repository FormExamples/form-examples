# Tasks: Eye Prescription

## Scaffolding
- [x] Run `bin/create-form eye-prescription`.
- [x] Verify the directory structure matches `bin/test-form` expectations.

## Documentation
- [x] Top-level `index.md` with the 11-step wizard table and scoring overview.
- [x] `AGENTS.md` with the engine shape, stack summary, and conventions.
- [x] `plan.md` with design principles and build order.
- [x] `tasks.md` (this file).
- [x] `doc/goc-standards.md` — GOC Standards of Practice mapping.
- [x] `doc/refractive-classification-rules.md` — band tables with sphere,
      cylinder, addition thresholds.
- [x] `doc/lens-recommendation-matrix.md` — material × prescription range
      decision table.
- [x] `doc/sign-convention-notes.md` — minus-cylinder vs. plus-cylinder.
- [x] `doc/fhir-vision-prescription-mapping.md` — SQL → FHIR R5 mapping.
- [x] `doc/safety-case-notes.md` — DCB0129 / DCB0160 placeholders.
- [x] `doc/uk-gos3-form-mapping.md` — NHS GOS3 voucher field mapping.

## Schema
- [x] `sql-migrations/00_create_extensions.sql`
- [x] `sql-migrations/01_create_function_set_updated_at.sql`
- [x] `sql-migrations/02_create_table_patient.sql`
- [x] `sql-migrations/03_create_table_prescriber.sql`
- [x] `sql-migrations/04_create_table_eye_prescription.sql`
- [x] `sql-migrations/05_create_table_eye_prescription_eye.sql`
- [x] `sql-migrations/06_create_table_eye_prescription_visual_acuity.sql`
- [x] `sql-migrations/07_create_table_eye_prescription_pupillary_distance.sql`
- [x] `sql-migrations/08_create_table_eye_prescription_lens_recommendation.sql`
- [x] `sql-migrations/09_create_table_eye_prescription_ocular_health_finding.sql`
- [x] `sql-migrations/90_create_table_eye_prescription_grade.sql`
- [x] `sql-migrations/91_create_table_eye_prescription_grade_rule.sql`
- [x] `sql-migrations/92_create_table_eye_prescription_grade_flag.sql`

## Interchange representations
- [x] XML + DTD generated by `bin/xml-representations/generate-xml-representations.py`.
- [x] FHIR R5 JSON generated by `bin/fhir-r5/generate-fhir-r5-representations.py`.
- [x] Protobuf generated by `bin/protobuf/generate-protobuf-representations.py`.
- [x] TypeSpec hand-written from the SQL schema.

## Full-stack scaffolding
- [x] `back-end-with-loco-setup` — executable shell
      script of `cargo loco generate scaffold` calls.

## Front-ends
- [x] `front-end-form-with-svelte/` — SvelteKit 2 + Svelte 5 + Tailwind 4.
      11-step wizard, typed engine, browser + Vitest tests passing.
- [x] `front-end-form-with-html/` — single-page 11-step wizard, vanilla JS
      with browser smoke test passing (all flags + complexity verified).
- [ ] `front-end-dashboard-with-svelte/` — SVAR DataGrid review.
- [x] `front-end-dashboard-with-html/` — sortable / filterable review
      table with sample data, browser smoke test passing.

## Full-stack backend
- [ ] `back-end-with-loco/` — Loco 0.16 + axum 0.8 +
      SeaORM + Tera + HTMX + Alpine.

## Tests
- [x] Vitest unit tests for the engine — 17/17 passing in the SvelteKit
      project (`composite.test.ts` covers refractive-rules,
      complexity-grader, flagged-issues, and end-to-end classify).
- [ ] `bin/test-form eye-prescription` passes (Rust crate check pending).

## Deferred / future
- [ ] Zod schemas for client-side validation.
- [ ] Plus-cylinder display toggle for US-trained prescribers.
- [ ] Side-by-side comparison view (current vs. prior).
- [ ] Contact lens sibling form.
- [ ] Lens-thickness estimate.
- [ ] NHS GOS3 voucher integration.
- [ ] Bilingual UI (English / Cymraeg).
- [ ] NHS number validation via PDS.
- [ ] Clinical safety case (DCB0129 / DCB0160).
- [ ] Audit log of every prescriber override.
- [ ] Electronic signature.
- [ ] DLM / OMA export to lens-grinding machines.
