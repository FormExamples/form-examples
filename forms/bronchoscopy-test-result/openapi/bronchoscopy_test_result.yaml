# Generated from sql/. Do not edit by hand.
# SOURCE: bronchoscopy_test_result
openapi: 3.1.0
info:
  title: Bronchoscopy Test Result — BronchoscopyTestResult
  version: 1.0.0
  description: Bronchoscopy (airway endoscopy) result (procedure report). Captures the performed procedure, sedation and extent examined, the clinical history, the narrative and structured findings, samples taken, any complication, the impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /bronchoscopy-test-results:
    get:
      summary: List BronchoscopyTestResult records
      operationId: listBronchoscopyTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of BronchoscopyTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BronchoscopyTestResult"
    post:
      summary: Create a new BronchoscopyTestResult
      operationId: createBronchoscopyTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BronchoscopyTestResult"
      responses:
        "201":
          description: The newly created BronchoscopyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BronchoscopyTestResult"
        "400":
          description: Validation error
  /bronchoscopy-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one BronchoscopyTestResult by id
      operationId: getBronchoscopyTestResult
      responses:
        "200":
          description: The requested BronchoscopyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BronchoscopyTestResult"
        "404":
          description: BronchoscopyTestResult not found
    patch:
      summary: Update an existing BronchoscopyTestResult
      operationId: updateBronchoscopyTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BronchoscopyTestResult"
      responses:
        "200":
          description: The updated BronchoscopyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BronchoscopyTestResult"
        "404":
          description: BronchoscopyTestResult not found
    delete:
      summary: Soft-delete a BronchoscopyTestResult
      operationId: deleteBronchoscopyTestResult
      responses:
        "204":
          description: BronchoscopyTestResult deleted
        "404":
          description: BronchoscopyTestResult not found
components:
  schemas:
    BronchoscopyTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the operating / reporting clinician (bronchoscopist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating bronchoscopy request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the bronchoscopy procedure was performed.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        procedure:
          type: string
          enum:
            - flexible-bronchoscopy
            - ebus
            - rigid-bronchoscopy
            - bronchoalveolar-lavage
            - other
            - ""
          description: "Performed procedure: flexible-bronchoscopy, ebus, rigid-bronchoscopy, bronchoalveolar-lavage, other."
        sedation_used:
          type: string
          enum:
            - none
            - local
            - conscious
            - deep
            - general
            - ""
          description: "Sedation actually used: none, local, conscious, deep, general."
        extent_examined:
          type: string
          maxLength: 1000
          description: Extent of the airway examined (e.g. trachea, carina, lobar and segmental bronchi reached).
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the procedure was performed to answer.
        endobronchial_lesion:
          type: boolean
          description: Whether an endobronchial lesion (e.g. tumour, mass) is present.
        mucosal_abnormality:
          type: boolean
          description: Whether a mucosal abnormality (e.g. erythema, infiltration, nodularity) is present.
        extrinsic_compression:
          type: boolean
          description: Whether extrinsic compression of the airway is present.
        bleeding:
          type: boolean
          description: Whether active bleeding or haemorrhage was observed.
        foreign_body:
          type: boolean
          description: Whether a foreign body is present in the airway.
        secretions_purulent:
          type: boolean
          description: Whether purulent secretions are present (suggesting infection).
        normal_examination:
          type: boolean
          description: Whether the examination was entirely normal with no abnormal findings.
        lesion_location:
          type: string
          maxLength: 255
          description: Anatomical location of the lesion or abnormality (e.g. right upper lobe bronchus).
        samples_taken:
          type: string
          maxLength: 1000
          description: Samples taken during the procedure (e.g. biopsy, BAL, brushings, EBUS-TBNA).
        complication:
          type: string
          enum:
            - none
            - bleeding
            - pneumothorax
            - hypoxia
            - other
            - ""
          description: "Procedural complication: none, bleeding, pneumothorax, hypoxia, other."
        findings_narrative:
          type: string
          maxLength: 2000
          description: Narrative description of the procedure findings (the body of the report).
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 100
          description: Structured-reporting score / category label (e.g. a lung-cancer-pathway or endobronchial-findings category).
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up, referral (e.g. lung-cancer MDT), or management.
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer / team.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - procedure
        - sedation_used
        - extent_examined
        - clinical_history
        - endobronchial_lesion
        - mucosal_abnormality
        - extrinsic_compression
        - bleeding
        - foreign_body
        - secretions_purulent
        - normal_examination
        - lesion_location
        - samples_taken
        - complication
        - findings_narrative
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Bronchoscopy (airway endoscopy) result (procedure report). Captures the performed procedure, sedation and extent examined, the clinical history, the narrative and structured findings, samples taken, any complication, the impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
