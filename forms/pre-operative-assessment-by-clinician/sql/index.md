# Pre-operative Assessment by Clinician — SQL migrations

PostgreSQL migrations for the Pre-operative Assessment by Clinician form — the source of truth for the data shape. Numbered `NN_*.sql` files plus the combined `schema.sql`.

See the form root [`../index.md`](../index.md) for the full design description.
