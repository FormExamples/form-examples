# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: x_ray_test_request
openapi: 3.1.0
info:
  title: X Ray Test Request — XRayTestRequest
  version: 1.0.0
  description: Plain-radiograph (X-ray) imaging request (referral). Captures the requested body region and laterality, the clinical indication and question, relevant history, radiation-safety details (pregnancy status, recent similar exposure), mobility, and triage urgency.
paths:
  /x-ray-test-requests:
    get:
      summary: List XRayTestRequest records
      operationId: listXRayTestRequest
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of XRayTestRequest records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/XRayTestRequest"
    post:
      summary: Create a new XRayTestRequest
      operationId: createXRayTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/XRayTestRequest"
      responses:
        "201":
          description: The newly created XRayTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/XRayTestRequest"
        "400":
          description: Validation error
  /x-ray-test-requests/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one XRayTestRequest by id
      operationId: getXRayTestRequest
      responses:
        "200":
          description: The requested XRayTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/XRayTestRequest"
        "404":
          description: XRayTestRequest not found
    patch:
      summary: Update an existing XRayTestRequest
      operationId: updateXRayTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/XRayTestRequest"
      responses:
        "200":
          description: The updated XRayTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/XRayTestRequest"
        "404":
          description: XRayTestRequest not found
    delete:
      summary: Soft-delete a XRayTestRequest
      operationId: deleteXRayTestRequest
      responses:
        "204":
          description: XRayTestRequest deleted
        "404":
          description: XRayTestRequest not found
components:
  schemas:
    XRayTestRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the requesting clinician (referrer).
        status:
          type: string
          enum:
            - draft
            - submitted
            - vetted
            - scheduled
            - rejected
            - ""
          description: "Request lifecycle status: draft, submitted, vetted, scheduled, rejected."
        site_name:
          type: string
          maxLength: 255
          description: Name of the requesting site or clinic.
        setting:
          type: string
          enum:
            - outpatient
            - inpatient
            - community
            - emergency
            - ""
          description: "Care setting: outpatient, inpatient, community, emergency."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        requested_by_date:
          type: string
          format: date
          description: Date by which the X-ray is requested to be performed.
        body_region:
          type: string
          enum:
            - chest
            - abdomen
            - spine-cervical
            - spine-thoracic
            - spine-lumbar
            - pelvis
            - hip
            - knee
            - ankle-foot
            - shoulder
            - wrist-hand
            - skull
            - dental
            - other
            - ""
          description: Body region to be imaged, used for appropriateness scoring and dose banding.
        laterality:
          type: string
          enum:
            - left
            - right
            - bilateral
            - not-applicable
            - ""
          description: "Laterality of the requested region: left, right, bilateral, not-applicable."
        primary_indication:
          type: string
          enum:
            - trauma-fracture
            - chest-infection
            - suspected-pneumothorax
            - foreign-body
            - joint-pain
            - arthritis
            - pre-operative
            - line-position-check
            - abdominal-obstruction
            - swallowed-object
            - follow-up
            - other
            - ""
          description: Primary clinical indication, used for appropriateness scoring.
        clinical_question:
          type: string
          maxLength: 1000
          description: Specific clinical question the X-ray should answer (highest-value field).
        relevant_history:
          type: string
          maxLength: 1000
          description: Relevant medical, surgical, and trauma history.
        pregnancy_status:
          type: string
          enum:
            - not-pregnant
            - pregnant
            - possible
            - unknown
            - not-applicable
            - ""
          description: "Patient pregnancy status for radiation safety: not-pregnant, pregnant, possible, unknown, not-applicable."
        recent_similar_xray:
          type: boolean
          description: Whether a similar X-ray of the same region was recently performed (repeat-exposure check).
        mobility:
          type: string
          enum:
            - ambulant
            - wheelchair
            - trolley
            - ""
          description: "Patient mobility for the examination: ambulant, wheelchair, trolley."
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Requested triage urgency: routine, urgent, emergency."
        ir_me_r_justification:
          type: string
          maxLength: 1000
          description: Referrer justification for the exposure under IR(ME)R 2017 (benefit weighed against radiation detriment).
        supervising_consultant:
          type: string
          maxLength: 255
          description: Name of the supervising / responsible consultant.
        requester_contact:
          type: string
          maxLength: 255
          description: Contact details (bleep / phone) for the requester.
        notes:
          type: string
          maxLength: 1000
          description: Free-text additional notes.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - status
        - site_name
        - setting
        - body_region
        - laterality
        - primary_indication
        - clinical_question
        - relevant_history
        - pregnancy_status
        - recent_similar_xray
        - mobility
        - urgency
        - ir_me_r_justification
        - supervising_consultant
        - requester_contact
        - notes
      description: Plain-radiograph (X-ray) imaging request (referral). Captures the requested body region and laterality, the clinical indication and question, relevant history, radiation-safety details (pregnancy status, recent similar exposure), mobility, and triage urgency.
