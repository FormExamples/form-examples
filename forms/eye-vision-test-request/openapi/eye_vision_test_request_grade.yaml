# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: eye_vision_test_request_grade
openapi: 3.1.0
info:
  title: Eye Vision Test Request — EyeVisionTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (RCOphth / NICE 1-9), urgency / triage priority, request completeness, and clinical priority."
paths:
  /eye-vision-test-request-grades:
    get:
      summary: List EyeVisionTestRequestGrade records
      operationId: listEyeVisionTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of EyeVisionTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EyeVisionTestRequestGrade"
    post:
      summary: Create a new EyeVisionTestRequestGrade
      operationId: createEyeVisionTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EyeVisionTestRequestGrade"
      responses:
        "201":
          description: The newly created EyeVisionTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EyeVisionTestRequestGrade"
        "400":
          description: Validation error
  /eye-vision-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one EyeVisionTestRequestGrade by id
      operationId: getEyeVisionTestRequestGrade
      responses:
        "200":
          description: The requested EyeVisionTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EyeVisionTestRequestGrade"
        "404":
          description: EyeVisionTestRequestGrade not found
    patch:
      summary: Update an existing EyeVisionTestRequestGrade
      operationId: updateEyeVisionTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EyeVisionTestRequestGrade"
      responses:
        "200":
          description: The updated EyeVisionTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EyeVisionTestRequestGrade"
        "404":
          description: EyeVisionTestRequestGrade not found
    delete:
      summary: Soft-delete a EyeVisionTestRequestGrade
      operationId: deleteEyeVisionTestRequestGrade
      responses:
        "204":
          description: EyeVisionTestRequestGrade deleted
        "404":
          description: EyeVisionTestRequestGrade not found
components:
  schemas:
    EyeVisionTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        eye_vision_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9) for the RCOphth / NICE indication match.
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Assigned triage tier: routine, urgent, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the examination implied by the triage tier (e.g. sudden visual loss / retinal-detachment symptoms / GCA -> emergency, same day).
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        priority_band:
          type: string
          enum:
            - low
            - moderate
            - high
            - ""
          description: "Clinical priority band: low, moderate, high."
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - eye_vision_test_request_id
        - appropriateness_band
        - triage_tier
        - target_timeframe
        - priority_band
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (RCOphth / NICE 1-9), urgency / triage priority, request completeness, and clinical priority."
