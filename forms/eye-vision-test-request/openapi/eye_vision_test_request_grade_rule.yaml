# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: eye_vision_test_request_grade_rule
openapi: 3.1.0
info:
  title: Eye Vision Test Request — EyeVisionTestRequestGradeRule
  version: 1.0.0
  description: Audit trail of every appropriateness / urgency / completeness / priority rule that fired for this request.
paths:
  /eye-vision-test-request-grade-rules:
    get:
      summary: List EyeVisionTestRequestGradeRule records
      operationId: listEyeVisionTestRequestGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of EyeVisionTestRequestGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EyeVisionTestRequestGradeRule"
    post:
      summary: Create a new EyeVisionTestRequestGradeRule
      operationId: createEyeVisionTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EyeVisionTestRequestGradeRule"
      responses:
        "201":
          description: The newly created EyeVisionTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EyeVisionTestRequestGradeRule"
        "400":
          description: Validation error
  /eye-vision-test-request-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one EyeVisionTestRequestGradeRule by id
      operationId: getEyeVisionTestRequestGradeRule
      responses:
        "200":
          description: The requested EyeVisionTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EyeVisionTestRequestGradeRule"
        "404":
          description: EyeVisionTestRequestGradeRule not found
    patch:
      summary: Update an existing EyeVisionTestRequestGradeRule
      operationId: updateEyeVisionTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EyeVisionTestRequestGradeRule"
      responses:
        "200":
          description: The updated EyeVisionTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EyeVisionTestRequestGradeRule"
        "404":
          description: EyeVisionTestRequestGradeRule not found
    delete:
      summary: Soft-delete a EyeVisionTestRequestGradeRule
      operationId: deleteEyeVisionTestRequestGradeRule
      responses:
        "204":
          description: EyeVisionTestRequestGradeRule deleted
        "404":
          description: EyeVisionTestRequestGradeRule not found
components:
  schemas:
    EyeVisionTestRequestGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        eye_vision_test_request_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-APPROP-GLAUCOMA-01).
        axis:
          type: string
          enum:
            - appropriateness
            - urgency
            - completeness
            - priority
            - ""
          description: "Scoring axis the rule belongs to: appropriateness, urgency, completeness, priority."
        category:
          type: string
          maxLength: 50
          description: Category or indication the rule relates to.
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - eye_vision_test_request_grade_id
        - rule_id
        - axis
        - category
        - description
      description: Audit trail of every appropriateness / urgency / completeness / priority rule that fired for this request.
