# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: tumor_marker_test_request_grade
openapi: 3.1.0
info:
  title: Tumor Marker Test Request — TumorMarkerTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (marker-to-indication fit, 1-9), interpretation safety, request completeness, and urgency / triage priority."
paths:
  /tumor-marker-test-request-grades:
    get:
      summary: List TumorMarkerTestRequestGrade records
      operationId: listTumorMarkerTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of TumorMarkerTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TumorMarkerTestRequestGrade"
    post:
      summary: Create a new TumorMarkerTestRequestGrade
      operationId: createTumorMarkerTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TumorMarkerTestRequestGrade"
      responses:
        "201":
          description: The newly created TumorMarkerTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TumorMarkerTestRequestGrade"
        "400":
          description: Validation error
  /tumor-marker-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one TumorMarkerTestRequestGrade by id
      operationId: getTumorMarkerTestRequestGrade
      responses:
        "200":
          description: The requested TumorMarkerTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TumorMarkerTestRequestGrade"
        "404":
          description: TumorMarkerTestRequestGrade not found
    patch:
      summary: Update an existing TumorMarkerTestRequestGrade
      operationId: updateTumorMarkerTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TumorMarkerTestRequestGrade"
      responses:
        "200":
          description: The updated TumorMarkerTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TumorMarkerTestRequestGrade"
        "404":
          description: TumorMarkerTestRequestGrade not found
    delete:
      summary: Soft-delete a TumorMarkerTestRequestGrade
      operationId: deleteTumorMarkerTestRequestGrade
      responses:
        "204":
          description: TumorMarkerTestRequestGrade deleted
        "404":
          description: TumorMarkerTestRequestGrade not found
components:
  schemas:
    TumorMarkerTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        tumor_marker_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9), anchored on marker-to-indication fit; not a single published instrument.
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        interpretation_band:
          type: string
          enum:
            - ok
            - caution
            - misuse-risk
            - ""
          description: "Interpretation-safety band reflecting timing, treatment effects, and screening misuse: ok, caution, misuse-risk."
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - two-week-wait
            - ""
          description: "Assigned triage tier: routine, urgent, two-week-wait."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the test implied by the triage tier.
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - tumor_marker_test_request_id
        - appropriateness_band
        - interpretation_band
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (marker-to-indication fit, 1-9), interpretation safety, request completeness, and urgency / triage priority."
