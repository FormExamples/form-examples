# Plan: Medical Language Speaking Assessment For English — clinician dashboard (SvelteKit + SVAR DataGrid)

## Current status

Scaffold only. Project structure and documentation are in place; the interactive clinician dashboard (SvelteKit + SVAR DataGrid) is pending.

## Future work

- Build the SVAR DataGrid view with sortable columns and dropdown filters
- Wire the API client with sample-data fallback
- Add export to CSV / JSON
