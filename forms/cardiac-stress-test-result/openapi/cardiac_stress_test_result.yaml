# Generated from sql/. Do not edit by hand.
# SOURCE: cardiac_stress_test_result
openapi: 3.1.0
info:
  title: Cardiac Stress Test Result — CardiacStressTestResult
  version: 1.0.0
  description: Cardiac stress / exercise test result (report). Captures the performed test type and protocol, the clinical history, the haemodynamic and exercise response (peak heart rate, percent-predicted heart rate, exercise duration, METs, blood-pressure response), the structured ECG / symptom / arrhythmia findings, the overall positive / negative / inconclusive conclusion, the Duke treadmill prognostic score, the impression and recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /cardiac-stress-test-results:
    get:
      summary: List CardiacStressTestResult records
      operationId: listCardiacStressTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CardiacStressTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CardiacStressTestResult"
    post:
      summary: Create a new CardiacStressTestResult
      operationId: createCardiacStressTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardiacStressTestResult"
      responses:
        "201":
          description: The newly created CardiacStressTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiacStressTestResult"
        "400":
          description: Validation error
  /cardiac-stress-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CardiacStressTestResult by id
      operationId: getCardiacStressTestResult
      responses:
        "200":
          description: The requested CardiacStressTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiacStressTestResult"
        "404":
          description: CardiacStressTestResult not found
    patch:
      summary: Update an existing CardiacStressTestResult
      operationId: updateCardiacStressTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardiacStressTestResult"
      responses:
        "200":
          description: The updated CardiacStressTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiacStressTestResult"
        "404":
          description: CardiacStressTestResult not found
    delete:
      summary: Soft-delete a CardiacStressTestResult
      operationId: deleteCardiacStressTestResult
      responses:
        "204":
          description: CardiacStressTestResult deleted
        "404":
          description: CardiacStressTestResult not found
components:
  schemas:
    CardiacStressTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (cardiologist / cardiac physiologist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating cardiac stress test request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the stress test was performed.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        test_type:
          type: string
          enum:
            - exercise-treadmill-ecg
            - stress-echo
            - dobutamine-stress-echo
            - myocardial-perfusion-spect
            - stress-cardiac-mri
            - other
            - ""
          description: "Performed stress-test modality: exercise-treadmill-ecg, stress-echo, dobutamine-stress-echo, myocardial-perfusion-spect, stress-cardiac-mri, other."
        protocol:
          type: string
          maxLength: 255
          description: Exercise / stress protocol used (e.g. Bruce, modified Bruce, dobutamine infusion, vasodilator).
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the test was performed to answer.
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous stress tests or imaging.
        maximum_heart_rate_bpm:
          type: integer
          format: int32
          description: Maximum heart rate achieved during the test, in beats per minute.
        percent_predicted_heart_rate:
          type: number
          format: double
          description: Percentage of the age-predicted maximum heart rate achieved (target ≥85% for an adequate exercise test).
        exercise_duration_minutes:
          type: number
          format: double
          description: Total exercise duration in minutes (a term in the Duke treadmill score).
        mets_achieved:
          type: number
          format: double
          description: Peak metabolic equivalents (METs) achieved, a measure of functional capacity.
        peak_blood_pressure:
          type: string
          maxLength: 20
          description: "Peak blood pressure recorded during the test (free text, e.g. \"180/90\")."
        blood_pressure_response:
          type: string
          enum:
            - normal
            - hypertensive
            - hypotensive
            - flat
            - ""
          description: "Blood-pressure response to exercise: normal, hypertensive, hypotensive (exertional hypotension), flat."
        ischaemic_st_changes:
          type: boolean
          description: Whether ischaemic ST-segment changes (depression or elevation) were induced.
        chest_pain_induced:
          type: boolean
          description: Whether typical / limiting chest pain (angina) was induced during the test.
        arrhythmia_induced:
          type: boolean
          description: Whether a significant arrhythmia was induced during the test.
        terminated_early:
          type: boolean
          description: Whether the test was terminated early before the target endpoint was reached.
        test_positive:
          type: boolean
          description: Whether the overall conclusion is positive (evidence of inducible ischaemia).
        test_negative:
          type: boolean
          description: Whether the overall conclusion is negative (no evidence of inducible ischaemia).
        test_inconclusive:
          type: boolean
          description: Whether the overall conclusion is inconclusive / non-diagnostic (e.g. submaximal, uninterpretable ECG).
        reason_for_termination:
          type: string
          maxLength: 1000
          description: Reason the test was stopped (target heart rate reached, fatigue, chest pain, ST changes, arrhythmia, exertional hypotension, etc.).
        duke_treadmill_score:
          type: number
          format: double
          description: Duke treadmill score = exercise minutes − (5 × ST deviation mm) − (4 × angina index); range about −25 (highest risk) to +15 (lowest risk). ≥+5 low risk, −10 to +4 intermediate risk, ≤−11 high risk.
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 50
          description: "Free-text structured risk category label derived from the result (e.g. \"low risk\", \"high-risk Duke score\", \"non-diagnostic\")."
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: "Recommended follow-up: further imaging, coronary angiography, optimise medical therapy, referral, or routine."
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer / responsible team.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - test_type
        - protocol
        - clinical_history
        - comparison_with_previous
        - peak_blood_pressure
        - blood_pressure_response
        - ischaemic_st_changes
        - chest_pain_induced
        - arrhythmia_induced
        - terminated_early
        - test_positive
        - test_negative
        - test_inconclusive
        - reason_for_termination
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Cardiac stress / exercise test result (report). Captures the performed test type and protocol, the clinical history, the haemodynamic and exercise response (peak heart rate, percent-predicted heart rate, exercise duration, METs, blood-pressure response), the structured ECG / symptom / arrhythmia findings, the overall positive / negative / inconclusive conclusion, the Duke treadmill prognostic score, the impression and recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
