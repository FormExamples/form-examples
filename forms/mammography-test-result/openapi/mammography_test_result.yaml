# Generated from sql/. Do not edit by hand.
# SOURCE: mammography_test_result
openapi: 3.1.0
info:
  title: Mammography Test Result — MammographyTestResult
  version: 1.0.0
  description: Breast mammography result (report). Captures the performed examination and laterality, the clinical history, the narrative and structured findings, breast density (ACR A-D), the largest lesion measurement, the impression, the BI-RADS final assessment category (the key structured score), recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /mammography-test-results:
    get:
      summary: List MammographyTestResult records
      operationId: listMammographyTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of MammographyTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MammographyTestResult"
    post:
      summary: Create a new MammographyTestResult
      operationId: createMammographyTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MammographyTestResult"
      responses:
        "201":
          description: The newly created MammographyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MammographyTestResult"
        "400":
          description: Validation error
  /mammography-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one MammographyTestResult by id
      operationId: getMammographyTestResult
      responses:
        "200":
          description: The requested MammographyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MammographyTestResult"
        "404":
          description: MammographyTestResult not found
    patch:
      summary: Update an existing MammographyTestResult
      operationId: updateMammographyTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MammographyTestResult"
      responses:
        "200":
          description: The updated MammographyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MammographyTestResult"
        "404":
          description: MammographyTestResult not found
    delete:
      summary: Soft-delete a MammographyTestResult
      operationId: deleteMammographyTestResult
      responses:
        "204":
          description: MammographyTestResult deleted
        "404":
          description: MammographyTestResult not found
components:
  schemas:
    MammographyTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (radiologist / consultant / reporting radiographer).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating mammography request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the mammography examination was performed.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        exam_type:
          type: string
          enum:
            - screening
            - diagnostic
            - symptomatic
            - surveillance
            - other
            - ""
          description: "Type of mammography performed: screening, diagnostic, symptomatic, surveillance, other. Only BI-RADS 0/1/2 may be assigned to a screening study; 3/4/5/6 require a complete diagnostic work-up."
        laterality:
          type: string
          enum:
            - left
            - right
            - bilateral
            - ""
          description: "Examined side: left, right, bilateral."
        examination_adequacy:
          type: string
          enum:
            - adequate
            - limited
            - non-diagnostic
            - ""
          description: "Diagnostic adequacy of the examination: adequate, limited, non-diagnostic."
        breast_density:
          type: string
          enum:
            - a
            - b
            - c
            - d
            - ""
          description: "ACR BI-RADS breast composition / density: a = almost entirely fatty, b = scattered fibroglandular, c = heterogeneously dense, d = extremely dense."
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the examination was performed to answer.
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous mammograms or breast imaging.
        findings_narrative:
          type: string
          maxLength: 2000
          description: Narrative description of the imaging findings (the body of the report).
        mass:
          type: boolean
          description: Whether a mass is present.
        calcifications:
          type: boolean
          description: Whether calcifications (e.g. clustered, pleomorphic) are present.
        architectural_distortion:
          type: boolean
          description: Whether architectural distortion is present.
        asymmetry:
          type: boolean
          description: Whether an asymmetry (developing, focal, or global) is present.
        skin_or_nipple_change:
          type: boolean
          description: Whether a skin thickening / retraction or nipple change is present.
        lymphadenopathy:
          type: boolean
          description: Whether axillary or intramammary lymphadenopathy is present.
        incidental_finding:
          type: boolean
          description: Whether one or more incidental findings warranting documentation are present.
        largest_lesion_size_mm:
          type: number
          format: double
          description: Largest lesion long-axis size in millimetres, for surveillance and categorisation.
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        bi_rads_category:
          type: string
          enum:
            - "0"
            - "1"
            - "2"
            - "3"
            - 4a
            - 4b
            - 4c
            - "5"
            - "6"
            - ""
          description: "ACR BI-RADS final assessment category (the key structured score): 0 = incomplete, need additional imaging; 1 = negative; 2 = benign; 3 = probably benign; 4a/4b/4c = suspicious (low / intermediate / moderate); 5 = highly suggestive of malignancy; 6 = known biopsy-proven malignancy."
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: "Recommended follow-up: routine screening interval, short-interval follow-up, additional imaging, biopsy, or specialist referral."
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - exam_type
        - laterality
        - examination_adequacy
        - breast_density
        - clinical_history
        - comparison_with_previous
        - findings_narrative
        - mass
        - calcifications
        - architectural_distortion
        - asymmetry
        - skin_or_nipple_change
        - lymphadenopathy
        - incidental_finding
        - impression
        - bi_rads_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Breast mammography result (report). Captures the performed examination and laterality, the clinical history, the narrative and structured findings, breast density (ACR A-D), the largest lesion measurement, the impression, the BI-RADS final assessment category (the key structured score), recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
