# Generated from sql/. Do not edit by hand.
# SOURCE: assessment, assessment_current_medications, assessment_medication_item
openapi: 3.1.0
info:
  title: Psychiatry Assessment — Assessment
  version: 1.0.0
  description: Psychiatry assessment form. Parent entity for all sections covering the GAF (Global Assessment of Functioning) evaluation.
paths:
  /assessments:
    get:
      summary: List Assessment records
      operationId: listAssessment
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Assessment records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Assessment"
    post:
      summary: Create a new Assessment
      operationId: createAssessment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Assessment"
      responses:
        "201":
          description: The newly created Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
        "400":
          description: Validation error
  /assessments/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Assessment by id
      operationId: getAssessment
      responses:
        "200":
          description: The requested Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
        "404":
          description: Assessment not found
    patch:
      summary: Update an existing Assessment
      operationId: updateAssessment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Assessment"
      responses:
        "200":
          description: The updated Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
        "404":
          description: Assessment not found
    delete:
      summary: Soft-delete a Assessment
      operationId: deleteAssessment
      responses:
        "204":
          description: Assessment deleted
        "404":
          description: Assessment not found
components:
  schemas:
    Assessment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient who owns this assessment.
        status:
          type: string
          enum:
            - draft
            - submitted
            - reviewed
            - urgent
          description: "Lifecycle status: draft, submitted, reviewed, or urgent."
        current_medications_deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        takes_psychiatric_medications:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient takes psychiatric medications.
        takes_other_medications:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient takes non-psychiatric medications.
        medication_adherence:
          type: string
          enum:
            - good
            - fair
            - poor
            - ""
          description: "Medication adherence: good, fair, poor, or empty."
        adherence_barriers:
          type: string
          description: Free-text description of barriers to medication adherence.
        side_effects:
          type: string
          description: Free-text description of medication side effects experienced.
        clozapine_registered:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient is registered on a clozapine monitoring programme.
        depot_injection:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient receives depot (long-acting injectable) medication.
        depot_details:
          type: string
          description: Depot details.
        adverse_drug_reactions:
          type: string
          description: Adverse drug reactions.
        additional_notes:
          type: string
          description: Additional notes.
        medication_item_deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        current_medications_id:
          type: string
          format: uuid
          description: Foreign key to the assessment_current_medications table.
        medication_name:
          type: string
          maxLength: 255
          description: Name of the medication.
        dose:
          type: string
          maxLength: 100
          description: Dosage amount and unit (e.g. 20mg, 100mg).
        frequency:
          type: string
          maxLength: 100
          description: Dosing frequency (e.g. once daily, twice daily, as needed).
        medication_type:
          type: string
          enum:
            - antidepressant
            - antipsychotic
            - anxiolytic
            - mood-stabiliser
            - hypnotic
            - stimulant
            - other
            - ""
          description: "Medication classification: antidepressant, antipsychotic, anxiolytic, mood-stabiliser, hypnotic, stimulant, other, or empty."
        indication:
          type: string
          description: Clinical indication for the medication.
        prescribing_clinician:
          type: string
          maxLength: 255
          description: Prescribing clinician.
        sort_order:
          type: integer
          format: int32
          description: Display order of the medication in the list.
      required:
        - created_at
        - updated_at
        - patient_id
        - status
        - takes_psychiatric_medications
        - takes_other_medications
        - medication_adherence
        - adherence_barriers
        - side_effects
        - clozapine_registered
        - depot_injection
        - depot_details
        - adverse_drug_reactions
        - additional_notes
        - current_medications_id
        - medication_name
        - dose
        - frequency
        - medication_type
        - indication
        - prescribing_clinician
        - sort_order
      description: Psychiatry assessment form. Parent entity for all sections covering the GAF (Global Assessment of Functioning) evaluation.
