# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: medical_operation_note_procedure
openapi: 3.1.0
info:
  title: Medical Operation Note — MedicalOperationNoteProcedure
  version: 1.0.0
  description: Planned and performed procedures linked to an operation note, with OPCS-4 coding and role (primary, secondary, unplanned).
paths:
  /medical-operation-note-procedures:
    get:
      summary: List MedicalOperationNoteProcedure records
      operationId: listMedicalOperationNoteProcedure
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of MedicalOperationNoteProcedure records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MedicalOperationNoteProcedure"
    post:
      summary: Create a new MedicalOperationNoteProcedure
      operationId: createMedicalOperationNoteProcedure
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MedicalOperationNoteProcedure"
      responses:
        "201":
          description: The newly created MedicalOperationNoteProcedure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicalOperationNoteProcedure"
        "400":
          description: Validation error
  /medical-operation-note-procedures/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one MedicalOperationNoteProcedure by id
      operationId: getMedicalOperationNoteProcedure
      responses:
        "200":
          description: The requested MedicalOperationNoteProcedure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicalOperationNoteProcedure"
        "404":
          description: MedicalOperationNoteProcedure not found
    patch:
      summary: Update an existing MedicalOperationNoteProcedure
      operationId: updateMedicalOperationNoteProcedure
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MedicalOperationNoteProcedure"
      responses:
        "200":
          description: The updated MedicalOperationNoteProcedure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicalOperationNoteProcedure"
        "404":
          description: MedicalOperationNoteProcedure not found
    delete:
      summary: Soft-delete a MedicalOperationNoteProcedure
      operationId: deleteMedicalOperationNoteProcedure
      responses:
        "204":
          description: MedicalOperationNoteProcedure deleted
        "404":
          description: MedicalOperationNoteProcedure not found
components:
  schemas:
    MedicalOperationNoteProcedure:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        medical_operation_note_id:
          type: string
          format: uuid
          description: Foreign key to the parent operation note.
        role:
          type: string
          enum:
            - planned-primary
            - planned-secondary
            - performed-primary
            - performed-secondary
            - unplanned
            - ""
          description: "Procedure role: planned-primary, planned-secondary, performed-primary, performed-secondary, or unplanned."
        opcs4_code:
          type: string
          maxLength: 10
          description: NHS Digital OPCS-4 procedure classification code.
        name:
          type: string
          maxLength: 255
          description: Short procedure name.
        description:
          type: string
          description: Free-text procedure description.
        laterality:
          type: string
          enum:
            - left
            - right
            - bilateral
            - midline
            - na
            - ""
          description: "Procedure laterality: left, right, bilateral, midline, or na."
        sequence_index:
          type: integer
          format: int32
          description: Sequence order within the case (0-based).
      required:
        - created_at
        - updated_at
        - medical_operation_note_id
        - role
        - opcs4_code
        - name
        - description
        - laterality
        - sequence_index
      description: Planned and performed procedures linked to an operation note, with OPCS-4 coding and role (primary, secondary, unplanned).
