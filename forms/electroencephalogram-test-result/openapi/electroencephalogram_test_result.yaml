# Generated from sql/. Do not edit by hand.
# SOURCE: electroencephalogram_test_result
openapi: 3.1.0
info:
  title: Electroencephalogram Test Result — ElectroencephalogramTestResult
  version: 1.0.0
  description: Electroencephalogram (EEG) test result (report). Captures the performed recording (EEG type, duration, quality), the clinical history, the background rhythm and structured neurophysiological findings, the narrative interpretation and clinical correlation, the impression and structured-reporting category, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /electroencephalogram-test-results:
    get:
      summary: List ElectroencephalogramTestResult records
      operationId: listElectroencephalogramTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ElectroencephalogramTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ElectroencephalogramTestResult"
    post:
      summary: Create a new ElectroencephalogramTestResult
      operationId: createElectroencephalogramTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ElectroencephalogramTestResult"
      responses:
        "201":
          description: The newly created ElectroencephalogramTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ElectroencephalogramTestResult"
        "400":
          description: Validation error
  /electroencephalogram-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ElectroencephalogramTestResult by id
      operationId: getElectroencephalogramTestResult
      responses:
        "200":
          description: The requested ElectroencephalogramTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ElectroencephalogramTestResult"
        "404":
          description: ElectroencephalogramTestResult not found
    patch:
      summary: Update an existing ElectroencephalogramTestResult
      operationId: updateElectroencephalogramTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ElectroencephalogramTestResult"
      responses:
        "200":
          description: The updated ElectroencephalogramTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ElectroencephalogramTestResult"
        "404":
          description: ElectroencephalogramTestResult not found
    delete:
      summary: Soft-delete a ElectroencephalogramTestResult
      operationId: deleteElectroencephalogramTestResult
      responses:
        "204":
          description: ElectroencephalogramTestResult deleted
        "404":
          description: ElectroencephalogramTestResult not found
components:
  schemas:
    ElectroencephalogramTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (neurologist / clinical neurophysiologist / physiologist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating EEG request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the EEG recording was performed.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        eeg_type:
          type: string
          enum:
            - routine-awake
            - sleep-deprived
            - ambulatory-24h
            - video-telemetry
            - other
            - ""
          description: "Performed EEG type: routine-awake, sleep-deprived, ambulatory-24h, video-telemetry, other."
        recording_duration_minutes:
          type: number
          format: double
          description: Total EEG recording duration in minutes.
        recording_quality:
          type: string
          enum:
            - good
            - adequate
            - limited
            - ""
          description: "Technical quality / interpretability of the recording: good, adequate, limited."
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the EEG was performed to answer.
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous EEG studies.
        background_rhythm:
          type: string
          enum:
            - normal
            - excess-slow
            - asymmetric
            - abnormal
            - ""
          description: "Background EEG rhythm assessment: normal, excess-slow, asymmetric, abnormal."
        epileptiform_discharges:
          type: boolean
          description: Whether interictal epileptiform discharges (spikes, sharp waves, spike-and-wave) are present.
        focal_slowing:
          type: boolean
          description: Whether focal (localised) slowing is present.
        generalised_slowing:
          type: boolean
          description: Whether generalised slowing (diffuse cerebral dysfunction) is present.
        seizure_recorded:
          type: boolean
          description: Whether an electrographic and/or clinical seizure was captured during the recording.
        status_epilepticus:
          type: boolean
          description: Whether status epilepticus (including non-convulsive status) was recorded — a critical finding.
        photoparoxysmal_response:
          type: boolean
          description: Whether a photoparoxysmal response to intermittent photic stimulation is present.
        normal_eeg:
          type: boolean
          description: Whether the recording is reported as a normal EEG.
        findings_narrative:
          type: string
          maxLength: 2000
          description: Narrative description of the EEG findings (the body of the report).
        clinical_correlation:
          type: string
          maxLength: 2000
          description: Correlation of the EEG findings with the clinical context and question.
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 100
          description: Free-text structured-reporting category label (e.g. a SCORE category or local epileptiform-classification label).
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up recording, referral, or management.
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer / clinical team.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - eeg_type
        - recording_quality
        - clinical_history
        - comparison_with_previous
        - background_rhythm
        - epileptiform_discharges
        - focal_slowing
        - generalised_slowing
        - seizure_recorded
        - status_epilepticus
        - photoparoxysmal_response
        - normal_eeg
        - findings_narrative
        - clinical_correlation
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Electroencephalogram (EEG) test result (report). Captures the performed recording (EEG type, duration, quality), the clinical history, the background rhythm and structured neurophysiological findings, the narrative interpretation and clinical correlation, the impression and structured-reporting category, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
