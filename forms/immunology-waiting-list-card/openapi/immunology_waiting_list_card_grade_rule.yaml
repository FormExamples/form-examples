# Generated from sql/. Do not edit by hand.
# SOURCE: immunology_waiting_list_card_grade_rule
openapi: 3.1.0
info:
  title: Immunology Waiting List Card — ImmunologyWaitingListCardGradeRule
  version: 1.0.0
  description: Audit trail of every waiting-time-status / clinical-priority / long-wait rule that fired during grading of a waiting list card.
paths:
  /immunology-waiting-list-card-grade-rules:
    get:
      summary: List ImmunologyWaitingListCardGradeRule records
      operationId: listImmunologyWaitingListCardGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ImmunologyWaitingListCardGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ImmunologyWaitingListCardGradeRule"
    post:
      summary: Create a new ImmunologyWaitingListCardGradeRule
      operationId: createImmunologyWaitingListCardGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImmunologyWaitingListCardGradeRule"
      responses:
        "201":
          description: The newly created ImmunologyWaitingListCardGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImmunologyWaitingListCardGradeRule"
        "400":
          description: Validation error
  /immunology-waiting-list-card-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ImmunologyWaitingListCardGradeRule by id
      operationId: getImmunologyWaitingListCardGradeRule
      responses:
        "200":
          description: The requested ImmunologyWaitingListCardGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImmunologyWaitingListCardGradeRule"
        "404":
          description: ImmunologyWaitingListCardGradeRule not found
    patch:
      summary: Update an existing ImmunologyWaitingListCardGradeRule
      operationId: updateImmunologyWaitingListCardGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImmunologyWaitingListCardGradeRule"
      responses:
        "200":
          description: The updated ImmunologyWaitingListCardGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImmunologyWaitingListCardGradeRule"
        "404":
          description: ImmunologyWaitingListCardGradeRule not found
    delete:
      summary: Soft-delete a ImmunologyWaitingListCardGradeRule
      operationId: deleteImmunologyWaitingListCardGradeRule
      responses:
        "204":
          description: ImmunologyWaitingListCardGradeRule deleted
        "404":
          description: ImmunologyWaitingListCardGradeRule not found
components:
  schemas:
    ImmunologyWaitingListCardGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        immunology_waiting_list_card_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade row.
        rule_id:
          type: string
          maxLength: 50
          description: Stable rule identifier (e.g. R-WTS-BREACHED-001).
        instrument:
          type: string
          enum:
            - waiting-time-status
            - clinical-priority
            - long-wait
          description: "Scoring instrument the rule belongs to: waiting-time-status, clinical-priority, long-wait."
        band:
          type: string
          enum:
            - within-target
            - approaching-breach
            - breached
            - long-wait
            - ""
          description: Band contributed by this rule (e.g. breached).
        category:
          type: string
          maxLength: 50
          description: Category descriptor (e.g. rtt-18-week, priority-target, long-waiter).
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - immunology_waiting_list_card_grade_id
        - rule_id
        - instrument
        - band
        - category
        - description
      description: Audit trail of every waiting-time-status / clinical-priority / long-wait rule that fired during grading of a waiting list card.
