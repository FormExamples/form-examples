# Generated from sql/. Do not edit by hand.
# SOURCE: cytology_test_result
openapi: 3.1.0
info:
  title: Cytology Test Result — CytologyTestResult
  version: 1.0.0
  description: Cytology test result (report). Captures the examined specimen and its adequacy, the clinical history, the cytology result category and HPV status, the microscopic description and diagnosis, the impression and structured-reporting category, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /cytology-test-results:
    get:
      summary: List CytologyTestResult records
      operationId: listCytologyTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CytologyTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CytologyTestResult"
    post:
      summary: Create a new CytologyTestResult
      operationId: createCytologyTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CytologyTestResult"
      responses:
        "201":
          description: The newly created CytologyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CytologyTestResult"
        "400":
          description: Validation error
  /cytology-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CytologyTestResult by id
      operationId: getCytologyTestResult
      responses:
        "200":
          description: The requested CytologyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CytologyTestResult"
        "404":
          description: CytologyTestResult not found
    patch:
      summary: Update an existing CytologyTestResult
      operationId: updateCytologyTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CytologyTestResult"
      responses:
        "200":
          description: The updated CytologyTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CytologyTestResult"
        "404":
          description: CytologyTestResult not found
    delete:
      summary: Soft-delete a CytologyTestResult
      operationId: deleteCytologyTestResult
      responses:
        "204":
          description: CytologyTestResult deleted
        "404":
          description: CytologyTestResult not found
components:
  schemas:
    CytologyTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (consultant cytopathologist / biomedical scientist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating cytology test request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the cytology examination was performed / specimen received.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        specimen_type:
          type: string
          enum:
            - cervical-smear
            - urine-cytology
            - sputum-cytology
            - fluid-pleural-ascitic
            - fine-needle-aspiration-thyroid
            - fine-needle-aspiration-breast
            - csf-cytology
            - other
            - ""
          description: "Examined cytology specimen type: cervical-smear, urine-cytology, sputum-cytology, fluid-pleural-ascitic, fine-needle-aspiration-thyroid, fine-needle-aspiration-breast, csf-cytology, other."
        specimen_adequacy:
          type: string
          enum:
            - satisfactory
            - unsatisfactory
            - ""
          description: "Specimen adequacy for cytological interpretation: satisfactory, unsatisfactory."
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the examination was performed to answer.
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous cytology / histology results.
        cytology_result_category:
          type: string
          maxLength: 500
          description: "Free-text cytology result category holding the relevant grading: e.g. cervical (negative / borderline / low-grade-dyskaryosis / high-grade-dyskaryosis / glandular), thyroid (Thy1-Thy5), breast (C1-C5), urine / serous-fluid system categories."
        hpv_result:
          type: string
          enum:
            - positive
            - negative
            - not-tested
            - not-applicable
            - ""
          description: "HPV (human papillomavirus) test result where applicable: positive, negative, not-tested, not-applicable."
        malignancy_present:
          type: boolean
          description: Whether malignant cells are present in the specimen.
        dysplasia_present:
          type: boolean
          description: Whether dysplasia / dyskaryosis (pre-malignant atypia) is present in the specimen.
        microscopic_description:
          type: string
          maxLength: 2000
          description: Microscopic description of the cellular findings (the body of the report).
        diagnosis:
          type: string
          maxLength: 2000
          description: Cytological diagnosis / interpretation.
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 100
          description: Structured-reporting grading category label (e.g. an NHS cervical-screening dyskaryosis grade, RCPath Thy or breast C category).
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: "Recommended follow-up: repeat cytology, colposcopy, biopsy, MDT referral, or routine recall."
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - specimen_type
        - specimen_adequacy
        - clinical_history
        - comparison_with_previous
        - cytology_result_category
        - hpv_result
        - malignancy_present
        - dysplasia_present
        - microscopic_description
        - diagnosis
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Cytology test result (report). Captures the examined specimen and its adequacy, the clinical history, the cytology result category and HPV status, the microscopic description and diagnosis, the impression and structured-reporting category, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
