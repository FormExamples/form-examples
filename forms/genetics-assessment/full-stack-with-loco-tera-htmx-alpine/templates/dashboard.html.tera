{% extends "base.html.tera" %}

{% block title %}Genetics Assessment - Dashboard{% endblock %}

{% block content %}
<div class="dashboard-header">
    <h1>Genetics Clinic Dashboard</h1>
    <a href="/" class="btn btn-primary">New Assessment</a>
</div>

<form method="GET" action="/dashboard" class="filter-bar">
    <div class="form-group">
        <label for="search">Search</label>
        <input type="text" id="search" name="search" value="{{ search }}"
               placeholder="Proband name, MRN, concern, or syndrome...">
    </div>
    <div class="form-group">
        <label for="risk_level">Risk Level</label>
        <select id="risk_level" name="risk_level">
            <option value="">All</option>
            <option value="low" {% if risk_level == "low" %}selected{% endif %}>Low</option>
            <option value="moderate" {% if risk_level == "moderate" %}selected{% endif %}>Moderate</option>
            <option value="high" {% if risk_level == "high" %}selected{% endif %}>High</option>
        </select>
    </div>
    <div class="form-group">
        <label for="referral_urgency">Referral Urgency</label>
        <select id="referral_urgency" name="referral_urgency">
            <option value="">All</option>
            <option value="routine" {% if referral_urgency == "routine" %}selected{% endif %}>Routine</option>
            <option value="soon" {% if referral_urgency == "soon" %}selected{% endif %}>Soon</option>
            <option value="urgent" {% if referral_urgency == "urgent" %}selected{% endif %}>Urgent</option>
        </select>
    </div>
    <div class="form-group">
        <label for="has_flags">High-Priority Flags</label>
        <select id="has_flags" name="has_flags">
            <option value="">All</option>
            <option value="yes" {% if has_flags == "yes" %}selected{% endif %}>With flags</option>
            <option value="no" {% if has_flags == "no" %}selected{% endif %}>Without flags</option>
        </select>
    </div>
    <div class="form-group" style="display: flex; gap: 0.5rem; align-items: flex-end;">
        <button type="submit" class="btn btn-primary">Filter</button>
        <a href="/dashboard" class="btn btn-secondary">Clear</a>
    </div>
</form>

<p class="total-count">Showing {{ total }} completed assessment{% if total != 1 %}s{% endif %}.</p>

{% if total > 0 %}
<table class="patient-table">
    <thead>
        <tr>
            <th>Proband</th>
            <th>MRN</th>
            <th>DOB</th>
            <th>Chief Concern</th>
            <th>Suspected Syndrome</th>
            <th>Urgency</th>
            <th>Risk</th>
            <th>Manchester</th>
            <th>Bethesda</th>
            <th>Flags</th>
        </tr>
    </thead>
    <tbody>
        {% for c in cases %}
        <tr>
            <td><a href="/assessment/{{ c.id }}/report">{{ c.probandFirstName }} {{ c.probandLastName }}</a></td>
            <td>{{ c.probandMrn }}</td>
            <td>{{ c.dateOfBirth }}</td>
            <td>{{ c.chiefConcern }}</td>
            <td>{{ c.suspectedSyndrome }}</td>
            <td>{{ c.referralUrgency }}</td>
            <td><span class="risk-badge risk-{{ c.riskLevel }}">{{ c.riskLevel }}</span></td>
            <td>{{ c.manchesterScore }}</td>
            <td>{{ c.bethesdaMet }}</td>
            <td>
                {% if c.highPriorityFlagCount > 0 %}
                    <span class="badge badge-no">{{ c.highPriorityFlagCount }}</span>
                {% else %}
                    <span class="badge badge-yes">0</span>
                {% endif %}
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{% else %}
<div class="card" style="text-align: center; color: var(--gray-500);">
    <p>No completed assessments found matching your filters.</p>
</div>
{% endif %}
{% endblock %}
