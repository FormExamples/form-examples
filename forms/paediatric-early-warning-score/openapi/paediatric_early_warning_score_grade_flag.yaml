# Generated from sql/. Do not edit by hand.
# SOURCE: paediatric_early_warning_score_grade_flag
openapi: 3.1.0
info:
  title: Paediatric Early Warning Score — PaediatricEarlyWarningScoreGradeFlag
  version: 1.0.0
  description: Safety-critical flags that fire independently of the aggregate escalation band, with priority and a suggested action for the ward and escalation team.
paths:
  /paediatric-early-warning-score-grade-flags:
    get:
      summary: List PaediatricEarlyWarningScoreGradeFlag records
      operationId: listPaediatricEarlyWarningScoreGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PaediatricEarlyWarningScoreGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PaediatricEarlyWarningScoreGradeFlag"
    post:
      summary: Create a new PaediatricEarlyWarningScoreGradeFlag
      operationId: createPaediatricEarlyWarningScoreGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaediatricEarlyWarningScoreGradeFlag"
      responses:
        "201":
          description: The newly created PaediatricEarlyWarningScoreGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaediatricEarlyWarningScoreGradeFlag"
        "400":
          description: Validation error
  /paediatric-early-warning-score-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PaediatricEarlyWarningScoreGradeFlag by id
      operationId: getPaediatricEarlyWarningScoreGradeFlag
      responses:
        "200":
          description: The requested PaediatricEarlyWarningScoreGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaediatricEarlyWarningScoreGradeFlag"
        "404":
          description: PaediatricEarlyWarningScoreGradeFlag not found
    patch:
      summary: Update an existing PaediatricEarlyWarningScoreGradeFlag
      operationId: updatePaediatricEarlyWarningScoreGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaediatricEarlyWarningScoreGradeFlag"
      responses:
        "200":
          description: The updated PaediatricEarlyWarningScoreGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaediatricEarlyWarningScoreGradeFlag"
        "404":
          description: PaediatricEarlyWarningScoreGradeFlag not found
    delete:
      summary: Soft-delete a PaediatricEarlyWarningScoreGradeFlag
      operationId: deletePaediatricEarlyWarningScoreGradeFlag
      responses:
        "204":
          description: PaediatricEarlyWarningScoreGradeFlag deleted
        "404":
          description: PaediatricEarlyWarningScoreGradeFlag not found
components:
  schemas:
    PaediatricEarlyWarningScoreGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        paediatric_early_warning_score_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-SINGLE-PARAMETER-3-001).
        category:
          type: string
          enum:
            - urgent-review
            - single-parameter-3
            - parent-nurse-concern
            - deteriorating-trend
            - incomplete
            - other
            - ""
          description: "Flag category: urgent-review, single-parameter-3, parent-nurse-concern, deteriorating-trend, incomplete, or other."
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical action (e.g. \"immediate senior review; consider critical-care outreach\", \"escalate and act on documented concern\")."
      required:
        - created_at
        - updated_at
        - paediatric_early_warning_score_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Safety-critical flags that fire independently of the aggregate escalation band, with priority and a suggested action for the ward and escalation team.
