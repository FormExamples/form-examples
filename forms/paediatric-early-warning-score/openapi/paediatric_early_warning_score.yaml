# Generated from sql/. Do not edit by hand.
# SOURCE: paediatric_early_warning_score
openapi: 3.1.0
info:
  title: Paediatric Early Warning Score — PaediatricEarlyWarningScore
  version: 1.0.0
  description: "Main PEWS observation record: raw physiological observations for a single measurement set plus recording context (age band, care setting, observation timestamp) and the documented-concern override triggers. Sub-scores, aggregate, escalation band, rules, and flags live in child tables."
paths:
  /paediatric-early-warning-scores:
    get:
      summary: List PaediatricEarlyWarningScore records
      operationId: listPaediatricEarlyWarningScore
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PaediatricEarlyWarningScore records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PaediatricEarlyWarningScore"
    post:
      summary: Create a new PaediatricEarlyWarningScore
      operationId: createPaediatricEarlyWarningScore
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaediatricEarlyWarningScore"
      responses:
        "201":
          description: The newly created PaediatricEarlyWarningScore
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaediatricEarlyWarningScore"
        "400":
          description: Validation error
  /paediatric-early-warning-scores/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PaediatricEarlyWarningScore by id
      operationId: getPaediatricEarlyWarningScore
      responses:
        "200":
          description: The requested PaediatricEarlyWarningScore
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaediatricEarlyWarningScore"
        "404":
          description: PaediatricEarlyWarningScore not found
    patch:
      summary: Update an existing PaediatricEarlyWarningScore
      operationId: updatePaediatricEarlyWarningScore
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaediatricEarlyWarningScore"
      responses:
        "200":
          description: The updated PaediatricEarlyWarningScore
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaediatricEarlyWarningScore"
        "404":
          description: PaediatricEarlyWarningScore not found
    delete:
      summary: Soft-delete a PaediatricEarlyWarningScore
      operationId: deletePaediatricEarlyWarningScore
      responses:
        "204":
          description: PaediatricEarlyWarningScore deleted
        "404":
          description: PaediatricEarlyWarningScore not found
components:
  schemas:
    PaediatricEarlyWarningScore:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: "Foreign key to the child observed (RESTRICT: patients are not deleted while observations exist)."
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the clinician who recorded the observation set (RESTRICT); NULL until assigned.
        status:
          type: string
          enum:
            - draft
            - submitted
            - amended
            - signed
          description: "Workflow status: draft, submitted, amended, or signed."
        observation_at:
          type: string
          format: date-time
          description: Date and time the observation set was taken; NULL when not yet recorded.
        care_setting:
          type: string
          enum:
            - ward
            - childrens-assessment-unit
            - emergency-department
            - other
            - ""
          description: "Care setting where the observations were recorded: ward, childrens-assessment-unit, emergency-department, or other."
        age_band:
          type: string
          enum:
            - neonate
            - infant
            - young-child
            - child
            - adolescent
            - ""
          description: "Age band selected first; sets the normal respiratory-rate and heart-rate ranges: neonate (0-<1 month), infant (1-11 months), young-child (1-4 years), child (5-11 years), or adolescent (>=12 years)."
        respiratory_rate:
          type: integer
          format: int32
          description: Respiratory rate in breaths per minute, scored against the age-band normal range; NULL when not recorded.
        respiratory_effort:
          type: string
          enum:
            - none
            - mild
            - moderate
            - severe
            - ""
          description: "Respiratory effort / recession: none, mild, moderate, or severe (grunting)."
        oxygen_saturation:
          type: integer
          format: int32
          description: Oxygen saturation (SpO2) as a percentage; NULL when not recorded.
        supplemental_oxygen:
          type: string
          enum:
            - room-air
            - low-flow
            - high-flow
            - ""
          description: "Supplemental-oxygen requirement: room-air, low-flow, or high-flow."
        heart_rate:
          type: integer
          format: int32
          description: Heart rate in beats per minute, scored against the age-band normal range; NULL when not recorded.
        capillary_refill:
          type: string
          enum:
            - under-2s
            - 2-3s
            - 3-4s
            - over-4s
            - ""
          description: "Capillary refill time / colour: under-2s, 2-3s, 3-4s, or over-4s."
        consciousness_acvpu:
          type: string
          enum:
            - alert
            - voice
            - pain
            - unresponsive
            - ""
          description: "Consciousness level on the ACVPU scale: alert, voice, pain, or unresponsive."
        nurse_concern:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Documented nurse / staff concern: an independent escalation trigger."
        parent_concern:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Documented parent / carer concern: an independent escalation trigger."
        clinical_notes:
          type: string
          description: Free-text clinical notes accompanying the observation set.
      required:
        - created_at
        - updated_at
        - patient_id
        - status
        - care_setting
        - age_band
        - respiratory_effort
        - supplemental_oxygen
        - capillary_refill
        - consciousness_acvpu
        - nurse_concern
        - parent_concern
        - clinical_notes
      description: "Main PEWS observation record: raw physiological observations for a single measurement set plus recording context (age band, care setting, observation timestamp) and the documented-concern override triggers. Sub-scores, aggregate, escalation band, rules, and flags live in child tables."
