# Generated from sql/. Do not edit by hand.
# SOURCE: paediatric_early_warning_score_grade
openapi: 3.1.0
info:
  title: Paediatric Early Warning Score — PaediatricEarlyWarningScoreGrade
  version: 1.0.0
  description: "Computed PEWS grading result: per-parameter 0-3 sub-scores, aggregate total (0-21), maximum single-parameter sub-score, derived escalation band, single-parameter-3 and documented-concern override triggers, and recommended monitoring frequency. One row per observation record (1:1)."
paths:
  /paediatric-early-warning-score-grades:
    get:
      summary: List PaediatricEarlyWarningScoreGrade records
      operationId: listPaediatricEarlyWarningScoreGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PaediatricEarlyWarningScoreGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PaediatricEarlyWarningScoreGrade"
    post:
      summary: Create a new PaediatricEarlyWarningScoreGrade
      operationId: createPaediatricEarlyWarningScoreGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaediatricEarlyWarningScoreGrade"
      responses:
        "201":
          description: The newly created PaediatricEarlyWarningScoreGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaediatricEarlyWarningScoreGrade"
        "400":
          description: Validation error
  /paediatric-early-warning-score-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PaediatricEarlyWarningScoreGrade by id
      operationId: getPaediatricEarlyWarningScoreGrade
      responses:
        "200":
          description: The requested PaediatricEarlyWarningScoreGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaediatricEarlyWarningScoreGrade"
        "404":
          description: PaediatricEarlyWarningScoreGrade not found
    patch:
      summary: Update an existing PaediatricEarlyWarningScoreGrade
      operationId: updatePaediatricEarlyWarningScoreGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaediatricEarlyWarningScoreGrade"
      responses:
        "200":
          description: The updated PaediatricEarlyWarningScoreGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaediatricEarlyWarningScoreGrade"
        "404":
          description: PaediatricEarlyWarningScoreGrade not found
    delete:
      summary: Soft-delete a PaediatricEarlyWarningScoreGrade
      operationId: deletePaediatricEarlyWarningScoreGrade
      responses:
        "204":
          description: PaediatricEarlyWarningScoreGrade deleted
        "404":
          description: PaediatricEarlyWarningScoreGrade not found
components:
  schemas:
    PaediatricEarlyWarningScoreGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        paediatric_early_warning_score_id:
          type: string
          format: uuid
          description: Foreign key to the parent observation record (unique, 1:1).
        aggregate_score:
          type: integer
          format: int32
          description: "Aggregate PEWS total (0-21): sum of the seven parameter sub-scores; NULL until computed."
        max_parameter_score:
          type: integer
          format: int32
          description: Maximum of the seven parameter sub-scores (0-3); a value of 3 fires the single-parameter override trigger.
        respiratory_rate_score:
          type: integer
          format: int32
          description: Respiratory-rate sub-score (0-3), scored against the age-band normal range; NULL when not recorded or the age band was unset.
        respiratory_effort_score:
          type: integer
          format: int32
          description: Respiratory-effort / recession sub-score (0-3); NULL when not recorded.
        oxygen_saturation_score:
          type: integer
          format: int32
          description: Oxygen-saturation (SpO2) sub-score (0-3); NULL when not recorded.
        supplemental_oxygen_score:
          type: integer
          format: int32
          description: Supplemental-oxygen sub-score (0, 1, or 3); NULL when not recorded.
        heart_rate_score:
          type: integer
          format: int32
          description: Heart-rate sub-score (0-3), scored against the age-band normal range; NULL when not recorded or the age band was unset.
        capillary_refill_score:
          type: integer
          format: int32
          description: Capillary-refill / colour sub-score (0-3); NULL when not recorded.
        consciousness_score:
          type: integer
          format: int32
          description: Consciousness (ACVPU) sub-score (0-3); NULL when not recorded.
        risk_band:
          type: string
          enum:
            - routine
            - low
            - medium
            - high
            - ""
          description: Derived escalation band from the aggregate total (>=6 high, 4-5 medium, 2-3 low, else routine), raised as needed by the override triggers.
        single_parameter_trigger:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether any single parameter scored 3 (max_parameter_score = 3), an independent escalation trigger to at least medium.
        concern_trigger:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether a documented nurse / staff or parent / carer concern fired an independent escalation trigger.
        monitoring_frequency:
          type: string
          description: Recommended monitoring frequency and escalation response for the escalation band (e.g. routine observations, hourly, continuous with immediate senior review).
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the grade.
      required:
        - created_at
        - updated_at
        - paediatric_early_warning_score_id
        - risk_band
        - single_parameter_trigger
        - concern_trigger
        - monitoring_frequency
        - graded_at
      description: "Computed PEWS grading result: per-parameter 0-3 sub-scores, aggregate total (0-21), maximum single-parameter sub-score, derived escalation band, single-parameter-3 and documented-concern override triggers, and recommended monitoring frequency. One row per observation record (1:1)."
