# Generated from sql/. Do not edit by hand.
# SOURCE: grade
openapi: 3.1.0
info:
  title: Mast Cell Activation Syndrome Assessment — Grade
  version: 1.0.0
  description: Computed MCAS symptom grading result across multiple organ systems. One-to-one child of assessment.
paths:
  /grades:
    get:
      summary: List Grade records
      operationId: listGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Grade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      summary: Create a new Grade
      operationId: createGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "201":
          description: The newly created Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "400":
          description: Validation error
  /grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Grade by id
      operationId: getGrade
      responses:
        "200":
          description: The requested Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    patch:
      summary: Update an existing Grade
      operationId: updateGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "200":
          description: The updated Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    delete:
      summary: Soft-delete a Grade
      operationId: deleteGrade
      responses:
        "204":
          description: Grade deleted
        "404":
          description: Grade not found
components:
  schemas:
    Grade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the parent assessment (unique, enforcing 1:1).
        total_symptom_score:
          type: integer
          format: int32
          description: Total cumulative symptom severity score across all organ systems.
        dermatological_score:
          type: integer
          format: int32
          description: Symptom severity score for dermatological system.
        gastrointestinal_score:
          type: integer
          format: int32
          description: Symptom severity score for gastrointestinal system.
        cardiovascular_score:
          type: integer
          format: int32
          description: Symptom severity score for cardiovascular system.
        respiratory_score:
          type: integer
          format: int32
          description: Symptom severity score for respiratory system.
        neurological_score:
          type: integer
          format: int32
          description: Symptom severity score for neurological system.
        organ_system_count:
          type: integer
          format: int32
          description: Number of organ systems affected (0-5).
        severity_level:
          type: string
          enum:
            - mild
            - moderate
            - severe
            - ""
          description: "Overall severity classification: mild, moderate, or severe."
        trigger_count:
          type: integer
          format: int32
          description: Number of identified symptom triggers.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the grading was computed.
      required:
        - created_at
        - updated_at
        - assessment_id
        - total_symptom_score
        - dermatological_score
        - gastrointestinal_score
        - cardiovascular_score
        - respiratory_score
        - neurological_score
        - organ_system_count
        - severity_level
        - trigger_count
        - graded_at
      description: Computed MCAS symptom grading result across multiple organ systems. One-to-one child of assessment.
