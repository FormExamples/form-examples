# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: grade
openapi: 3.1.0
info:
  title: Pulmonology Assessment — Grade
  version: 1.0.0
  description: Computed GOLD (Global Initiative for Chronic Obstructive Lung Disease) staging result. Stage I-IV based on FEV1%, Group A/B/E based on symptoms and exacerbations. One-to-one child of assessment.
paths:
  /grades:
    get:
      summary: List Grade records
      operationId: listGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Grade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      summary: Create a new Grade
      operationId: createGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "201":
          description: The newly created Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "400":
          description: Validation error
  /grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Grade by id
      operationId: getGrade
      responses:
        "200":
          description: The requested Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    patch:
      summary: Update an existing Grade
      operationId: updateGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "200":
          description: The updated Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    delete:
      summary: Soft-delete a Grade
      operationId: deleteGrade
      responses:
        "204":
          description: Grade deleted
        "404":
          description: Grade not found
components:
  schemas:
    Grade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the parent assessment (unique, enforcing 1:1).
        gold_stage:
          type: string
          enum:
            - I
            - II
            - III
            - IV
            - ""
          description: "GOLD spirometric stage: I (>=80%), II (50-79%), III (30-49%), IV (<30%), or empty."
        gold_group:
          type: string
          enum:
            - A
            - B
            - E
            - ""
          description: "GOLD group classification (2023 revision): A (low symptoms, low exacerbations), B (high symptoms, low exacerbations), E (exacerbation history), or empty."
        fev1_predicted_pct:
          type: number
          format: double
          description: Post-bronchodilator FEV1 as percentage of predicted used for staging.
        symptom_burden:
          type: string
          enum:
            - low
            - high
            - ""
          description: "Symptom burden classification: low (CAT<10, mMRC 0-1), high (CAT>=10, mMRC>=2), or empty."
        exacerbation_risk:
          type: string
          enum:
            - low
            - high
            - ""
          description: "Exacerbation risk: low (0-1 without hospitalisation), high (>=2 or >=1 with hospitalisation), or empty."
        composite_score:
          type: integer
          format: int32
          description: Aggregate composite severity score across all domains.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the grading was computed.
      required:
        - created_at
        - updated_at
        - assessment_id
        - gold_stage
        - gold_group
        - symptom_burden
        - exacerbation_risk
        - composite_score
        - graded_at
      description: Computed GOLD (Global Initiative for Chronic Obstructive Lung Disease) staging result. Stage I-IV based on FEV1%, Group A/B/E based on symptoms and exacerbations. One-to-one child of assessment.
