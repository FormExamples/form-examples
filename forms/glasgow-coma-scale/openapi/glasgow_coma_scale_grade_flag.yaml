# Generated from sql/. Do not edit by hand.
# SOURCE: glasgow_coma_scale_grade_flag
openapi: 3.1.0
info:
  title: Glasgow Coma Scale — GlasgowComaScaleGradeFlag
  version: 1.0.0
  description: Red-flag issues that fire independently of the severity band, with priority and a suggested action for the clinical team.
paths:
  /glasgow-coma-scale-grade-flags:
    get:
      summary: List GlasgowComaScaleGradeFlag records
      operationId: listGlasgowComaScaleGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of GlasgowComaScaleGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GlasgowComaScaleGradeFlag"
    post:
      summary: Create a new GlasgowComaScaleGradeFlag
      operationId: createGlasgowComaScaleGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GlasgowComaScaleGradeFlag"
      responses:
        "201":
          description: The newly created GlasgowComaScaleGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GlasgowComaScaleGradeFlag"
        "400":
          description: Validation error
  /glasgow-coma-scale-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one GlasgowComaScaleGradeFlag by id
      operationId: getGlasgowComaScaleGradeFlag
      responses:
        "200":
          description: The requested GlasgowComaScaleGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GlasgowComaScaleGradeFlag"
        "404":
          description: GlasgowComaScaleGradeFlag not found
    patch:
      summary: Update an existing GlasgowComaScaleGradeFlag
      operationId: updateGlasgowComaScaleGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GlasgowComaScaleGradeFlag"
      responses:
        "200":
          description: The updated GlasgowComaScaleGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GlasgowComaScaleGradeFlag"
        "404":
          description: GlasgowComaScaleGradeFlag not found
    delete:
      summary: Soft-delete a GlasgowComaScaleGradeFlag
      operationId: deleteGlasgowComaScaleGradeFlag
      responses:
        "204":
          description: GlasgowComaScaleGradeFlag deleted
        "404":
          description: GlasgowComaScaleGradeFlag not found
components:
  schemas:
    GlasgowComaScaleGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        glasgow_coma_scale_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-AIRWAY-RISK-001, F-UNEQUAL-PUPILS-001).
        category:
          type: string
          enum:
            - airway-risk
            - deteriorating
            - unequal-pupils
            - untestable-component
            - falling-motor
            - other
            - ""
          description: "Flag category: airway-risk (GCS <= 8 / coma), deteriorating, unequal-pupils, untestable-component, falling-motor, or other."
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical action (e.g. \"consider definitive airway management and senior escalation\", \"urgent CT head and neurosurgical referral\")."
      required:
        - created_at
        - updated_at
        - glasgow_coma_scale_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Red-flag issues that fire independently of the severity band, with priority and a suggested action for the clinical team.
