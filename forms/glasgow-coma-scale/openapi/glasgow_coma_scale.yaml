# Generated from sql/. Do not edit by hand.
# SOURCE: glasgow_coma_scale
openapi: 3.1.0
info:
  title: Glasgow Coma Scale — GlasgowComaScale
  version: 1.0.0
  description: "Main Glasgow Coma Scale assessment record: assessment context, patient identification, confounders, the three component responses (eye, verbal, motor) with not-testable handling, pupillary findings for GCS-Pupils, and trend fields."
paths:
  /glasgow-coma-scales:
    get:
      summary: List GlasgowComaScale records
      operationId: listGlasgowComaScale
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of GlasgowComaScale records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GlasgowComaScale"
    post:
      summary: Create a new GlasgowComaScale
      operationId: createGlasgowComaScale
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GlasgowComaScale"
      responses:
        "201":
          description: The newly created GlasgowComaScale
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GlasgowComaScale"
        "400":
          description: Validation error
  /glasgow-coma-scales/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one GlasgowComaScale by id
      operationId: getGlasgowComaScale
      responses:
        "200":
          description: The requested GlasgowComaScale
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GlasgowComaScale"
        "404":
          description: GlasgowComaScale not found
    patch:
      summary: Update an existing GlasgowComaScale
      operationId: updateGlasgowComaScale
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GlasgowComaScale"
      responses:
        "200":
          description: The updated GlasgowComaScale
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GlasgowComaScale"
        "404":
          description: GlasgowComaScale not found
    delete:
      summary: Soft-delete a GlasgowComaScale
      operationId: deleteGlasgowComaScale
      responses:
        "204":
          description: GlasgowComaScale deleted
        "404":
          description: GlasgowComaScale not found
components:
  schemas:
    GlasgowComaScale:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient being assessed (delete restricted).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the assessing clinician (optional; delete restricted).
        assessor_name:
          type: string
          maxLength: 255
          description: Name of the assessing observer as recorded on the assessment.
        assessor_role:
          type: string
          enum:
            - doctor
            - nurse
            - paramedic
            - emergency-medical-technician
            - advanced-clinical-practitioner
            - neuro-observation-staff
            - other
            - ""
          description: "Role of the assessing observer: doctor, nurse, paramedic, emergency-medical-technician, advanced-clinical-practitioner, neuro-observation-staff, or other."
        assessed_at:
          type: string
          format: date-time
          description: Date and time the assessment was performed.
        setting:
          type: string
          enum:
            - ed
            - neuro
            - critical-care
            - pre-hospital
            - other
            - ""
          description: "Care setting: ed (emergency department), neuro, critical-care, pre-hospital, or other."
        reason:
          type: string
          description: Free-text reason for the assessment (e.g. head injury, stroke, reduced consciousness).
        intubated:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient is intubated or has a tracheostomy (confounder; may force the verbal component to NT).
        sedated:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient is sedated (confounder; may force a component to NT).
        paralysed:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient has neuromuscular blockade (confounder; may force the motor component to NT).
        eye_response:
          type: string
          enum:
            - spontaneous
            - to-sound
            - to-pressure
            - none
            - NT
            - ""
          description: "Best eye-opening response (E, 1-4): spontaneous (4), to-sound (3), to-pressure (2), none (1), or NT when a local factor prevents testing."
        eye_not_testable_reason:
          type: string
          description: Free-text reason the eye component is not testable (e.g. periorbital swelling, dressings).
        verbal_response:
          type: string
          enum:
            - orientated
            - confused
            - words
            - sounds
            - none
            - NT
            - ""
          description: "Best verbal response (V, 1-5): orientated (5), confused (4), words (3), sounds (2), none (1), or NT when a local factor prevents testing."
        verbal_not_testable_reason:
          type: string
          description: Free-text reason the verbal component is not testable (e.g. intubation, tracheostomy, language barrier).
        motor_response:
          type: string
          enum:
            - obeys-commands
            - localising
            - normal-flexion
            - abnormal-flexion
            - extension
            - none
            - NT
            - ""
          description: "Best motor response (M, 1-6): obeys-commands (6), localising (5), normal-flexion (4), abnormal-flexion (3), extension (2), none (1), or NT when a local factor prevents testing."
        motor_not_testable_reason:
          type: string
          description: Free-text reason the motor component is not testable (e.g. neuromuscular blockade, spinal injury, limb immobilisation).
        left_pupil_reactivity:
          type: string
          enum:
            - reactive
            - sluggish
            - unreactive
            - ""
          description: "Left pupil reactivity to light: reactive, sluggish, or unreactive (used for the Pupil Reactivity Score)."
        right_pupil_reactivity:
          type: string
          enum:
            - reactive
            - sluggish
            - unreactive
            - ""
          description: "Right pupil reactivity to light: reactive, sluggish, or unreactive (used for the Pupil Reactivity Score)."
        left_pupil_size_mm:
          type: number
          format: double
          description: Left pupil size in millimetres (for the record).
        right_pupil_size_mm:
          type: number
          format: double
          description: Right pupil size in millimetres (for the record).
        previous_total:
          type: integer
          format: int32
          description: Previous total GCS (3-15) for the trend comparison, when available.
        previous_motor_score:
          type: integer
          format: int32
          description: Previous motor component score (1-6) for the trend comparison, when available.
        previous_assessed_at:
          type: string
          format: date-time
          description: Date and time of the previous assessment used for the trend comparison.
        clinical_note:
          type: string
          description: Free-text clinical note recorded with the assessment.
      required:
        - created_at
        - updated_at
        - patient_id
        - assessor_name
        - assessor_role
        - setting
        - reason
        - intubated
        - sedated
        - paralysed
        - eye_response
        - eye_not_testable_reason
        - verbal_response
        - verbal_not_testable_reason
        - motor_response
        - motor_not_testable_reason
        - left_pupil_reactivity
        - right_pupil_reactivity
        - clinical_note
      description: "Main Glasgow Coma Scale assessment record: assessment context, patient identification, confounders, the three component responses (eye, verbal, motor) with not-testable handling, pupillary findings for GCS-Pupils, and trend fields."
