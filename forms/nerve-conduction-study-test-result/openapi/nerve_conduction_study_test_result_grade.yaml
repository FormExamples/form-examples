# Generated from sql/. Do not edit by hand.
# SOURCE: nerve_conduction_study_test_result_grade
openapi: 3.1.0
info:
  title: Nerve Conduction Study Test Result — NerveConductionStudyTestResultGrade
  version: 1.0.0
  description: "Computed four-axis interpretation grade: result classification, abnormality severity / structured-reporting category, report completeness, and follow-up urgency. A critical result (e.g. motor neurone disease features, or a severe acute neuropathy such as a Guillain-Barre syndrome pattern) auto-escalates the follow-up urgency to critical-alert regardless of the other axes."
paths:
  /nerve-conduction-study-test-result-grades:
    get:
      summary: List NerveConductionStudyTestResultGrade records
      operationId: listNerveConductionStudyTestResultGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of NerveConductionStudyTestResultGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NerveConductionStudyTestResultGrade"
    post:
      summary: Create a new NerveConductionStudyTestResultGrade
      operationId: createNerveConductionStudyTestResultGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NerveConductionStudyTestResultGrade"
      responses:
        "201":
          description: The newly created NerveConductionStudyTestResultGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NerveConductionStudyTestResultGrade"
        "400":
          description: Validation error
  /nerve-conduction-study-test-result-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one NerveConductionStudyTestResultGrade by id
      operationId: getNerveConductionStudyTestResultGrade
      responses:
        "200":
          description: The requested NerveConductionStudyTestResultGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NerveConductionStudyTestResultGrade"
        "404":
          description: NerveConductionStudyTestResultGrade not found
    patch:
      summary: Update an existing NerveConductionStudyTestResultGrade
      operationId: updateNerveConductionStudyTestResultGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NerveConductionStudyTestResultGrade"
      responses:
        "200":
          description: The updated NerveConductionStudyTestResultGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NerveConductionStudyTestResultGrade"
        "404":
          description: NerveConductionStudyTestResultGrade not found
    delete:
      summary: Soft-delete a NerveConductionStudyTestResultGrade
      operationId: deleteNerveConductionStudyTestResultGrade
      responses:
        "204":
          description: NerveConductionStudyTestResultGrade deleted
        "404":
          description: NerveConductionStudyTestResultGrade not found
components:
  schemas:
    NerveConductionStudyTestResultGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        nerve_conduction_study_test_result_id:
          type: string
          format: uuid
          description: Foreign key to the parent result (unique, 1:1).
        result_classification:
          type: string
          enum:
            - normal
            - abnormal
            - critical
            - inconclusive
            - ""
          description: "Axis A overall classification: normal, abnormal, critical, inconclusive."
        abnormality_severity:
          type: string
          enum:
            - none
            - minor
            - moderate
            - major
            - ""
          description: "Axis B abnormality severity: none, minor, moderate, major."
        reporting_category:
          type: string
          maxLength: 50
          description: Axis B structured-reporting score / category label (e.g. an AANEM CTS severity grade or polyneuropathy descriptor).
        report_completeness_percent:
          type: integer
          format: int32
          description: Axis C report completeness as a percentage of mandatory report sections (0-100).
        follow_up_urgency:
          type: string
          enum:
            - routine
            - recommended
            - urgent
            - critical-alert
            - ""
          description: "Axis D follow-up urgency: routine, recommended, urgent, critical-alert."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the recommended follow-up implied by the urgency.
        recommended_action:
          type: string
          maxLength: 500
          description: Axis D concise recommended action derived from the urgency.
        recommendation:
          type: string
          enum:
            - no-action
            - routine-follow-up
            - further-imaging
            - specialist-referral
            - urgent-review
            - ""
          description: "Overall recommendation: no-action, routine-follow-up, further-imaging, specialist-referral, urgent-review."
        clinician_notes:
          type: string
          description: Free-text interpretation / sign-off notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of the reporting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - nerve_conduction_study_test_result_id
        - result_classification
        - abnormality_severity
        - reporting_category
        - follow_up_urgency
        - target_timeframe
        - recommended_action
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis interpretation grade: result classification, abnormality severity / structured-reporting category, report completeness, and follow-up urgency. A critical result (e.g. motor neurone disease features, or a severe acute neuropathy such as a Guillain-Barre syndrome pattern) auto-escalates the follow-up urgency to critical-alert regardless of the other axes."
