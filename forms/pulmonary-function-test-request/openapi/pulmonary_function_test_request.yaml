# Generated from sql/. Do not edit by hand.
# SOURCE: pulmonary_function_test_request
openapi: 3.1.0
info:
  title: Pulmonary Function Test Request — PulmonaryFunctionTestRequest
  version: 1.0.0
  description: Pulmonary function test (lung-function / spirometry) request (referral). Captures the requested test, the clinical indication and question, symptoms, smoking and inhaler background, a safety / contraindication screen, and triage urgency.
paths:
  /pulmonary-function-test-requests:
    get:
      summary: List PulmonaryFunctionTestRequest records
      operationId: listPulmonaryFunctionTestRequest
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PulmonaryFunctionTestRequest records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PulmonaryFunctionTestRequest"
    post:
      summary: Create a new PulmonaryFunctionTestRequest
      operationId: createPulmonaryFunctionTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PulmonaryFunctionTestRequest"
      responses:
        "201":
          description: The newly created PulmonaryFunctionTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PulmonaryFunctionTestRequest"
        "400":
          description: Validation error
  /pulmonary-function-test-requests/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PulmonaryFunctionTestRequest by id
      operationId: getPulmonaryFunctionTestRequest
      responses:
        "200":
          description: The requested PulmonaryFunctionTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PulmonaryFunctionTestRequest"
        "404":
          description: PulmonaryFunctionTestRequest not found
    patch:
      summary: Update an existing PulmonaryFunctionTestRequest
      operationId: updatePulmonaryFunctionTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PulmonaryFunctionTestRequest"
      responses:
        "200":
          description: The updated PulmonaryFunctionTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PulmonaryFunctionTestRequest"
        "404":
          description: PulmonaryFunctionTestRequest not found
    delete:
      summary: Soft-delete a PulmonaryFunctionTestRequest
      operationId: deletePulmonaryFunctionTestRequest
      responses:
        "204":
          description: PulmonaryFunctionTestRequest deleted
        "404":
          description: PulmonaryFunctionTestRequest not found
components:
  schemas:
    PulmonaryFunctionTestRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the requesting clinician (referrer).
        status:
          type: string
          enum:
            - draft
            - submitted
            - vetted
            - scheduled
            - rejected
            - ""
          description: "Request lifecycle status: draft, submitted, vetted, scheduled, rejected."
        site_name:
          type: string
          maxLength: 255
          description: Name of the requesting site or clinic.
        setting:
          type: string
          enum:
            - outpatient
            - inpatient
            - community
            - emergency
            - ""
          description: "Care setting: outpatient, inpatient, community, emergency."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        requested_by_date:
          type: string
          format: date
          description: Date by which the test is requested to be performed.
        test_type:
          type: string
          enum:
            - spirometry
            - spirometry-with-reversibility
            - full-lung-function
            - gas-transfer-dlco
            - peak-flow
            - feno
            - other
            - ""
          description: "Requested test type: spirometry, spirometry-with-reversibility, full-lung-function, gas-transfer-dlco, peak-flow, feno, other."
        primary_indication:
          type: string
          enum:
            - suspected-asthma
            - suspected-copd
            - breathlessness
            - chronic-cough
            - pre-operative
            - occupational-lung-disease
            - monitoring
            - restrictive-disease
            - other
            - ""
          description: Primary clinical indication, used for appropriateness scoring.
        clinical_question:
          type: string
          maxLength: 1000
          description: Specific clinical question the test should answer (highest-value field).
        relevant_history:
          type: string
          maxLength: 1000
          description: Relevant medical, respiratory, and occupational history.
        symptom_breathlessness:
          type: boolean
          description: Whether the patient reports breathlessness.
        symptom_cough:
          type: boolean
          description: Whether the patient reports cough.
        symptom_wheeze:
          type: boolean
          description: Whether the patient reports wheeze.
        smoking_status:
          type: string
          enum:
            - never
            - ex
            - current
            - ""
          description: "Smoking status: never, ex, current."
        current_inhalers:
          type: string
          maxLength: 1000
          description: Current inhaler / respiratory medication, relevant to withhold instructions before reversibility testing.
        recent_respiratory_infection:
          type: boolean
          description: Whether the patient had a recent respiratory infection (infection-control and result-validity flag).
        recent_mi_or_eye_abdominal_surgery:
          type: boolean
          description: Whether the patient had a recent myocardial infarction or recent eye / thoracic / abdominal surgery (forced-expiration contraindication screen).
        suspected_active_tuberculosis:
          type: boolean
          description: Whether active tuberculosis is suspected (infection-control contraindication for shared equipment).
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - ""
          description: "Requested triage urgency: routine, urgent."
        supervising_consultant:
          type: string
          maxLength: 255
          description: Name of the supervising / responsible consultant.
        requester_contact:
          type: string
          maxLength: 255
          description: Contact details (bleep / phone) for the requester.
        notes:
          type: string
          maxLength: 1000
          description: Free-text additional notes.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - status
        - site_name
        - setting
        - test_type
        - primary_indication
        - clinical_question
        - relevant_history
        - symptom_breathlessness
        - symptom_cough
        - symptom_wheeze
        - smoking_status
        - current_inhalers
        - recent_respiratory_infection
        - recent_mi_or_eye_abdominal_surgery
        - suspected_active_tuberculosis
        - urgency
        - supervising_consultant
        - requester_contact
        - notes
      description: Pulmonary function test (lung-function / spirometry) request (referral). Captures the requested test, the clinical indication and question, symptoms, smoking and inhaler background, a safety / contraindication screen, and triage urgency.
