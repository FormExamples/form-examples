# Generated from sql/. Do not edit by hand.
# SOURCE: paediatrics_waiting_list_card
openapi: 3.1.0
info:
  title: Paediatrics Waiting List Card — PaediatricsWaitingListCard
  version: 1.0.0
  description: Top-level record placing a patient on a paediatrics waiting list. Parent of appointment(s) and grading result.
paths:
  /paediatrics-waiting-list-cards:
    get:
      summary: List PaediatricsWaitingListCard records
      operationId: listPaediatricsWaitingListCard
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PaediatricsWaitingListCard records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PaediatricsWaitingListCard"
    post:
      summary: Create a new PaediatricsWaitingListCard
      operationId: createPaediatricsWaitingListCard
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaediatricsWaitingListCard"
      responses:
        "201":
          description: The newly created PaediatricsWaitingListCard
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaediatricsWaitingListCard"
        "400":
          description: Validation error
  /paediatrics-waiting-list-cards/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PaediatricsWaitingListCard by id
      operationId: getPaediatricsWaitingListCard
      responses:
        "200":
          description: The requested PaediatricsWaitingListCard
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaediatricsWaitingListCard"
        "404":
          description: PaediatricsWaitingListCard not found
    patch:
      summary: Update an existing PaediatricsWaitingListCard
      operationId: updatePaediatricsWaitingListCard
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaediatricsWaitingListCard"
      responses:
        "200":
          description: The updated PaediatricsWaitingListCard
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaediatricsWaitingListCard"
        "404":
          description: PaediatricsWaitingListCard not found
    delete:
      summary: Soft-delete a PaediatricsWaitingListCard
      operationId: deletePaediatricsWaitingListCard
      responses:
        "204":
          description: PaediatricsWaitingListCard deleted
        "404":
          description: PaediatricsWaitingListCard not found
components:
  schemas:
    PaediatricsWaitingListCard:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient placed on the list.
        practitioner_id:
          type: string
          format: uuid
          description: Foreign key to the practitioner who placed the patient on the list.
        status:
          type: string
          enum:
            - draft
            - submitted
            - reviewed
            - closed
          description: "Lifecycle status: draft, submitted, reviewed, or closed."
        entry_date:
          type: string
          format: date
          description: Date the practitioner entered the card.
        entry_time:
          type: string
          format: time
          description: Time the practitioner entered the card.
        referral_source:
          type: string
          enum:
            - gp
            - consultant
            - a-and-e
            - self
            - community
            - screening
            - other
            - ""
          description: "Where the referral originated: gp, consultant, a-and-e, self, community, screening, other."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        referral_letter_reference:
          type: string
          maxLength: 100
          description: Reference number / identifier for the originating referral letter.
        reason_for_referral:
          type: string
          description: Free-text reason for referral.
        presenting_condition:
          type: string
          description: Free-text presenting condition / clinical summary.
        icd_10_code:
          type: string
          maxLength: 20
          description: ICD-10 diagnostic code for the presenting condition.
        snomed_ct_code:
          type: string
          maxLength: 30
          description: SNOMED CT clinical concept code for the presenting condition.
        suspected_cancer:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether this is a suspected-cancer (two-week wait) referral.
        list_name:
          type: string
          maxLength: 255
          description: "Display name for the waiting list (e.g. \"Orthopaedic — knee replacement\")."
        specialty:
          type: string
          maxLength: 100
          description: "NHS specialty (e.g. \"Trauma & Orthopaedics\")."
        sub_specialty:
          type: string
          maxLength: 100
          description: "NHS sub-specialty (e.g. \"Knee surgery\")."
        procedure_description:
          type: string
          description: Plain-language description of what the patient is waiting to do.
        opcs_4_code:
          type: string
          maxLength: 20
          description: OPCS-4 procedure code if a specific procedure is planned.
        clinical_priority:
          type: string
          enum:
            - P1a
            - P1b
            - P2
            - P3
            - P4
            - P5
            - P6
            - ""
          description: "NHS England clinical priority: P1a, P1b, P2, P3, P4, P5, P6."
        rtt_clock_start_date:
          type: string
          format: date
          description: Referral to Treatment (RTT) clock-start date. The date the patient joined the list.
        expected_procedure_type:
          type: string
          enum:
            - first-outpatient-appointment
            - follow-up-appointment
            - day-case-procedure
            - inpatient-procedure
            - diagnostic
            - therapy
            - multi-disciplinary-team-review
            - other
            - ""
          description: Expected next event for the patient.
        expected_wait_weeks:
          type: integer
          format: int32
          description: Expected wait, in weeks, communicated to the patient at entry.
        consent_to_reminders:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient has consented to appointment reminders.
        communication_notes:
          type: string
          description: Free-text communication preferences and notes.
        additional_notes:
          type: string
          description: Free-text practitioner sign-off notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of practitioner electronic signature.
      required:
        - created_at
        - updated_at
        - patient_id
        - practitioner_id
        - status
        - referral_source
        - referral_letter_reference
        - reason_for_referral
        - presenting_condition
        - icd_10_code
        - snomed_ct_code
        - suspected_cancer
        - list_name
        - specialty
        - sub_specialty
        - procedure_description
        - opcs_4_code
        - clinical_priority
        - expected_procedure_type
        - consent_to_reminders
        - communication_notes
        - additional_notes
      description: Top-level record placing a patient on a paediatrics waiting list. Parent of appointment(s) and grading result.
