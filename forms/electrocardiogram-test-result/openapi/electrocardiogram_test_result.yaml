# Generated from sql/. Do not edit by hand.
# SOURCE: electrocardiogram_test_result
openapi: 3.1.0
info:
  title: Electrocardiogram Test Result — ElectrocardiogramTestResult
  version: 1.0.0
  description: ECG (electrocardiogram) test result (report). Captures the recorded ECG type and quality, the clinical history, the ventricular rate, rhythm and the PR / QRS / QT / QTc intervals and cardiac axis, the structured electrophysiological findings, the narrative interpretation and impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /electrocardiogram-test-results:
    get:
      summary: List ElectrocardiogramTestResult records
      operationId: listElectrocardiogramTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ElectrocardiogramTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ElectrocardiogramTestResult"
    post:
      summary: Create a new ElectrocardiogramTestResult
      operationId: createElectrocardiogramTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ElectrocardiogramTestResult"
      responses:
        "201":
          description: The newly created ElectrocardiogramTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ElectrocardiogramTestResult"
        "400":
          description: Validation error
  /electrocardiogram-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ElectrocardiogramTestResult by id
      operationId: getElectrocardiogramTestResult
      responses:
        "200":
          description: The requested ElectrocardiogramTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ElectrocardiogramTestResult"
        "404":
          description: ElectrocardiogramTestResult not found
    patch:
      summary: Update an existing ElectrocardiogramTestResult
      operationId: updateElectrocardiogramTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ElectrocardiogramTestResult"
      responses:
        "200":
          description: The updated ElectrocardiogramTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ElectrocardiogramTestResult"
        "404":
          description: ElectrocardiogramTestResult not found
    delete:
      summary: Soft-delete a ElectrocardiogramTestResult
      operationId: deleteElectrocardiogramTestResult
      responses:
        "204":
          description: ElectrocardiogramTestResult deleted
        "404":
          description: ElectrocardiogramTestResult not found
components:
  schemas:
    ElectrocardiogramTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (cardiologist / cardiac physiologist / reporting clinician).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating ECG request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        ecg_type:
          type: string
          enum:
            - resting-12-lead
            - exercise-stress
            - ambulatory-holter-24h
            - ambulatory-48h
            - event-recorder
            - other
            - ""
          description: "Recorded ECG type: resting-12-lead, exercise-stress, ambulatory-holter-24h, ambulatory-48h, event-recorder, other."
        performed_date:
          type: string
          format: date
          description: Date the ECG examination was recorded.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        recording_quality:
          type: string
          enum:
            - good
            - adequate
            - poor
            - ""
          description: "Technical recording quality of the trace: good, adequate, poor."
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the examination was performed to answer.
        ventricular_rate_bpm:
          type: integer
          format: int32
          description: Ventricular rate in beats per minute (bpm).
        rhythm:
          type: string
          enum:
            - sinus
            - atrial-fibrillation
            - atrial-flutter
            - svt
            - ventricular-tachycardia
            - heart-block
            - paced
            - other
            - ""
          description: "Dominant rhythm: sinus, atrial-fibrillation, atrial-flutter, svt, ventricular-tachycardia, heart-block, paced, other."
        pr_interval_ms:
          type: integer
          format: int32
          description: PR interval in milliseconds (normal ~120-200 ms); prolongation suggests atrioventricular conduction delay / heart block.
        qrs_duration_ms:
          type: integer
          format: int32
          description: QRS duration in milliseconds (normal < 120 ms); widening suggests bundle branch block or ventricular origin.
        qt_interval_ms:
          type: integer
          format: int32
          description: Uncorrected QT interval in milliseconds.
        qtc_ms:
          type: integer
          format: int32
          description: Rate-corrected QT interval (QTc, e.g. Bazett) in milliseconds; >= 500 ms is markedly prolonged and confers torsades-de-pointes risk.
        cardiac_axis:
          type: string
          enum:
            - normal
            - left-deviation
            - right-deviation
            - ""
          description: "Frontal-plane cardiac axis: normal, left-deviation, right-deviation."
        st_elevation:
          type: boolean
          description: Whether ST-segment elevation (a potential STEMI / acute injury pattern) is present.
        st_depression:
          type: boolean
          description: Whether ST-segment depression (an ischaemia pattern) is present.
        t_wave_inversion:
          type: boolean
          description: Whether pathological T-wave inversion is present.
        pathological_q_waves:
          type: boolean
          description: Whether pathological Q waves (prior infarction) are present.
        left_ventricular_hypertrophy:
          type: boolean
          description: Whether voltage criteria for left ventricular hypertrophy (LVH) are met.
        bundle_branch_block:
          type: boolean
          description: Whether bundle branch block (left or right) is present.
        ischaemia:
          type: boolean
          description: Whether an ischaemic pattern is present.
        normal_ecg:
          type: boolean
          description: Whether the ECG is reported as normal.
        interpretation:
          type: string
          maxLength: 2000
          description: Narrative interpretation of the ECG findings (the body of the report).
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous ECGs.
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 50
          description: Free-text structured-reporting category or coded label for the dominant interpretation.
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up investigation, referral, or management.
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer / responsible team.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - ecg_type
        - recording_quality
        - clinical_history
        - rhythm
        - cardiac_axis
        - st_elevation
        - st_depression
        - t_wave_inversion
        - pathological_q_waves
        - left_ventricular_hypertrophy
        - bundle_branch_block
        - ischaemia
        - normal_ecg
        - interpretation
        - comparison_with_previous
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: ECG (electrocardiogram) test result (report). Captures the recorded ECG type and quality, the clinical history, the ventricular rate, rhythm and the PR / QRS / QT / QTc intervals and cardiac axis, the structured electrophysiological findings, the narrative interpretation and impression, recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
