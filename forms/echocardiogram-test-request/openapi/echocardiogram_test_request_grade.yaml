# Generated from sql/. Do not edit by hand.
# SOURCE: echocardiogram_test_request_grade
openapi: 3.1.0
info:
  title: Echocardiogram Test Request — EchocardiogramTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (ACC/AHA/ASE & BSE AUC 1-9), urgency triage, request completeness, and clinical priority."
paths:
  /echocardiogram-test-request-grades:
    get:
      summary: List EchocardiogramTestRequestGrade records
      operationId: listEchocardiogramTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of EchocardiogramTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EchocardiogramTestRequestGrade"
    post:
      summary: Create a new EchocardiogramTestRequestGrade
      operationId: createEchocardiogramTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EchocardiogramTestRequestGrade"
      responses:
        "201":
          description: The newly created EchocardiogramTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EchocardiogramTestRequestGrade"
        "400":
          description: Validation error
  /echocardiogram-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one EchocardiogramTestRequestGrade by id
      operationId: getEchocardiogramTestRequestGrade
      responses:
        "200":
          description: The requested EchocardiogramTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EchocardiogramTestRequestGrade"
        "404":
          description: EchocardiogramTestRequestGrade not found
    patch:
      summary: Update an existing EchocardiogramTestRequestGrade
      operationId: updateEchocardiogramTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EchocardiogramTestRequestGrade"
      responses:
        "200":
          description: The updated EchocardiogramTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EchocardiogramTestRequestGrade"
        "404":
          description: EchocardiogramTestRequestGrade not found
    delete:
      summary: Soft-delete a EchocardiogramTestRequestGrade
      operationId: deleteEchocardiogramTestRequestGrade
      responses:
        "204":
          description: EchocardiogramTestRequestGrade deleted
        "404":
          description: EchocardiogramTestRequestGrade not found
components:
  schemas:
    EchocardiogramTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        echocardiogram_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: ACC/AHA/ASE & BSE Appropriate Use Criteria score (1-9).
        appropriateness_band:
          type: string
          enum:
            - appropriate
            - may-be-appropriate
            - rarely-appropriate
            - ""
          description: "Appropriateness band: appropriate (7-9), may-be-appropriate (4-6), rarely-appropriate (1-3)."
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Assigned urgency triage tier: routine, urgent, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the echocardiogram implied by the triage tier.
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100); indication and clinical question weighted highest.
        priority_band:
          type: string
          enum:
            - low
            - moderate
            - high
            - ""
          description: "Clinical priority band: low, moderate, high, driven by NYHA class, natriuretic peptide, and suspected severe pathology."
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - echocardiogram_test_request_id
        - appropriateness_band
        - triage_tier
        - target_timeframe
        - priority_band
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (ACC/AHA/ASE & BSE AUC 1-9), urgency triage, request completeness, and clinical priority."
