# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: echocardiogram_test_request
openapi: 3.1.0
info:
  title: Echocardiogram Test Request — EchocardiogramTestRequest
  version: 1.0.0
  description: Cardiac echocardiogram request (referral). Captures the requested echo type, clinical indication and question, relevant history, symptoms, NYHA class, ECG and natriuretic-peptide findings, prior echo, cardiotoxic-chemotherapy status, and triage urgency.
paths:
  /echocardiogram-test-requests:
    get:
      summary: List EchocardiogramTestRequest records
      operationId: listEchocardiogramTestRequest
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of EchocardiogramTestRequest records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EchocardiogramTestRequest"
    post:
      summary: Create a new EchocardiogramTestRequest
      operationId: createEchocardiogramTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EchocardiogramTestRequest"
      responses:
        "201":
          description: The newly created EchocardiogramTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EchocardiogramTestRequest"
        "400":
          description: Validation error
  /echocardiogram-test-requests/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one EchocardiogramTestRequest by id
      operationId: getEchocardiogramTestRequest
      responses:
        "200":
          description: The requested EchocardiogramTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EchocardiogramTestRequest"
        "404":
          description: EchocardiogramTestRequest not found
    patch:
      summary: Update an existing EchocardiogramTestRequest
      operationId: updateEchocardiogramTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EchocardiogramTestRequest"
      responses:
        "200":
          description: The updated EchocardiogramTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EchocardiogramTestRequest"
        "404":
          description: EchocardiogramTestRequest not found
    delete:
      summary: Soft-delete a EchocardiogramTestRequest
      operationId: deleteEchocardiogramTestRequest
      responses:
        "204":
          description: EchocardiogramTestRequest deleted
        "404":
          description: EchocardiogramTestRequest not found
components:
  schemas:
    EchocardiogramTestRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the requesting clinician (referrer).
        status:
          type: string
          enum:
            - draft
            - submitted
            - vetted
            - scheduled
            - rejected
            - ""
          description: "Request lifecycle status: draft, submitted, vetted, scheduled, rejected."
        site_name:
          type: string
          maxLength: 255
          description: Name of the requesting site or clinic.
        setting:
          type: string
          enum:
            - outpatient
            - inpatient
            - community
            - emergency
            - ""
          description: "Care setting: outpatient, inpatient, community, emergency."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        requested_by_date:
          type: string
          format: date
          description: Date by which the echocardiogram is requested to be performed.
        echo_type:
          type: string
          enum:
            - transthoracic-tte
            - transoesophageal-toe
            - stress-echo
            - contrast-echo
            - other
            - ""
          description: "Requested echocardiogram type: transthoracic-tte, transoesophageal-toe, stress-echo, contrast-echo, other."
        primary_indication:
          type: string
          enum:
            - heart-failure
            - murmur
            - suspected-valve-disease
            - breathlessness
            - palpitations
            - chest-pain
            - hypertension
            - cardiomyopathy
            - endocarditis
            - post-mi
            - pulmonary-hypertension
            - pre-chemotherapy
            - stroke-tia-source
            - congenital
            - surveillance-known-disease
            - other
            - ""
          description: Primary clinical indication, used for appropriateness scoring.
        clinical_question:
          type: string
          maxLength: 1000
          description: Specific clinical question the echocardiogram should answer (highest-value field).
        relevant_history:
          type: string
          maxLength: 1000
          description: Relevant cardiac, medical, and surgical history.
        breathlessness:
          type: boolean
          description: Whether the patient reports breathlessness (dyspnoea).
        chest_pain:
          type: boolean
          description: Whether the patient reports chest pain.
        palpitations:
          type: boolean
          description: Whether the patient reports palpitations.
        syncope:
          type: boolean
          description: Whether the patient reports syncope (transient loss of consciousness).
        oedema:
          type: boolean
          description: Whether the patient has peripheral oedema.
        nyha_class:
          type: string
          enum:
            - i
            - ii
            - iii
            - iv
            - ""
          description: "New York Heart Association functional class: i, ii, iii, iv."
        ecg_findings:
          type: string
          maxLength: 1000
          description: Relevant electrocardiogram (ECG) findings.
        bnp_or_nt_probnp:
          type: number
          format: double
          description: Natriuretic peptide level (BNP or NT-proBNP), in ng/L, used for heart-failure priority.
        known_murmur:
          type: boolean
          description: Whether a cardiac murmur has been auscultated.
        previous_echo:
          type: string
          enum:
            - none
            - normal
            - abnormal
            - unknown
            - ""
          description: "Result of any previous echocardiogram: none, normal, abnormal, unknown."
        previous_echo_date:
          type: string
          format: date
          description: Date of the most recent previous echocardiogram.
        ejection_fraction_known:
          type: number
          format: double
          description: Known left-ventricular ejection fraction (%) from a prior study, if available.
        on_cardiotoxic_chemotherapy:
          type: boolean
          description: Whether the patient is on, or about to start, cardiotoxic chemotherapy (e.g. anthracyclines, trastuzumab).
        relevant_medications:
          type: string
          maxLength: 1000
          description: Relevant cardiac and other medications.
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Requested triage urgency: routine, urgent, emergency."
        supervising_consultant:
          type: string
          maxLength: 255
          description: Name of the supervising / responsible consultant.
        requester_contact:
          type: string
          maxLength: 255
          description: Contact details (bleep / phone) for the requester.
        notes:
          type: string
          maxLength: 1000
          description: Free-text additional notes.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - status
        - site_name
        - setting
        - echo_type
        - primary_indication
        - clinical_question
        - relevant_history
        - breathlessness
        - chest_pain
        - palpitations
        - syncope
        - oedema
        - nyha_class
        - ecg_findings
        - known_murmur
        - previous_echo
        - on_cardiotoxic_chemotherapy
        - relevant_medications
        - urgency
        - supervising_consultant
        - requester_contact
        - notes
      description: Cardiac echocardiogram request (referral). Captures the requested echo type, clinical indication and question, relevant history, symptoms, NYHA class, ECG and natriuretic-peptide findings, prior echo, cardiotoxic-chemotherapy status, and triage urgency.
