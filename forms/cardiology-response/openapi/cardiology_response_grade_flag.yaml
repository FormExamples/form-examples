# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: cardiology_response_grade_flag
openapi: 3.1.0
info:
  title: Cardiology Response — CardiologyResponseGradeFlag
  version: 1.0.0
  description: Safety-critical flags that fire independently of the four axes, with priority and a suggested action for the responsible team.
paths:
  /cardiology-response-grade-flags:
    get:
      summary: List CardiologyResponseGradeFlag records
      operationId: listCardiologyResponseGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CardiologyResponseGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CardiologyResponseGradeFlag"
    post:
      summary: Create a new CardiologyResponseGradeFlag
      operationId: createCardiologyResponseGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardiologyResponseGradeFlag"
      responses:
        "201":
          description: The newly created CardiologyResponseGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiologyResponseGradeFlag"
        "400":
          description: Validation error
  /cardiology-response-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CardiologyResponseGradeFlag by id
      operationId: getCardiologyResponseGradeFlag
      responses:
        "200":
          description: The requested CardiologyResponseGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiologyResponseGradeFlag"
        "404":
          description: CardiologyResponseGradeFlag not found
    patch:
      summary: Update an existing CardiologyResponseGradeFlag
      operationId: updateCardiologyResponseGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardiologyResponseGradeFlag"
      responses:
        "200":
          description: The updated CardiologyResponseGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiologyResponseGradeFlag"
        "404":
          description: CardiologyResponseGradeFlag not found
    delete:
      summary: Soft-delete a CardiologyResponseGradeFlag
      operationId: deleteCardiologyResponseGradeFlag
      responses:
        "204":
          description: CardiologyResponseGradeFlag deleted
        "404":
          description: CardiologyResponseGradeFlag not found
components:
  schemas:
    CardiologyResponseGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        cardiology_response_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-CRITICAL-FINDING-001).
        category:
          type: string
          enum:
            - critical-finding
            - severe-valve-disease
            - significant-arrhythmia
            - reduced-ejection-fraction
            - incomplete-response
            - missing-diagnosis
            - other
            - ""
          description: Flag category.
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical action (e.g. \"communicate critical result to referrer and arrange urgent review\")."
      required:
        - created_at
        - updated_at
        - cardiology_response_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Safety-critical flags that fire independently of the four axes, with priority and a suggested action for the responsible team.
