# Generated from sql/. Do not edit by hand.
# SOURCE: diabetes_eye_screening_grade
openapi: 3.1.0
info:
  title: Diabetes Eye Screening — DiabetesEyeScreeningGrade
  version: 1.0.0
  description: "Computed worst-eye classification and outcome for a diabetes-eye-screening record: worst retinopathy and maculopathy grade, any-ungradable marker, recall / referral pathway, recall interval, and completeness status."
paths:
  /diabetes-eye-screening-grades:
    get:
      summary: List DiabetesEyeScreeningGrade records
      operationId: listDiabetesEyeScreeningGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of DiabetesEyeScreeningGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DiabetesEyeScreeningGrade"
    post:
      summary: Create a new DiabetesEyeScreeningGrade
      operationId: createDiabetesEyeScreeningGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiabetesEyeScreeningGrade"
      responses:
        "201":
          description: The newly created DiabetesEyeScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiabetesEyeScreeningGrade"
        "400":
          description: Validation error
  /diabetes-eye-screening-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one DiabetesEyeScreeningGrade by id
      operationId: getDiabetesEyeScreeningGrade
      responses:
        "200":
          description: The requested DiabetesEyeScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiabetesEyeScreeningGrade"
        "404":
          description: DiabetesEyeScreeningGrade not found
    patch:
      summary: Update an existing DiabetesEyeScreeningGrade
      operationId: updateDiabetesEyeScreeningGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiabetesEyeScreeningGrade"
      responses:
        "200":
          description: The updated DiabetesEyeScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiabetesEyeScreeningGrade"
        "404":
          description: DiabetesEyeScreeningGrade not found
    delete:
      summary: Soft-delete a DiabetesEyeScreeningGrade
      operationId: deleteDiabetesEyeScreeningGrade
      responses:
        "204":
          description: DiabetesEyeScreeningGrade deleted
        "404":
          description: DiabetesEyeScreeningGrade not found
components:
  schemas:
    DiabetesEyeScreeningGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        diabetes_eye_screening_id:
          type: string
          format: uuid
          description: Foreign key to the parent diabetes-eye-screening record (unique, 1:1).
        worst_retinopathy:
          type: string
          enum:
            - R0
            - R1
            - R2
            - R3A
            - R3S
            - ""
          description: "Worst retinopathy (R) grade across both eyes by severity R0 < R1 < R2 < R3S < R3A: R0, R1, R2, R3A, or R3S."
        worst_maculopathy:
          type: string
          enum:
            - M0
            - M1
            - ""
          description: "Worst maculopathy (M) grade across both eyes: M1 if either eye is M1, otherwise M0."
        any_ungradable:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether either eye is ungradable (yes/no); yes routes to slit-lamp unless referable disease routes to HES.
        outcome:
          type: string
          enum:
            - refer-hes-urgent
            - refer-hes
            - refer-slit-lamp
            - surveillance-6-month
            - routine-12-month
            - routine-24-month
            - ""
          description: "Recall / referral pathway (most urgent wins): refer-hes-urgent, refer-hes, refer-slit-lamp, surveillance-6-month, routine-12-month, or routine-24-month."
        referral:
          type: string
          enum:
            - none
            - hes-routine
            - hes-urgent
            - slit-lamp
            - ""
          description: "Referral destination derived from the outcome: none, hes-routine, hes-urgent, or slit-lamp."
        recall_interval_months:
          type: integer
          format: int32
          description: Recall interval in months (6, 12, or 24); null for referral pathways with no routine recall interval.
        status:
          type: string
          enum:
            - complete
            - incomplete
            - ""
          description: "Completeness status: complete when both eyes have an R and M grade (or are marked ungradable); otherwise incomplete."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the classification.
      required:
        - created_at
        - updated_at
        - diabetes_eye_screening_id
        - worst_retinopathy
        - worst_maculopathy
        - any_ungradable
        - outcome
        - referral
        - status
        - graded_at
      description: "Computed worst-eye classification and outcome for a diabetes-eye-screening record: worst retinopathy and maculopathy grade, any-ungradable marker, recall / referral pathway, recall interval, and completeness status."
