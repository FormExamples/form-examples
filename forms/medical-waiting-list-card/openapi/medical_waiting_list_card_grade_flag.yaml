# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: medical_waiting_list_card_grade_flag
openapi: 3.1.0
info:
  title: Medical Waiting List Card — MedicalWaitingListCardGradeFlag
  version: 1.0.0
  description: Safety / operational flags that fire independently of the Waiting Time Status, with priority and a suggested action for the booking team.
paths:
  /medical-waiting-list-card-grade-flags:
    get:
      summary: List MedicalWaitingListCardGradeFlag records
      operationId: listMedicalWaitingListCardGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of MedicalWaitingListCardGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MedicalWaitingListCardGradeFlag"
    post:
      summary: Create a new MedicalWaitingListCardGradeFlag
      operationId: createMedicalWaitingListCardGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MedicalWaitingListCardGradeFlag"
      responses:
        "201":
          description: The newly created MedicalWaitingListCardGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicalWaitingListCardGradeFlag"
        "400":
          description: Validation error
  /medical-waiting-list-card-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one MedicalWaitingListCardGradeFlag by id
      operationId: getMedicalWaitingListCardGradeFlag
      responses:
        "200":
          description: The requested MedicalWaitingListCardGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicalWaitingListCardGradeFlag"
        "404":
          description: MedicalWaitingListCardGradeFlag not found
    patch:
      summary: Update an existing MedicalWaitingListCardGradeFlag
      operationId: updateMedicalWaitingListCardGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MedicalWaitingListCardGradeFlag"
      responses:
        "200":
          description: The updated MedicalWaitingListCardGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicalWaitingListCardGradeFlag"
        "404":
          description: MedicalWaitingListCardGradeFlag not found
    delete:
      summary: Soft-delete a MedicalWaitingListCardGradeFlag
      operationId: deleteMedicalWaitingListCardGradeFlag
      responses:
        "204":
          description: MedicalWaitingListCardGradeFlag deleted
        "404":
          description: MedicalWaitingListCardGradeFlag not found
components:
  schemas:
    MedicalWaitingListCardGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        medical_waiting_list_card_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade row.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-BREACH-RISK-001).
        category:
          type: string
          enum:
            - breach-risk
            - long-waiter-52-week
            - priority-1-escalation
            - two-week-wait-cancer
            - missing-appointment
            - accessibility-unmet
            - interpreter-required
            - contact-details-missing
            - other
            - ""
          description: Flag category.
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested action (e.g. \"contact patient to confirm next appointment\")."
      required:
        - created_at
        - updated_at
        - medical_waiting_list_card_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Safety / operational flags that fire independently of the Waiting Time Status, with priority and a suggested action for the booking team.
