# Generated from sql/. Do not edit by hand.
# SOURCE: toxicology_test_result_grade_flag
openapi: 3.1.0
info:
  title: Toxicology Test Result — ToxicologyTestResultGradeFlag
  version: 1.0.0
  description: Safety-critical flags that fire independently of the four axes, with priority and a suggested action for the reporting / requesting team.
paths:
  /toxicology-test-result-grade-flags:
    get:
      summary: List ToxicologyTestResultGradeFlag records
      operationId: listToxicologyTestResultGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ToxicologyTestResultGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ToxicologyTestResultGradeFlag"
    post:
      summary: Create a new ToxicologyTestResultGradeFlag
      operationId: createToxicologyTestResultGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToxicologyTestResultGradeFlag"
      responses:
        "201":
          description: The newly created ToxicologyTestResultGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToxicologyTestResultGradeFlag"
        "400":
          description: Validation error
  /toxicology-test-result-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ToxicologyTestResultGradeFlag by id
      operationId: getToxicologyTestResultGradeFlag
      responses:
        "200":
          description: The requested ToxicologyTestResultGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToxicologyTestResultGradeFlag"
        "404":
          description: ToxicologyTestResultGradeFlag not found
    patch:
      summary: Update an existing ToxicologyTestResultGradeFlag
      operationId: updateToxicologyTestResultGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToxicologyTestResultGradeFlag"
      responses:
        "200":
          description: The updated ToxicologyTestResultGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToxicologyTestResultGradeFlag"
        "404":
          description: ToxicologyTestResultGradeFlag not found
    delete:
      summary: Soft-delete a ToxicologyTestResultGradeFlag
      operationId: deleteToxicologyTestResultGradeFlag
      responses:
        "204":
          description: ToxicologyTestResultGradeFlag deleted
        "404":
          description: ToxicologyTestResultGradeFlag not found
components:
  schemas:
    ToxicologyTestResultGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        toxicology_test_result_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-CRITICAL-RESULT-001).
        category:
          type: string
          enum:
            - critical-result-alert
            - incidental-finding
            - discrepancy-with-request
            - abnormal-requiring-action
            - urgent-referral
            - inadequate-technique
            - unexpected-finding
            - missing-impression
            - missing-measurement
            - other
            - ""
          description: Flag category.
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical action (e.g. \"communicate critical result to requester and start N-acetylcysteine\")."
      required:
        - created_at
        - updated_at
        - toxicology_test_result_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Safety-critical flags that fire independently of the four axes, with priority and a suggested action for the reporting / requesting team.
