# Generated from sql/. Do not edit by hand.
# SOURCE: bronchoscopy_test_request_grade
openapi: 3.1.0
info:
  title: Bronchoscopy Test Request — BronchoscopyTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (BTS 1-9), cancer-pathway urgency (NICE NG12 two-week-wait), request completeness, and pre-procedure bleeding / hypoxia risk."
paths:
  /bronchoscopy-test-request-grades:
    get:
      summary: List BronchoscopyTestRequestGrade records
      operationId: listBronchoscopyTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of BronchoscopyTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BronchoscopyTestRequestGrade"
    post:
      summary: Create a new BronchoscopyTestRequestGrade
      operationId: createBronchoscopyTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BronchoscopyTestRequestGrade"
      responses:
        "201":
          description: The newly created BronchoscopyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BronchoscopyTestRequestGrade"
        "400":
          description: Validation error
  /bronchoscopy-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one BronchoscopyTestRequestGrade by id
      operationId: getBronchoscopyTestRequestGrade
      responses:
        "200":
          description: The requested BronchoscopyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BronchoscopyTestRequestGrade"
        "404":
          description: BronchoscopyTestRequestGrade not found
    patch:
      summary: Update an existing BronchoscopyTestRequestGrade
      operationId: updateBronchoscopyTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BronchoscopyTestRequestGrade"
      responses:
        "200":
          description: The updated BronchoscopyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BronchoscopyTestRequestGrade"
        "404":
          description: BronchoscopyTestRequestGrade not found
    delete:
      summary: Soft-delete a BronchoscopyTestRequestGrade
      operationId: deleteBronchoscopyTestRequestGrade
      responses:
        "204":
          description: BronchoscopyTestRequestGrade deleted
        "404":
          description: BronchoscopyTestRequestGrade not found
components:
  schemas:
    BronchoscopyTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        bronchoscopy_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: Appropriateness score (1-9) from BTS bronchoscopy guidance and indication match.
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - two-week-wait
            - emergency
            - ""
          description: "Assigned cancer-pathway triage tier: routine, urgent, two-week-wait, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the procedure implied by the triage tier.
        two_week_wait_eligible:
          type: boolean
          description: Whether the request meets NICE NG12 two-week-wait suspected-cancer criteria.
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        risk_band:
          type: string
          enum:
            - low
            - moderate
            - high
            - ""
          description: "Pre-procedure risk band: low, moderate, high (anticoagulation, platelets, hypoxia, ASA)."
        anticoagulant_action:
          type: string
          maxLength: 500
          description: Recommended anticoagulant / antiplatelet management action before the procedure.
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - bronchoscopy_test_request_id
        - appropriateness_band
        - triage_tier
        - target_timeframe
        - two_week_wait_eligible
        - risk_band
        - anticoagulant_action
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (BTS 1-9), cancer-pathway urgency (NICE NG12 two-week-wait), request completeness, and pre-procedure bleeding / hypoxia risk."
