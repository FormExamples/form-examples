# Generated from sql/. Do not edit by hand.
# SOURCE: audit_c
openapi: 3.1.0
info:
  title: Alcohol Use Disorders Identification Test Consumption — AuditC
  version: 1.0.0
  description: "Main AUDIT-C assessment record: assessment context, patient identification, and the three scored consumption item inputs (Q1 frequency of drinking, Q2 typical quantity, Q3 frequency of heavy episodic drinking). Uses the short table base audit_c to stay within PostgreSQL identifier limits."
paths:
  /audit-cs:
    get:
      summary: List AuditC records
      operationId: listAuditC
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of AuditC records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AuditC"
    post:
      summary: Create a new AuditC
      operationId: createAuditC
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuditC"
      responses:
        "201":
          description: The newly created AuditC
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditC"
        "400":
          description: Validation error
  /audit-cs/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one AuditC by id
      operationId: getAuditC
      responses:
        "200":
          description: The requested AuditC
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditC"
        "404":
          description: AuditC not found
    patch:
      summary: Update an existing AuditC
      operationId: updateAuditC
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuditC"
      responses:
        "200":
          description: The updated AuditC
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditC"
        "404":
          description: AuditC not found
    delete:
      summary: Soft-delete a AuditC
      operationId: deleteAuditC
      responses:
        "204":
          description: AuditC deleted
        "404":
          description: AuditC not found
components:
  schemas:
    AuditC:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient being screened (delete restricted).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the administering clinician (optional; delete restricted).
        clinician_name:
          type: string
          maxLength: 255
          description: Name of the administering clinician as recorded on the assessment.
        clinician_role:
          type: string
          enum:
            - gp
            - nurse
            - healthcare-assistant
            - other
            - ""
          description: "Role of the administering clinician: gp, nurse, healthcare-assistant, or other."
        assessed_at:
          type: string
          format: date-time
          description: Date and time the AUDIT-C screen was administered.
        care_setting:
          type: string
          enum:
            - primary-care
            - emergency-department
            - health-check
            - inpatient
            - other
            - ""
          description: "Care setting: primary-care, emergency-department, health-check, inpatient, or other."
        administration_mode:
          type: string
          enum:
            - self-completed
            - interview
            - ""
          description: "How the screen was administered: self-completed by the patient or by interview."
        patient_identifier:
          type: string
          maxLength: 100
          description: Local patient identifier as recorded on the assessment.
        age_band:
          type: string
          enum:
            - 16-24
            - 25-39
            - 40-59
            - 60-74
            - 75-plus
            - ""
          description: "Adult age band: 16-24, 25-39, 40-59, 60-74, or 75-plus."
        sex:
          type: string
          enum:
            - female
            - male
            - intersex
            - unknown
            - ""
          description: "Patient sex recorded for the assessment: female, male, intersex, or unknown. Selects the Q3 heavy-episode threshold (>= 6 units female, >= 8 units male) and the sex-specific low-cut flag."
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_name
        - clinician_role
        - care_setting
        - administration_mode
        - patient_identifier
        - age_band
        - sex
      description: "Main AUDIT-C assessment record: assessment context, patient identification, and the three scored consumption item inputs (Q1 frequency of drinking, Q2 typical quantity, Q3 frequency of heavy episodic drinking). Uses the short table base audit_c to stay within PostgreSQL identifier limits."
