# Generated from sql/. Do not edit by hand.
# SOURCE: assessment, assessment_current_medications, assessment_medication_item
openapi: 3.1.0
info:
  title: Ophthalmology Assessment — Assessment
  version: 1.0.0
  description: Ophthalmology assessment form using Visual Acuity Grade. Parent entity for all assessment sections.
paths:
  /assessments:
    get:
      summary: List Assessment records
      operationId: listAssessment
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Assessment records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Assessment"
    post:
      summary: Create a new Assessment
      operationId: createAssessment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Assessment"
      responses:
        "201":
          description: The newly created Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
        "400":
          description: Validation error
  /assessments/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Assessment by id
      operationId: getAssessment
      responses:
        "200":
          description: The requested Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
        "404":
          description: Assessment not found
    patch:
      summary: Update an existing Assessment
      operationId: updateAssessment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Assessment"
      responses:
        "200":
          description: The updated Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
        "404":
          description: Assessment not found
    delete:
      summary: Soft-delete a Assessment
      operationId: deleteAssessment
      responses:
        "204":
          description: Assessment deleted
        "404":
          description: Assessment not found
components:
  schemas:
    Assessment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient who owns this assessment.
        status:
          type: string
          enum:
            - draft
            - submitted
            - reviewed
            - urgent
          description: "Lifecycle status: draft, submitted, reviewed, or urgent."
        current_medications_deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        has_ocular_medications:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Whether the patient uses ocular medications: yes, no, or empty."
        has_systemic_medications:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Whether the patient takes systemic medications: yes, no, or empty."
        medication_allergies:
          type: string
          description: Known medication allergies.
        medications_notes:
          type: string
          description: Additional clinician notes on medications.
        medication_item_deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        current_medications_id:
          type: string
          format: uuid
          description: Foreign key to the assessment_current_medications table.
        medication_name:
          type: string
          maxLength: 255
          description: Name of the medication.
        dose:
          type: string
          maxLength: 100
          description: Dose of the medication.
        frequency:
          type: string
          maxLength: 100
          description: Frequency of administration.
        route:
          type: string
          enum:
            - topical
            - oral
            - intravitreal
            - periocular
            - iv
            - other
            - ""
          description: "Route of administration: topical, oral, intravitreal, periocular, iv, other, or empty."
        eye:
          type: string
          enum:
            - right
            - left
            - both
            - systemic
            - ""
          description: "Target eye: right, left, both, systemic, or empty."
        indication:
          type: string
          maxLength: 255
          description: Indication for the medication.
        sort_order:
          type: integer
          format: int32
          description: Display order of the item within the list.
      required:
        - created_at
        - updated_at
        - patient_id
        - status
        - has_ocular_medications
        - has_systemic_medications
        - medication_allergies
        - medications_notes
        - current_medications_id
        - medication_name
        - dose
        - frequency
        - route
        - eye
        - indication
        - sort_order
      description: Ophthalmology assessment form using Visual Acuity Grade. Parent entity for all assessment sections.
