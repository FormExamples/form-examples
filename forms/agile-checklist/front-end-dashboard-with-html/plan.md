# Agile Checklist — Static HTML Dashboard Plan

## Goal

A static, dependency-free dashboard that lists agile-checklist
submissions in a sortable HTML table.

## Build order

1. Sample data in `js/sample.js` (5–10 representative rows).
2. `index.html` and `js/app.js` rendering the table.
3. Sortable column headers with vanilla JS.
4. Dropdown filter for maturity.
5. Style with `css/style.css`.

## Status

Pending — not yet authored.
