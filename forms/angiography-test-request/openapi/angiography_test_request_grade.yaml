# Generated from sql/. Do not edit by hand.
# SOURCE: angiography_test_request_grade
openapi: 3.1.0
info:
  title: Angiography Test Request — AngiographyTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (ACR 1-9), contrast / radiation safety, request completeness, and triage priority."
paths:
  /angiography-test-request-grades:
    get:
      summary: List AngiographyTestRequestGrade records
      operationId: listAngiographyTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of AngiographyTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AngiographyTestRequestGrade"
    post:
      summary: Create a new AngiographyTestRequestGrade
      operationId: createAngiographyTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AngiographyTestRequestGrade"
      responses:
        "201":
          description: The newly created AngiographyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AngiographyTestRequestGrade"
        "400":
          description: Validation error
  /angiography-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one AngiographyTestRequestGrade by id
      operationId: getAngiographyTestRequestGrade
      responses:
        "200":
          description: The requested AngiographyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AngiographyTestRequestGrade"
        "404":
          description: AngiographyTestRequestGrade not found
    patch:
      summary: Update an existing AngiographyTestRequestGrade
      operationId: updateAngiographyTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AngiographyTestRequestGrade"
      responses:
        "200":
          description: The updated AngiographyTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AngiographyTestRequestGrade"
        "404":
          description: AngiographyTestRequestGrade not found
    delete:
      summary: Soft-delete a AngiographyTestRequestGrade
      operationId: deleteAngiographyTestRequestGrade
      responses:
        "204":
          description: AngiographyTestRequestGrade deleted
        "404":
          description: AngiographyTestRequestGrade not found
components:
  schemas:
    AngiographyTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        angiography_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: ACR Appropriateness Criteria score (1-9).
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        safety_band:
          type: string
          enum:
            - ok
            - caution
            - contraindicated
            - ""
          description: "Contrast / radiation safety band: ok, caution, contraindicated (driven by eGFR, contrast allergy, anticoagulation, pregnancy)."
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Assigned triage tier: routine, urgent, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the examination implied by the triage tier.
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - angiography_test_request_id
        - appropriateness_band
        - safety_band
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (ACR 1-9), contrast / radiation safety, request completeness, and triage priority."
