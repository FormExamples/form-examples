# Generated from sql/. Do not edit by hand.
# SOURCE: ultrasound_test_result
openapi: 3.1.0
info:
  title: Ultrasound Test Result — UltrasoundTestResult
  version: 1.0.0
  description: General (non-obstetric) ultrasound result (report). Captures the performed examination (body region, laterality), diagnostic adequacy, clinical history, the narrative and structured findings, the largest lesion measurement, the impression, a free-text structured-reporting category (e.g. TI-RADS for thyroid, U-classification for breast), recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /ultrasound-test-results:
    get:
      summary: List UltrasoundTestResult records
      operationId: listUltrasoundTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of UltrasoundTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UltrasoundTestResult"
    post:
      summary: Create a new UltrasoundTestResult
      operationId: createUltrasoundTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UltrasoundTestResult"
      responses:
        "201":
          description: The newly created UltrasoundTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UltrasoundTestResult"
        "400":
          description: Validation error
  /ultrasound-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one UltrasoundTestResult by id
      operationId: getUltrasoundTestResult
      responses:
        "200":
          description: The requested UltrasoundTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UltrasoundTestResult"
        "404":
          description: UltrasoundTestResult not found
    patch:
      summary: Update an existing UltrasoundTestResult
      operationId: updateUltrasoundTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UltrasoundTestResult"
      responses:
        "200":
          description: The updated UltrasoundTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UltrasoundTestResult"
        "404":
          description: UltrasoundTestResult not found
    delete:
      summary: Soft-delete a UltrasoundTestResult
      operationId: deleteUltrasoundTestResult
      responses:
        "204":
          description: UltrasoundTestResult deleted
        "404":
          description: UltrasoundTestResult not found
components:
  schemas:
    UltrasoundTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (sonographer / radiologist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating ultrasound request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the ultrasound examination was performed.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        body_region:
          type: string
          enum:
            - abdomen
            - pelvis
            - renal-tract
            - liver-biliary
            - thyroid-neck
            - scrotum-testes
            - breast
            - soft-tissue
            - vascular-doppler
            - dvt-leg
            - carotid
            - msk-joint
            - other
            - ""
          description: "Examined ultrasound body region: abdomen, pelvis, renal-tract, liver-biliary, thyroid-neck, scrotum-testes, breast, soft-tissue, vascular-doppler, dvt-leg, carotid, msk-joint, other."
        laterality:
          type: string
          enum:
            - left
            - right
            - bilateral
            - not-applicable
            - ""
          description: "Laterality of the examination: left, right, bilateral, not-applicable."
        examination_adequacy:
          type: string
          enum:
            - adequate
            - limited-body-habitus
            - non-diagnostic
            - ""
          description: "Diagnostic adequacy of the examination: adequate, limited-body-habitus, non-diagnostic."
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the examination was performed to answer.
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous imaging studies.
        findings_narrative:
          type: string
          maxLength: 2000
          description: Narrative description of the imaging findings (the body of the report).
        mass_or_lesion:
          type: boolean
          description: Whether a mass or focal solid lesion is present.
        cyst:
          type: boolean
          description: Whether a cyst (e.g. simple or complex cystic lesion) is present.
        gallstones:
          type: boolean
          description: Whether gallstones / cholelithiasis are present.
        hydronephrosis:
          type: boolean
          description: Whether hydronephrosis / urinary-tract dilatation is present.
        free_fluid:
          type: boolean
          description: Whether free fluid (e.g. ascites, pelvic free fluid) is present.
        dvt_present:
          type: boolean
          description: Whether deep vein thrombosis (DVT) is present on venous Doppler — a critical finding.
        aneurysm:
          type: boolean
          description: Whether an aneurysm (e.g. abdominal aortic aneurysm) is present; a ruptured / large AAA is a critical finding.
        organ_enlargement:
          type: boolean
          description: Whether organ enlargement (e.g. hepatomegaly, splenomegaly) is present.
        incidental_finding:
          type: boolean
          description: Whether one or more incidental findings warranting documentation are present.
        largest_lesion_size_mm:
          type: number
          format: double
          description: Largest lesion long-axis size in millimetres, for surveillance and categorisation.
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 50
          description: Free-text structured-reporting score / category label where applicable (e.g. an ACR TI-RADS level for thyroid, or a breast U-classification U1-U5).
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up imaging, referral, or management.
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - body_region
        - laterality
        - examination_adequacy
        - clinical_history
        - comparison_with_previous
        - findings_narrative
        - mass_or_lesion
        - cyst
        - gallstones
        - hydronephrosis
        - free_fluid
        - dvt_present
        - aneurysm
        - organ_enlargement
        - incidental_finding
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: General (non-obstetric) ultrasound result (report). Captures the performed examination (body region, laterality), diagnostic adequacy, clinical history, the narrative and structured findings, the largest lesion measurement, the impression, a free-text structured-reporting category (e.g. TI-RADS for thyroid, U-classification for breast), recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
