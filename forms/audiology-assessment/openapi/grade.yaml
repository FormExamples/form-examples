# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: grade
openapi: 3.1.0
info:
  title: Audiology Assessment — Grade
  version: 1.0.0
  description: Computed hearing level grading result based on pure tone average (PTA). Normal (<=25 dB), Mild (26-40 dB), Moderate (41-55 dB), Moderately Severe (56-70 dB), Severe (71-90 dB), Profound (>90 dB). One-to-one child of assessment.
paths:
  /grades:
    get:
      summary: List Grade records
      operationId: listGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Grade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      summary: Create a new Grade
      operationId: createGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "201":
          description: The newly created Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "400":
          description: Validation error
  /grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Grade by id
      operationId: getGrade
      responses:
        "200":
          description: The requested Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    patch:
      summary: Update an existing Grade
      operationId: updateGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "200":
          description: The updated Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    delete:
      summary: Soft-delete a Grade
      operationId: deleteGrade
      responses:
        "204":
          description: Grade deleted
        "404":
          description: Grade not found
components:
  schemas:
    Grade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the parent assessment (unique, enforcing 1:1).
        right_pta_db:
          type: number
          format: double
          description: Right ear pure tone average in dB HL, NULL if not tested.
        left_pta_db:
          type: number
          format: double
          description: Left ear pure tone average in dB HL, NULL if not tested.
        better_ear_pta_db:
          type: number
          format: double
          description: Better ear pure tone average in dB HL, NULL if not tested.
        right_hearing_grade:
          type: string
          enum:
            - normal
            - mild
            - moderate
            - moderately_severe
            - severe
            - profound
          description: "Right ear hearing level grade: normal, mild, moderate, moderately_severe, severe, or profound."
        left_hearing_grade:
          type: string
          enum:
            - normal
            - mild
            - moderate
            - moderately_severe
            - severe
            - profound
          description: "Left ear hearing level grade: normal, mild, moderate, moderately_severe, severe, or profound."
        overall_hearing_grade:
          type: string
          enum:
            - normal
            - mild
            - moderate
            - moderately_severe
            - severe
            - profound
          description: Overall hearing level grade based on the better ear PTA.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the hearing level grading was computed.
      required:
        - created_at
        - updated_at
        - assessment_id
        - right_hearing_grade
        - left_hearing_grade
        - overall_hearing_grade
        - graded_at
      description: Computed hearing level grading result based on pure tone average (PTA). Normal (<=25 dB), Mild (26-40 dB), Moderate (41-55 dB), Moderately Severe (56-70 dB), Severe (71-90 dB), Profound (>90 dB). One-to-one child of assessment.
