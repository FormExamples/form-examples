# Generated from sql/. Do not edit by hand.
# SOURCE: pet_scan_test_result
openapi: 3.1.0
info:
  title: Pet Scan Test Result — PetScanTestResult
  version: 1.0.0
  description: PET-CT (positron emission tomography) scan result (report). Captures the tracer / scan type, the clinical history and acquisition data (blood glucose, injected activity), the narrative and structured metabolic findings, key measurements (SUVmax, largest lesion size), comparison and treatment response, the impression, the structured reporting category (e.g. Deauville score or PERCIST category), recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /pet-scan-test-results:
    get:
      summary: List PetScanTestResult records
      operationId: listPetScanTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PetScanTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PetScanTestResult"
    post:
      summary: Create a new PetScanTestResult
      operationId: createPetScanTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PetScanTestResult"
      responses:
        "201":
          description: The newly created PetScanTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PetScanTestResult"
        "400":
          description: Validation error
  /pet-scan-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PetScanTestResult by id
      operationId: getPetScanTestResult
      responses:
        "200":
          description: The requested PetScanTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PetScanTestResult"
        "404":
          description: PetScanTestResult not found
    patch:
      summary: Update an existing PetScanTestResult
      operationId: updatePetScanTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PetScanTestResult"
      responses:
        "200":
          description: The updated PetScanTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PetScanTestResult"
        "404":
          description: PetScanTestResult not found
    delete:
      summary: Soft-delete a PetScanTestResult
      operationId: deletePetScanTestResult
      responses:
        "204":
          description: PetScanTestResult deleted
        "404":
          description: PetScanTestResult not found
components:
  schemas:
    PetScanTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (nuclear-medicine physician / radiologist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating PET-CT scan request (referral).
        scan_type:
          type: string
          enum:
            - fdg-pet-ct
            - psma-pet
            - dotatate-pet
            - amyloid-pet
            - cardiac-pet
            - other
            - ""
          description: "PET tracer / scan type reported: fdg-pet-ct, psma-pet, dotatate-pet, amyloid-pet, cardiac-pet, other."
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the PET-CT examination was performed.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the examination was performed to answer.
        blood_glucose_mmol_l:
          type: number
          format: double
          description: Pre-injection blood glucose in mmol/L; FDG uptake quality depends on glucose control, typically below 11 mmol/L.
        injected_activity_mbq:
          type: number
          format: double
          description: Administered radiopharmaceutical activity in megabecquerels (MBq), for dose audit.
        examination_adequacy:
          type: string
          enum:
            - adequate
            - limited
            - non-diagnostic
            - ""
          description: "Diagnostic adequacy of the examination: adequate, limited, non-diagnostic."
        findings_narrative:
          type: string
          maxLength: 2000
          description: Narrative description of the metabolic and CT findings (the body of the report).
        hypermetabolic_lesion:
          type: boolean
          description: Whether one or more abnormally hypermetabolic (FDG / tracer-avid) lesions are present.
        nodal_uptake:
          type: boolean
          description: Whether abnormal nodal (lymph-node) tracer uptake is present.
        distant_metastasis:
          type: boolean
          description: Whether distant metastatic tracer-avid disease is present.
        no_abnormal_uptake:
          type: boolean
          description: Whether the study shows no abnormal tracer uptake (negative / complete metabolic response).
        physiological_uptake_only:
          type: boolean
          description: Whether only normal physiological tracer uptake is present (e.g. brain, myocardium, bowel, urinary tract).
        incidental_finding:
          type: boolean
          description: Whether one or more incidental findings warranting documentation are present.
        suv_max:
          type: number
          format: double
          description: Maximum standardised uptake value (SUVmax) of the most-avid reference lesion.
        largest_lesion_size_mm:
          type: number
          format: double
          description: Largest lesion long-axis size in millimetres, for surveillance and categorisation.
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous PET-CT or other imaging studies.
        treatment_response:
          type: string
          enum:
            - complete
            - partial
            - stable
            - progressive
            - not-applicable
            - ""
          description: "Metabolic treatment-response category: complete, partial, stable, progressive, not-applicable."
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 50
          description: Free-text structured-reporting label, e.g. a Deauville score (1-5) for lymphoma or a PERCIST response category.
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up imaging, referral, or management.
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - scan_type
        - report_status
        - clinical_history
        - examination_adequacy
        - findings_narrative
        - hypermetabolic_lesion
        - nodal_uptake
        - distant_metastasis
        - no_abnormal_uptake
        - physiological_uptake_only
        - incidental_finding
        - comparison_with_previous
        - treatment_response
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: PET-CT (positron emission tomography) scan result (report). Captures the tracer / scan type, the clinical history and acquisition data (blood glucose, injected activity), the narrative and structured metabolic findings, key measurements (SUVmax, largest lesion size), comparison and treatment response, the impression, the structured reporting category (e.g. Deauville score or PERCIST category), recommended follow-up, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
