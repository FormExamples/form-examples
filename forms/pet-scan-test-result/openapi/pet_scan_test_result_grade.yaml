# Generated from sql/. Do not edit by hand.
# SOURCE: pet_scan_test_result_grade
openapi: 3.1.0
info:
  title: Pet Scan Test Result — PetScanTestResultGrade
  version: 1.0.0
  description: "Computed four-axis interpretation grade: result classification, abnormality severity / structured-reporting category, report completeness, and follow-up urgency. A critical result (e.g. distant metastasis or progressive disease) auto-escalates the follow-up urgency to critical-alert regardless of the other axes."
paths:
  /pet-scan-test-result-grades:
    get:
      summary: List PetScanTestResultGrade records
      operationId: listPetScanTestResultGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PetScanTestResultGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PetScanTestResultGrade"
    post:
      summary: Create a new PetScanTestResultGrade
      operationId: createPetScanTestResultGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PetScanTestResultGrade"
      responses:
        "201":
          description: The newly created PetScanTestResultGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PetScanTestResultGrade"
        "400":
          description: Validation error
  /pet-scan-test-result-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PetScanTestResultGrade by id
      operationId: getPetScanTestResultGrade
      responses:
        "200":
          description: The requested PetScanTestResultGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PetScanTestResultGrade"
        "404":
          description: PetScanTestResultGrade not found
    patch:
      summary: Update an existing PetScanTestResultGrade
      operationId: updatePetScanTestResultGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PetScanTestResultGrade"
      responses:
        "200":
          description: The updated PetScanTestResultGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PetScanTestResultGrade"
        "404":
          description: PetScanTestResultGrade not found
    delete:
      summary: Soft-delete a PetScanTestResultGrade
      operationId: deletePetScanTestResultGrade
      responses:
        "204":
          description: PetScanTestResultGrade deleted
        "404":
          description: PetScanTestResultGrade not found
components:
  schemas:
    PetScanTestResultGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        pet_scan_test_result_id:
          type: string
          format: uuid
          description: Foreign key to the parent result (unique, 1:1).
        result_classification:
          type: string
          enum:
            - normal
            - abnormal
            - critical
            - inconclusive
            - ""
          description: "Axis A overall classification: normal, abnormal, critical, inconclusive."
        abnormality_severity:
          type: string
          enum:
            - none
            - minor
            - moderate
            - major
            - ""
          description: "Axis B abnormality severity: none, minor, moderate, major."
        reporting_category:
          type: string
          maxLength: 50
          description: Axis B structured-reporting score / category label (e.g. a Deauville score 1-5 for lymphoma or a PERCIST response category).
        report_completeness_percent:
          type: integer
          format: int32
          description: Axis C report completeness as a percentage of mandatory report sections (0-100).
        follow_up_urgency:
          type: string
          enum:
            - routine
            - recommended
            - urgent
            - critical-alert
            - ""
          description: "Axis D follow-up urgency: routine, recommended, urgent, critical-alert."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the recommended follow-up implied by the urgency.
        recommended_action:
          type: string
          maxLength: 500
          description: Axis D concise recommended action derived from the urgency.
        recommendation:
          type: string
          enum:
            - no-action
            - routine-follow-up
            - further-imaging
            - specialist-referral
            - urgent-review
            - ""
          description: "Overall recommendation: no-action, routine-follow-up, further-imaging, specialist-referral, urgent-review."
        clinician_notes:
          type: string
          description: Free-text interpretation / sign-off notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of the reporting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - pet_scan_test_result_id
        - result_classification
        - abnormality_severity
        - reporting_category
        - follow_up_urgency
        - target_timeframe
        - recommended_action
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis interpretation grade: result classification, abnormality severity / structured-reporting category, report completeness, and follow-up urgency. A critical result (e.g. distant metastasis or progressive disease) auto-escalates the follow-up urgency to critical-alert regardless of the other axes."
