# Generated from sql/. Do not edit by hand.
# SOURCE: coagulation_test_result
openapi: 3.1.0
info:
  title: Coagulation Test Result — CoagulationTestResult
  version: 1.0.0
  description: Coagulation / haemostasis test result (report). Captures the reported result values (PT/INR, APTT/ratio, fibrinogen, D-dimer, thrombin time, factor assays), the specimen condition, clinical history and anticoagulant status, the narrative and impression, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
paths:
  /coagulation-test-results:
    get:
      summary: List CoagulationTestResult records
      operationId: listCoagulationTestResult
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CoagulationTestResult records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CoagulationTestResult"
    post:
      summary: Create a new CoagulationTestResult
      operationId: createCoagulationTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CoagulationTestResult"
      responses:
        "201":
          description: The newly created CoagulationTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CoagulationTestResult"
        "400":
          description: Validation error
  /coagulation-test-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CoagulationTestResult by id
      operationId: getCoagulationTestResult
      responses:
        "200":
          description: The requested CoagulationTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CoagulationTestResult"
        "404":
          description: CoagulationTestResult not found
    patch:
      summary: Update an existing CoagulationTestResult
      operationId: updateCoagulationTestResult
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CoagulationTestResult"
      responses:
        "200":
          description: The updated CoagulationTestResult
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CoagulationTestResult"
        "404":
          description: CoagulationTestResult not found
    delete:
      summary: Soft-delete a CoagulationTestResult
      operationId: deleteCoagulationTestResult
      responses:
        "204":
          description: CoagulationTestResult deleted
        "404":
          description: CoagulationTestResult not found
components:
  schemas:
    CoagulationTestResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reporting clinician (consultant haematologist / biomedical scientist).
        originating_request_reference:
          type: string
          maxLength: 255
          description: Free-text reference linking this report back to the originating coagulation test request (referral).
        report_status:
          type: string
          enum:
            - preliminary
            - final
            - amended
            - cancelled
            - ""
          description: "Report lifecycle status: preliminary, final, amended, cancelled."
        performed_date:
          type: string
          format: date
          description: Date the coagulation analysis was performed.
        reported_date:
          type: string
          format: date
          description: Date the report was authored / signed.
        specimen_condition:
          type: string
          enum:
            - satisfactory
            - clotted
            - underfilled
            - haemolysed
            - insufficient
            - ""
          description: "Pre-analytical specimen condition: satisfactory, clotted, underfilled, haemolysed, insufficient."
        clinical_history:
          type: string
          maxLength: 1000
          description: Clinical history and the question the test was performed to answer.
        on_anticoagulant:
          type: boolean
          description: Whether the patient is on anticoagulant therapy at the time of sampling (affects interpretation of PT/INR/APTT).
        anticoagulant_agent:
          type: string
          maxLength: 255
          description: Anticoagulant agent, if any (e.g. warfarin, LMWH, apixaban, rivaroxaban, dabigatran).
        prothrombin_time_seconds:
          type: number
          format: double
          description: Prothrombin time (PT) in seconds.
        inr:
          type: number
          format: double
          description: International normalised ratio (INR) derived from the prothrombin time.
        activated_partial_thromboplastin_time_seconds:
          type: number
          format: double
          description: Activated partial thromboplastin time (APTT) in seconds.
        aptt_ratio:
          type: number
          format: double
          description: APTT ratio (patient APTT divided by the control / mean normal APTT).
        fibrinogen_g_l:
          type: number
          format: double
          description: Fibrinogen (Clauss) concentration in grams per litre (g/L).
        d_dimer:
          type: number
          format: double
          description: D-dimer concentration (units as reported by the assay, e.g. ng/mL FEU or mg/L).
        thrombin_time_seconds:
          type: number
          format: double
          description: Thrombin time (TT) in seconds.
        factor_assays:
          type: string
          maxLength: 2000
          description: Free-text factor-assay and special-test results (e.g. factor VIII/IX levels, von Willebrand panel, lupus anticoagulant, anti-Xa).
        overall_result_status:
          type: string
          enum:
            - normal
            - abnormal
            - critical
            - ""
          description: "Overall result status: normal, abnormal, critical."
        critical_value_present:
          type: boolean
          description: Whether a critical (panic) value is present (e.g. INR > 8, fibrinogen < 1.0 g/L, or a DIC picture) requiring immediate communication.
        critical_value_detail:
          type: string
          maxLength: 1000
          description: Description of the critical value(s) present and the threshold(s) breached.
        findings_narrative:
          type: string
          maxLength: 2000
          description: Narrative description of the coagulation findings (the body of the report).
        comparison_with_previous:
          type: string
          maxLength: 1000
          description: Comparison with relevant previous coagulation results (trend / delta check).
        impression:
          type: string
          maxLength: 2000
          description: Summary impression / conclusion answering the clinical question.
        reporting_category:
          type: string
          maxLength: 50
          description: Structured-reporting category label (e.g. anticoagulant-effect, DIC-picture, isolated-APTT-prolongation, normal-haemostasis).
        recommended_follow_up:
          type: string
          maxLength: 1000
          description: Recommended follow-up testing, referral, or management.
        critical_result_communicated:
          type: boolean
          description: Whether a critical or unexpected significant result was communicated directly to the referrer.
        reported_to:
          type: string
          maxLength: 500
          description: Who the critical / urgent result was communicated to, with date and time if applicable.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - originating_request_reference
        - report_status
        - specimen_condition
        - clinical_history
        - on_anticoagulant
        - anticoagulant_agent
        - factor_assays
        - overall_result_status
        - critical_value_present
        - critical_value_detail
        - findings_narrative
        - comparison_with_previous
        - impression
        - reporting_category
        - recommended_follow_up
        - critical_result_communicated
        - reported_to
      description: Coagulation / haemostasis test result (report). Captures the reported result values (PT/INR, APTT/ratio, fibrinogen, D-dimer, thrombin time, factor assays), the specimen condition, clinical history and anticoagulant status, the narrative and impression, and critical-result communication. This is the source-of-truth record that the four-axis interpretation grade is computed from.
