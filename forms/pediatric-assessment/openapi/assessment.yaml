# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: assessment, assessment_current_medications, assessment_medication_item
openapi: 3.1.0
info:
  title: Pediatric Assessment — Assessment
  version: 1.0.0
  description: Pediatric assessment form. Parent entity for all assessment sections covering child development screening.
paths:
  /assessments:
    get:
      summary: List Assessment records
      operationId: listAssessment
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Assessment records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Assessment"
    post:
      summary: Create a new Assessment
      operationId: createAssessment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Assessment"
      responses:
        "201":
          description: The newly created Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
        "400":
          description: Validation error
  /assessments/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Assessment by id
      operationId: getAssessment
      responses:
        "200":
          description: The requested Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
        "404":
          description: Assessment not found
    patch:
      summary: Update an existing Assessment
      operationId: updateAssessment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Assessment"
      responses:
        "200":
          description: The updated Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
        "404":
          description: Assessment not found
    delete:
      summary: Soft-delete a Assessment
      operationId: deleteAssessment
      responses:
        "204":
          description: Assessment deleted
        "404":
          description: Assessment not found
components:
  schemas:
    Assessment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient (child) who owns this assessment.
        status:
          type: string
          enum:
            - draft
            - submitted
            - reviewed
            - urgent
          description: "Lifecycle status: draft, submitted, reviewed, or urgent."
        current_medications_deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        takes_regular_medications:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the child takes any regular prescribed medications.
        takes_over_the_counter:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the child takes any over-the-counter medications.
        over_the_counter_details:
          type: string
          description: Over the counter details.
        takes_supplements:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the child takes any vitamin or nutritional supplements.
        supplement_details:
          type: string
          description: Supplement details.
        medication_adherence:
          type: string
          enum:
            - good
            - fair
            - poor
            - ""
          description: "Reported medication adherence: good, fair, poor, or empty."
        adverse_drug_reactions:
          type: string
          description: Free-text description of any adverse drug reactions experienced.
        additional_notes:
          type: string
          description: Additional notes.
        medication_item_deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        current_medications_id:
          type: string
          format: uuid
          description: Foreign key to the assessment_current_medications table.
        medication_name:
          type: string
          maxLength: 255
          description: Name of the medication.
        dose:
          type: string
          maxLength: 100
          description: Dosage amount and unit (e.g. 5mg, 2.5ml).
        frequency:
          type: string
          maxLength: 100
          description: Dosing frequency (e.g. twice daily, as needed).
        route:
          type: string
          enum:
            - oral
            - topical
            - inhaled
            - injection
            - rectal
            - other
            - ""
          description: "Route of administration: oral, topical, inhaled, injection, rectal, other, or empty."
        indication:
          type: string
          description: Clinical indication for the medication.
        prescribing_clinician:
          type: string
          maxLength: 255
          description: Prescribing clinician.
        sort_order:
          type: integer
          format: int32
          description: Display order of the medication in the list.
      required:
        - created_at
        - updated_at
        - patient_id
        - status
        - takes_regular_medications
        - takes_over_the_counter
        - over_the_counter_details
        - takes_supplements
        - supplement_details
        - medication_adherence
        - adverse_drug_reactions
        - additional_notes
        - current_medications_id
        - medication_name
        - dose
        - frequency
        - route
        - indication
        - prescribing_clinician
        - sort_order
      description: Pediatric assessment form. Parent entity for all assessment sections covering child development screening.
