# Generated from sql/. Do not edit by hand.
# SOURCE: hospital_performance_indicator_value
openapi: 3.1.0
info:
  title: Hospital Performance Indicators — HospitalPerformanceIndicatorValue
  version: 1.0.0
  description: One recorded Balanced Scorecard indicator (of 50) within a hospital performance reporting period.
paths:
  /hospital-performance-indicator-values:
    get:
      summary: List HospitalPerformanceIndicatorValue records
      operationId: listHospitalPerformanceIndicatorValue
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of HospitalPerformanceIndicatorValue records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HospitalPerformanceIndicatorValue"
    post:
      summary: Create a new HospitalPerformanceIndicatorValue
      operationId: createHospitalPerformanceIndicatorValue
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HospitalPerformanceIndicatorValue"
      responses:
        "201":
          description: The newly created HospitalPerformanceIndicatorValue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HospitalPerformanceIndicatorValue"
        "400":
          description: Validation error
  /hospital-performance-indicator-values/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one HospitalPerformanceIndicatorValue by id
      operationId: getHospitalPerformanceIndicatorValue
      responses:
        "200":
          description: The requested HospitalPerformanceIndicatorValue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HospitalPerformanceIndicatorValue"
        "404":
          description: HospitalPerformanceIndicatorValue not found
    patch:
      summary: Update an existing HospitalPerformanceIndicatorValue
      operationId: updateHospitalPerformanceIndicatorValue
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HospitalPerformanceIndicatorValue"
      responses:
        "200":
          description: The updated HospitalPerformanceIndicatorValue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HospitalPerformanceIndicatorValue"
        "404":
          description: HospitalPerformanceIndicatorValue not found
    delete:
      summary: Soft-delete a HospitalPerformanceIndicatorValue
      operationId: deleteHospitalPerformanceIndicatorValue
      responses:
        "204":
          description: HospitalPerformanceIndicatorValue deleted
        "404":
          description: HospitalPerformanceIndicatorValue not found
components:
  schemas:
    HospitalPerformanceIndicatorValue:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        hospital_performance_indicators_id:
          type: string
          format: uuid
          description: Foreign key to the parent reporting period.
        indicator_code:
          type: string
          maxLength: 10
          description: Stable dotted indicator identifier from spec/index.md, e.g. 1.1, 2.15, 4.5.
        category_number:
          type: integer
          format: int32
          description: Balanced Scorecard perspective number (1=Finance, 2=Process, 3=Learning and Growth, 4=Customer).
        category_title:
          type: string
          maxLength: 255
          description: Balanced Scorecard perspective title, mirrored from the catalogue for reporting convenience.
        indicator_text:
          type: string
          description: Full indicator name, mirrored from the catalogue for reporting convenience.
        indicator_value:
          type: number
          format: double
          description: Recorded numeric value for this indicator; unit is implied by the indicator name. NULL if not yet recorded.
        notes:
          type: string
          description: Optional free-text note about this indicator value.
      required:
        - created_at
        - updated_at
        - hospital_performance_indicators_id
        - indicator_code
        - category_number
        - category_title
        - indicator_text
        - notes
      description: One recorded Balanced Scorecard indicator (of 50) within a hospital performance reporting period.
