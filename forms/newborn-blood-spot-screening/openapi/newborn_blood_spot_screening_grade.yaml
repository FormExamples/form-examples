# Generated from sql/. Do not edit by hand.
# SOURCE: newborn_blood_spot_screening_grade
openapi: 3.1.0
info:
  title: Newborn Blood Spot Screening — NewbornBloodSpotScreeningGrade
  version: 1.0.0
  description: "Computed classification for a newborn-blood-spot-screening record: overall screening outcome, referral status, and derived sample-quality flags."
paths:
  /newborn-blood-spot-screening-grades:
    get:
      summary: List NewbornBloodSpotScreeningGrade records
      operationId: listNewbornBloodSpotScreeningGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of NewbornBloodSpotScreeningGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NewbornBloodSpotScreeningGrade"
    post:
      summary: Create a new NewbornBloodSpotScreeningGrade
      operationId: createNewbornBloodSpotScreeningGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewbornBloodSpotScreeningGrade"
      responses:
        "201":
          description: The newly created NewbornBloodSpotScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NewbornBloodSpotScreeningGrade"
        "400":
          description: Validation error
  /newborn-blood-spot-screening-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one NewbornBloodSpotScreeningGrade by id
      operationId: getNewbornBloodSpotScreeningGrade
      responses:
        "200":
          description: The requested NewbornBloodSpotScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NewbornBloodSpotScreeningGrade"
        "404":
          description: NewbornBloodSpotScreeningGrade not found
    patch:
      summary: Update an existing NewbornBloodSpotScreeningGrade
      operationId: updateNewbornBloodSpotScreeningGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewbornBloodSpotScreeningGrade"
      responses:
        "200":
          description: The updated NewbornBloodSpotScreeningGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NewbornBloodSpotScreeningGrade"
        "404":
          description: NewbornBloodSpotScreeningGrade not found
    delete:
      summary: Soft-delete a NewbornBloodSpotScreeningGrade
      operationId: deleteNewbornBloodSpotScreeningGrade
      responses:
        "204":
          description: NewbornBloodSpotScreeningGrade deleted
        "404":
          description: NewbornBloodSpotScreeningGrade not found
components:
  schemas:
    NewbornBloodSpotScreeningGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        newborn_blood_spot_screening_id:
          type: string
          format: uuid
          description: Foreign key to the parent newborn-blood-spot-screening record (unique, 1:1).
        overall_outcome:
          type: string
          enum:
            - referral-required
            - repeat-required
            - incomplete
            - declined-only-outstanding
            - all-not-suspected
            - ""
          description: "Overall screening outcome by precedence: referral-required (any suspected), repeat-required, incomplete, declined-only-outstanding, or all-not-suspected."
        referral_status:
          type: string
          enum:
            - routine
            - repeat
            - urgent
            - ""
          description: "Referral status: urgent when referral-required, repeat when repeat-required, otherwise routine."
        sample_adequate:
          type: boolean
          description: "Derived: true when sample_adequacy is adequate."
        within_window:
          type: boolean
          description: "Derived: true when age_at_sample_days is between day 5 and day 8 inclusive."
        avoidable_repeat:
          type: boolean
          description: "Derived: true when this is a repeat whose reason is inadequate-sample, too-early, or technical."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the classification.
      required:
        - created_at
        - updated_at
        - newborn_blood_spot_screening_id
        - overall_outcome
        - referral_status
        - graded_at
      description: "Computed classification for a newborn-blood-spot-screening record: overall screening outcome, referral status, and derived sample-quality flags."
