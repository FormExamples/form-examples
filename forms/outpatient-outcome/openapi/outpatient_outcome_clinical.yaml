# Generated from sql/. Do not edit by hand.
# SOURCE: outpatient_outcome_clinical
openapi: 3.1.0
info:
  title: Outpatient Outcome — OutpatientOutcomeClinical
  version: 1.0.0
  description: "Clinician-rated clinical outcome for this encounter: presenting complaint, diagnosis, treatment, and five-level outcome classification."
paths:
  /outpatient-outcome-clinicals:
    get:
      summary: List OutpatientOutcomeClinical records
      operationId: listOutpatientOutcomeClinical
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of OutpatientOutcomeClinical records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OutpatientOutcomeClinical"
    post:
      summary: Create a new OutpatientOutcomeClinical
      operationId: createOutpatientOutcomeClinical
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OutpatientOutcomeClinical"
      responses:
        "201":
          description: The newly created OutpatientOutcomeClinical
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutpatientOutcomeClinical"
        "400":
          description: Validation error
  /outpatient-outcome-clinicals/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one OutpatientOutcomeClinical by id
      operationId: getOutpatientOutcomeClinical
      responses:
        "200":
          description: The requested OutpatientOutcomeClinical
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutpatientOutcomeClinical"
        "404":
          description: OutpatientOutcomeClinical not found
    patch:
      summary: Update an existing OutpatientOutcomeClinical
      operationId: updateOutpatientOutcomeClinical
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OutpatientOutcomeClinical"
      responses:
        "200":
          description: The updated OutpatientOutcomeClinical
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutpatientOutcomeClinical"
        "404":
          description: OutpatientOutcomeClinical not found
    delete:
      summary: Soft-delete a OutpatientOutcomeClinical
      operationId: deleteOutpatientOutcomeClinical
      responses:
        "204":
          description: OutpatientOutcomeClinical deleted
        "404":
          description: OutpatientOutcomeClinical not found
components:
  schemas:
    OutpatientOutcomeClinical:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted i.e. soft-removed.
        outpatient_outcome_id:
          type: string
          format: uuid
          description: Foreign key to the parent assessment (unique, enforcing 1:1).
        presenting_complaint:
          type: string
          description: Presenting complaint at the outpatient encounter.
        diagnosis:
          type: string
          description: Diagnosis confirmed or updated at this encounter.
        treatment_delivered:
          type: string
          description: Treatment, procedure, or intervention delivered at this encounter.
        outcome_classification:
          type: string
          enum:
            - resolved
            - improved
            - unchanged
            - worsened
            - died
            - ""
          description: "Clinician-rated outcome: resolved, improved, unchanged, worsened, died, or empty."
      required:
        - created_at
        - updated_at
        - outpatient_outcome_id
        - presenting_complaint
        - diagnosis
        - treatment_delivered
        - outcome_classification
      description: "Clinician-rated clinical outcome for this encounter: presenting complaint, diagnosis, treatment, and five-level outcome classification."
