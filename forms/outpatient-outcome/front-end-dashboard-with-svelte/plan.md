# Plan: Outpatient Outcome Report — SvelteKit dashboard

## Current status

Scaffold only. Placeholder `+page.svelte`. SVAR DataGrid implementation pending.

## Future work

- Build the SVAR DataGrid view (Willow theme, sortable columns, dropdown filters)
- Wire to the backend API client with sample-data fallback
- Add filtering by overall grade, domain, and date range
