# Generated from sql/. Do not edit by hand.
# SOURCE: grade
openapi: 3.1.0
info:
  title: Hormone Replacement Therapy Assessment — Grade
  version: 1.0.0
  description: Computed Menopause Rating Scale result with HRT eligibility determination. One-to-one child of assessment.
paths:
  /grades:
    get:
      summary: List Grade records
      operationId: listGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Grade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      summary: Create a new Grade
      operationId: createGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "201":
          description: The newly created Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "400":
          description: Validation error
  /grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Grade by id
      operationId: getGrade
      responses:
        "200":
          description: The requested Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    patch:
      summary: Update an existing Grade
      operationId: updateGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "200":
          description: The updated Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    delete:
      summary: Soft-delete a Grade
      operationId: deleteGrade
      responses:
        "204":
          description: Grade deleted
        "404":
          description: Grade not found
components:
  schemas:
    Grade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the assessment table.
        mrs_total_score:
          type: integer
          format: int32
          description: MRS total score across all 11 items (0-44).
        somatic_subscale_score:
          type: integer
          format: int32
          description: Somatic subscale score (hot flushes, heart, sleep, joint/muscle), range 0-16.
        psychological_subscale_score:
          type: integer
          format: int32
          description: Psychological subscale score (depressive mood, irritability, anxiety, exhaustion), range 0-16.
        urogenital_subscale_score:
          type: integer
          format: int32
          description: Urogenital subscale score (sexual, bladder, vaginal dryness), range 0-12.
        overall_severity:
          type: string
          enum:
            - none
            - mild
            - moderate
            - severe
          description: "Overall symptom severity: none, mild, moderate, or severe."
        hrt_eligibility:
          type: string
          enum:
            - eligible
            - eligible-with-caution
            - contraindicated
            - ""
          description: "HRT eligibility: eligible, eligible-with-caution, contraindicated, or empty string."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the grading was computed.
      required:
        - created_at
        - updated_at
        - assessment_id
        - mrs_total_score
        - somatic_subscale_score
        - psychological_subscale_score
        - urogenital_subscale_score
        - overall_severity
        - hrt_eligibility
        - graded_at
      description: Computed Menopause Rating Scale result with HRT eligibility determination. One-to-one child of assessment.
