# Dietetic Assessment — SQL migrations

PostgreSQL migrations for the Dietetic Assessment form — the source of truth for the data shape. Numbered `NN_*.sql` files plus the combined `schema.sql`.

See the form root [`../index.md`](../index.md) for the full design description, and [`../spec/index.md`](../spec/index.md) §6 for the table-by-table map.
