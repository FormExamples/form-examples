# Generated from sql/. Do not edit by hand.
# SOURCE: sequential_organ_failure_assessment_grade
openapi: 3.1.0
info:
  title: Sequential Organ Failure Assessment — SequentialOrganFailureAssessmentGrade
  version: 1.0.0
  description: "Computed SOFA grading result: per-system 0-4 sub-scores, summed total (0-24), delta-SOFA versus baseline, derived mortality band, and the Sepsis-3 flag."
paths:
  /sequential-organ-failure-assessment-grades:
    get:
      summary: List SequentialOrganFailureAssessmentGrade records
      operationId: listSequentialOrganFailureAssessmentGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of SequentialOrganFailureAssessmentGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SequentialOrganFailureAssessmentGrade"
    post:
      summary: Create a new SequentialOrganFailureAssessmentGrade
      operationId: createSequentialOrganFailureAssessmentGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SequentialOrganFailureAssessmentGrade"
      responses:
        "201":
          description: The newly created SequentialOrganFailureAssessmentGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SequentialOrganFailureAssessmentGrade"
        "400":
          description: Validation error
  /sequential-organ-failure-assessment-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one SequentialOrganFailureAssessmentGrade by id
      operationId: getSequentialOrganFailureAssessmentGrade
      responses:
        "200":
          description: The requested SequentialOrganFailureAssessmentGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SequentialOrganFailureAssessmentGrade"
        "404":
          description: SequentialOrganFailureAssessmentGrade not found
    patch:
      summary: Update an existing SequentialOrganFailureAssessmentGrade
      operationId: updateSequentialOrganFailureAssessmentGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SequentialOrganFailureAssessmentGrade"
      responses:
        "200":
          description: The updated SequentialOrganFailureAssessmentGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SequentialOrganFailureAssessmentGrade"
        "404":
          description: SequentialOrganFailureAssessmentGrade not found
    delete:
      summary: Soft-delete a SequentialOrganFailureAssessmentGrade
      operationId: deleteSequentialOrganFailureAssessmentGrade
      responses:
        "204":
          description: SequentialOrganFailureAssessmentGrade deleted
        "404":
          description: SequentialOrganFailureAssessmentGrade not found
components:
  schemas:
    SequentialOrganFailureAssessmentGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        sequential_organ_failure_assessment_id:
          type: string
          format: uuid
          description: Foreign key to the parent assessment (unique, 1:1).
        respiration_score:
          type: integer
          format: int32
          description: Respiration organ-system sub-score (0-4); null when the input is incomplete.
        coagulation_score:
          type: integer
          format: int32
          description: Coagulation organ-system sub-score (0-4); null when the input is incomplete.
        liver_score:
          type: integer
          format: int32
          description: Liver organ-system sub-score (0-4); null when the input is incomplete.
        cardiovascular_score:
          type: integer
          format: int32
          description: Cardiovascular organ-system sub-score (0-4), max of MAP and vasopressor bands; null when the input is incomplete.
        cns_score:
          type: integer
          format: int32
          description: Central nervous system organ-system sub-score (0-4); null when the input is incomplete.
        renal_score:
          type: integer
          format: int32
          description: Renal organ-system sub-score (0-4), max of creatinine and urine-output bands; null when the input is incomplete.
        total_score:
          type: integer
          format: int32
          description: Summed total SOFA score across the six systems (0-24 when complete).
        delta_sofa:
          type: integer
          format: int32
          description: Change in total SOFA from baseline (total_score minus baseline_sofa_total); null when no baseline is present.
        mortality_band:
          type: string
          enum:
            - low
            - moderate
            - high
            - veryHigh
            - extreme
            - ""
          description: "Derived mortality-risk band: low (0-6), moderate (7-9), high (10-12), veryHigh (13-14), or extreme (15-24)."
        sepsis3:
          type: boolean
          description: "Sepsis-3 flag: true when infection is suspected and delta-SOFA is >= 2."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the grade.
      required:
        - created_at
        - updated_at
        - sequential_organ_failure_assessment_id
        - mortality_band
        - sepsis3
        - graded_at
      description: "Computed SOFA grading result: per-system 0-4 sub-scores, summed total (0-24), delta-SOFA versus baseline, derived mortality band, and the Sepsis-3 flag."
