# Generated from sql/. Do not edit by hand.
# SOURCE: apgar_score_grade_flag
openapi: 3.1.0
info:
  title: Apgar Score — ApgarScoreGradeFlag
  version: 1.0.0
  description: Red-flag issues that fire independently of the totals, with priority and a suggested action for the clinical team.
paths:
  /apgar-score-grade-flags:
    get:
      summary: List ApgarScoreGradeFlag records
      operationId: listApgarScoreGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ApgarScoreGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ApgarScoreGradeFlag"
    post:
      summary: Create a new ApgarScoreGradeFlag
      operationId: createApgarScoreGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApgarScoreGradeFlag"
      responses:
        "201":
          description: The newly created ApgarScoreGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApgarScoreGradeFlag"
        "400":
          description: Validation error
  /apgar-score-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ApgarScoreGradeFlag by id
      operationId: getApgarScoreGradeFlag
      responses:
        "200":
          description: The requested ApgarScoreGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApgarScoreGradeFlag"
        "404":
          description: ApgarScoreGradeFlag not found
    patch:
      summary: Update an existing ApgarScoreGradeFlag
      operationId: updateApgarScoreGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApgarScoreGradeFlag"
      responses:
        "200":
          description: The updated ApgarScoreGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApgarScoreGradeFlag"
        "404":
          description: ApgarScoreGradeFlag not found
    delete:
      summary: Soft-delete a ApgarScoreGradeFlag
      operationId: deleteApgarScoreGradeFlag
      responses:
        "204":
          description: ApgarScoreGradeFlag deleted
        "404":
          description: ApgarScoreGradeFlag not found
components:
  schemas:
    ApgarScoreGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        apgar_score_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-RESUSCITATION-NEEDED-001).
        category:
          type: string
          enum:
            - resuscitation-needed
            - continue-scoring
            - falling-trend
            - support-and-stimulation
            - missing-ten-minute-score
            - incomplete-assessment
            - other
            - ""
          description: "Flag category (see CHECK constraint for enumeration): resuscitation-needed, continue-scoring, falling-trend, support-and-stimulation, missing-ten-minute-score, incomplete-assessment, or other."
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical action (e.g. \"commence active resuscitation and obtain neonatal support\")."
      required:
        - created_at
        - updated_at
        - apgar_score_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Red-flag issues that fire independently of the totals, with priority and a suggested action for the clinical team.
