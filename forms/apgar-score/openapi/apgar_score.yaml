# Generated from sql/. Do not edit by hand.
# SOURCE: apgar_score
openapi: 3.1.0
info:
  title: Apgar Score — ApgarScore
  version: 1.0.0
  description: "Parent Apgar-score assessment record: birth context, newborn identification, and resuscitation notes; the repeated five-sign scores are held in the apgar_score_timepoint child table."
paths:
  /apgar-scores:
    get:
      summary: List ApgarScore records
      operationId: listApgarScore
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ApgarScore records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ApgarScore"
    post:
      summary: Create a new ApgarScore
      operationId: createApgarScore
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApgarScore"
      responses:
        "201":
          description: The newly created ApgarScore
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApgarScore"
        "400":
          description: Validation error
  /apgar-scores/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ApgarScore by id
      operationId: getApgarScore
      responses:
        "200":
          description: The requested ApgarScore
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApgarScore"
        "404":
          description: ApgarScore not found
    patch:
      summary: Update an existing ApgarScore
      operationId: updateApgarScore
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApgarScore"
      responses:
        "200":
          description: The updated ApgarScore
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApgarScore"
        "404":
          description: ApgarScore not found
    delete:
      summary: Soft-delete a ApgarScore
      operationId: deleteApgarScore
      responses:
        "204":
          description: ApgarScore deleted
        "404":
          description: ApgarScore not found
components:
  schemas:
    ApgarScore:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the newborn patient being assessed (delete restricted).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the attending clinician (optional; delete restricted).
        clinician_name:
          type: string
          maxLength: 255
          description: Name of the attending clinician as recorded on the assessment.
        clinician_role:
          type: string
          enum:
            - midwife
            - obstetrician
            - neonatologist
            - neonatal-nurse
            - paediatrician
            - other
            - ""
          description: "Role of the attending clinician: midwife, obstetrician, neonatologist, neonatal-nurse, paediatrician, or other."
        born_at:
          type: string
          format: date-time
          description: Date and time of birth.
        care_setting:
          type: string
          enum:
            - delivery-room
            - theatre
            - birth-centre
            - home
            - neonatal-unit
            - other
            - ""
          description: "Care setting where the birth took place: delivery-room, theatre, birth-centre, home, neonatal-unit, or other."
        gestational_age_weeks:
          type: number
          format: double
          description: Gestational age at birth, in completed weeks.
        mode_of_delivery:
          type: string
          enum:
            - vaginal
            - assisted
            - caesarean
            - other
            - ""
          description: "Mode of delivery: vaginal, assisted, caesarean, or other."
        newborn_identifier:
          type: string
          maxLength: 100
          description: Local identifier for the newborn as recorded on the assessment.
        sex:
          type: string
          enum:
            - female
            - male
            - intersex
            - unknown
            - ""
          description: "Newborn sex: female, male, intersex, or unknown."
        birth_order:
          type: integer
          format: int32
          description: Birth order for multiple births (1 for a singleton).
        resuscitation_measures:
          type: string
          description: Free-text description of resuscitation measures given (drying, stimulation, oxygen, IPPV, chest compressions, etc.).
        clinician_note:
          type: string
          description: Free-text clinical note recorded with the assessment.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_name
        - clinician_role
        - care_setting
        - mode_of_delivery
        - newborn_identifier
        - sex
        - resuscitation_measures
        - clinician_note
      description: "Parent Apgar-score assessment record: birth context, newborn identification, and resuscitation notes; the repeated five-sign scores are held in the apgar_score_timepoint child table."
