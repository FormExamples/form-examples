# Generated from sql/. Do not edit by hand.
# SOURCE: news2_result
openapi: 3.1.0
info:
  title: Casualty Card Form — News2Result
  version: 1.0.0
  description: 1:1 with casualty_card. Computed NEWS2 score with clinical response threshold and parameter breakdown.
paths:
  /news2-results:
    get:
      summary: List News2Result records
      operationId: listNews2Result
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of News2Result records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/News2Result"
    post:
      summary: Create a new News2Result
      operationId: createNews2Result
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/News2Result"
      responses:
        "201":
          description: The newly created News2Result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/News2Result"
        "400":
          description: Validation error
  /news2-results/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one News2Result by id
      operationId: getNews2Result
      responses:
        "200":
          description: The requested News2Result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/News2Result"
        "404":
          description: News2Result not found
    patch:
      summary: Update an existing News2Result
      operationId: updateNews2Result
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/News2Result"
      responses:
        "200":
          description: The updated News2Result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/News2Result"
        "404":
          description: News2Result not found
    delete:
      summary: Soft-delete a News2Result
      operationId: deleteNews2Result
      responses:
        "204":
          description: News2Result deleted
        "404":
          description: News2Result not found
components:
  schemas:
    News2Result:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        casualty_card_id:
          type: string
          format: uuid
          description: FK to casualty_card (UNIQUE = 1:1 relationship).
        total_score:
          type: integer
          format: int32
          description: NEWS2 aggregate score (0-20). Sum of 7 parameter scores.
        clinical_response:
          type: string
          enum:
            - low
            - low-medium
            - medium
            - high
            - ""
          description: "Clinical response threshold: low (0-4), low-medium (single param 3), medium (5-6), high (>=7), or empty string if unanswered."
        has_any_single_score_3:
          type: boolean
          description: TRUE if any single NEWS2 parameter scored 3. Triggers urgent ward review regardless of total.
        supplemental_oxygen:
          type: string
          description: Supplemental oxygen.
        scored_at:
          type: string
          format: date-time
          description: Timestamp when the NEWS2 scoring engine produced this result.
      required:
        - created_at
        - updated_at
        - casualty_card_id
        - total_score
        - clinical_response
        - has_any_single_score_3
        - supplemental_oxygen
        - scored_at
      description: 1:1 with casualty_card. Computed NEWS2 score with clinical response threshold and parameter breakdown.
