# Generated from sql/. Do not edit by hand.
# SOURCE: grade
openapi: 3.1.0
info:
  title: Gerontology Assessment — Grade
  version: 1.0.0
  description: Computed Clinical Frailty Scale result. CFS 1 = Very fit, 4 = Vulnerable, 7 = Severely frail, 9 = Terminally ill. One-to-one child of assessment.
paths:
  /grades:
    get:
      summary: List Grade records
      operationId: listGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Grade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      summary: Create a new Grade
      operationId: createGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "201":
          description: The newly created Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "400":
          description: Validation error
  /grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Grade by id
      operationId: getGrade
      responses:
        "200":
          description: The requested Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    patch:
      summary: Update an existing Grade
      operationId: updateGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "200":
          description: The updated Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    delete:
      summary: Soft-delete a Grade
      operationId: deleteGrade
      responses:
        "204":
          description: Grade deleted
        "404":
          description: Grade not found
components:
  schemas:
    Grade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the assessment table.
        cfs_score:
          type: integer
          format: int32
          description: Clinical Frailty Scale score (1-9).
        cfs_category:
          type: string
          enum:
            - very-fit
            - well
            - managing-well
            - vulnerable
            - mildly-frail
            - moderately-frail
            - severely-frail
            - very-severely-frail
            - terminally-ill
            - ""
          description: "CFS category: very-fit, well, managing-well, vulnerable, mildly-frail, moderately-frail, severely-frail, very-severely-frail, terminally-ill, or empty string."
        functional_domain_score:
          type: integer
          format: int32
          description: Sub-score for the functional assessment domain, NULL if not yet scored.
        cognitive_domain_score:
          type: integer
          format: int32
          description: Sub-score for the cognitive screening domain, NULL if not yet scored.
        mobility_domain_score:
          type: integer
          format: int32
          description: Sub-score for the mobility and falls domain, NULL if not yet scored.
        nutrition_domain_score:
          type: integer
          format: int32
          description: Sub-score for the nutrition domain, NULL if not yet scored.
        polypharmacy_domain_score:
          type: integer
          format: int32
          description: Sub-score for the polypharmacy review domain, NULL if not yet scored.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the grading was computed.
      required:
        - created_at
        - updated_at
        - assessment_id
        - cfs_score
        - cfs_category
        - graded_at
      description: Computed Clinical Frailty Scale result. CFS 1 = Very fit, 4 = Vulnerable, 7 = Severely frail, 9 = Terminally ill. One-to-one child of assessment.
