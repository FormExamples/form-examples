# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: mri_scan_test_request_grade
openapi: 3.1.0
info:
  title: Mri Scan Test Request — MriScanTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (ACR 1-9), MRI safety, request completeness, and triage priority."
paths:
  /mri-scan-test-request-grades:
    get:
      summary: List MriScanTestRequestGrade records
      operationId: listMriScanTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of MriScanTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MriScanTestRequestGrade"
    post:
      summary: Create a new MriScanTestRequestGrade
      operationId: createMriScanTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MriScanTestRequestGrade"
      responses:
        "201":
          description: The newly created MriScanTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MriScanTestRequestGrade"
        "400":
          description: Validation error
  /mri-scan-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one MriScanTestRequestGrade by id
      operationId: getMriScanTestRequestGrade
      responses:
        "200":
          description: The requested MriScanTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MriScanTestRequestGrade"
        "404":
          description: MriScanTestRequestGrade not found
    patch:
      summary: Update an existing MriScanTestRequestGrade
      operationId: updateMriScanTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MriScanTestRequestGrade"
      responses:
        "200":
          description: The updated MriScanTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MriScanTestRequestGrade"
        "404":
          description: MriScanTestRequestGrade not found
    delete:
      summary: Soft-delete a MriScanTestRequestGrade
      operationId: deleteMriScanTestRequestGrade
      responses:
        "204":
          description: MriScanTestRequestGrade deleted
        "404":
          description: MriScanTestRequestGrade not found
components:
  schemas:
    MriScanTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        mri_scan_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: ACR Appropriateness Criteria score (1-9).
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        mri_safety_band:
          type: string
          enum:
            - cleared
            - conditional
            - needs-mri-physics-review
            - contraindicated
            - ""
          description: "MRI safety band: cleared, conditional, needs-mri-physics-review, contraindicated."
        contrast_renal_flag:
          type: string
          enum:
            - none
            - caution
            - contraindicated
            - unknown
            - ""
          description: "Gadolinium-versus-eGFR / NSF risk: none, caution (eGFR 30-60), contraindicated (eGFR < 30), unknown."
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Assigned triage tier: routine, urgent, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the scan implied by the triage tier.
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - mri_scan_test_request_id
        - appropriateness_band
        - mri_safety_band
        - contrast_renal_flag
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (ACR 1-9), MRI safety, request completeness, and triage priority."
