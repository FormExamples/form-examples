# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: medical_practice
openapi: 3.1.0
info:
  title: United Kingdom Statement Of Fitness For Work — MedicalPractice
  version: 1.0.0
  description: The medical practice that owns the fit note (mandatory per DWP policy 3.7).
paths:
  /medical-practices:
    get:
      summary: List MedicalPractice records
      operationId: listMedicalPractice
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of MedicalPractice records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MedicalPractice"
    post:
      summary: Create a new MedicalPractice
      operationId: createMedicalPractice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MedicalPractice"
      responses:
        "201":
          description: The newly created MedicalPractice
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicalPractice"
        "400":
          description: Validation error
  /medical-practices/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one MedicalPractice by id
      operationId: getMedicalPractice
      responses:
        "200":
          description: The requested MedicalPractice
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicalPractice"
        "404":
          description: MedicalPractice not found
    patch:
      summary: Update an existing MedicalPractice
      operationId: updateMedicalPractice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MedicalPractice"
      responses:
        "200":
          description: The updated MedicalPractice
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicalPractice"
        "404":
          description: MedicalPractice not found
    delete:
      summary: Soft-delete a MedicalPractice
      operationId: deleteMedicalPractice
      responses:
        "204":
          description: MedicalPractice deleted
        "404":
          description: MedicalPractice not found
components:
  schemas:
    MedicalPractice:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        name:
          type: string
          description: Practice or organisation name as printed on the fit note.
        postal_address_as_full_text:
          type: string
          description: Practice postal address.
        country_as_iso_3166_1_alpha_2:
          type: string
          maxLength: 2
          description: Country as ISO 3166-1 alpha-2 (typically GB).
        postcode:
          type: string
          description: Practice postcode.
        phone:
          type: string
          description: Practice phone number.
        email:
          type: string
          description: Practice email address.
        ods_code:
          type: string
          description: NHS Organisation Data Service (ODS) code identifying the practice.
        setting:
          type: string
          enum:
            - primary_care
            - secondary_care_inpatient
            - secondary_care_discharge
            - occupational_health
            - pharmacy
            - community_clinic
            - private_practice
            - other
            - ""
          description: "Care setting: primary_care, secondary_care_inpatient, secondary_care_discharge, occupational_health, pharmacy, community_clinic, or private_practice."
      required:
        - created_at
        - updated_at
        - name
        - postal_address_as_full_text
        - setting
      description: The medical practice that owns the fit note (mandatory per DWP policy 3.7).
