# Generated from sql/. Do not edit by hand.
# SOURCE: patient_allergy
openapi: 3.1.0
info:
  title: Pre Anaesthesia Assessment — PatientAllergy
  version: 1.0.0
  description: Patient allergy.
paths:
  /patient-allergies:
    get:
      summary: List PatientAllergy records
      operationId: listPatientAllergy
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PatientAllergy records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PatientAllergy"
    post:
      summary: Create a new PatientAllergy
      operationId: createPatientAllergy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatientAllergy"
      responses:
        "201":
          description: The newly created PatientAllergy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PatientAllergy"
        "400":
          description: Validation error
  /patient-allergies/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PatientAllergy by id
      operationId: getPatientAllergy
      responses:
        "200":
          description: The requested PatientAllergy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PatientAllergy"
        "404":
          description: PatientAllergy not found
    patch:
      summary: Update an existing PatientAllergy
      operationId: updatePatientAllergy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatientAllergy"
      responses:
        "200":
          description: The updated PatientAllergy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PatientAllergy"
        "404":
          description: PatientAllergy not found
    delete:
      summary: Soft-delete a PatientAllergy
      operationId: deletePatientAllergy
      responses:
        "204":
          description: PatientAllergy deleted
        "404":
          description: PatientAllergy not found
components:
  schemas:
    PatientAllergy:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient table.
        allergy_id:
          type: string
          format: uuid
          description: Foreign key to the allergy table.
      required:
        - created_at
        - updated_at
        - patient_id
        - allergy_id
      description: Patient allergy.
