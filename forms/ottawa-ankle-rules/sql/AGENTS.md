# Ottawa Ankle Rules (and Ottawa Foot Rules) — SQL migrations

Agent instructions for this directory. PostgreSQL migrations for the Ottawa Ankle Rules (and Ottawa Foot Rules) form — the source of truth for the data shape. Numbered `NN_*.sql` files plus the combined `schema.sql`.

See the form root [`../AGENTS.md`](../AGENTS.md) for the form-wide conventions and the repo root [`../../AGENTS.md`](../../AGENTS.md) for the system-wide tooling.
