# Generated from sql/. Do not edit by hand.
# SOURCE: child_safeguarding_referral
openapi: 3.1.0
info:
  title: Child Safeguarding Referral — ChildSafeguardingReferral
  version: 1.0.0
  description: "Main child-safeguarding-referral record: referrer context, child details, family and household, the concern or allegation, category of abuse, presenting evidence, immediate risk and safety, consent and information-sharing basis, who else is informed, and the action requested. Grade, fired rules, and flags live in child tables."
paths:
  /child-safeguarding-referrals:
    get:
      summary: List ChildSafeguardingReferral records
      operationId: listChildSafeguardingReferral
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of ChildSafeguardingReferral records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChildSafeguardingReferral"
    post:
      summary: Create a new ChildSafeguardingReferral
      operationId: createChildSafeguardingReferral
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChildSafeguardingReferral"
      responses:
        "201":
          description: The newly created ChildSafeguardingReferral
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildSafeguardingReferral"
        "400":
          description: Validation error
  /child-safeguarding-referrals/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one ChildSafeguardingReferral by id
      operationId: getChildSafeguardingReferral
      responses:
        "200":
          description: The requested ChildSafeguardingReferral
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildSafeguardingReferral"
        "404":
          description: ChildSafeguardingReferral not found
    patch:
      summary: Update an existing ChildSafeguardingReferral
      operationId: updateChildSafeguardingReferral
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChildSafeguardingReferral"
      responses:
        "200":
          description: The updated ChildSafeguardingReferral
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildSafeguardingReferral"
        "404":
          description: ChildSafeguardingReferral not found
    delete:
      summary: Soft-delete a ChildSafeguardingReferral
      operationId: deleteChildSafeguardingReferral
      responses:
        "204":
          description: ChildSafeguardingReferral deleted
        "404":
          description: ChildSafeguardingReferral not found
components:
  schemas:
    ChildSafeguardingReferral:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the child this referral concerns (restrict delete).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the referrer making the referral (restrict delete); optional.
        referrer_organisation:
          type: string
          description: Employing organisation of the referrer.
        referred_at:
          type: string
          format: date-time
          description: Date and time the referral was made.
        relationship_to_child:
          type: string
          description: How the referrer knows the child.
        child_age:
          type: number
          format: double
          description: Age of the child in years, used as a fallback when the date of birth is unknown.
        child_setting:
          type: string
          description: Education or care setting the child attends (school, nursery, college).
        child_reference:
          type: string
          description: NHS number or local unique reference for the child.
        child_ethnicity:
          type: string
          description: Self-described ethnicity of the child where known.
        child_first_language:
          type: string
          description: First language of the child, recorded for any interpreter need.
        child_disability:
          type: string
          description: Disability or communication need of the child.
        carers:
          type: string
          description: Parents or carers with parental responsibility.
        household_members:
          type: string
          description: Other members of the household.
        other_children:
          type: string
          description: Siblings or other children in the household.
        professionals_involved:
          type: string
          description: Professionals already involved (GP, school, existing social worker).
        concern_description:
          type: string
          description: What the concern or allegation is (mandatory for a valid referral).
        concern_onset:
          type: string
          description: When and how the concern came to light.
        child_disclosed:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the child disclosed abuse (yes/no); yes raises the disclosure-of-abuse flag and drives urgent classification.
        referrer_observations:
          type: string
          description: Direct observations recorded by the referrer.
        primary_category:
          type: string
          enum:
            - physical
            - emotional
            - sexual
            - neglect
            - ""
          description: "Primary category of abuse (mandatory): physical, emotional, sexual, or neglect."
        additional_categories:
          type: string
          description: Any further categories of abuse beyond the primary category.
        presenting_evidence:
          type: string
          description: Indicators and evidence presenting the concern.
        immediate_danger:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the child is in immediate danger (mandatory, yes/no); yes classifies the referral as an emergency.
        child_whereabouts:
          type: string
          description: Where the child is now.
        who_with_child:
          type: string
          description: Who is currently with the child.
        alleged_person_in_contact:
          type: string
          enum:
            - "yes"
            - "no"
            - unknown
            - ""
          description: Whether the alleged person is in contact with the child (yes/no/unknown); yes drives urgent classification.
        other_children_at_risk:
          type: string
          enum:
            - "yes"
            - "no"
            - unknown
            - ""
          description: Whether siblings or other children are at risk (yes/no/unknown); yes raises the other-children-at-risk flag and drives urgent classification.
        consent_sought:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether consent to refer was sought (yes/no).
        consent_status:
          type: string
          enum:
            - given
            - refused
            - not-sought
            - ""
          description: "Consent status: given, refused, or not-sought."
        sharing_basis_without_consent:
          type: string
          enum:
            - risk-of-serious-harm
            - seeking-consent-increases-risk
            - not-applicable
            - ""
          description: "Lawful basis for sharing without consent (mandatory when consent is not given): risk-of-serious-harm, seeking-consent-increases-risk, or not-applicable."
        family_aware:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the child or family is aware of the referral (yes/no).
        unsafe_to_inform_reason:
          type: string
          description: Why informing the child or family would increase risk.
        agencies_contacted:
          type: string
          description: Agencies already contacted (police, health, education).
        strategy_discussion_held:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether a strategy discussion has already been held (yes/no).
        previous_safeguarding_history:
          type: string
          description: Prior safeguarding involvement if known; non-empty raises the previous-history flag.
        requested_action:
          type: string
          description: Action requested of children's social care.
        referrer_declaration:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the referrer confirms the accuracy of the referral (yes/no).
        notes:
          type: string
          description: Free-text notes shown in the summary.
      required:
        - created_at
        - updated_at
        - patient_id
        - referrer_organisation
        - relationship_to_child
        - child_setting
        - child_reference
        - child_ethnicity
        - child_first_language
        - child_disability
        - carers
        - household_members
        - other_children
        - professionals_involved
        - concern_description
        - concern_onset
        - child_disclosed
        - referrer_observations
        - primary_category
        - additional_categories
        - presenting_evidence
        - immediate_danger
        - child_whereabouts
        - who_with_child
        - alleged_person_in_contact
        - other_children_at_risk
        - consent_sought
        - consent_status
        - sharing_basis_without_consent
        - family_aware
        - unsafe_to_inform_reason
        - agencies_contacted
        - strategy_discussion_held
        - previous_safeguarding_history
        - requested_action
        - referrer_declaration
        - notes
      description: "Main child-safeguarding-referral record: referrer context, child details, family and household, the concern or allegation, category of abuse, presenting evidence, immediate risk and safety, consent and information-sharing basis, who else is informed, and the action requested. Grade, fired rules, and flags live in child tables."
