# front-end-dashboard-with-html — Tasks

- [x] `index.html` shell with sortable register table
- [x] `css/style.css` hand-written styling with status pills
- [x] `js/data.js` sample register data
- [x] `js/app.js` to render and sort/filter rows
- [x] Status filter (radio buttons)
- [x] Group filter (select)
- [x] Free-text search across title / slug / author
- [x] Per-row link out to the rendered Markdown
- [ ] Replace `js/data.js` with a generated `data/adrs.json` produced from
      the database
- [ ] Wire pagination if the register grows beyond ~200 rows
