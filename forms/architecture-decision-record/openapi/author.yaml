# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: author
openapi: 3.1.0
info:
  title: Architecture Decision Record — Author
  version: 1.0.0
  description: Author of an architecture decision record (the architect or decision-maker).
paths:
  /authors:
    get:
      summary: List Author records
      operationId: listAuthor
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Author records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Author"
    post:
      summary: Create a new Author
      operationId: createAuthor
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Author"
      responses:
        "201":
          description: The newly created Author
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Author"
        "400":
          description: Validation error
  /authors/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Author by id
      operationId: getAuthor
      responses:
        "200":
          description: The requested Author
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Author"
        "404":
          description: Author not found
    patch:
      summary: Update an existing Author
      operationId: updateAuthor
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Author"
      responses:
        "200":
          description: The updated Author
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Author"
        "404":
          description: Author not found
    delete:
      summary: Soft-delete a Author
      operationId: deleteAuthor
      responses:
        "204":
          description: Author deleted
        "404":
          description: Author not found
components:
  schemas:
    Author:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        name:
          type: string
          description: Author name.
        email:
          type: string
          description: Author email address.
        phone:
          type: string
          description: Author phone number.
        role:
          type: string
          enum:
            - architect
            - principal-engineer
            - staff-engineer
            - engineering-manager
            - cto
            - product-manager
            - security-officer
            - compliance-officer
            - consultant
            - other
            - ""
          description: Author role within the organization.
        organization_name:
          type: string
          description: Free-text organization name; for richer context use the organization table.
        team_name:
          type: string
          description: Team within the organization.
      required:
        - created_at
        - updated_at
        - name
        - email
        - phone
        - role
        - organization_name
        - team_name
      description: Author of an architecture decision record (the architect or decision-maker).
