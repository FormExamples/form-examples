# Generated from sql/. Do not edit by hand.
# SOURCE: cardiac_stress_test_request_grade
openapi: 3.1.0
info:
  title: Cardiac Stress Test Request — CardiacStressTestRequestGrade
  version: 1.0.0
  description: "Computed four-axis grading result: appropriateness (ACC/AHA AUC 1-9), safety / contraindication, request completeness, and triage priority."
paths:
  /cardiac-stress-test-request-grades:
    get:
      summary: List CardiacStressTestRequestGrade records
      operationId: listCardiacStressTestRequestGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CardiacStressTestRequestGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CardiacStressTestRequestGrade"
    post:
      summary: Create a new CardiacStressTestRequestGrade
      operationId: createCardiacStressTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardiacStressTestRequestGrade"
      responses:
        "201":
          description: The newly created CardiacStressTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiacStressTestRequestGrade"
        "400":
          description: Validation error
  /cardiac-stress-test-request-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CardiacStressTestRequestGrade by id
      operationId: getCardiacStressTestRequestGrade
      responses:
        "200":
          description: The requested CardiacStressTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiacStressTestRequestGrade"
        "404":
          description: CardiacStressTestRequestGrade not found
    patch:
      summary: Update an existing CardiacStressTestRequestGrade
      operationId: updateCardiacStressTestRequestGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardiacStressTestRequestGrade"
      responses:
        "200":
          description: The updated CardiacStressTestRequestGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiacStressTestRequestGrade"
        "404":
          description: CardiacStressTestRequestGrade not found
    delete:
      summary: Soft-delete a CardiacStressTestRequestGrade
      operationId: deleteCardiacStressTestRequestGrade
      responses:
        "204":
          description: CardiacStressTestRequestGrade deleted
        "404":
          description: CardiacStressTestRequestGrade not found
components:
  schemas:
    CardiacStressTestRequestGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        cardiac_stress_test_request_id:
          type: string
          format: uuid
          description: Foreign key to the parent request (unique, 1:1).
        appropriateness_score:
          type: integer
          format: int32
          description: ACC/AHA Appropriate Use Criteria score (1-9) for stress testing.
        appropriateness_band:
          type: string
          enum:
            - usually-appropriate
            - may-be-appropriate
            - usually-not-appropriate
            - ""
          description: "Appropriateness band: usually-appropriate (7-9), may-be-appropriate (4-6), usually-not-appropriate (1-3)."
        contraindication_band:
          type: string
          enum:
            - ok
            - caution
            - contraindicated
            - ""
          description: "Safety band from contraindication screen: ok, caution, contraindicated (recent ACS, severe symptomatic AS, uncontrolled HTN, inability to exercise)."
        completeness_percent:
          type: integer
          format: int32
          description: Request completeness as a percentage of mandatory fields (0-100).
        triage_tier:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Assigned triage tier: routine, urgent, emergency."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the test implied by the triage tier.
        recommendation:
          type: string
          enum:
            - accept
            - query-referrer
            - redirect
            - reject
            - ""
          description: "Vetting recommendation: accept, query-referrer, redirect, reject."
        clinician_notes:
          type: string
          description: Free-text vetting notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of vetting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - cardiac_stress_test_request_id
        - appropriateness_band
        - contraindication_band
        - triage_tier
        - target_timeframe
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis grading result: appropriateness (ACC/AHA AUC 1-9), safety / contraindication, request completeness, and triage priority."
