# Generated from sql/. Do not edit by hand.
# SOURCE: cardiac_stress_test_request
openapi: 3.1.0
info:
  title: Cardiac Stress Test Request — CardiacStressTestRequest
  version: 1.0.0
  description: Cardiac stress / exercise test request (referral). Captures the requested test type, the clinical indication and question, relevant history, presenting symptoms, ability to exercise, and the cardiac risk / safety factors (known CAD, recent ACS, aortic stenosis, uncontrolled hypertension, beta-blocker), plus triage urgency.
paths:
  /cardiac-stress-test-requests:
    get:
      summary: List CardiacStressTestRequest records
      operationId: listCardiacStressTestRequest
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CardiacStressTestRequest records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CardiacStressTestRequest"
    post:
      summary: Create a new CardiacStressTestRequest
      operationId: createCardiacStressTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardiacStressTestRequest"
      responses:
        "201":
          description: The newly created CardiacStressTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiacStressTestRequest"
        "400":
          description: Validation error
  /cardiac-stress-test-requests/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CardiacStressTestRequest by id
      operationId: getCardiacStressTestRequest
      responses:
        "200":
          description: The requested CardiacStressTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiacStressTestRequest"
        "404":
          description: CardiacStressTestRequest not found
    patch:
      summary: Update an existing CardiacStressTestRequest
      operationId: updateCardiacStressTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardiacStressTestRequest"
      responses:
        "200":
          description: The updated CardiacStressTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardiacStressTestRequest"
        "404":
          description: CardiacStressTestRequest not found
    delete:
      summary: Soft-delete a CardiacStressTestRequest
      operationId: deleteCardiacStressTestRequest
      responses:
        "204":
          description: CardiacStressTestRequest deleted
        "404":
          description: CardiacStressTestRequest not found
components:
  schemas:
    CardiacStressTestRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the requesting clinician (referrer).
        status:
          type: string
          enum:
            - draft
            - submitted
            - vetted
            - scheduled
            - rejected
            - ""
          description: "Request lifecycle status: draft, submitted, vetted, scheduled, rejected."
        site_name:
          type: string
          maxLength: 255
          description: Name of the requesting site or clinic.
        setting:
          type: string
          enum:
            - outpatient
            - inpatient
            - community
            - emergency
            - ""
          description: "Care setting: outpatient, inpatient, community, emergency."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        requested_by_date:
          type: string
          format: date
          description: Date by which the test is requested to be performed.
        test_type:
          type: string
          enum:
            - exercise-treadmill-ecg
            - stress-echo
            - dobutamine-stress-echo
            - myocardial-perfusion-spect
            - stress-cardiac-mri
            - other
            - ""
          description: "Requested stress-test modality: exercise-treadmill-ecg, stress-echo, dobutamine-stress-echo, myocardial-perfusion-spect, stress-cardiac-mri, other."
        primary_indication:
          type: string
          enum:
            - suspected-angina
            - known-cad-assessment
            - risk-stratification-post-mi
            - pre-operative-cardiac
            - exercise-tolerance
            - arrhythmia-evaluation
            - valve-disease
            - other
            - ""
          description: Primary clinical indication, used for appropriateness scoring.
        clinical_question:
          type: string
          maxLength: 1000
          description: Specific clinical question the test should answer (highest-value field).
        relevant_history:
          type: string
          maxLength: 1000
          description: Relevant cardiac, medical, and surgical history.
        symptom_chest_pain:
          type: boolean
          description: Whether the patient reports chest pain.
        symptom_breathlessness:
          type: boolean
          description: Whether the patient reports breathlessness (dyspnoea).
        symptom_palpitations:
          type: boolean
          description: Whether the patient reports palpitations.
        able_to_exercise:
          type: boolean
          description: Whether the patient is able to exercise (determines exercise vs pharmacological stress).
        resting_ecg_findings:
          type: string
          maxLength: 1000
          description: Relevant resting ECG findings (e.g. LBBB, paced, ST changes) affecting test selection.
        known_coronary_artery_disease:
          type: boolean
          description: Whether the patient has known coronary artery disease.
        recent_acute_coronary_syndrome:
          type: boolean
          description: Whether the patient has had a recent acute coronary syndrome (safety contraindication if very recent).
        aortic_stenosis:
          type: string
          enum:
            - none
            - mild
            - moderate
            - severe
            - unknown
            - ""
          description: "Aortic stenosis severity: none, mild, moderate, severe, unknown (severe symptomatic AS contraindicates exercise testing)."
        uncontrolled_hypertension:
          type: boolean
          description: Whether the patient has uncontrolled hypertension (relative contraindication).
        beta_blocker:
          type: boolean
          description: Whether the patient is taking a beta-blocker (may blunt heart-rate response on exercise testing).
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - emergency
            - ""
          description: "Requested triage urgency: routine, urgent, emergency."
        supervising_consultant:
          type: string
          maxLength: 255
          description: Name of the supervising / responsible consultant.
        requester_contact:
          type: string
          maxLength: 255
          description: Contact details (bleep / phone) for the requester.
        notes:
          type: string
          maxLength: 1000
          description: Free-text additional notes.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - status
        - site_name
        - setting
        - test_type
        - primary_indication
        - clinical_question
        - relevant_history
        - symptom_chest_pain
        - symptom_breathlessness
        - symptom_palpitations
        - able_to_exercise
        - resting_ecg_findings
        - known_coronary_artery_disease
        - recent_acute_coronary_syndrome
        - aortic_stenosis
        - uncontrolled_hypertension
        - beta_blocker
        - urgency
        - supervising_consultant
        - requester_contact
        - notes
      description: Cardiac stress / exercise test request (referral). Captures the requested test type, the clinical indication and question, relevant history, presenting symptoms, ability to exercise, and the cardiac risk / safety factors (known CAD, recent ACS, aortic stenosis, uncontrolled hypertension, beta-blocker), plus triage urgency.
