# Generated from sql/. Do not edit by hand.
# SOURCE: blood_cross_match_test_request_grade_rule
openapi: 3.1.0
info:
  title: Blood Cross Match Test Request — BloodCrossMatchTestRequestGradeRule
  version: 1.0.0
  description: Audit trail of every appropriateness / identity / completeness / triage rule that fired for this request.
paths:
  /blood-cross-match-test-request-grade-rules:
    get:
      summary: List BloodCrossMatchTestRequestGradeRule records
      operationId: listBloodCrossMatchTestRequestGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of BloodCrossMatchTestRequestGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BloodCrossMatchTestRequestGradeRule"
    post:
      summary: Create a new BloodCrossMatchTestRequestGradeRule
      operationId: createBloodCrossMatchTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BloodCrossMatchTestRequestGradeRule"
      responses:
        "201":
          description: The newly created BloodCrossMatchTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BloodCrossMatchTestRequestGradeRule"
        "400":
          description: Validation error
  /blood-cross-match-test-request-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one BloodCrossMatchTestRequestGradeRule by id
      operationId: getBloodCrossMatchTestRequestGradeRule
      responses:
        "200":
          description: The requested BloodCrossMatchTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BloodCrossMatchTestRequestGradeRule"
        "404":
          description: BloodCrossMatchTestRequestGradeRule not found
    patch:
      summary: Update an existing BloodCrossMatchTestRequestGradeRule
      operationId: updateBloodCrossMatchTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BloodCrossMatchTestRequestGradeRule"
      responses:
        "200":
          description: The updated BloodCrossMatchTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BloodCrossMatchTestRequestGradeRule"
        "404":
          description: BloodCrossMatchTestRequestGradeRule not found
    delete:
      summary: Soft-delete a BloodCrossMatchTestRequestGradeRule
      operationId: deleteBloodCrossMatchTestRequestGradeRule
      responses:
        "204":
          description: BloodCrossMatchTestRequestGradeRule deleted
        "404":
          description: BloodCrossMatchTestRequestGradeRule not found
components:
  schemas:
    BloodCrossMatchTestRequestGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        blood_cross_match_test_request_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-APPROP-NG24-01).
        axis:
          type: string
          enum:
            - appropriateness
            - identity
            - completeness
            - triage
            - ""
          description: "Scoring axis the rule belongs to: appropriateness, identity, completeness, triage."
        category:
          type: string
          maxLength: 50
          description: Category or indication the rule relates to.
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - blood_cross_match_test_request_grade_id
        - rule_id
        - axis
        - category
        - description
      description: Audit trail of every appropriateness / identity / completeness / triage rule that fired for this request.
