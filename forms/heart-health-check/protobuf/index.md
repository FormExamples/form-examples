# Heart Health Check — Protocol Buffers representations

Protocol Buffers `.proto` schemas for the Heart Health Check form, generated per SQL entity. Do not hand-edit; re-run `bin/protobuf/generate-protobuf-representations.py`.

See the form root [`../index.md`](../index.md) for the full design description.
