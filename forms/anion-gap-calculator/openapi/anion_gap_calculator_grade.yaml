# Generated from sql/. Do not edit by hand.
# SOURCE: anion_gap_calculator_grade
openapi: 3.1.0
info:
  title: Anion Gap Calculator — AnionGapCalculatorGrade
  version: 1.0.0
  description: "Computed anion-gap result: the raw anion gap, the albumin-corrected anion gap, and the classification band (low / normal / high / very-high), 1:1 with the parent calculation record."
paths:
  /anion-gap-calculator-grades:
    get:
      summary: List AnionGapCalculatorGrade records
      operationId: listAnionGapCalculatorGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of AnionGapCalculatorGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AnionGapCalculatorGrade"
    post:
      summary: Create a new AnionGapCalculatorGrade
      operationId: createAnionGapCalculatorGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnionGapCalculatorGrade"
      responses:
        "201":
          description: The newly created AnionGapCalculatorGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnionGapCalculatorGrade"
        "400":
          description: Validation error
  /anion-gap-calculator-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one AnionGapCalculatorGrade by id
      operationId: getAnionGapCalculatorGrade
      responses:
        "200":
          description: The requested AnionGapCalculatorGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnionGapCalculatorGrade"
        "404":
          description: AnionGapCalculatorGrade not found
    patch:
      summary: Update an existing AnionGapCalculatorGrade
      operationId: updateAnionGapCalculatorGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnionGapCalculatorGrade"
      responses:
        "200":
          description: The updated AnionGapCalculatorGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnionGapCalculatorGrade"
        "404":
          description: AnionGapCalculatorGrade not found
    delete:
      summary: Soft-delete a AnionGapCalculatorGrade
      operationId: deleteAnionGapCalculatorGrade
      responses:
        "204":
          description: AnionGapCalculatorGrade deleted
        "404":
          description: AnionGapCalculatorGrade not found
components:
  schemas:
    AnionGapCalculatorGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        anion_gap_calculator_id:
          type: string
          format: uuid
          description: Foreign key to the parent calculation record (unique, 1:1).
        anion_gap:
          type: number
          format: double
          description: Raw anion gap in mmol/L (null when any required electrolyte is missing).
        corrected_anion_gap:
          type: number
          format: double
          description: Albumin-corrected anion gap in mmol/L (null when albumin is missing).
        classification:
          type: string
          enum:
            - normal
            - high
            - very-high
            - low
            - ""
          description: "Classification band using the corrected gap when available, else the raw gap: low, normal, high, or very-high."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - anion_gap_calculator_id
        - classification
        - graded_at
      description: "Computed anion-gap result: the raw anion gap, the albumin-corrected anion gap, and the classification band (low / normal / high / very-high), 1:1 with the parent calculation record."
