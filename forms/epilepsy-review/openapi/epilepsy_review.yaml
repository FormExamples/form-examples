# Generated from sql/. Do not edit by hand.
# SOURCE: epilepsy_review
openapi: 3.1.0
info:
  title: Epilepsy Review — EpilepsyReview
  version: 1.0.0
  description: "Main epilepsy annual-review record (NICE NG217): patient and clinician identification, review context, epilepsy profile, seizures and anti-seizure medication with adherence and side effects, triggers, SUDEP discussion, injuries and status epilepticus, safety (DVLA driving, bathing, occupation), valproate and pregnancy-prevention arrangements, mental health, and the care plan. The computed seizure-control classification, completeness grade, fired rules, and safety flags live in child tables."
paths:
  /epilepsy-reviews:
    get:
      summary: List EpilepsyReview records
      operationId: listEpilepsyReview
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of EpilepsyReview records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EpilepsyReview"
    post:
      summary: Create a new EpilepsyReview
      operationId: createEpilepsyReview
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EpilepsyReview"
      responses:
        "201":
          description: The newly created EpilepsyReview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EpilepsyReview"
        "400":
          description: Validation error
  /epilepsy-reviews/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one EpilepsyReview by id
      operationId: getEpilepsyReview
      responses:
        "200":
          description: The requested EpilepsyReview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EpilepsyReview"
        "404":
          description: EpilepsyReview not found
    patch:
      summary: Update an existing EpilepsyReview
      operationId: updateEpilepsyReview
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EpilepsyReview"
      responses:
        "200":
          description: The updated EpilepsyReview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EpilepsyReview"
        "404":
          description: EpilepsyReview not found
    delete:
      summary: Soft-delete a EpilepsyReview
      operationId: deleteEpilepsyReview
      responses:
        "204":
          description: EpilepsyReview deleted
        "404":
          description: EpilepsyReview not found
components:
  schemas:
    EpilepsyReview:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient this review documents (restrict delete).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the reviewing clinician (restrict delete); optional.
        reviewer_name:
          type: string
          description: Name of the reviewing clinician as recorded on the review.
        reviewer_role:
          type: string
          enum:
            - gp
            - practice-nurse
            - epilepsy-nurse
            - neurologist
            - other
            - ""
          description: "Reviewing clinician role: gp, practice-nurse, epilepsy-nurse, neurologist, or other."
        reviewed_at:
          type: string
          format: date
          description: Date the annual review was conducted.
        care_setting:
          type: string
          enum:
            - general-practice
            - epilepsy-clinic
            - community
            - other
            - ""
          description: "Care setting where the review was conducted: general-practice, epilepsy-clinic, community, or other."
        review_type:
          type: string
          enum:
            - annual
            - interim
            - ""
          description: "Type of review: annual or interim."
        months_since_last_review:
          type: number
          format: double
          description: Interval in months since the last review; drives the review-overdue flag when greater than 12.
        patient_identifier:
          type: string
          maxLength: 100
          description: NHS number or local patient identifier as recorded on the review.
        age_band:
          type: string
          enum:
            - 18-39
            - 40-59
            - 60-79
            - >=80
            - ""
          description: "Adult age band: 18-39, 40-59, 60-79, or >=80."
        sex:
          type: string
          enum:
            - female
            - male
            - intersex
            - unknown
            - ""
          description: "Patient sex: female, male, intersex, or unknown."
        epilepsy_type:
          type: string
          enum:
            - focal
            - generalised
            - combined
            - unknown
            - ""
          description: "Epilepsy type: focal, generalised, combined, or unknown."
        age_at_onset:
          type: number
          format: double
          description: Age at epilepsy onset in years.
        years_since_diagnosis:
          type: number
          format: double
          description: Years since epilepsy diagnosis.
        learning_disability:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient has a learning disability (yes/no).
        seizure_types:
          type: string
          description: Free-text record of the seizure type(s) present.
        seizure_frequency:
          type: string
          enum:
            - none
            - less-than-monthly
            - monthly
            - weekly
            - daily
            - ""
          description: "Seizure frequency: none, less-than-monthly, monthly, weekly, or daily (weekly/daily drive an uncontrolled classification)."
        last_seizure_date:
          type: string
          format: date
          description: Date of the most recent seizure.
        seizure_free_months:
          type: number
          format: double
          description: Documented seizure-free duration in months.
        seizure_trend:
          type: string
          enum:
            - seizure-free
            - decreasing
            - stable
            - increasing
            - ""
          description: "Seizure trend since the last review: seizure-free, decreasing, stable, or increasing (increasing drives an uncontrolled classification)."
        current_asms:
          type: string
          description: Free-text record of current anti-seizure medication(s) and doses.
        asm_adherence:
          type: string
          enum:
            - good
            - partial
            - poor
            - ""
          description: "Self-reported ASM adherence: good, partial, or poor (poor drives an adherence flag)."
        asm_side_effects:
          type: string
          enum:
            - none
            - mild
            - significant
            - ""
          description: "ASM side-effect burden: none, mild, or significant (significant drives a side-effects flag)."
        drug_level:
          type: number
          format: double
          description: Therapeutic ASM drug level where relevant (units per the specific drug).
        triggers:
          type: string
          description: Free-text record of reported seizure triggers.
        sudep_discussed:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether Sudden Unexpected Death in Epilepsy (SUDEP) risk was discussed (yes/no); a non-yes value drives a SUDEP-not-documented flag.
        status_epilepticus:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether status epilepticus occurred since the last review (yes/no); yes drives an uncontrolled classification and a high-priority flag.
        seizure_injury:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether a seizure-related injury occurred since the last review (yes/no).
        dvla_eligible:
          type: string
          enum:
            - eligible
            - not-eligible
            - not-applicable
            - ""
          description: "DVLA driving eligibility: eligible, not-eligible, or not-applicable."
        currently_driving:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient is currently driving (yes/no); driving while not DVLA-eligible drives a driving-safety flag.
        bathing_advice_given:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether bathing / showering safety advice was given (yes/no).
        woman_of_childbearing_potential:
          type: string
          enum:
            - "yes"
            - "no"
            - not-applicable
            - ""
          description: Whether the patient is a woman of childbearing potential (yes/no/not-applicable); when yes, the valproate/PPP, folic-acid, and contraception domains become required for completeness.
        on_valproate:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether the patient is prescribed sodium valproate (yes/no).
        pregnancy_prevention_programme:
          type: string
          enum:
            - in-place
            - not-in-place
            - not-applicable
            - ""
          description: "Valproate Pregnancy Prevention Programme status: in-place, not-in-place, or not-applicable."
        folic_acid:
          type: string
          enum:
            - "yes"
            - "no"
            - not-applicable
            - ""
          description: "Whether folic acid is prescribed: yes, no, or not-applicable."
        contraception_interaction_reviewed:
          type: string
          enum:
            - "yes"
            - "no"
            - not-applicable
            - ""
          description: "Whether contraception and ASM interactions were reviewed: yes, no, or not-applicable."
        mental_health_concern:
          type: string
          enum:
            - none
            - low-mood
            - anxiety
            - depression
            - suicidality
            - ""
          description: "Mental-health concern: none, low-mood, anxiety, depression, or suicidality (suicidality high priority; depression/anxiety/low-mood medium)."
        specialist_review_needed:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether specialist (neurology) review is needed (yes/no).
        next_review_due:
          type: string
          format: date
          description: Date the next review is planned.
        care_plan:
          type: string
          description: Free-text record of the agreed epilepsy care plan.
        review_context:
          type: string
          description: Optional free-text review context or narrative shown in the summary.
      required:
        - created_at
        - updated_at
        - patient_id
        - reviewer_name
        - reviewer_role
        - care_setting
        - review_type
        - patient_identifier
        - age_band
        - sex
        - epilepsy_type
        - learning_disability
        - seizure_types
        - seizure_frequency
        - seizure_trend
        - current_asms
        - asm_adherence
        - asm_side_effects
        - triggers
        - sudep_discussed
        - status_epilepticus
        - seizure_injury
        - dvla_eligible
        - currently_driving
        - bathing_advice_given
        - woman_of_childbearing_potential
        - on_valproate
        - pregnancy_prevention_programme
        - folic_acid
        - contraception_interaction_reviewed
        - mental_health_concern
        - specialist_review_needed
        - care_plan
        - review_context
      description: "Main epilepsy annual-review record (NICE NG217): patient and clinician identification, review context, epilepsy profile, seizures and anti-seizure medication with adherence and side effects, triggers, SUDEP discussion, injuries and status epilepticus, safety (DVLA driving, bathing, occupation), valproate and pregnancy-prevention arrangements, mental health, and the care plan. The computed seizure-control classification, completeness grade, fired rules, and safety flags live in child tables."
