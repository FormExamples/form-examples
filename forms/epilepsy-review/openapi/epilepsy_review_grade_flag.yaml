# Generated from sql/. Do not edit by hand.
# SOURCE: epilepsy_review_grade_flag
openapi: 3.1.0
info:
  title: Epilepsy Review — EpilepsyReviewGradeFlag
  version: 1.0.0
  description: Safety flags that fire independently of the seizure-control class and the completeness status, with priority and a suggested action for the clinician or governance team.
paths:
  /epilepsy-review-grade-flags:
    get:
      summary: List EpilepsyReviewGradeFlag records
      operationId: listEpilepsyReviewGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of EpilepsyReviewGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EpilepsyReviewGradeFlag"
    post:
      summary: Create a new EpilepsyReviewGradeFlag
      operationId: createEpilepsyReviewGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EpilepsyReviewGradeFlag"
      responses:
        "201":
          description: The newly created EpilepsyReviewGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EpilepsyReviewGradeFlag"
        "400":
          description: Validation error
  /epilepsy-review-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one EpilepsyReviewGradeFlag by id
      operationId: getEpilepsyReviewGradeFlag
      responses:
        "200":
          description: The requested EpilepsyReviewGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EpilepsyReviewGradeFlag"
        "404":
          description: EpilepsyReviewGradeFlag not found
    patch:
      summary: Update an existing EpilepsyReviewGradeFlag
      operationId: updateEpilepsyReviewGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EpilepsyReviewGradeFlag"
      responses:
        "200":
          description: The updated EpilepsyReviewGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EpilepsyReviewGradeFlag"
        "404":
          description: EpilepsyReviewGradeFlag not found
    delete:
      summary: Soft-delete a EpilepsyReviewGradeFlag
      operationId: deleteEpilepsyReviewGradeFlag
      responses:
        "204":
          description: EpilepsyReviewGradeFlag deleted
        "404":
          description: EpilepsyReviewGradeFlag not found
components:
  schemas:
    EpilepsyReviewGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        epilepsy_review_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-VALPROATE-PPP-001).
        category:
          type: string
          enum:
            - specialist-review
            - valproate-ppp
            - status-epilepticus-history
            - driving-safety
            - mental-health
            - sudep-not-documented
            - poor-adherence
            - asm-side-effects
            - folic-acid-missing
            - incomplete
            - other
            - ""
          description: "Flag category: specialist-review, valproate-ppp, status-epilepticus-history, driving-safety, mental-health, sudep-not-documented, poor-adherence, asm-side-effects, folic-acid-missing, incomplete, or other."
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical or governance action (e.g. \"refer to neurology\", \"arrange valproate pregnancy-prevention programme review\")."
      required:
        - created_at
        - updated_at
        - epilepsy_review_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Safety flags that fire independently of the seizure-control class and the completeness status, with priority and a suggested action for the clinician or governance team.
