# Generated from sql/. Do not edit by hand.
# SOURCE: biopsy_test_request
openapi: 3.1.0
info:
  title: Biopsy Test Request — BiopsyTestRequest
  version: 1.0.0
  description: Tissue biopsy procedure / pathology request (referral). Captures the requested procedure (site, method, laterality), the clinical indication and question, lesion description, bleeding / coagulation status, and triage urgency.
paths:
  /biopsy-test-requests:
    get:
      summary: List BiopsyTestRequest records
      operationId: listBiopsyTestRequest
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of BiopsyTestRequest records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BiopsyTestRequest"
    post:
      summary: Create a new BiopsyTestRequest
      operationId: createBiopsyTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BiopsyTestRequest"
      responses:
        "201":
          description: The newly created BiopsyTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BiopsyTestRequest"
        "400":
          description: Validation error
  /biopsy-test-requests/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one BiopsyTestRequest by id
      operationId: getBiopsyTestRequest
      responses:
        "200":
          description: The requested BiopsyTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BiopsyTestRequest"
        "404":
          description: BiopsyTestRequest not found
    patch:
      summary: Update an existing BiopsyTestRequest
      operationId: updateBiopsyTestRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BiopsyTestRequest"
      responses:
        "200":
          description: The updated BiopsyTestRequest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BiopsyTestRequest"
        "404":
          description: BiopsyTestRequest not found
    delete:
      summary: Soft-delete a BiopsyTestRequest
      operationId: deleteBiopsyTestRequest
      responses:
        "204":
          description: BiopsyTestRequest deleted
        "404":
          description: BiopsyTestRequest not found
components:
  schemas:
    BiopsyTestRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient.
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the requesting clinician (referrer).
        status:
          type: string
          enum:
            - draft
            - submitted
            - vetted
            - scheduled
            - rejected
            - ""
          description: "Request lifecycle status: draft, submitted, vetted, scheduled, rejected."
        site_name:
          type: string
          maxLength: 255
          description: Name of the requesting site or clinic.
        setting:
          type: string
          enum:
            - outpatient
            - inpatient
            - community
            - emergency
            - ""
          description: "Care setting: outpatient, inpatient, community, emergency."
        referral_date:
          type: string
          format: date
          description: Date the referral was made.
        requested_by_date:
          type: string
          format: date
          description: Date by which the biopsy is requested to be performed.
        biopsy_site:
          type: string
          enum:
            - skin
            - breast
            - lymph-node
            - liver
            - kidney
            - prostate
            - lung
            - bone-marrow
            - gi-tract
            - thyroid
            - soft-tissue
            - other
            - ""
          description: "Anatomical biopsy site: skin, breast, lymph-node, liver, kidney, prostate, lung, bone-marrow, gi-tract, thyroid, soft-tissue, other."
        biopsy_method:
          type: string
          enum:
            - punch
            - excision
            - incision
            - core-needle
            - fine-needle-aspiration
            - image-guided
            - endoscopic
            - other
            - ""
          description: "Biopsy method / technique: punch, excision, incision, core-needle, fine-needle-aspiration, image-guided, endoscopic, other."
        laterality:
          type: string
          enum:
            - left
            - right
            - bilateral
            - not-applicable
            - ""
          description: "Laterality of the target: left, right, bilateral, not-applicable."
        primary_indication:
          type: string
          enum:
            - suspected-malignancy
            - cancer-staging
            - suspected-infection
            - inflammatory-disease
            - transplant-monitoring
            - lymphadenopathy
            - characterise-lesion
            - other
            - ""
          description: Primary clinical indication, used for appropriateness scoring.
        clinical_question:
          type: string
          maxLength: 1000
          description: Specific clinical question the biopsy should answer (highest-value field).
        relevant_history:
          type: string
          maxLength: 1000
          description: Relevant medical, surgical, and oncological history.
        lesion_description:
          type: string
          maxLength: 1000
          description: Description of the target lesion (size, location, imaging correlate).
        imaging_guidance_required:
          type: boolean
          description: Whether image guidance (ultrasound / CT / MRI) is required to target the lesion.
        taking_anticoagulant:
          type: boolean
          description: Whether the patient is taking an anticoagulant (e.g. warfarin, DOAC).
        anticoagulant_agent:
          type: string
          maxLength: 255
          description: Name of the anticoagulant agent, if any.
        taking_antiplatelet:
          type: boolean
          description: Whether the patient is taking an antiplatelet agent (e.g. clopidogrel).
        antiplatelet_agent:
          type: string
          maxLength: 255
          description: Name of the antiplatelet agent, if any.
        inr:
          type: number
          format: double
          description: International normalised ratio (INR), affecting bleeding-risk stratification.
        platelet_count:
          type: number
          format: double
          description: Platelet count (x10^9/L), affecting bleeding-risk stratification.
        bleeding_disorder:
          type: boolean
          description: Whether the patient has a known bleeding disorder / coagulopathy.
        immunosuppressed:
          type: boolean
          description: Whether the patient is immunosuppressed.
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - two-week-wait
            - emergency
            - ""
          description: "Requested triage urgency: routine, urgent, two-week-wait, emergency."
        supervising_consultant:
          type: string
          maxLength: 255
          description: Name of the supervising / responsible consultant.
        requester_contact:
          type: string
          maxLength: 255
          description: Contact details (bleep / phone) for the requester.
        notes:
          type: string
          maxLength: 1000
          description: Free-text additional notes.
      required:
        - created_at
        - updated_at
        - patient_id
        - clinician_id
        - status
        - site_name
        - setting
        - biopsy_site
        - biopsy_method
        - laterality
        - primary_indication
        - clinical_question
        - relevant_history
        - lesion_description
        - imaging_guidance_required
        - taking_anticoagulant
        - anticoagulant_agent
        - taking_antiplatelet
        - antiplatelet_agent
        - bleeding_disorder
        - immunosuppressed
        - urgency
        - supervising_consultant
        - requester_contact
        - notes
      description: Tissue biopsy procedure / pathology request (referral). Captures the requested procedure (site, method, laterality), the clinical indication and question, lesion description, bleeding / coagulation status, and triage urgency.
