# Generated from sql/. Do not edit by hand.
# SOURCE: biopsy_test_request_grade_rule
openapi: 3.1.0
info:
  title: Biopsy Test Request — BiopsyTestRequestGradeRule
  version: 1.0.0
  description: Audit trail of every appropriateness / bleeding-risk / completeness / urgency rule that fired for this request.
paths:
  /biopsy-test-request-grade-rules:
    get:
      summary: List BiopsyTestRequestGradeRule records
      operationId: listBiopsyTestRequestGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of BiopsyTestRequestGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BiopsyTestRequestGradeRule"
    post:
      summary: Create a new BiopsyTestRequestGradeRule
      operationId: createBiopsyTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BiopsyTestRequestGradeRule"
      responses:
        "201":
          description: The newly created BiopsyTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BiopsyTestRequestGradeRule"
        "400":
          description: Validation error
  /biopsy-test-request-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one BiopsyTestRequestGradeRule by id
      operationId: getBiopsyTestRequestGradeRule
      responses:
        "200":
          description: The requested BiopsyTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BiopsyTestRequestGradeRule"
        "404":
          description: BiopsyTestRequestGradeRule not found
    patch:
      summary: Update an existing BiopsyTestRequestGradeRule
      operationId: updateBiopsyTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BiopsyTestRequestGradeRule"
      responses:
        "200":
          description: The updated BiopsyTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BiopsyTestRequestGradeRule"
        "404":
          description: BiopsyTestRequestGradeRule not found
    delete:
      summary: Soft-delete a BiopsyTestRequestGradeRule
      operationId: deleteBiopsyTestRequestGradeRule
      responses:
        "204":
          description: BiopsyTestRequestGradeRule deleted
        "404":
          description: BiopsyTestRequestGradeRule not found
components:
  schemas:
    BiopsyTestRequestGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        biopsy_test_request_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-APPROP-MALIGNANCY-01).
        axis:
          type: string
          enum:
            - appropriateness
            - bleeding-risk
            - completeness
            - urgency
            - ""
          description: "Scoring axis the rule belongs to: appropriateness, bleeding-risk, completeness, urgency."
        category:
          type: string
          maxLength: 50
          description: Category or indication the rule relates to.
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - biopsy_test_request_grade_id
        - rule_id
        - axis
        - category
        - description
      description: Audit trail of every appropriateness / bleeding-risk / completeness / urgency rule that fired for this request.
