# Static HTML MEDIF dashboard — task list

- [x] Scaffold `index.html` page shell with header, filter bar, and
  empty assessments table.
- [x] Author `css/style.css` table styling (badges, hover, mobile
  scroll, sort indicators).
- [x] Bake 8 illustrative MEDIF rows into `js/app.js`, covering every
  fitness band (`fit`, `fit-with-conditions`, `requires-review`,
  `unfit-to-fly`).
- [x] Implement column sort with `aria-sort` and direction cycling.
- [x] Implement search + fitness-band + status + flags filters.
- [x] Render empty-state message when filters exclude all rows.
- [ ] Wire to a backend assessment-list endpoint (future enhancement).
- [ ] CSV / TSV export of the visible rows (future enhancement).
