//! Database migration placeholder.
//!
//! Will be replaced by a `sea-orm-migration` crate listing one migration per
//! SQL file in `../../sql-migrations/`.

pub fn placeholder() {
    // Intentionally empty.
}
