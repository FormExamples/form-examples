# Medical Information Form for Air Travel — TypeSpec

TypeSpec (`.tsp`) interface definitions, one file per SQL table in
`../sql-migrations/`. See [`AGENTS.md`](./AGENTS.md) for the SQL → TypeSpec
type mapping, naming conventions, and file list.
