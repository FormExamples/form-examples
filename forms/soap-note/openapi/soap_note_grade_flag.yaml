# Generated from sql/. Do not edit by hand.
# SOURCE: soap_note_grade_flag
openapi: 3.1.0
info:
  title: Soap Note — SoapNoteGradeFlag
  version: 1.0.0
  description: Safety flags that fire independently of the completeness status, with priority and a suggested action for the clinician or governance team.
paths:
  /soap-note-grade-flags:
    get:
      summary: List SoapNoteGradeFlag records
      operationId: listSoapNoteGradeFlag
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of SoapNoteGradeFlag records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SoapNoteGradeFlag"
    post:
      summary: Create a new SoapNoteGradeFlag
      operationId: createSoapNoteGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SoapNoteGradeFlag"
      responses:
        "201":
          description: The newly created SoapNoteGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoapNoteGradeFlag"
        "400":
          description: Validation error
  /soap-note-grade-flags/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one SoapNoteGradeFlag by id
      operationId: getSoapNoteGradeFlag
      responses:
        "200":
          description: The requested SoapNoteGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoapNoteGradeFlag"
        "404":
          description: SoapNoteGradeFlag not found
    patch:
      summary: Update an existing SoapNoteGradeFlag
      operationId: updateSoapNoteGradeFlag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SoapNoteGradeFlag"
      responses:
        "200":
          description: The updated SoapNoteGradeFlag
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoapNoteGradeFlag"
        "404":
          description: SoapNoteGradeFlag not found
    delete:
      summary: Soft-delete a SoapNoteGradeFlag
      operationId: deleteSoapNoteGradeFlag
      responses:
        "204":
          description: SoapNoteGradeFlag deleted
        "404":
          description: SoapNoteGradeFlag not found
components:
  schemas:
    SoapNoteGradeFlag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        soap_note_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        flag_id:
          type: string
          maxLength: 50
          description: Stable flag identifier (e.g. F-MISSING-ASSESSMENT-001).
        category:
          type: string
          enum:
            - missing-assessment
            - missing-plan
            - red-flag-no-plan
            - no-safety-netting
            - abnormal-vitals-unaddressed
            - incomplete-documentation
            - other
            - ""
          description: "Flag category: missing-assessment, missing-plan, red-flag-no-plan, no-safety-netting, abnormal-vitals-unaddressed, incomplete-documentation, or other."
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - ""
          description: "Priority: low, medium, high."
        description:
          type: string
          maxLength: 500
          description: Human-readable description of what fired the flag.
        suggested_action:
          type: string
          maxLength: 500
          description: "Suggested clinical or governance action (e.g. \"record an assessment\", \"document safety-netting advice\")."
      required:
        - created_at
        - updated_at
        - soap_note_grade_id
        - flag_id
        - category
        - priority
        - description
        - suggested_action
      description: Safety flags that fire independently of the completeness status, with priority and a suggested action for the clinician or governance team.
