# Generated from sql/. Do not edit by hand.
# SOURCE: soap_note_grade_rule
openapi: 3.1.0
info:
  title: Soap Note — SoapNoteGradeRule
  version: 1.0.0
  description: "Audit trail of every completeness rule that fired during grading: section, category, and description."
paths:
  /soap-note-grade-rules:
    get:
      summary: List SoapNoteGradeRule records
      operationId: listSoapNoteGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of SoapNoteGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SoapNoteGradeRule"
    post:
      summary: Create a new SoapNoteGradeRule
      operationId: createSoapNoteGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SoapNoteGradeRule"
      responses:
        "201":
          description: The newly created SoapNoteGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoapNoteGradeRule"
        "400":
          description: Validation error
  /soap-note-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one SoapNoteGradeRule by id
      operationId: getSoapNoteGradeRule
      responses:
        "200":
          description: The requested SoapNoteGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoapNoteGradeRule"
        "404":
          description: SoapNoteGradeRule not found
    patch:
      summary: Update an existing SoapNoteGradeRule
      operationId: updateSoapNoteGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SoapNoteGradeRule"
      responses:
        "200":
          description: The updated SoapNoteGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoapNoteGradeRule"
        "404":
          description: SoapNoteGradeRule not found
    delete:
      summary: Soft-delete a SoapNoteGradeRule
      operationId: deleteSoapNoteGradeRule
      responses:
        "204":
          description: SoapNoteGradeRule deleted
        "404":
          description: SoapNoteGradeRule not found
components:
  schemas:
    SoapNoteGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the row was most recently updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the row was soft-deleted.
        soap_note_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-ASSESSMENT-PRESENT-01).
        section:
          type: string
          enum:
            - subjective
            - objective
            - assessment
            - plan
            - completeness
            - ""
          description: "SOAP section the rule concerns: subjective, objective, assessment, plan, or completeness."
        category:
          type: string
          maxLength: 50
          description: Subject category (e.g. required-component, conditional-requirement).
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - soap_note_grade_id
        - rule_id
        - section
        - category
        - description
      description: "Audit trail of every completeness rule that fired during grading: section, category, and description."
