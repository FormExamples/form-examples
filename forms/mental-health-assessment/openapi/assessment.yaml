# Generated from sql/. Do not edit by hand.
# SOURCE: assessment, assessment_current_medications, assessment_current_medication_item
openapi: 3.1.0
info:
  title: Mental Health Assessment — Assessment
  version: 1.0.0
  description: Mental health assessment form using PHQ-9 and GAD-7 instruments. Parent entity for all assessment sections.
paths:
  /assessments:
    get:
      summary: List Assessment records
      operationId: listAssessment
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Assessment records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Assessment"
    post:
      summary: Create a new Assessment
      operationId: createAssessment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Assessment"
      responses:
        "201":
          description: The newly created Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
        "400":
          description: Validation error
  /assessments/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Assessment by id
      operationId: getAssessment
      responses:
        "200":
          description: The requested Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
        "404":
          description: Assessment not found
    patch:
      summary: Update an existing Assessment
      operationId: updateAssessment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Assessment"
      responses:
        "200":
          description: The updated Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
        "404":
          description: Assessment not found
    delete:
      summary: Soft-delete a Assessment
      operationId: deleteAssessment
      responses:
        "204":
          description: Assessment deleted
        "404":
          description: Assessment not found
components:
  schemas:
    Assessment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient who owns this assessment.
        status:
          type: string
          enum:
            - draft
            - submitted
            - reviewed
            - urgent
          description: "Lifecycle status: draft, submitted, reviewed, or urgent."
        current_medications_deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        taking_psychotropic_medication:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Whether the patient is taking psychotropic medication: yes, no, or empty string."
        medication_adherence:
          type: string
          enum:
            - good
            - partial
            - poor
            - ""
          description: "Self-reported medication adherence: good, partial, poor, or empty string."
        side_effects:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: "Whether the patient reports medication side effects: yes, no, or empty string."
        side_effects_details:
          type: string
          description: Free-text description of medication side effects.
        over_the_counter_medications:
          type: string
          description: Free-text list of over-the-counter medications.
        supplements:
          type: string
          description: Free-text list of dietary supplements.
        medication_notes:
          type: string
          description: Free-text clinician notes on medications.
        current_medication_item_deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        current_medications_id:
          type: string
          format: uuid
          description: Foreign key to the assessment_current_medications table.
        medication_name:
          type: string
          maxLength: 255
          description: Name of the medication.
        dose:
          type: string
          maxLength: 100
          description: Dose of the medication (e.g. 50mg).
        frequency:
          type: string
          maxLength: 100
          description: Frequency of administration (e.g. once daily, twice daily).
        prescriber:
          type: string
          maxLength: 255
          description: Name of the prescribing clinician.
        start_date:
          type: string
          format: date
          description: Date the medication was started.
        medication_class:
          type: string
          enum:
            - antidepressant
            - anxiolytic
            - antipsychotic
            - mood-stabiliser
            - hypnotic
            - stimulant
            - other
            - ""
          description: "Medication class: antidepressant, anxiolytic, antipsychotic, mood-stabiliser, hypnotic, stimulant, other, or empty string."
        sort_order:
          type: integer
          format: int32
          description: Sort order.
      required:
        - created_at
        - updated_at
        - patient_id
        - status
        - taking_psychotropic_medication
        - medication_adherence
        - side_effects
        - side_effects_details
        - over_the_counter_medications
        - supplements
        - medication_notes
        - current_medications_id
        - medication_name
        - dose
        - frequency
        - prescriber
        - medication_class
        - sort_order
      description: Mental health assessment form using PHQ-9 and GAD-7 instruments. Parent entity for all assessment sections.
