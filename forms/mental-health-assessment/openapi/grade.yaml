# Generated from sql-migrations/. Do not edit by hand.
# SOURCE: grade
openapi: 3.1.0
info:
  title: Mental Health Assessment — Grade
  version: 1.0.0
  description: Computed PHQ-9 and GAD-7 grading result for the mental health assessment. One-to-one child of assessment.
paths:
  /grades:
    get:
      summary: List Grade records
      operationId: listGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Grade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Grade"
    post:
      summary: Create a new Grade
      operationId: createGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "201":
          description: The newly created Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "400":
          description: Validation error
  /grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Grade by id
      operationId: getGrade
      responses:
        "200":
          description: The requested Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    patch:
      summary: Update an existing Grade
      operationId: updateGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Grade"
      responses:
        "200":
          description: The updated Grade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grade"
        "404":
          description: Grade not found
    delete:
      summary: Soft-delete a Grade
      operationId: deleteGrade
      responses:
        "204":
          description: Grade deleted
        "404":
          description: Grade not found
components:
  schemas:
    Grade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted.
        assessment_id:
          type: string
          format: uuid
          description: Foreign key to the parent assessment (unique, enforcing 1:1).
        phq9_total_score:
          type: integer
          format: int32
          description: PHQ-9 total score (0-27).
        phq9_severity:
          type: string
          enum:
            - minimal
            - mild
            - moderate
            - moderately_severe
            - severe
            - ""
          description: "PHQ-9 severity: minimal (0-4), mild (5-9), moderate (10-14), moderately_severe (15-19), severe (20-27)."
        gad7_total_score:
          type: integer
          format: int32
          description: GAD-7 total score (0-21).
        gad7_severity:
          type: string
          enum:
            - minimal
            - mild
            - moderate
            - severe
            - ""
          description: "GAD-7 severity: minimal (0-4), mild (5-9), moderate (10-14), severe (15-21)."
        risk_level:
          type: string
          enum:
            - low
            - moderate
            - high
            - imminent
            - ""
          description: "Overall risk level from risk assessment: low, moderate, high, imminent, or empty string."
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the grading was computed.
      required:
        - created_at
        - updated_at
        - assessment_id
        - phq9_total_score
        - phq9_severity
        - gad7_total_score
        - gad7_severity
        - risk_level
        - graded_at
      description: Computed PHQ-9 and GAD-7 grading result for the mental health assessment. One-to-one child of assessment.
