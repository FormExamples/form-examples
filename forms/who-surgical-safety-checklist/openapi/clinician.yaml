# Generated from sql/. Do not edit by hand.
# SOURCE: clinician
openapi: 3.1.0
info:
  title: Who Surgical Safety Checklist — Clinician
  version: 1.0.0
  description: Operating-room clinician who can fill in or sign off the WHO Surgical Safety Checklist.
paths:
  /clinicians:
    get:
      summary: List Clinician records
      operationId: listClinician
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Clinician records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Clinician"
    post:
      summary: Create a new Clinician
      operationId: createClinician
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Clinician"
      responses:
        "201":
          description: The newly created Clinician
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Clinician"
        "400":
          description: Validation error
  /clinicians/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Clinician by id
      operationId: getClinician
      responses:
        "200":
          description: The requested Clinician
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Clinician"
        "404":
          description: Clinician not found
    patch:
      summary: Update an existing Clinician
      operationId: updateClinician
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Clinician"
      responses:
        "200":
          description: The updated Clinician
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Clinician"
        "404":
          description: Clinician not found
    delete:
      summary: Soft-delete a Clinician
      operationId: deleteClinician
      responses:
        "204":
          description: Clinician deleted
        "404":
          description: Clinician not found
components:
  schemas:
    Clinician:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when this row was updated.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when this row was deleted (soft delete).
        name:
          type: string
          description: Clinician full name.
        email:
          type: string
          description: Work email address.
        phone:
          type: string
          description: Work phone number.
        role:
          type: string
          enum:
            - surgeon
            - assistant-surgeon
            - anaesthetist
            - circulating-nurse
            - scrub-nurse
            - anaesthetic-assistant
            - perfusionist
            - technician
            - other
            - ""
          description: "Operating-room role. One of: surgeon, assistant-surgeon, anaesthetist, circulating-nurse, scrub-nurse, anaesthetic-assistant, perfusionist, technician, other."
        registration_body:
          type: string
          enum:
            - GMC
            - NMC
            - HCPC
            - GPhC
            - other
            - ""
          description: "Professional registration body. One of: GMC, NMC, HCPC, GPhC, other."
        registration_number:
          type: string
          description: Registration number issued by the registration body.
        employee_number:
          type: string
          description: Local employee or smart-card number at the operating facility.
        united_kingdom_nhs_number:
          type: string
          maxLength: 12
          description: United Kingdom NHS number, unique per person.
      required:
        - created_at
        - updated_at
        - name
        - role
        - registration_body
        - registration_number
        - employee_number
      description: Operating-room clinician who can fill in or sign off the WHO Surgical Safety Checklist.
