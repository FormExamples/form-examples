# Generated from sql/. Do not edit by hand.
# SOURCE: patient_allergy
openapi: 3.1.0
info:
  title: Perioperative Optimization — PatientAllergy
  version: 1.0.0
  description: Patient allergy, i.e. one allergen that one patient reacts to, with the reaction and its severity.
paths:
  /patient-allergies:
    get:
      summary: List PatientAllergy records
      operationId: listPatientAllergy
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of PatientAllergy records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PatientAllergy"
    post:
      summary: Create a new PatientAllergy
      operationId: createPatientAllergy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatientAllergy"
      responses:
        "201":
          description: The newly created PatientAllergy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PatientAllergy"
        "400":
          description: Validation error
  /patient-allergies/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one PatientAllergy by id
      operationId: getPatientAllergy
      responses:
        "200":
          description: The requested PatientAllergy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PatientAllergy"
        "404":
          description: PatientAllergy not found
    patch:
      summary: Update an existing PatientAllergy
      operationId: updatePatientAllergy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatientAllergy"
      responses:
        "200":
          description: The updated PatientAllergy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PatientAllergy"
        "404":
          description: PatientAllergy not found
    delete:
      summary: Soft-delete a PatientAllergy
      operationId: deletePatientAllergy
      responses:
        "204":
          description: PatientAllergy deleted
        "404":
          description: PatientAllergy not found
components:
  schemas:
    PatientAllergy:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient table.
        allergy_id:
          type: string
          format: uuid
          description: Foreign key to the allergy table.
        reaction:
          type: string
          description: Reaction the patient experiences, such as urticaria, angioedema, vomiting, or abdominal pain.
        severity:
          type: string
          enum:
            - mild
            - moderate
            - severe
            - anaphylaxis
            - ""
          description: Severity of the reaction, where anaphylaxis fires the allergy-anaphylaxis-risk safety flag.
        kind:
          type: string
          enum:
            - allergy
            - intolerance
            - avoidance-by-choice
            - unknown
            - ""
          description: Whether this is a true allergy, an intolerance, or an avoidance by choice, because the three lead to different dietetic advice.
        diagnosis_method:
          type: string
          enum:
            - clinically-diagnosed
            - skin-prick-test
            - specific-ige
            - oral-food-challenge
            - elimination-and-reintroduction
            - self-reported
            - ""
          description: How the allergy or intolerance was established, such as for judging the confidence of a self-reported avoidance.
        onset_on:
          type: string
          format: date
          description: Date of first known reaction, where known.
        adrenaline_auto_injector:
          type: boolean
          description: Whether the patient carries an adrenaline auto-injector.
        notes:
          type: string
          description: Free-text notes about this allergen for this patient.
      required:
        - created_at
        - updated_at
        - patient_id
        - allergy_id
        - reaction
        - severity
        - kind
        - diagnosis_method
        - adrenaline_auto_injector
        - notes
      description: Patient allergy, i.e. one allergen that one patient reacts to, with the reaction and its severity.
