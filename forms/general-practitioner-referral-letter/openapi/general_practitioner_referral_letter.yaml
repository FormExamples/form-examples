# Generated from sql/. Do not edit by hand.
# SOURCE: general_practitioner_referral_letter
openapi: 3.1.0
info:
  title: General Practitioner Referral Letter — GeneralPractitionerReferralLetter
  version: 1.0.0
  description: "Main general-practitioner referral letter record: referral destination, urgency, reason and history, examination and investigation findings, medications and allergies, and the patient's expectations, consent, and safety-netting, plus patient and referrer identification. Completeness grade, fired rules, and flags live in child tables."
paths:
  /general-practitioner-referral-letters:
    get:
      summary: List GeneralPractitionerReferralLetter records
      operationId: listGeneralPractitionerReferralLetter
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of GeneralPractitionerReferralLetter records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeneralPractitionerReferralLetter"
    post:
      summary: Create a new GeneralPractitionerReferralLetter
      operationId: createGeneralPractitionerReferralLetter
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneralPractitionerReferralLetter"
      responses:
        "201":
          description: The newly created GeneralPractitionerReferralLetter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralPractitionerReferralLetter"
        "400":
          description: Validation error
  /general-practitioner-referral-letters/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one GeneralPractitionerReferralLetter by id
      operationId: getGeneralPractitionerReferralLetter
      responses:
        "200":
          description: The requested GeneralPractitionerReferralLetter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralPractitionerReferralLetter"
        "404":
          description: GeneralPractitionerReferralLetter not found
    patch:
      summary: Update an existing GeneralPractitionerReferralLetter
      operationId: updateGeneralPractitionerReferralLetter
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneralPractitionerReferralLetter"
      responses:
        "200":
          description: The updated GeneralPractitionerReferralLetter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralPractitionerReferralLetter"
        "404":
          description: GeneralPractitionerReferralLetter not found
    delete:
      summary: Soft-delete a GeneralPractitionerReferralLetter
      operationId: deleteGeneralPractitionerReferralLetter
      responses:
        "204":
          description: GeneralPractitionerReferralLetter deleted
        "404":
          description: GeneralPractitionerReferralLetter not found
components:
  schemas:
    GeneralPractitionerReferralLetter:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        patient_id:
          type: string
          format: uuid
          description: Foreign key to the patient being referred (restrict delete).
        clinician_id:
          type: string
          format: uuid
          description: Foreign key to the referring clinician (restrict delete); optional.
        patient_identifier:
          type: string
          maxLength: 100
          description: NHS number or local patient identifier as recorded on the letter.
        referrer_role:
          type: string
          enum:
            - gp
            - gp-registrar
            - nurse-practitioner
            - pharmacist
            - paramedic
            - other
            - ""
          description: "Referrer role: gp, gp-registrar, nurse-practitioner, pharmacist, paramedic, or other."
        referring_practice:
          type: string
          description: Referring practice name.
        referral_date:
          type: string
          format: date
          description: Date of referral.
        access_needs:
          type: string
          description: Interpreter or accessibility needs recorded for the patient.
        referral_specialty:
          type: string
          description: Specialty or service being referred to (always mandatory).
        named_clinician:
          type: string
          description: Named consultant or team being referred to (optional).
        receiving_organisation:
          type: string
          description: Receiving hospital, trust, or community provider.
        urgency:
          type: string
          enum:
            - routine
            - urgent
            - two-week-wait
            - emergency
            - ""
          description: "Urgency classification: routine, urgent, two-week-wait, or emergency (always mandatory)."
        urgency_reason:
          type: string
          description: Why the referral is urgent; mandatory for urgent and two-week-wait.
        suspected_cancer_criterion:
          type: string
          description: Named NICE NG12 criterion; mandatory for two-week-wait.
        suspected_cancer_pathway:
          type: string
          description: Tumour-site suspected-cancer pathway; mandatory for two-week-wait.
        reason_for_referral:
          type: string
          description: Primary reason for the referral (always mandatory).
        relevant_history:
          type: string
          description: Relevant clinical history (always mandatory).
        presenting_problem:
          type: string
          description: Presenting problem.
        symptom_duration:
          type: string
          description: Duration of symptoms.
        red_flag_symptoms:
          type: string
          description: Documented red-flag symptoms; presence drives the emergency-features flag.
        examination_findings:
          type: string
          description: Examination findings.
        investigation_results:
          type: string
          description: Bloods, imaging, or other investigations already done.
        current_medications:
          type: string
          description: Current medications.
        allergies:
          type: string
          description: Allergies and reactions.
        patient_expectations:
          type: string
          description: Patient's expectations or question to the specialist.
        consent_to_share:
          type: string
          enum:
            - "yes"
            - "no"
            - ""
          description: Whether consent to share information is documented (yes/no); anything other than yes raises the consent-not-documented flag.
        safety_netting:
          type: string
          description: Safety-netting advice given to the patient while waiting; absence raises the no-safety-netting flag.
        clinical_note:
          type: string
          description: Optional free-text note shown in the summary.
      required:
        - created_at
        - updated_at
        - patient_id
        - patient_identifier
        - referrer_role
        - referring_practice
        - access_needs
        - referral_specialty
        - named_clinician
        - receiving_organisation
        - urgency
        - urgency_reason
        - suspected_cancer_criterion
        - suspected_cancer_pathway
        - reason_for_referral
        - relevant_history
        - presenting_problem
        - symptom_duration
        - red_flag_symptoms
        - examination_findings
        - investigation_results
        - current_medications
        - allergies
        - patient_expectations
        - consent_to_share
        - safety_netting
        - clinical_note
      description: "Main general-practitioner referral letter record: referral destination, urgency, reason and history, examination and investigation findings, medications and allergies, and the patient's expectations, consent, and safety-netting, plus patient and referrer identification. Completeness grade, fired rules, and flags live in child tables."
