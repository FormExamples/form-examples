# Generated from sql/. Do not edit by hand.
# SOURCE: cystoscopy_test_result_grade
openapi: 3.1.0
info:
  title: Cystoscopy Test Result — CystoscopyTestResultGrade
  version: 1.0.0
  description: "Computed four-axis interpretation grade: result classification, abnormality severity / structured-reporting category, report completeness, and follow-up urgency. A bladder tumour or suspicious lesion auto-escalates the follow-up urgency to critical-alert regardless of the other axes."
paths:
  /cystoscopy-test-result-grades:
    get:
      summary: List CystoscopyTestResultGrade records
      operationId: listCystoscopyTestResultGrade
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of CystoscopyTestResultGrade records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CystoscopyTestResultGrade"
    post:
      summary: Create a new CystoscopyTestResultGrade
      operationId: createCystoscopyTestResultGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CystoscopyTestResultGrade"
      responses:
        "201":
          description: The newly created CystoscopyTestResultGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CystoscopyTestResultGrade"
        "400":
          description: Validation error
  /cystoscopy-test-result-grades/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one CystoscopyTestResultGrade by id
      operationId: getCystoscopyTestResultGrade
      responses:
        "200":
          description: The requested CystoscopyTestResultGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CystoscopyTestResultGrade"
        "404":
          description: CystoscopyTestResultGrade not found
    patch:
      summary: Update an existing CystoscopyTestResultGrade
      operationId: updateCystoscopyTestResultGrade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CystoscopyTestResultGrade"
      responses:
        "200":
          description: The updated CystoscopyTestResultGrade
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CystoscopyTestResultGrade"
        "404":
          description: CystoscopyTestResultGrade not found
    delete:
      summary: Soft-delete a CystoscopyTestResultGrade
      operationId: deleteCystoscopyTestResultGrade
      responses:
        "204":
          description: CystoscopyTestResultGrade deleted
        "404":
          description: CystoscopyTestResultGrade not found
components:
  schemas:
    CystoscopyTestResultGrade:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        cystoscopy_test_result_id:
          type: string
          format: uuid
          description: Foreign key to the parent result (unique, 1:1).
        result_classification:
          type: string
          enum:
            - normal
            - abnormal
            - critical
            - inconclusive
            - ""
          description: "Axis A overall classification: normal, abnormal, critical, inconclusive."
        abnormality_severity:
          type: string
          enum:
            - none
            - minor
            - moderate
            - major
            - ""
          description: "Axis B abnormality severity: none, minor, moderate, major."
        reporting_category:
          type: string
          maxLength: 50
          description: Axis B structured-reporting score / category label (e.g. an EAU NMIBC risk group or a suspected-tumour category).
        report_completeness_percent:
          type: integer
          format: int32
          description: Axis C report completeness as a percentage of mandatory report sections (0-100).
        follow_up_urgency:
          type: string
          enum:
            - routine
            - recommended
            - urgent
            - critical-alert
            - ""
          description: "Axis D follow-up urgency: routine, recommended, urgent, critical-alert."
        target_timeframe:
          type: string
          maxLength: 50
          description: Target timeframe for the recommended follow-up implied by the urgency.
        recommended_action:
          type: string
          maxLength: 500
          description: Axis D concise recommended action derived from the urgency.
        recommendation:
          type: string
          enum:
            - no-action
            - routine-follow-up
            - further-imaging
            - specialist-referral
            - urgent-review
            - ""
          description: "Overall recommendation: no-action, routine-follow-up, further-imaging, specialist-referral, urgent-review."
        clinician_notes:
          type: string
          description: Free-text interpretation / sign-off notes.
        signed_at:
          type: string
          format: date-time
          description: Timestamp of the reporting clinician electronic signature.
        graded_at:
          type: string
          format: date-time
          description: Timestamp when the engine last computed the result.
      required:
        - created_at
        - updated_at
        - cystoscopy_test_result_id
        - result_classification
        - abnormality_severity
        - reporting_category
        - follow_up_urgency
        - target_timeframe
        - recommended_action
        - recommendation
        - clinician_notes
        - graded_at
      description: "Computed four-axis interpretation grade: result classification, abnormality severity / structured-reporting category, report completeness, and follow-up urgency. A bladder tumour or suspicious lesion auto-escalates the follow-up urgency to critical-alert regardless of the other axes."
