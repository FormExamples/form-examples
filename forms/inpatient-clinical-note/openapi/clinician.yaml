# Generated from sql/. Do not edit by hand.
# SOURCE: clinician
openapi: 3.1.0
info:
  title: Inpatient Clinical Note — Clinician
  version: 1.0.0
  description: Clinician identifying information for the author of an inpatient clinical note, the responsible consultant, and any named senior reviewer.
paths:
  /clinicians:
    get:
      summary: List Clinician records
      operationId: listClinician
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of Clinician records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Clinician"
    post:
      summary: Create a new Clinician
      operationId: createClinician
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Clinician"
      responses:
        "201":
          description: The newly created Clinician
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Clinician"
        "400":
          description: Validation error
  /clinicians/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one Clinician by id
      operationId: getClinician
      responses:
        "200":
          description: The requested Clinician
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Clinician"
        "404":
          description: Clinician not found
    patch:
      summary: Update an existing Clinician
      operationId: updateClinician
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Clinician"
      responses:
        "200":
          description: The updated Clinician
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Clinician"
        "404":
          description: Clinician not found
    delete:
      summary: Soft-delete a Clinician
      operationId: deleteClinician
      responses:
        "204":
          description: Clinician deleted
        "404":
          description: Clinician not found
components:
  schemas:
    Clinician:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when the record was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        name:
          type: string
          description: Clinician full name as recorded on the note.
        email:
          type: string
          description: Email address.
        phone:
          type: string
          description: Phone number.
        postal_address_as_full_text:
          type: string
          description: Postal address as a single text block.
        country_as_iso_3166_1_alpha_2:
          type: string
          maxLength: 2
          description: Country as ISO 3166-1 alpha-2.
        postcode:
          type: string
          description: Postal code.
        role:
          type: string
          enum:
            - foundation-doctor
            - core-trainee
            - specialty-registrar
            - consultant
            - acp
            - physician-associate
            - nurse
            - nurse-practitioner
            - pharmacist
            - allied-health-professional
            - other
            - ""
          description: "Professional role: foundation-doctor, core-trainee, specialty-registrar, consultant, acp (advanced clinical practitioner), physician-associate, nurse, nurse-practitioner, pharmacist, allied-health-professional, or other."
        grade:
          type: string
          enum:
            - FY1
            - FY2
            - CT1
            - CT2
            - CT3
            - ST1
            - ST2
            - ST3
            - ST4
            - ST5
            - ST6
            - ST7
            - ST8
            - SAS
            - consultant
            - other
            - ""
          description: "Training or employment grade: FY1, FY2, CT1..CT3, ST1..ST8, SAS, consultant, or other."
        specialty:
          type: string
          maxLength: 60
          description: Clinical specialty the clinician works in.
        registration_body:
          type: string
          enum:
            - GMC
            - NMC
            - HCPC
            - GPhC
            - other
            - ""
          description: "Professional registration body: GMC, NMC, HCPC, GPhC, or other."
        registration_number:
          type: string
          description: Professional registration number issued by the registration body.
      required:
        - created_at
        - updated_at
        - name
        - role
        - grade
        - specialty
        - registration_body
        - registration_number
      description: Clinician identifying information for the author of an inpatient clinical note, the responsible consultant, and any named senior reviewer.
