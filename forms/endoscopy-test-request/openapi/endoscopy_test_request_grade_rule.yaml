# Generated from sql/. Do not edit by hand.
# SOURCE: endoscopy_test_request_grade_rule
openapi: 3.1.0
info:
  title: Endoscopy Test Request — EndoscopyTestRequestGradeRule
  version: 1.0.0
  description: Audit trail of every appropriateness / urgency / completeness / risk rule that fired for this request.
paths:
  /endoscopy-test-request-grade-rules:
    get:
      summary: List EndoscopyTestRequestGradeRule records
      operationId: listEndoscopyTestRequestGradeRule
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
          required: false
      responses:
        "200":
          description: A page of EndoscopyTestRequestGradeRule records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EndoscopyTestRequestGradeRule"
    post:
      summary: Create a new EndoscopyTestRequestGradeRule
      operationId: createEndoscopyTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EndoscopyTestRequestGradeRule"
      responses:
        "201":
          description: The newly created EndoscopyTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EndoscopyTestRequestGradeRule"
        "400":
          description: Validation error
  /endoscopy-test-request-grade-rules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Fetch one EndoscopyTestRequestGradeRule by id
      operationId: getEndoscopyTestRequestGradeRule
      responses:
        "200":
          description: The requested EndoscopyTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EndoscopyTestRequestGradeRule"
        "404":
          description: EndoscopyTestRequestGradeRule not found
    patch:
      summary: Update an existing EndoscopyTestRequestGradeRule
      operationId: updateEndoscopyTestRequestGradeRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EndoscopyTestRequestGradeRule"
      responses:
        "200":
          description: The updated EndoscopyTestRequestGradeRule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EndoscopyTestRequestGradeRule"
        "404":
          description: EndoscopyTestRequestGradeRule not found
    delete:
      summary: Soft-delete a EndoscopyTestRequestGradeRule
      operationId: deleteEndoscopyTestRequestGradeRule
      responses:
        "204":
          description: EndoscopyTestRequestGradeRule deleted
        "404":
          description: EndoscopyTestRequestGradeRule not found
components:
  schemas:
    EndoscopyTestRequestGradeRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Primary key UUID, auto-generated.
        created_at:
          type: string
          format: date-time
          description: Timestamp when this row was created.
        updated_at:
          type: string
          format: date-time
          description: Timestamp when the record was updated most-recently.
        deleted_at:
          type: string
          format: date-time
          description: Timestamp when the record was deleted a.k.a. soft-removed.
        endoscopy_test_request_grade_id:
          type: string
          format: uuid
          description: Foreign key to the parent grade.
        rule_id:
          type: string
          maxLength: 40
          description: Stable rule identifier (e.g. R-URGENCY-2WW-DYSPHAGIA-01).
        axis:
          type: string
          enum:
            - appropriateness
            - urgency
            - completeness
            - risk
            - ""
          description: "Scoring axis the rule belongs to: appropriateness, urgency, completeness, risk."
        category:
          type: string
          maxLength: 50
          description: Category or indication the rule relates to.
        description:
          type: string
          maxLength: 500
          description: Human-readable description of why the rule fired.
      required:
        - created_at
        - updated_at
        - endoscopy_test_request_grade_id
        - rule_id
        - axis
        - category
        - description
      description: Audit trail of every appropriateness / urgency / completeness / risk rule that fired for this request.
